---
phase: 15-global-error-recovery-and-progress
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/merge/SKILL.md
autonomous: true
requirements:
  - PROG-03

must_haves:
  truths:
    - "During merge reviewer subagent operation, developer sees progress text with set name and review stage"
    - "During cleanup subagent operation, developer sees progress text with round number and action"
    - "Progress banner format matches context skill and Phase 12 execute progress style"
  artifacts:
    - path: "skills/merge/SKILL.md"
      provides: "Merge skill with subagent-level progress banners"
      contains: "Reviewing set"
  key_links:
    - from: "skills/merge/SKILL.md"
      to: "Agent tool"
      via: "Progress banners before/after reviewer and cleanup subagent spawns"
      pattern: "Reviewing set.*Checking contracts"
---

<objective>
Add subagent-level progress banners to merge SKILL.md during reviewer and cleanup operations.

Purpose: Developers see what is happening during long merge reviewer and cleanup subagent operations instead of waiting with no feedback.
Output: Updated skills/merge/SKILL.md with progress banners around subagent spawns
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-global-error-recovery-and-progress/15-CONTEXT.md

@skills/merge/SKILL.md
@skills/execute/SKILL.md (reference for progress banner format)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reviewer and cleanup progress banners to merge SKILL.md</name>
  <files>skills/merge/SKILL.md</files>
  <action>
Edit skills/merge/SKILL.md to add subagent-level progress banners around reviewer and cleanup subagent spawns. Use the same unified banner format as context skill (right-arrow, short informative text, no timestamps).

**Step 4 (Agent Review) -- add reviewer progress banners:**

1. Before spawning the reviewer subagent (before item 3 "Spawn reviewer subagent"), add:
   ```
   Print progress banner before spawning reviewer:
   > Reviewing set: {setName}...
   ```

2. After step 4 item 3 (after "Spawn reviewer subagent" and before item 4 "After reviewer returns"), add:
   ```
   Print progress banner after reviewer returns:
   > Checking contracts...
   ```

Note: The existing "Display progress" lines at the end of Step 4 (line ~174-175 with `[{waveNum}/{totalWaves}]` format) are wave-level progress and should be KEPT as-is. The new banners are subagent-level progress that appears DURING the operation, complementing the wave-level summary that appears AFTER.

**Step 5 (Cleanup Loop) -- add cleanup progress banners:**

1. The existing cleanup progress banner at line ~183 (`Cleanup round {round}/2 -- re-reviewing {setName}...`) already exists and is good. Keep it.

2. Before spawning the cleanup subagent (before item 4 "Spawn cleanup subagent"), add:
   ```
   Print progress banner before spawning cleanup:
   > Cleanup round {round}/2: fixing issues in {setName}...
   ```

3. Before re-spawning the reviewer after cleanup (before item 6 "re-spawn reviewer"), add:
   ```
   Print progress banner:
   > Re-reviewing {setName}...
   ```

Do NOT modify any existing logic, AskUserQuestion prompts, verdict handling, or wave-level progress lines. Only ADD the new subagent-level banners.
  </action>
  <verify>
Run: grep -n "Reviewing set\|Checking contracts\|Cleanup round.*fixing\|Re-reviewing" skills/merge/SKILL.md
Expected: 4 matches -- one for each new progress banner.
  </verify>
  <done>
Merge SKILL.md shows reviewer progress ("Reviewing set: {setName}..." before spawn, "Checking contracts..." after return), cleanup progress ("Cleanup round {round}/2: fixing issues in {setName}..." before cleanup spawn, "Re-reviewing {setName}..." before re-review). Banner format matches unified style used by context and execute skills.
  </done>
</task>

</tasks>

<verification>
1. `grep -c 'Reviewing set\|Checking contracts\|Cleanup round.*fixing\|Re-reviewing' skills/merge/SKILL.md` returns 4
2. Existing wave-level progress lines (`[{waveNum}/{totalWaves}]` format) are still present
3. Existing AskUserQuestion prompts and verdict handling are unchanged
4. skills/merge/SKILL.md has no syntax errors in markdown structure
</verification>

<success_criteria>
- Reviewer subagent spawn is preceded by "Reviewing set: {setName}..." banner
- Reviewer return is followed by "Checking contracts..." banner
- Cleanup subagent spawn is preceded by round/action banner
- Re-review spawn is preceded by "Re-reviewing {setName}..." banner
- All existing merge skill logic, prompts, and wave-level progress are preserved
</success_criteria>

<output>
After completion, create `.planning/phases/15-global-error-recovery-and-progress/15-02-SUMMARY.md`
</output>
