---
phase: 31-wave-orchestration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/execute/SKILL.md
autonomous: true
requirements: [WAVE-04]

must_haves:
  truths:
    - "PASS reconciliation auto-advances to next wave without AskUserQuestion"
    - "PASS_WITH_WARNINGS auto-advances with inline summary printed"
    - "FAIL reconciliation is the only remaining per-wave gate with retry/cancel options"
    - "/rapid:execute 1 --retry-wave wave-2 targets only wave-2 for re-execution"
    - "Smart re-entry still works: complete waves skipped, failed retried"
  artifacts:
    - path: "skills/execute/SKILL.md"
      provides: "Modified execute skill with auto-advance and --retry-wave"
      contains: "auto-advance"
  key_links:
    - from: "skills/execute/SKILL.md"
      to: "src/bin/rapid-tools.cjs"
      via: "state transition and reconcile-jobs CLI calls"
      pattern: "state transition wave"
---

<objective>
Modify the execute skill to auto-advance on PASS/PASS_WITH_WARNINGS and add --retry-wave flag for targeted wave retry.

Purpose: WAVE-04 requires that execute runs waves sequentially without per-wave user approval gates. Currently Step 3i uses AskUserQuestion for ALL outcomes. After this change, only FAIL retains the user gate. The --retry-wave flag enables targeted retry of a specific failed wave.

Output: Modified skills/execute/SKILL.md with auto-advance behavior and --retry-wave support.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-wave-orchestration/31-CONTEXT.md
@.planning/phases/31-wave-orchestration/31-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From skills/execute/SKILL.md Step 3i (CURRENT -- to be modified):
```markdown
### Step 3i: User decision after wave reconciliation

Use AskUserQuestion with dynamic options based on the reconciliation result:

**If PASS:**
- **question:** "Wave {waveId} reconciliation passed"
- **options:**
  - "Continue to next wave" -- description: "All deliverables verified. Proceed to wave {nextWaveId}."
  - "Pause here" -- description: "Save state and exit. Resume with /rapid:execute {set-id}."

**If PASS_WITH_WARNINGS:**
- **question:** "Wave {waveId} reconciliation passed with warnings"
- **options:**
  - "Continue anyway" -- description: "Accept warnings and proceed to wave {nextWaveId}"
  - "Retry failed jobs" -- description: "Re-execute only the jobs with issues in this wave"
  - "Pause" -- description: "Save state and exit. Resume with /rapid:execute {set-id}."

**If FAIL:**
- **question:** "Wave {waveId} reconciliation failed"
- **options:**
  - "Retry failed jobs" -- description: "Re-execute failed jobs in this wave"
  - "Cancel execution" -- description: "Save state and exit"
```

From skills/execute/SKILL.md Step 3g.1 (lean review -- context for auto-fix behavior):
```markdown
**If autoFixed > 0 and needsAttention is empty:** Print silently:
> Lean review: {autoFixed} issue(s) auto-fixed.
Continue to next step.

**If needsAttention is not empty:** Present each issue briefly, then use AskUserQuestion:
- "Log and continue" / "Pause execution"
```

From skills/execute/SKILL.md Step 0b (current argument parsing -- needs --retry-wave addition):
```markdown
### 0b: Parse set-id argument
The user invokes this skill with a set identifier: `/rapid:execute <set-id>`
```

From skills/execute/SKILL.md Step 2 (smart re-entry -- context for --retry-wave integration):
```markdown
## Step 2: Smart Re-entry
Read all job statuses across all waves in this set...
```

Established patterns:
- AskUserQuestion kept ONLY for mid-flow decisions, NOT for end-of-skill or routine advancement (Phase 28)
- Wave transition banners use `node "${RAPID_TOOLS}" display banner execute "Wave {N}/{total}: {waveId}"`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modify execute Step 3i for auto-advance and add --retry-wave flag</name>
  <files>skills/execute/SKILL.md</files>
  <action>
    Read `skills/execute/SKILL.md` in full first. Make these targeted modifications:

    **Modification 1: Replace Step 3i with auto-advance behavior**

    Replace the current Step 3i section with:

    ```markdown
    ### Step 3i: Auto-advance after wave reconciliation

    **If PASS:**
    Print inline: "Wave {waveId}: PASS. Continuing to wave {nextWaveId}..."
    Proceed directly to Step 3a for the next wave. No AskUserQuestion.
    If this is the LAST wave, proceed to Step 4 (Final Summary).

    **If PASS_WITH_WARNINGS:**
    Print inline summary: "Wave {waveId}: PASS_WITH_WARNINGS ({softBlocks.length} soft blocks). Continuing..."
    Proceed directly to Step 3a for the next wave. No AskUserQuestion.
    If this is the LAST wave, proceed to Step 4 (Final Summary).

    **If FAIL:**
    Use AskUserQuestion:
    - **question:** "Wave {waveId} reconciliation failed"
    - **options:**
      - "Retry failed jobs" -- description: "Re-execute failed jobs in this wave"
      - "Cancel execution" -- description: "Save state and exit"

    **Action based on user choice:**
    - "Retry failed jobs": Re-execute only failed jobs in this wave (go back to Step 3d for those jobs only, with other jobs showing as complete)
    - "Cancel execution": Commit state and exit:
      > Execution cancelled. State saved. Resume with `/rapid:execute {set-id}`.
      Exit.

    This is the ONLY remaining per-wave gate. PASS and PASS_WITH_WARNINGS always auto-advance.
    ```

    **Modification 2: Add --retry-wave flag to Step 0b**

    After the existing `### 0b: Parse set-id argument` section and the resolve set block, add a new subsection:

    ```markdown
    ### 0b.2: Check for --retry-wave flag

    If the user invoked `/rapid:execute <set-id> --retry-wave <wave-id>`, enter targeted retry mode:

    1. Resolve the wave reference: `node "${RAPID_TOOLS}" resolve wave "<wave-id>" --set "<set-id>"`
    2. Parse the resolved wave ID
    3. Load job statuses for all waves: `node "${RAPID_TOOLS}" execute job-status <set-id>`
    4. Verify all predecessor waves (earlier waves in STATE.json ordering) are in `complete` state
       - If any predecessor is not complete: inform user "Cannot retry {waveId} -- predecessor wave {predWaveId} is in '{status}' state. Fix predecessor waves first." and STOP.
    5. Set execution to start from the target wave only:
       - Skip all waves before the target wave
       - Execute only the target wave (retrying failed/pending jobs, skipping complete)
       - After the target wave completes, continue with subsequent waves (normal auto-advance behavior)
    ```

    **Modification 3: Update Important Notes section**

    Add a new bullet to the "Important Notes" section at the end of the skill:

    ```markdown
    - **Auto-advance:** PASS and PASS_WITH_WARNINGS reconciliation results automatically advance to the next wave without user approval. Only FAIL retains the AskUserQuestion gate. This reduces N-1 unnecessary approval prompts for a set with N waves.
    - **--retry-wave:** Targets a specific wave for retry (e.g., `/rapid:execute 1 --retry-wave wave-2`). Verifies predecessor waves are complete before proceeding. After retrying the target wave, continues with subsequent waves.
    ```

    **What NOT to change:**
    - Step 0b.1 (--fix-issues flag) -- leave untouched
    - Step 1 (execution mode detection) -- leave untouched
    - Step 2 (smart re-entry) -- leave untouched (works correctly with auto-advance)
    - Steps 3a-3h -- leave untouched
    - Step 3g.1 (lean review) -- leave untouched (already has its own needsAttention gate)
    - Step 4 (final summary) -- leave untouched
  </action>
  <verify>
    <automated>grep -q "Auto-advance\|auto-advance" ~/Projects/RAPID/skills/execute/SKILL.md && grep -q "retry-wave" ~/Projects/RAPID/skills/execute/SKILL.md && grep -q "PASS_WITH_WARNINGS.*soft blocks.*Continuing" ~/Projects/RAPID/skills/execute/SKILL.md && ! grep -q "Continue to next wave.*Pause here" ~/Projects/RAPID/skills/execute/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Execute skill auto-advances on PASS/PASS_WITH_WARNINGS (no AskUserQuestion), retains FAIL gate only, supports --retry-wave flag with predecessor validation, Important Notes updated</done>
</task>

</tasks>

<verification>
```bash
# Verify auto-advance replaces old approval gate
grep -c "AskUserQuestion" ~/Projects/RAPID/skills/execute/SKILL.md
# Should be reduced (removed from PASS/PASS_WITH_WARNINGS, kept for FAIL and other gates)

# Verify FAIL still has AskUserQuestion
grep -A5 "If FAIL" ~/Projects/RAPID/skills/execute/SKILL.md | grep -q "AskUserQuestion" && echo "PASS: FAIL gate preserved"

# Verify --retry-wave flag documented
grep -q "retry-wave" ~/Projects/RAPID/skills/execute/SKILL.md && echo "PASS: --retry-wave flag exists"

# Verify old PASS gate is gone
grep -q "Continue to next wave.*Pause here" ~/Projects/RAPID/skills/execute/SKILL.md && echo "FAIL: old PASS gate still present" || echo "PASS: old PASS gate removed"

# Verify auto-advance language
grep -q "auto-advance" ~/Projects/RAPID/skills/execute/SKILL.md && echo "PASS: auto-advance documented"
```
</verification>

<success_criteria>
- Step 3i PASS: prints inline message and auto-advances (no AskUserQuestion)
- Step 3i PASS_WITH_WARNINGS: prints inline summary with soft block count and auto-advances
- Step 3i FAIL: retains AskUserQuestion with Retry/Cancel options only
- --retry-wave flag: resolves target wave, validates predecessors complete, executes target wave, continues with subsequent
- Old approval gates (Continue/Pause/Continue anyway) removed from PASS and PASS_WITH_WARNINGS
- Lean review behavior (Step 3g.1) unchanged
- Smart re-entry (Step 2) unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/31-wave-orchestration/31-03-SUMMARY.md`
</output>
