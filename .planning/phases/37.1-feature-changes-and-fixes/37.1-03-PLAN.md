---
phase: 37.1-feature-changes-and-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/help/SKILL.md
  - skills/status/SKILL.md
  - skills/init/SKILL.md
  - skills/set-init/SKILL.md
  - skills/execute/SKILL.md
  - skills/plan-set/SKILL.md
  - skills/wave-plan/SKILL.md
  - skills/review/SKILL.md
  - skills/merge/SKILL.md
  - skills/cleanup/SKILL.md
  - src/modules/core/core-identity.md
autonomous: true
requirements: [FIX-01, FIX-05, FIX-07]

must_haves:
  truths:
    - "No user-facing skill suggests /rapid:wave-plan or wave-level discuss"
    - "/rapid:help shows simplified lifecycle without wave-plan"
    - "/rapid:help lists /rapid:migrate and /rapid:quick commands"
    - "/rapid:status default view shows sets only (no wave expansion)"
    - "/rapid:status --detail flag expands to show wave-level breakdown"
    - "/rapid:init completion shows lifecycle numbered list"
    - "/rapid:execute commits STATE.md on completion"
    - "core-identity.md workflow sequence removes wave-plan step"
    - "All next-step suggestions use set-level commands only"
  artifacts:
    - path: "skills/help/SKILL.md"
      provides: "Updated command reference without wave-plan, with /migrate and /quick"
      contains: "/rapid:migrate"
    - path: "skills/status/SKILL.md"
      provides: "Sets-only default view with --detail flag"
      contains: "--detail"
    - path: "skills/init/SKILL.md"
      provides: "Lifecycle banner after init completion"
      contains: "set-init"
    - path: "skills/execute/SKILL.md"
      provides: "STATE.md commit on execution completion"
      contains: "commit-state"
    - path: "src/modules/core/core-identity.md"
      provides: "Updated workflow sequence"
  key_links:
    - from: "skills/set-init/SKILL.md"
      to: "skills/discuss/SKILL.md"
      via: "next-step suggestion"
      pattern: "rapid:discuss.*setIndex"
    - from: "skills/execute/SKILL.md"
      to: "src/bin/rapid-tools.cjs"
      via: "commit-state CLI call"
      pattern: "execute commit-state"
---

<objective>
Hide waves from user-facing commands, add workflow awareness banner, update STATE.md after execute, and ensure all next-step references use set-level commands.

Purpose: Per locked decisions, waves become an internal implementation detail. Every user-facing skill must reference set-level commands only. The init completion must show the lifecycle, and execute must commit state on completion.

Output: 11 updated files across skills and core modules.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37.1-feature-changes-and-fixes/37.1-CONTEXT.md
@.planning/phases/37.1-feature-changes-and-fixes/37.1-RESEARCH.md

<interfaces>
<!-- Current next-step references that need updating -->

From skills/set-init/SKILL.md (line 171):
```markdown
> **Next step:** `/rapid:discuss {setIndex}.1`
> *(Discuss wave 1 of {setId})*
```
CHANGE TO: `/rapid:discuss {setIndex}` (set-level, no wave)

From skills/discuss/SKILL.md (line 331):
```markdown
> **Next step:** `/rapid:wave-plan {setIndex}.{waveIndex}`
```
CHANGE TO: handled by Plan 02 (discuss rewrite)

From skills/execute/SKILL.md (line 498):
```markdown
> **Next step:** `/rapid:review {setIndex}`
```
KEEP as-is (already set-level) but ADD state commit before this

From skills/plan-set/SKILL.md (line 560):
```markdown
> **Next step:** `/rapid:execute {SET_INDEX}`
```
KEEP as-is (already set-level)

From skills/status/SKILL.md (line 88, 105-106):
```markdown
> **Tip:** Use numeric shorthand (e.g., `/rapid:set-init 1`, `/rapid:wave-plan 1.1`)
- `/rapid:wave-plan auth wave-1` becomes `/rapid:wave-plan 1.1`
```
REMOVE wave-plan references

From skills/wave-plan/SKILL.md (line 491):
```markdown
> **Next step:** `/rapid:execute {setIndex}`
```
ADD internal-only note at top of file

From src/modules/core/core-identity.md (lines 23-27):
```markdown
3. **discuss** -- Capture developer implementation vision per wave
4. **wave-plan** -- Research specifics and plan jobs for a wave
```
CHANGE TO: set-level descriptions, remove wave-plan
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update help, status, and core-identity for wave hiding</name>
  <files>skills/help/SKILL.md, skills/status/SKILL.md, src/modules/core/core-identity.md</files>
  <action>
    **skills/help/SKILL.md** -- Full update of the static help content:

    1. Update the workflow diagram at the top:
       ```
       INIT --> SET-INIT --> DISCUSS --> PLAN --> EXECUTE --> REVIEW --> MERGE --> CLEANUP
       ```
       Remove the CONTEXT line (it is part of init). Remove wave-plan references.

    2. Update Available Commands tables:
       - Planning section: Remove any mention of wave-plan. Update discuss description to "Capture implementation vision for a set before detailed planning". Update plan description to "Plan all waves in a set with automatic sequencing".
       - Add new section "Utility" with:
         | `/rapid:quick` | Run a quick task on the current branch (tracked in project state) |
         | `/rapid:migrate` | Migrate an existing project from another framework (GSD, openspec, etc.) to RAPID |
       - Remove Internal / Developer section or keep only build-agents.

    3. Update Typical Workflow section to match new lifecycle:
       1. `/rapid:install` -- setup
       2. `/rapid:init` -- project init
       3. For each set: `/rapid:set-init` -> `/rapid:discuss` -> `/rapid:plan` -> `/rapid:execute` -> `/rapid:review`
       4. After sets: `/rapid:merge` -> `/rapid:cleanup`
       Remove wave-plan from the workflow. Remove context as separate step (part of init).

    4. Update version footer: `RAPID v2.2 | 17 commands | ...`

    **skills/status/SKILL.md** -- Add --detail flag:

    1. In Step 2: Check if user passed `--detail` flag. Store as boolean.

    2. In Step 3 Display Dashboard:
       - DEFAULT (no --detail): Show sets only with wave count summary:
         ```
         1: set-01-foundation [executing]   2/3 waves complete
         2: set-02-api [pending]             0/2 waves
         ```
         Do NOT show individual waves or jobs.
       - WITH --detail: Show the full set > wave > job hierarchy as currently implemented (keep existing behavior).

    3. Update tip line: Remove wave-plan reference. Change to:
       `> **Tip:** Use numeric shorthand (e.g., /rapid:set-init 1, /rapid:discuss 1). Use --detail for wave-level breakdown.`

    4. In Step 4 action conversions: Remove wave-plan conversion examples. Only show set-level command conversions.

    **src/modules/core/core-identity.md** -- Update workflow sequence:

    1. Change the RAPID Workflow section to:
       ```
       1. **init** -- Research and generate project roadmap
       2. **set-init** -- Claim a set, create isolated worktree
       3. **discuss** -- Capture developer implementation vision for the set
       4. **plan** -- Plan all waves and jobs for the set
       5. **execute** -- Dispatch parallel agents per job
       6. **review** -- Unit test, adversarial bug hunt, UAT
       7. **merge** -- Merge set branch into main with conflict resolution
       ```
       Remove wave-plan. Change discuss description from "per wave" to "for the set". Change step numbering to reflect removal.

    2. Update the "Steps 3-6 repeat for each wave" note to: "Steps 2-7 repeat for each set in the milestone."

    After editing core-identity.md, rebuild agents:
    ```bash
    cd ~/Projects/RAPID_alt && node src/bin/rapid-tools.cjs build-agents
    ```
    This propagates the workflow change to all 31 agent files.
  </action>
  <verify>
    <automated>cd ~/Projects/RAPID_alt && ! grep -q "rapid:wave-plan" skills/help/SKILL.md && grep -q "rapid:migrate" skills/help/SKILL.md && grep -q "rapid:quick" skills/help/SKILL.md && grep -q "\-\-detail" skills/status/SKILL.md && ! grep -q "wave-plan" src/modules/core/core-identity.md && echo "PASS"</automated>
  </verify>
  <done>help/SKILL.md has no wave-plan refs, lists /migrate and /quick; status/SKILL.md has --detail flag for wave expansion; core-identity.md has updated workflow without wave-plan; all agents rebuilt</done>
</task>

<task type="auto">
  <name>Task 2: Update init, set-init, execute, and remaining skills for wave hiding + state commit</name>
  <files>skills/init/SKILL.md, skills/set-init/SKILL.md, skills/execute/SKILL.md, skills/plan-set/SKILL.md, skills/wave-plan/SKILL.md, skills/review/SKILL.md, skills/merge/SKILL.md, skills/cleanup/SKILL.md</files>
  <action>
    **skills/init/SKILL.md** -- Add lifecycle banner:

    1. Between Step 10 (completion summary) and Step 11 (next step), add a new section:

       ```markdown
       ## Lifecycle

       After the summary, display the RAPID workflow for each set:

       ```
       The RAPID workflow for each set follows this sequence:

       1. set-init
       2. discuss
       3. plan
       4. execute
       5. review
       6. merge
       7. cleanup
       ```

       Only show this after project-level init. No visual flair -- flat numbered list.
       ```

    2. This is per locked decision: "After /rapid:init completes, show the full lifecycle as a simple numbered list."

    **skills/set-init/SKILL.md** -- Update next step:

    1. Change Step 5 next step from:
       ```
       > **Next step:** `/rapid:discuss {setIndex}.1`
       > *(Discuss wave 1 of {setId})*
       ```
       To:
       ```
       > **Next step:** `/rapid:discuss {setIndex}`
       > *(Discuss set {setId})*
       ```

    **skills/execute/SKILL.md** -- Add STATE.md commit and update references:

    1. In Step 4 (Final Summary), BEFORE the next-step suggestion, add:
       ```markdown
       ### Commit Execution State

       Commit the final state to ensure it reflects execution completion:

       ```bash
       node "${RAPID_TOOLS}" execute commit-state "chore({setId}): execution complete"
       ```
       ```

    2. Update any references to `/rapid:discuss` and `/rapid:wave-plan` in the Important Notes:
       - Change "Run `/rapid:discuss` and `/rapid:wave-plan` first" to "Run `/rapid:discuss` and `/rapid:plan` first"
       - Change all user-facing wave-plan references to plan references
       - Keep internal `wave-plan list-jobs` CLI calls as-is (those are internal tool commands, not user-facing)

    **skills/plan-set/SKILL.md** -- Minor updates:

    1. Ensure all user-facing text refers to this as `/rapid:plan` (not `/rapid:plan-set`). The frontmatter and internal logic stay the same.
    2. Update the error message for undiscussed waves: change "Run /rapid:discuss for each undiscussed wave" to "Run /rapid:discuss {setIndex} to discuss the set first"
    3. Keep the internal wave-plan pipeline references (these are implementation details).

    **skills/wave-plan/SKILL.md** -- Mark as internal:

    1. Add a prominent note at the top (after frontmatter, before title):
       ```markdown
       > **Internal skill.** This is invoked programmatically by `/rapid:plan` (plan-set). Users should not call this directly. It is not listed in `/rapid:help`.
       ```

    **skills/review/SKILL.md** -- Check and update next-step references:

    1. Verify Step 6 next steps are set-level (they already are: `/rapid:merge {setIndex}`). No changes needed unless wave-plan is referenced anywhere.
    2. Search for any `wave-plan` references in the file and replace user-facing ones. Keep internal `wave-plan list-jobs` CLI calls.

    **skills/merge/SKILL.md** -- Verify next-step references:

    1. Step 9 already shows `/rapid:cleanup` and `/rapid:status` -- these are fine. No changes needed.

    **skills/cleanup/SKILL.md** -- Verify:

    1. No wave-plan references expected. Verify and leave unchanged.
  </action>
  <verify>
    <automated>cd ~/Projects/RAPID_alt && grep -q "set-init" skills/init/SKILL.md && grep -rn "rapid:discuss.*\." skills/set-init/SKILL.md | grep -v "^#" | wc -l | grep -q "^0$" && grep -q "commit-state" skills/execute/SKILL.md && ! grep -q "rapid:wave-plan" skills/execute/SKILL.md skills/plan-set/SKILL.md skills/set-init/SKILL.md && grep -q "Internal skill" skills/wave-plan/SKILL.md && echo "PASS"</automated>
  </verify>
  <done>init shows lifecycle banner; set-init next step is /rapid:discuss {setIndex} (no wave); execute commits state and has no user-facing wave-plan refs; plan-set referenced as /rapid:plan; wave-plan marked internal; review/merge/cleanup verified clean</done>
</task>

</tasks>

<verification>
Comprehensive grep across ALL skill files to confirm no user-facing wave-plan leaks:
```bash
cd ~/Projects/RAPID_alt
# Should return ZERO results (excluding wave-plan/SKILL.md itself and internal CLI calls):
grep -rn "rapid:wave-plan" skills/*/SKILL.md | grep -v "wave-plan/SKILL.md" | grep -v "Internal skill"

# Verify /rapid:discuss references use set-level (no dot notation):
grep -rn "rapid:discuss.*\.[0-9]" skills/*/SKILL.md

# Verify lifecycle banner in init:
grep -c "set-init" skills/init/SKILL.md

# Verify commit-state in execute:
grep -c "commit-state" skills/execute/SKILL.md
```
</verification>

<success_criteria>
- Zero user-facing references to /rapid:wave-plan across all skills
- Zero wave-level discuss references (e.g., /rapid:discuss 1.1) in next-step suggestions
- /rapid:help lists /rapid:migrate and /rapid:quick
- /rapid:help does not list /rapid:wave-plan
- /rapid:status default view shows sets only, --detail shows waves
- /rapid:init shows lifecycle numbered list after completion
- /rapid:execute commits STATE.md via commit-state before suggesting next step
- core-identity.md workflow sequence has no wave-plan step
- All 31 agents rebuilt with updated core-identity
</success_criteria>

<output>
After completion, create `.planning/phases/37.1-feature-changes-and-fixes/37.1-03-SUMMARY.md`
</output>
