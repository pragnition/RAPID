---
phase: 37.1
slug: feature-changes-and-fixes
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-11
---

# Phase 37.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | node:test (built-in, Node.js 18+) |
| **Config file** | none — tests use `node --test` directly |
| **Quick run command** | `node --test src/lib/{module}.test.cjs` |
| **Full suite command** | `node --test src/lib/*.test.cjs src/bin/*.test.cjs` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `node --test src/lib/{changed_module}.test.cjs`
- **After every plan wave:** Run `node --test src/lib/*.test.cjs src/bin/*.test.cjs`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 10 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 37.1-01-01 | 01 | 1 | Quick task schema | unit | `node --test src/lib/state-schemas.test.cjs` | Exists (update) | pending |
| 37.1-01-02 | 01 | 1 | Quick task CRUD | unit | `node --test src/lib/quick.test.cjs` | New (Wave 0) | pending |
| 37.1-01-03 | 01 | 1 | Migration detection | unit | `node --test src/lib/migrate.test.cjs` | New (Wave 0) | pending |
| 37.1-01-04 | 01 | 1 | Migration transform | unit | `node --test src/lib/migrate.test.cjs` | New (Wave 0) | pending |
| 37.1-01-05 | 01 | 1 | State commit after execute | manual-only | Skill is Markdown, not testable | N/A | pending |
| 37.1-01-06 | 01 | 1 | Status --detail flag | unit | `node --test src/lib/worktree.test.cjs` | Exists (update) | pending |

*Status: pending · green · red · flaky*

---

## Wave 0 Requirements

- [ ] `src/lib/quick.cjs` + `src/lib/quick.test.cjs` — quick-task tracking logic stubs
- [ ] `src/lib/migrate.cjs` + `src/lib/migrate.test.cjs` — migration logic stubs
- [ ] Update `src/lib/state-schemas.test.cjs` — cover quickTasks optional field

*Existing infrastructure covers unit test framework; only new test files needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| State commit after execute | STATE.md updated on execute completion | Skill is Markdown orchestration, not testable as unit | Run `/rapid:execute` on a test set, verify STATE.md is updated |
| Discussion flow UX | Single-round set-level discussion | Interactive AskUserQuestion flow | Run `/rapid:discuss` and verify single-round per area, final "Looks good" gate |
| Workflow awareness banner | Lifecycle shown after init | Output verification in skill | Run `/rapid:init` and verify numbered lifecycle list appears |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
