---
phase: 38-state-machine-simplification
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - src/lib/state-machine.cjs
  - src/lib/state-machine.test.cjs
  - src/lib/state-machine.lifecycle.test.cjs
  - src/lib/dag.state-alignment.test.cjs
  - src/lib/phase17-integration.test.cjs
autonomous: true
requirements: [STATE-01, STATE-03, STATE-04, STATE-05]

must_haves:
  truths:
    - "transitionSet follows the transaction pattern: acquire lock, read state, validate, mutate, atomic write, release lock"
    - "findWave, findJob, transitionWave, transitionJob, deriveWaveStatus, deriveSetStatus are removed from exports"
    - "withStateTransaction helper acquires lock once and writes inline (no double-lock)"
    - "validateDiskArtifacts returns warnings when state says planning but no CONTEXT.md exists"
    - "validateDiskArtifacts returns warnings when state says executing but no wave plans exist"
    - "validateDiskArtifacts never modifies STATE.json (read-only, advisory warnings)"
    - "detectCorruption identifies bad JSON and bad schema in STATE.json"
    - "recoverFromGit restores STATE.json from the last git commit"
    - "Atomic writes use tmp-file-then-rename pattern leaving no .tmp files"
    - "Sets are independent -- transitioning one set never reads another set's status"
    - "After crash (process kill mid-write), detectCorruption identifies the bad state"
  artifacts:
    - path: "src/lib/state-machine.cjs"
      provides: "Core state machine with simplified set-only operations"
      exports: ["createInitialState", "readState", "writeState", "withStateTransaction", "findMilestone", "findSet", "transitionSet", "addMilestone", "validateDiskArtifacts", "detectCorruption", "recoverFromGit", "commitState"]
    - path: "src/lib/state-machine.test.cjs"
      provides: "Unit tests for state machine core operations"
      min_lines: 200
    - path: "src/lib/state-machine.lifecycle.test.cjs"
      provides: "Lifecycle integration tests for transition flows and crash recovery"
      min_lines: 200
  key_links:
    - from: "src/lib/state-machine.cjs"
      to: "src/lib/state-schemas.cjs"
      via: "imports ProjectState for validation"
      pattern: "require.*state-schemas"
    - from: "src/lib/state-machine.cjs"
      to: "src/lib/state-transitions.cjs"
      via: "imports validateTransition for status checks"
      pattern: "require.*state-transitions"
    - from: "src/lib/state-machine.cjs"
      to: "src/lib/lock.cjs"
      via: "imports acquireLock for mutex on state writes"
      pattern: "require.*lock"
    - from: "src/lib/state-machine.cjs"
      to: "git checkout HEAD"
      via: "recoverFromGit restores STATE.json from last commit"
      pattern: "execFileSync.*git.*checkout.*HEAD"
---

<objective>
Rewrite state-machine.cjs to remove all wave/job functions (findWave, findJob, transitionWave, transitionJob, deriveWaveStatus, deriveSetStatus), add withStateTransaction helper, add validateDiskArtifacts for STATE-04 bootstrap, and rewrite both test files. Also clean up ancillary test files (delete dag.state-alignment.test.cjs, update phase17-integration.test.cjs).

Purpose: This is the main state machine module that all RAPID commands import. After this plan, the state layer is fully simplified to set-level hierarchy with crash recovery preserved.
Output: state-machine.cjs rewritten (~160 lines, down from 462), 2 test files rewritten, 1 test file deleted, 1 test file updated.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-state-machine-simplification/38-CONTEXT.md
@.planning/phases/38-state-machine-simplification/38-RESEARCH.md
@.planning/phases/38-state-machine-simplification/38-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (completed): -->

From src/lib/state-schemas.cjs:
```javascript
const SetStatus = z.enum(['pending', 'discussing', 'planning', 'executing', 'complete', 'merged']);
const SetState = z.object({ id: z.string(), status: SetStatus.default('pending') });
const MilestoneState = z.object({ id: z.string(), name: z.string(), sets: z.array(SetState).default([]) });
const ProjectState = z.object({
  version: z.literal(1), projectName: z.string(), currentMilestone: z.string(),
  milestones: z.array(MilestoneState).default([]), lastUpdatedAt: z.string(), createdAt: z.string(),
});
module.exports = { SetStatus, SetState, MilestoneState, ProjectState };
```

From src/lib/state-transitions.cjs:
```javascript
// validateTransition(currentStatus, nextStatus) -- 2 args, NOT 3
// SET_TRANSITIONS = { pending: ['discussing', 'planning'], discussing: ['planning'], ... }
module.exports = { SET_TRANSITIONS, validateTransition };
```

From src/lib/lock.cjs:
```javascript
module.exports = { acquireLock, isLocked, ensureLocksDir, cleanStaleLocks };
```

From src/lib/init.cjs (used by phase17-integration.test.cjs):
```javascript
const { scaffoldProject } = require('./init.cjs');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite state-machine.cjs</name>
  <files>src/lib/state-machine.cjs</files>
  <behavior>
    - createInitialState('MyProject', 'v1.0') returns valid ProjectState with empty sets
    - readState(cwd) returns null for missing file, { valid: false, errors } for bad JSON, { valid: true, state } for valid
    - writeState(cwd, state) validates, acquires lock('state'), writes atomically via tmp+rename, releases lock
    - withStateTransaction(cwd, fn) acquires lock ONCE, reads, calls fn(state), validates, writes atomically, releases -- no double-lock
    - findMilestone and findSet return entity or throw
    - findWave, findJob, transitionWave, transitionJob, deriveWaveStatus, deriveSetStatus are NOT exported
    - transitionSet uses withStateTransaction, calls validateTransition with 2 args
    - validateDiskArtifacts returns warnings array, never writes STATE.json
    - detectCorruption identifies bad JSON and bad schema
    - recoverFromGit restores from last git commit
  </behavior>
  <action>
Rewrite src/lib/state-machine.cjs as a clean module. Target ~160 lines.

Keep these functions (simplified):
- `createInitialState(projectName, milestoneName)` -- unchanged
- `readState(cwd)` -- unchanged
- `writeState(cwd, state)` -- change lock name from 'state-machine' to 'state'
- `findMilestone(state, milestoneId)` -- unchanged
- `findSet(state, milestoneId, setId)` -- unchanged
- `addMilestone(cwd, milestoneId, name, carryForwardSets)` -- unchanged
- `detectCorruption(cwd)` -- unchanged
- `recoverFromGit(cwd)` -- unchanged
- `commitState(cwd, message)` -- unchanged

Add these new functions:
- `withStateTransaction(cwd, mutationFn)` -- per 38-RESEARCH.md Pattern 3. Acquires lock('state'), reads state, calls mutationFn(state) which mutates in-place, validates with ProjectState.parse, updates lastUpdatedAt, atomic write via tmp+rename, releases lock. Returns validated state.
- `validateDiskArtifacts(cwd, milestoneId, setId)` -- per 38-RESEARCH.md Pattern 4. Reads state synchronously (readState is async, so use sync read+parse inline or make this function async). Checks CONTEXT.md exists for planning/executing/complete/merged, checks wave plan directory exists for executing/complete/merged. Returns array of { type, message } objects. NEVER writes state.
- `transitionSet(cwd, milestoneId, setId, newStatus)` -- simplified to use withStateTransaction. Inside mutationFn: find set, call validateTransition(set.status, newStatus) with 2 args (not 3), set.status = newStatus. No wave/job derivation.

Remove completely (no stubs): findWave, findJob, transitionWave, transitionJob, deriveWaveStatus, deriveSetStatus, isDerivedStatusValid, WAVE_STATUS_ORDER, SET_STATUS_ORDER.

CRITICAL: validateTransition is called with 2 args (currentStatus, nextStatus), not 3. Plan 01 changed the signature.

CRITICAL: withStateTransaction acquires lock and writes inline. transitionSet uses withStateTransaction. writeState has its own lock. Do NOT call writeState from inside withStateTransaction -- double-lock deadlock.

For validateDiskArtifacts disk paths:
- CONTEXT.md: path.join(cwd, '.planning', 'sets', setId, 'CONTEXT.md')
- Wave plans dir: path.join(cwd, '.planning', 'waves', setId)

Export: createInitialState, readState, writeState, withStateTransaction, findMilestone, findSet, transitionSet, addMilestone, validateDiskArtifacts, detectCorruption, recoverFromGit, commitState
  </action>
  <verify>Run: node -e "const sm = require('./src/lib/state-machine.cjs'); const keys = Object.keys(sm); console.log(keys.join(', ')); const bad = ['findWave','findJob','transitionWave','transitionJob','deriveWaveStatus','deriveSetStatus'].filter(k => keys.includes(k)); if(bad.length) process.exit(1); const need = ['withStateTransaction','validateDiskArtifacts'].filter(k => !keys.includes(k)); if(need.length) process.exit(1);"</verify>
  <done>state-machine.cjs exports exactly 12 functions (no wave/job functions), includes withStateTransaction and validateDiskArtifacts, module loads without error</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite state-machine.test.cjs and state-machine.lifecycle.test.cjs</name>
  <files>src/lib/state-machine.test.cjs, src/lib/state-machine.lifecycle.test.cjs</files>
  <behavior>
    state-machine.test.cjs (unit tests):
    - createInitialState produces valid ProjectState
    - readState returns null for missing, { valid: false } for bad JSON, { valid: true, state } for valid
    - writeState round-trip preserves data, updates lastUpdatedAt, leaves no .tmp file
    - withStateTransaction acquires lock, mutates state, writes atomically -- verify no double-lock by checking isLocked is false after completion
    - findMilestone throws for unknown milestone
    - findSet throws for unknown set
    - Removed exports (findWave, findJob, transitionWave, transitionJob, deriveWaveStatus, deriveSetStatus) are undefined
    - transitionSet updates set status and persists to STATE.json
    - transitionSet rejects invalid transitions (throws)
    - addMilestone creates new milestone, updates currentMilestone, rejects duplicates
    - validateDiskArtifacts returns empty array when artifacts match status
    - validateDiskArtifacts returns warning for planning status without CONTEXT.md
    - validateDiskArtifacts returns warning for executing status without wave plans dir
    - validateDiskArtifacts returns error for missing/invalid STATE.json
    - validateDiskArtifacts does NOT write to STATE.json (check mtime before/after)
    - detectCorruption returns { exists: false } for missing file
    - detectCorruption returns { exists: true, corrupt: true } for bad JSON
    - detectCorruption returns { exists: true, corrupt: true } for bad schema
    - detectCorruption returns { exists: true, corrupt: false } for valid state
    - Sets are independent: transitioning set A does not read set B's status (verify with 2 sets in different states)

    state-machine.lifecycle.test.cjs (integration tests):
    - Full lifecycle: pending -> discussing -> planning -> executing -> complete -> merged via transitionSet
    - Skip lifecycle: pending -> planning -> executing -> complete -> merged
    - Crash recovery: corrupt JSON detected, recoverFromGit restores, state is valid after recovery
    - Crash recovery: bad schema detected, recoverFromGit restores
    - Atomic write: no .tmp files left after normal write or after error during validation
    - commitState: git add + commit succeeds, second commit with no changes returns { committed: false }
  </behavior>
  <action>
Rewrite BOTH test files from scratch. Use `node:test` (describe/it/beforeEach/afterEach) and `node:assert/strict`.

For both test files, each test needs a temp directory with:
- A git repo initialized (git init, git add, git commit)
- .planning/ directory with a valid STATE.json (use createInitialState + writeState)
- A milestone and at least one set added via addMilestone

state-machine.test.cjs focuses on individual function behavior (unit tests). Each describe block tests one exported function. Include a "removed exports" describe block that asserts findWave/findJob/transitionWave/transitionJob/deriveWaveStatus/deriveSetStatus are all undefined on the module exports.

state-machine.lifecycle.test.cjs focuses on end-to-end flows:
1. Full forward chain: create state, add set, transition through ALL statuses in order, verify STATE.json at each step
2. Skip chain: pending -> planning directly (skip discussing)
3. Crash recovery: write corrupt JSON manually, call detectCorruption (assert corrupt: true), call recoverFromGit, call detectCorruption again (assert corrupt: false)
4. Set independence: create 2 sets, transition set A to 'executing' while set B stays 'pending', verify both states are correct

For validateDiskArtifacts tests, create the expected directory structure:
- .planning/sets/{setId}/CONTEXT.md for context checks
- .planning/waves/{setId}/ directory for wave plan checks

IMPORTANT: The old test files (752 + 682 lines) tested wave/job transitions extensively. ALL those tests are deleted. Write new tests ONLY for the simplified set-only state machine. Target ~250-350 lines per test file.

IMPORTANT: Use ~ for home directory paths in any shell commands per project conventions.
  </action>
  <verify>node --test src/lib/state-machine.test.cjs src/lib/state-machine.lifecycle.test.cjs</verify>
  <done>All unit and lifecycle tests pass, coverage includes: every exported function, full and skip lifecycle paths, crash recovery, validateDiskArtifacts warnings, set independence, removed export assertions</done>
</task>

<task type="auto">
  <name>Task 3: Clean up ancillary test files</name>
  <files>src/lib/dag.state-alignment.test.cjs, src/lib/phase17-integration.test.cjs</files>
  <action>
1. DELETE src/lib/dag.state-alignment.test.cjs entirely. This test validates DAG output alignment with the Set > Wave > Job hierarchy. That hierarchy no longer exists. The DAG module itself will be updated in Phase 43/44 -- these tests test a contract that no longer exists.

2. UPDATE src/lib/phase17-integration.test.cjs:
   - In the "state-machine.cjs succeeds" test (line 37-44), remove the assertion `assert.ok(typeof stateMachine.transitionJob === 'function')` because transitionJob no longer exists
   - Replace it with `assert.ok(typeof stateMachine.transitionSet === 'function')` to verify the simplified API
   - All other tests in this file should still pass as-is (they test scaffoldProject, readState, and rapid-tools.cjs imports which are unchanged)
  </action>
  <verify>node --test src/lib/phase17-integration.test.cjs</verify>
  <done>dag.state-alignment.test.cjs is deleted, phase17-integration.test.cjs passes with transitionSet assertion instead of transitionJob</done>
</task>

</tasks>

<verification>
Run the full state layer test suite:

```bash
node --test src/lib/state-schemas.test.cjs src/lib/state-transitions.test.cjs src/lib/state-machine.test.cjs src/lib/state-machine.lifecycle.test.cjs src/lib/lock.test.cjs src/lib/phase17-integration.test.cjs
```

Verify dag.state-alignment.test.cjs is deleted:
```bash
test ! -f src/lib/dag.state-alignment.test.cjs && echo "DELETED" || echo "FAIL: still exists"
```

Verify no wave/job exports remain in state layer:
```bash
node -e "const sm = require('./src/lib/state-machine.cjs'); const bad = ['findWave','findJob','transitionWave','transitionJob','deriveWaveStatus','deriveSetStatus'].filter(k => typeof sm[k] === 'function'); if(bad.length) { console.error('FAIL:', bad); process.exit(1); } console.log('OK: no wave/job exports');"
```
</verification>

<success_criteria>
- state-machine.cjs exports exactly 12 functions (no wave/job functions)
- withStateTransaction prevents double-lock deadlock
- validateDiskArtifacts returns correct warnings for mismatched state/artifacts
- Full lifecycle (6 transitions) and skip lifecycle (5 transitions) both work
- Crash recovery (detectCorruption + recoverFromGit) works end-to-end
- dag.state-alignment.test.cjs is deleted
- phase17-integration.test.cjs passes with updated assertion
- All 6 test files pass: schemas, transitions, machine, lifecycle, lock, phase17-integration
</success_criteria>

<output>
After completion, create `.planning/phases/38-state-machine-simplification/38-02-SUMMARY.md`
</output>
