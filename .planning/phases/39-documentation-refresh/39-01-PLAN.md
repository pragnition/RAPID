---
phase: 39-documentation-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/planning.md
autonomous: true
requirements:
  - DOC-01
  - DOC-03

must_haves:
  truths:
    - "/rapid:discuss documented with <set-id> argument and set-level single-round description in both README.md and docs/planning.md"
    - "/rapid:plan-set does not appear as a separate command anywhere -- merged into /rapid:plan row/section"
    - "/rapid:wave-plan does not appear as a user-facing command in README.md command reference"
    - "No stale references to <wave-id>, 2-round, or 5-8 gray areas remain in either file"
  artifacts:
    - path: "README.md"
      provides: "Accurate command reference and quick start for post-37.1 interfaces"
      contains: "/rapid:discuss.*<set-id>"
    - path: "docs/planning.md"
      provides: "Accurate planning skill documentation for post-37.1 interfaces"
      contains: "/rapid:discuss <set-id>"
  key_links:
    - from: "README.md command reference"
      to: "skills/discuss/SKILL.md"
      via: "command argument accuracy"
      pattern: "discuss.*<set-id>"
    - from: "README.md command reference"
      to: "skills/plan-set/SKILL.md"
      via: "/rapid:plan row covers both decomposition and set-level planning"
      pattern: "plan.*none.*or.*set-id"
    - from: "docs/planning.md discuss section"
      to: "skills/discuss/SKILL.md"
      via: "section header and body text accuracy"
      pattern: "discuss <set-id>"
---

<objective>
Update README.md and docs/planning.md to accurately reflect post-37.1 interface changes.

Purpose: Phase 37.1 changed /rapid:discuss from wave-level to set-level with single-round discussion, renamed /rapid:plan-set to /rapid:plan, and made /rapid:wave-plan internal-only. Both documentation files still describe the old interfaces. This plan applies targeted edits to close DOC-01 and DOC-03.

Output: Two updated Markdown files with accurate command references and descriptions.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-documentation-refresh/39-RESEARCH.md
@.planning/v2.2-MILESTONE-AUDIT.md

Source-of-truth files (read these for current interfaces):
@skills/discuss/SKILL.md
@skills/plan-set/SKILL.md
@skills/wave-plan/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md command reference and quick start (DOC-01)</name>
  <files>README.md</files>
  <action>
Apply 6 targeted edits to README.md. Use the Edit tool for each change -- do NOT rewrite the entire file.

**Edit 1 -- Line 134-136 (Per-Set Development, Step 2):** Replace the discuss step:
Old:
```
2. **`/rapid:discuss <wave-id>`** -- Structured discussion about a wave's
   implementation approach. Surfaces gray areas and assumptions, lets you make design
   decisions before autonomous planning begins. Run once per wave.
```
New:
```
2. **`/rapid:discuss <set-id>`** -- Structured discussion about a set's
   implementation approach. Identifies product and UX gray areas across all waves,
   conducts a single-round discussion (one question per area), and splits decisions
   into per-wave WAVE-CONTEXT.md files. Run once per set.
```

**Edit 2 -- Lines 138-145 (Per-Set Development, Steps 3-4):** Replace wave-plan step and plan-set alternative with a single step 3 for /rapid:plan:
Old:
```
3. **`/rapid:wave-plan <wave-id>`** -- Full planning pipeline for a wave: spawns a
   research agent for implementation specifics, a wave planner for job decomposition,
   per-job planners for detailed implementation steps, and a verifier for plan
   consistency and contract compliance.

   Alternatively, use **`/rapid:plan-set <set-id>`** to plan all waves in one command
   with automatic dependency sequencing -- it runs the full wave-plan pipeline for
   each wave in dependency order.
```
New:
```
3. **`/rapid:plan <set-id>`** -- Plans all waves in a set with a single command.
   Detects dependencies between waves, groups them into parallel batches, then runs
   the full planning pipeline (research, wave plan, job plans, verification) for each
   wave in dependency order. Supports smart re-entry -- re-running skips already-planned
   waves.
```

**Edit 3 -- Line 200 (Command Reference Table):** Remove the `/rapid:plan-set` row and update the `/rapid:plan` row (line 199):
Old line 199:
```
| `/rapid:plan` | _(none)_ | Decompose work into parallelizable sets with contracts |
```
New:
```
| `/rapid:plan` | _(none)_ or `<set-id>` | Decompose into sets (no args) or plan all waves in a set |
```
Then delete line 200 (`/rapid:plan-set` row) entirely.

**Edit 4 -- Line 203 (Command Reference Table, discuss row):** Update the discuss arguments:
Old:
```
| `/rapid:discuss` | `<wave-id>` or `<set-id> <wave-id>` | Capture implementation vision for a wave |
```
New:
```
| `/rapid:discuss` | `<set-id>` | Capture implementation vision for a set |
```

**Edit 5 -- Line 204 (Command Reference Table):** Delete the `/rapid:wave-plan` row entirely. It is now internal-only.

**Edit 6 -- Line 217 (Notes section):** Remove the second bullet about wave-id dot notation. The entire Notes section becomes:
Old:
```
**Notes:**
- Commands accepting `<set-id>` support both string IDs (e.g., `auth-system`) and numeric indices (e.g., `1`).
- Commands accepting `<wave-id>` support dot notation (e.g., `1.1` for set 1, wave 1), string IDs, or the two-argument `<set-id> <wave-id>` form.
```
New:
```
**Notes:**
- Commands accepting `<set-id>` support both string IDs (e.g., `auth-system`) and numeric indices (e.g., `1`).
```

After all edits, verify no stale references remain:
```bash
grep -n "plan-set\|wave-id\|<wave-id>\|2-round\|5-8 gray\|/rapid:wave-plan" README.md
```
This command must return zero matches (exit code 1).
  </action>
  <verify>
    <automated>grep -cn "plan-set\|wave-id\|2-round\|5-8 gray\|/rapid:wave-plan" README.md | grep -q ":0$" && echo "PASS: no stale references" || echo "FAIL: stale references remain"</automated>
  </verify>
  <done>README.md command reference shows /rapid:discuss with set-id argument, /rapid:plan covers both decomposition and set-level planning, /rapid:wave-plan and /rapid:plan-set rows removed, wave-id dot notation note removed. Zero matches for stale patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/planning.md skill documentation (DOC-03)</name>
  <files>docs/planning.md</files>
  <action>
Apply 4 targeted edits to docs/planning.md. Use the Edit tool for each change -- do NOT rewrite the entire file.

**Edit 1 -- Line 5 (Argument patterns paragraph):** Remove the wave-id sentence:
Old:
```
**Argument patterns:** Commands accepting `<set-id>` support both string IDs (e.g., `auth-system`) and numeric indices (e.g., `1`). Commands accepting `<wave-id>` support dot notation (e.g., `1.1` for set 1, wave 1), string IDs (e.g., `wave-1`), or the two-argument `<set-id> <wave-id>` form.
```
New:
```
**Argument patterns:** Commands accepting `<set-id>` support both string IDs (e.g., `auth-system`) and numeric indices (e.g., `1`).
```

**Edit 2 -- Lines 19-23 (Discuss section):** Rewrite the discuss section header and body:
Old:
```
## `/rapid:discuss <wave-id>` or `<set-id> <wave-id>`

Captures implementation vision for a wave before autonomous planning begins. Reads the set's CONTRACT.json, DEFINITION.md, and SET-OVERVIEW.md, then identifies 5-8 gray areas where multiple valid approaches exist. You select which areas to discuss, then a structured 2-round conversation (approach selection followed by specifics confirmation) locks decisions for each. Decisions and Claude's discretion areas are written to WAVE-CONTEXT.md. Supports "Let Claude decide all" for full delegation.

See [skills/discuss/SKILL.md](../skills/discuss/SKILL.md) for full details.
```
New:
```
## `/rapid:discuss <set-id>`

Captures implementation vision for an entire set before autonomous planning begins. Reads the set's CONTRACT.json, DEFINITION.md, SET-OVERVIEW.md, and per-wave artifacts, then identifies product and UX gray areas across all waves. A single-round discussion asks one question per area with concrete options. Decisions are split into per-wave WAVE-CONTEXT.md files based on relevance. Supports "Revise" to re-identify areas if the developer wants changes.

See [skills/discuss/SKILL.md](../skills/discuss/SKILL.md) for full details.
```

**Edit 3 -- Lines 25-29 (Wave-plan section):** Add "Internal" label and update description:
Old:
```
## `/rapid:wave-plan <wave-id>` or `<set-id> <wave-id>`

Runs the full planning pipeline for a single wave: spawns a research agent for implementation specifics, a wave planner for job decomposition and file assignments, per-job planners for detailed implementation steps (parallel when 3+ jobs), and a plan verifier for coverage and consistency checks. Contract validation runs after verification -- major violations are escalated. Writes WAVE-RESEARCH.md, WAVE-PLAN.md, per-job JOB-PLAN.md files, and VERIFICATION-REPORT.md.

See [skills/wave-plan/SKILL.md](../skills/wave-plan/SKILL.md) for full details.
```
New:
```
## `/rapid:wave-plan` (internal)

> **Internal skill.** Invoked programmatically by `/rapid:plan <set-id>`. Users should not call this directly.

Runs the full planning pipeline for a single wave: spawns a research agent for implementation specifics, a wave planner for job decomposition and file assignments, per-job planners for detailed implementation steps (parallel when 3+ jobs), and a plan verifier for coverage and consistency checks. Contract validation runs after verification -- major violations are escalated. Writes WAVE-RESEARCH.md, WAVE-PLAN.md, per-job JOB-PLAN.md files, and VERIFICATION-REPORT.md.

See [skills/wave-plan/SKILL.md](../skills/wave-plan/SKILL.md) for full details.
```

**Edit 4 -- Lines 31-35 (Plan-set section):** Merge plan-set into the existing /rapid:plan section (lines 7-11). Delete the separate plan-set section and update the plan section:
Old plan section (lines 7-11):
```
## `/rapid:plan`

Decomposes the project's work into parallelizable sets -- independent workstreams that run simultaneously in isolated worktrees. Spawns a planner subagent that analyzes REQUIREMENTS.md, PROJECT.md, and any existing codebase context to propose sets with strict file ownership, interface contracts (JSON Schema), dependency edges, and a DAG. The proposal goes through a review loop: you approve, request modifications, or cancel. On approval, writes DEFINITION.md, CONTRACT.json, DAG.json, OWNERSHIP.json, and GATES.json to `.planning/sets/`.

See [skills/plan/SKILL.md](../skills/plan/SKILL.md) for full details.
```
New plan section:
```
## `/rapid:plan` or `/rapid:plan <set-id>`

**Without arguments:** Decomposes the project's work into parallelizable sets -- independent workstreams that run simultaneously in isolated worktrees. Spawns a planner subagent that analyzes REQUIREMENTS.md, PROJECT.md, and any existing codebase context to propose sets with strict file ownership, interface contracts (JSON Schema), dependency edges, and a DAG. The proposal goes through a review loop: you approve, request modifications, or cancel. On approval, writes DEFINITION.md, CONTRACT.json, DAG.json, OWNERSHIP.json, and GATES.json to `.planning/sets/`.

See [skills/plan/SKILL.md](../skills/plan/SKILL.md) for full details.

**With `<set-id>` argument:** Plans all waves in a set with a single command. Spawns a wave-analyzer agent to detect dependencies between waves, groups them into parallel batches, then runs the full wave-plan pipeline (research, wave plan, job plans, verify, validate) for each wave in dependency order. Independent waves plan in parallel batches; dependent waves plan sequentially with predecessor artifacts available. Supports smart re-entry -- re-running after partial completion skips already-planned waves. Fails fast if any wave has not been discussed.

See [skills/plan-set/SKILL.md](../skills/plan-set/SKILL.md) for full details.
```
Then delete the separate `## /rapid:plan-set <set-id>` section (lines 31-35) entirely.

Also update the opening sentence (line 3) from "Six skills" to "Five skills" since plan-set is now merged into plan and wave-plan is internal:
Old: `Six skills handle work decomposition, wave-level discussion, detailed job planning, and assumption validation. Planning runs after initialization and before execution.`
New: `Five skills handle work decomposition, set-level discussion, detailed job planning, and assumption validation. Planning runs after initialization and before execution.`

After all edits, verify no stale references remain:
```bash
grep -n "plan-set\|wave-id\|<wave-id>\|2-round\|5-8 gray" docs/planning.md
```
This command must return zero matches (exit code 1).
  </action>
  <verify>
    <automated>grep -cn "plan-set\|<wave-id>\|2-round\|5-8 gray" docs/planning.md | grep -q ":0$" && echo "PASS: no stale references" || echo "FAIL: stale references remain"</automated>
  </verify>
  <done>docs/planning.md discuss section describes set-level single-round flow with set-id argument. Plan-set merged into /rapid:plan section with both uses documented. Wave-plan marked as internal. Opening line updated. Zero matches for stale patterns.</done>
</task>

</tasks>

<verification>
Combined verification across both files:

```bash
# Must return zero matches (exit code 1) for ALL stale patterns
grep -rn "plan-set\|<wave-id>\|2-round\|5-8 gray" README.md docs/planning.md && echo "FAIL" || echo "PASS: all stale references removed"

# Must find the new correct patterns
grep -c "discuss.*<set-id>" README.md docs/planning.md
# Expected: README.md:2 (quick start + command ref), docs/planning.md:1 (section header)

# /rapid:plan row should cover both uses
grep "plan.*none.*set-id" README.md
# Expected: 1 match in command reference table
```
</verification>

<success_criteria>
1. README.md command reference shows `/rapid:discuss <set-id>` (not `<wave-id>`) and `/rapid:plan` covers both decomposition and set-level planning (not separate `/rapid:plan-set`)
2. docs/planning.md describes discuss as set-level single-round flow (not wave-level 2-round)
3. docs/planning.md merges plan-set into plan entry
4. Zero grep matches for stale patterns: `plan-set`, `<wave-id>`, `2-round`, `5-8 gray` across both files
</success_criteria>

<output>
After completion, create `.planning/phases/39-documentation-refresh/39-01-SUMMARY.md`
</output>
