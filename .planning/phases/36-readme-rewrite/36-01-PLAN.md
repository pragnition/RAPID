---
phase: 36-readme-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: false
requirements:
  - DOC-01
  - DOC-02

must_haves:
  truths:
    - "A newcomer reading the README understands what RAPID does and why it exists within the first two paragraphs"
    - "A user can follow the quick start from install through cleanup and successfully use RAPID on a real project"
    - "Every command listed in the command reference table matches an actual working skill with correct argument syntax"
    - "The architecture diagram conveys both the Sets/Waves/Jobs hierarchy and the agent dispatch pattern including merge subagent nesting"
    - "The README references technical_documentation.md as the power-user reference (not DOCS.md)"
  artifacts:
    - path: "README.md"
      provides: "Complete RAPID documentation landing page"
      min_lines: 200
      contains: "rapid:install"
  key_links:
    - from: "README.md command reference"
      to: "skills/*/SKILL.md"
      via: "command names and argument syntax"
      pattern: "/rapid:\\w+"
    - from: "README.md"
      to: "technical_documentation.md"
      via: "further reading link"
      pattern: "technical_documentation\\.md"
---

<objective>
Rewrite README.md from scratch to accurately describe RAPID's current capabilities through v2.2 including subagent merge delegation, with a full lifecycle quick start walkthrough, ASCII architecture diagram, and verified command reference.

Purpose: README.md is the repo landing page on GitHub -- it is the first thing any potential user sees. The current README (78 lines) is a quick-task stub from v1.0 that references outdated DOCS.md and omits v2.1/v2.2 capabilities entirely.

Output: A complete README.md (~250-350 lines) that serves both newcomers discovering RAPID and Claude Code power users looking for command reference.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-readme-rewrite/36-CONTEXT.md
@.planning/phases/36-readme-rewrite/36-RESEARCH.md

Source files to extract facts from (DO NOT guess -- read these):
@DOCS.md (v2.0 reference -- use for descriptions but cross-check for accuracy)
@README.md (current stub -- overwrite entirely)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write complete README.md from scratch</name>
  <files>README.md</files>
  <action>
Write README.md from scratch. The 36-RESEARCH.md file contains ALL verified source material needed: the command reference table (18 entries with verified arguments), the agent inventory (31 agents with recommended 5-7 for diagram), a diagram sketch, the workflow lifecycle grouped by phase, and problem-first opening inspiration.

Structure the README in this exact order (per locked decision: concept-explanation-first layout):

1. **Title + tagline**: "RAPID" with "Rapid Agentic Parallelizable and Isolatable Development" as subtitle. One-sentence value prop.

2. **Problem-first opening** (2-3 paragraphs): Start with the pain of multiple Claude Code users working on the same project -- merge conflicts, context pollution, no file ownership, agents overwriting each other. Then introduce RAPID as the solution. Tone: conversational-technical, like Vercel/Astro docs. Do NOT be fluffy or corporate.

3. **Install section**: Just the `claude plugin add fishjojo1/RAPID` command, brief.

4. **How It Works** (2-3 paragraphs): Explain the Sets/Waves/Jobs model in prose. Sets are independent workstreams (own worktree, own branch), Waves are dependency-ordered groups within a set, Jobs are atomic work units within a wave. Sets run in parallel across developers, waves run sequentially, jobs run in parallel via subagents. Mention the research pipeline, interface contracts, adversarial review, and intelligent merging briefly.

5. **Architecture Diagram**: A single combined Unicode box-drawing diagram inside a fenced code block (no language specifier). Use ONLY Unicode box-drawing characters (U+2500 series: ─ │ ┌ ┐ └ ┘ ├ ┤ ┬ ┴ ┼) for lines/corners. Regular ASCII for labels. Must show BOTH:
   - Top half: Sets/Waves/Jobs hierarchy (Milestone > Sets > Waves > Jobs) with developer labels showing parallel work
   - Bottom half: Agent dispatch pattern showing orchestrator dispatching to job-executor (parallel), scoper+reviewer (review), and set-merger -> conflict-resolver (merge nesting)
   Keep under 25 lines and under 80 characters wide. Use the sketch from 36-RESEARCH.md as a starting point but refine for clarity.

6. **Quick Start**: Group steps by phase per locked decision:
   - **Prerequisites**: Node.js 18+, git 2.30+, Claude Code (latest)
   - **Installation**: `/rapid:install`
   - **Project Setup**: `/rapid:init`, `/rapid:context` (brownfield only), `/rapid:plan`
   - **Per-Set Development**: `/rapid:set-init`, `/rapid:discuss`, `/rapid:wave-plan`, `/rapid:execute`, `/rapid:review` -- show the team angle here: "Developer A works on Set 1 while Developer B works on Set 2"
   - **Finalization**: `/rapid:merge`, `/rapid:cleanup`, `/rapid:new-milestone`

   For greenfield vs brownfield: use `<details>` + `<summary>` HTML tags. Both default to open. Remember: BLANK LINE required after `</summary>` and before Markdown content inside `<details>`. Each step shows command + what it does (not just a one-liner). Example: "`/rapid:init` -- Runs 5 parallel research agents to analyze your project domain, then generates a roadmap and scaffolds the `.planning/` directory."

   Show the team angle naturally: illustrate that sets run in parallel across developers as the default workflow, not a special mode.

7. **Command Reference**: An accurate table with columns: Command | Arguments | Description. Use the EXACT table from 36-RESEARCH.md (it was verified against SKILL.md files). Add notes below about numeric index support and dot notation for wave IDs.

8. **Further Reading**: Link to `technical_documentation.md` with a note like "(comprehensive reference for all 31 agents, configuration, state machines, and troubleshooting)". Do NOT reference DOCS.md (per locked decision). The file won't exist yet -- that is acceptable.

9. **License**: MIT -- see LICENSE.

Key constraints:
- Describe current state only -- NO version callouts, changelogs, or "What's New" sections (per locked decision)
- Primary audience: both newcomers AND power users -- layered writing (context for newcomers, skip-to-action for power users)
- Do NOT list all 31 agents anywhere -- the diagram shows 5-7, the rest belong in technical_documentation.md
- Use the 18-skill count as verified (assumptions, cleanup, context, discuss, execute, help, init, install, merge, new-milestone, pause, plan, plan-set, resume, review, set-init, status, wave-plan)
  </action>
  <verify>
    <automated>test -f /home/kek/Projects/RAPID/README.md && wc -l /home/kek/Projects/RAPID/README.md | awk '{if ($1 >= 200) print "PASS: "$1" lines"; else print "FAIL: only "$1" lines"}'</automated>
  </verify>
  <done>README.md exists, is 200+ lines, contains all required sections (problem opening, How It Works, architecture diagram, quick start with greenfield/brownfield, command reference table with 18 entries, further reading link to technical_documentation.md)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and factual verification of README</name>
  <action>User reviews the completed README.md for visual rendering, factual accuracy, tone, and completeness.</action>
  <what-built>Complete README.md rewrite covering all RAPID capabilities through v2.2, with problem-first opening, How It Works prose, Unicode architecture diagram, greenfield/brownfield quick start, and 18-command reference table</what-built>
  <how-to-verify>
    1. Open README.md in your editor or view on GitHub after pushing to a branch
    2. Verify the problem-first opening resonates -- does it accurately describe the pain of parallel Claude Code work?
    3. Verify the architecture diagram renders correctly -- box-drawing characters should form clean connected lines in monospace
    4. Verify the quick start covers both greenfield and brownfield paths with clear collapsible sections
    5. Verify the command reference table has all 18 commands with correct argument syntax
    6. Verify tone is conversational-technical (Vercel/Astro style) -- not corporate or fluffy
    7. Verify NO references to DOCS.md, NO version callouts or changelogs
    8. Verify technical_documentation.md is referenced as further reading
    9. Spot-check 2-3 command descriptions against their SKILL.md files for accuracy
  </how-to-verify>
  <verify>User approval at checkpoint</verify>
  <done>User types "approved" confirming README meets quality bar</done>
  <resume-signal>Type "approved" to finalize, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
Cross-reference verification (executor should perform before checkpoint):
- For each of the 18 commands in the reference table, verify the command name matches `skills/<name>/SKILL.md` and the argument syntax matches the skill's argument handling
- Verify the architecture diagram is under 80 characters wide (count characters in longest line)
- Verify `<details>` tags have blank lines before Markdown content
- Verify no references to DOCS.md exist in the final README
- Verify technical_documentation.md link is present
</verification>

<success_criteria>
- README.md is rewritten from scratch (not patched) and accurately describes all capabilities through v2.2 including subagent merge delegation (DOC-01)
- README.md includes a full lifecycle quick start walkthrough covering init through cleanup with accurate command names and arguments (DOC-02)
- README.md includes an ASCII architecture diagram showing the Sets/Waves/Jobs hierarchy and agent dispatch pattern (DOC-02)
- Every command listed in the command reference table exists as a working skill and has correct argument syntax (DOC-01)
- User approves the README at the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/36-readme-rewrite/36-01-SUMMARY.md`
</output>
