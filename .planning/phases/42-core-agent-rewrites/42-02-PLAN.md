---
phase: 42-core-agent-rewrites
plan: 02
type: execute
wave: 2
depends_on: [42-01]
files_modified:
  - agents/rapid-planner.md
  - agents/rapid-executor.md
autonomous: true
requirements: [AGENT-04]

must_haves:
  truths:
    - "rapid-planner.md has a hand-written role section describing v3 PLAN.md-based decomposition"
    - "rapid-executor.md has a hand-written role section describing PLAN.md task implementation"
    - "Both agents are under 12KB and classified as GUIDED with escape hatches"
    - "Both agents have updated identity sections matching v3 workflow from core-identity.md"
    - "Both agent files start with CORE comment (not STUB)"
  artifacts:
    - path: "agents/rapid-planner.md"
      provides: "Complete planner agent with v3 role section"
      contains: "# Role: Planner"
    - path: "agents/rapid-executor.md"
      provides: "Complete executor agent with v3 role section"
      contains: "# Role: Executor"
  key_links:
    - from: "agents/rapid-planner.md"
      to: "skills/*/SKILL.md"
      via: "Planner is spawned by plan-set skill"
      pattern: "PLAN.md"
    - from: "agents/rapid-executor.md"
      to: "skills/*/SKILL.md"
      via: "Executor is spawned by execute-set skill"
      pattern: "PLAN.md"
---

<objective>
Hand-write the `<role>` sections for the planner and executor core agents, replacing the Phase 42 TODO placeholders with v3-ready content.

Purpose: The planner and executor are the production/consumption pair for PLAN.md files -- the planner produces them, the executor implements them. Both need v3 role content.
Output: Two complete, hand-written core agent files (planner ~3.3KB role, executor ~2KB role).
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-core-agent-rewrites/42-CONTEXT.md
@.planning/phases/42-core-agent-rewrites/42-RESEARCH.md
@.planning/phases/42-core-agent-rewrites/42-01-SUMMARY.md

<!-- Source material for role content: -->
@src/modules/roles/role-planner.md
@src/modules/roles/role-executor.md

<!-- Updated identity to inline: -->
@src/modules/core/core-identity.md

<interfaces>
<!-- Current stub structure (both planner and executor follow this pattern): -->
<!-- Line 1: <!-- STUB: Core agent -- role section is hand-written in Phase 42 --> -->
<!-- Then YAML frontmatter (name, description, tools, model, color) -->
<!-- Then <identity> (inlined core-identity.md -- MUST be updated to v3 from Plan 01) -->
<!-- Then <conventions> (inlined core-conventions.md -- leave as-is) -->
<!-- Then <tools> (injected tool docs -- leave as-is) -->
<!-- Then <role> <!-- TODO: Phase 42 --> </role> (THIS IS WHAT WE WRITE) -->
<!-- Then <returns> (inlined core-returns.md -- leave as-is) -->

<!-- Planner stub: 8.7KB, leaves ~3.3KB budget for role section (12KB cap) -->
<!-- Executor stub: 8.4KB, leaves ~3.6KB budget for role section (12KB cap) -->

<!-- v3 Planner key differences from v2: -->
<!-- v2: Produces JSON set proposals with waves/jobs decomposition (264-line role) -->
<!-- v3: Produces PLAN.md per wave with tasks. Focus on decomposition thinking, not output format -->
<!-- v3: 2-4 agent spawns for planning (not 15-20). Single-pass planning. -->
<!-- v3: No wave/job state tracking. Set-level state only. -->

<!-- v3 Executor key differences from v2: -->
<!-- v2: Dispatched per-job by orchestrator, wave/job state tracking (69-line role) -->
<!-- v3: Reads PLAN.md, implements tasks, determines completion from artifacts -->
<!-- v3: No wave/job state. Re-entry after crash via artifact inspection. -->
<!-- v3: Atomic commits, set boundary enforcement, verification per task -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hand-write planner agent role section and update identity</name>
  <files>agents/rapid-planner.md</files>
  <action>
    Rewrite `agents/rapid-planner.md` as a complete hand-written core agent. The file must:

    1. **Change header comment** from `<!-- STUB: Core agent -- role section is hand-written in Phase 42 -->` to `<!-- CORE: Hand-written agent -- do not overwrite with build-agents -->`

    2. **Keep frontmatter** exactly as-is (name, description, tools, model, color)

    3. **Update `<identity>` section** -- Replace the inlined identity content with the updated v3 version from `src/modules/core/core-identity.md` (after Plan 01 updated it). Copy the full content of core-identity.md into the `<identity>` tags.

    4. **Keep `<conventions>`, `<tools>`, `<returns>` sections** exactly as-is from the existing stub

    5. **Write the `<role>` section** with the following structure (budget: ~3.3KB max):

    ```markdown
    <role>
    # Role: Planner

    You decompose project work into parallelizable sets with clear boundaries, producing PLAN.md files that executors implement without interpretation.

    ## Responsibilities
    - Analyze project requirements and codebase structure to identify natural work boundaries
    - Decompose work into independent sets that can execute in parallel
    - Produce one PLAN.md per wave with specific, actionable tasks
    - Define interface contracts between dependent sets
    - Ensure each set has clear file ownership (no overlapping modifications)

    ## Decomposition Process

    ### 1. Understand the Scope
    Read the project roadmap, requirements, and any CONTEXT.md from the discuss phase. Identify what needs to be built and what already exists.

    ### 2. Find Natural Boundaries
    Look for work that can proceed independently:
    - Features with no shared file modifications
    - Backend vs frontend splits
    - Data model vs API vs UI layers (when genuinely independent)
    Prefer vertical slices (full feature) over horizontal layers (all models, then all APIs).

    ### 3. Define Sets
    Each set must be:
    - **Independent**: Can be developed without waiting for other sets
    - **Mergeable**: Changes will not conflict with other sets' file modifications
    - **Testable**: Has its own verification criteria
    - **Bounded**: Clear list of files created or modified

    ### 4. Produce PLAN.md Files
    For each wave, produce a PLAN.md containing:
    - Objective (what and why)
    - Tasks with specific file paths, implementation actions, and verification commands
    - Success criteria that an executor can verify

    ### 5. Define Contracts
    When sets must interact (e.g., one produces types another consumes):
    - Define the interface contract explicitly (types, exports, API shapes)
    - Include the contract in both sets' PLAN.md files
    - The producing set creates the contract first; the consuming set implements against it

    ## Planning Principles
    - Plans are prompts -- they must be specific enough for an executor to implement without asking questions
    - Each task should modify 1-5 files. More than 5 signals the task should split.
    - Include "what NOT to do" when common mistakes are likely
    - Verification commands must be automated (not "check manually")
    - Budget each plan for ~50% context window to maintain quality

    ## Escape Hatches
    - If the project is too small for parallel sets, produce a single set with sequential waves
    - If requirements are ambiguous, surface assumptions explicitly rather than guessing
    - If a natural boundary doesn't exist, it's better to have sequential sets than to force artificial parallelism
    - If the codebase structure contradicts the roadmap decomposition, flag the conflict

    ## Constraints
    - Never produce plans that modify the same file in parallel sets (file ownership is exclusive)
    - Never reference wave/job state -- use set-level state only
    - Never embed implementation code in plans -- describe behavior, not code
    - Do not spawn subagents -- you are a leaf agent dispatched by the plan-set skill
    </role>
    ```

    Adapt the exact wording as needed to fit within the 3.3KB budget while covering all the above sections. Use the v2 role-planner.md as conceptual source material but do NOT port its JSON output format or wave/job decomposition language.

    6. **Verify file size**: Run `wc -c agents/rapid-planner.md` -- must be under 12,288 bytes (12KB).
  </action>
  <verify>
    <automated>cd /home/kek/Projects/RAPID && wc -c agents/rapid-planner.md | awk '{if ($1 <= 12288) print "PASS: planner is " $1 " bytes (under 12KB)"; else {print "FAIL: planner is " $1 " bytes (over 12KB)"; exit 1}}' && grep -q "# Role: Planner" agents/rapid-planner.md && grep -q "CORE: Hand-written" agents/rapid-planner.md && grep -q "plan-set" agents/rapid-planner.md && echo "PASS: planner role content verified"</automated>
  </verify>
  <done>
    - agents/rapid-planner.md starts with `<!-- CORE: Hand-written agent` comment
    - Contains `# Role: Planner` header in the `<role>` section
    - Identity section contains v3 workflow (plan-set, execute-set, start-set)
    - File is under 12KB
    - No references to JSON set proposals, wave/job state, or orchestrator
  </done>
</task>

<task type="auto">
  <name>Task 2: Hand-write executor agent role section and update identity</name>
  <files>agents/rapid-executor.md</files>
  <action>
    Rewrite `agents/rapid-executor.md` as a complete hand-written core agent. The file must:

    1. **Change header comment** from `<!-- STUB: ...` to `<!-- CORE: Hand-written agent -- do not overwrite with build-agents -->`

    2. **Keep frontmatter** exactly as-is

    3. **Update `<identity>` section** -- Replace with v3 content from `src/modules/core/core-identity.md`

    4. **Keep `<conventions>`, `<tools>`, `<returns>` sections** exactly as-is

    5. **Write the `<role>` section** with the following structure (budget: ~3.6KB max, but aim for ~1.5-2KB -- executor is compact):

    ```markdown
    <role>
    # Role: Executor

    You implement tasks from PLAN.md files, producing working code with atomic commits and verification at each step.

    ## Responsibilities
    - Read PLAN.md and implement each task in order
    - Make atomic commits per task (one logical change per commit)
    - Run verification commands after each task
    - Respect set file ownership boundaries
    - Determine completion state from planning artifacts

    ## Execution Flow

    ### 1. Load the Plan
    Read the PLAN.md for your assigned wave. Understand the objective, tasks, and success criteria before writing any code.

    ### 2. Implement Tasks Sequentially
    For each task in the plan:
    1. Read the task's action description and file list
    2. Implement the changes described
    3. Run the task's verification command
    4. If verification passes, commit with a descriptive message
    5. If verification fails, fix the issue before proceeding

    ### 3. Verify Completion
    After all tasks, run the plan's overall verification checks. Report success or failure.

    ## Artifact-Based Completion Detection
    Determine what work is done by inspecting planning artifacts:
    - A task is complete if its implementation commit exists and its verification passes
    - A plan is complete if all its tasks have passing verification
    - On re-entry after crash, check which tasks already have commits -- resume from the first incomplete task

    ## Commit Discipline
    - One commit per task (not per file, not per plan)
    - Commit message format: `feat(set-name): task description`
    - Never commit failing tests or broken builds
    - Stage only the files listed in the task's file list

    ## Escape Hatches
    - If a plan task is ambiguous, implement the most reasonable interpretation and note the assumption in the commit message
    - If a verification command fails due to environment issues (not code issues), skip it and note in the return
    - If a task requires modifying a file outside the set's ownership, flag it as BLOCKED rather than making the change

    ## Constraints
    - Never modify files outside your set's file ownership list
    - Never skip verification -- if a command fails, fix the code or report BLOCKED
    - Never spawn subagents -- you are a leaf agent dispatched by the execute-set skill
    - Never modify .planning/ files directly -- use rapid-tools.cjs CLI for state transitions
    </role>
    ```

    Adapt wording as needed. Use v2 role-executor.md (69 lines) as conceptual source but adapt for PLAN.md-based flow and artifact-based completion detection. Keep it compact (~1.5-2KB).

    6. **Verify file size**: `wc -c agents/rapid-executor.md` -- must be under 12,288 bytes.
  </action>
  <verify>
    <automated>cd /home/kek/Projects/RAPID && wc -c agents/rapid-executor.md | awk '{if ($1 <= 12288) print "PASS: executor is " $1 " bytes (under 12KB)"; else {print "FAIL: executor is " $1 " bytes (over 12KB)"; exit 1}}' && grep -q "# Role: Executor" agents/rapid-executor.md && grep -q "CORE: Hand-written" agents/rapid-executor.md && echo "PASS: executor role content verified" && node --test src/lib/build-agents.test.cjs</automated>
  </verify>
  <done>
    - agents/rapid-executor.md starts with `<!-- CORE: Hand-written agent` comment
    - Contains `# Role: Executor` header in the `<role>` section
    - Identity section contains v3 workflow
    - File is under 12KB
    - Describes artifact-based completion detection and PLAN.md-based execution
    - No references to wave/job state or orchestrator
    - build-agents tests pass
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `wc -c agents/rapid-planner.md agents/rapid-executor.md` -- both under 12,288 bytes
2. `grep "# Role: Planner" agents/rapid-planner.md` -- found
3. `grep "# Role: Executor" agents/rapid-executor.md` -- found
4. `grep "CORE: Hand-written" agents/rapid-planner.md agents/rapid-executor.md` -- found in both
5. `grep "TODO" agents/rapid-planner.md agents/rapid-executor.md` -- NOT found (Phase 42 TODOs replaced)
6. `node --test src/lib/build-agents.test.cjs` -- all pass
</verification>

<success_criteria>
- Planner agent has a complete, hand-written GUIDED role for v3 PLAN.md-based decomposition
- Executor agent has a complete, hand-written GUIDED role for PLAN.md task implementation
- Both agents under 12KB, with CORE comment prefix and v3 identity
- All build-agents tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/42-core-agent-rewrites/42-02-SUMMARY.md`
</output>
