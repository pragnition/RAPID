---
phase: 24-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DOCS.md
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "DOCS.md documents all 17 commands with descriptions, usage, and behavior details"
    - "DOCS.md has an accurate Quick Start showing the Mark II workflow (init through cleanup)"
    - "DOCS.md covers all v2.0 Key Concepts (Jobs, State Machine, Milestones, Set Init, Wave Discussion, Wave Planning, Review Pipeline, 5-Level Detection, 4-Tier Resolution, Bisection, Rollback)"
    - "DOCS.md workflow lifecycle section shows the complete per-set loop (set-init -> discuss -> wave-plan -> execute -> review)"
  artifacts:
    - path: "DOCS.md"
      provides: "Command reference, workflow lifecycle, key concepts for Mark II"
      contains: "/rapid:discuss"
    - path: "DOCS.md"
      provides: "All 6 new commands documented"
      contains: "/rapid:review"
  key_links:
    - from: "DOCS.md command descriptions"
      to: "skills/*/SKILL.md"
      via: "Command behavior derived from SKILL.md files"
      pattern: "/rapid:(discuss|wave-plan|set-init|review|new-milestone|resume)"
---

<objective>
Rewrite the first half of DOCS.md for Mark II: introduction, installation, quick start, all 17 command reference sections, workflow lifecycle, and key concepts.

Purpose: The current DOCS.md reflects v1.0 (11 commands, old workflow). Mark II has 17 commands, a different hierarchy (Sets/Waves/Jobs), and 6 entirely new commands that are undocumented. This plan creates the user-facing reference sections.

Output: DOCS.md rewritten from header through key concepts, with accurate v2.0 content derived from reading each SKILL.md file.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-documentation/24-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite DOCS.md with introduction, installation, quick start, and all 17 command reference sections</name>
  <files>DOCS.md</files>
  <action>
Rewrite DOCS.md from scratch. Read every SKILL.md file to get accurate, current descriptions for each command.

**Source files to read for command documentation:**
Read each of these to extract what the command does, its workflow, subagents spawned, and state transitions:
- skills/install/SKILL.md
- skills/init/SKILL.md
- skills/help/SKILL.md
- skills/context/SKILL.md
- skills/plan/SKILL.md
- skills/assumptions/SKILL.md
- skills/set-init/SKILL.md (NEW in v2.0)
- skills/discuss/SKILL.md (NEW in v2.0)
- skills/wave-plan/SKILL.md (NEW in v2.0)
- skills/execute/SKILL.md
- skills/review/SKILL.md (NEW in v2.0)
- skills/status/SKILL.md
- skills/pause/SKILL.md
- skills/resume/SKILL.md (NEW in v2.0)
- skills/merge/SKILL.md
- skills/cleanup/SKILL.md
- skills/new-milestone/SKILL.md (NEW in v2.0)

**Document structure to produce:**

```markdown
# RAPID - Plugin Documentation

[One paragraph description referencing Mark II and Sets/Waves/Jobs hierarchy]

**Version:** [read from .claude-plugin/plugin.json]

## Installation
[Keep existing install methods -- marketplace and git clone. Update requirements if needed.]

## Quick Start
[Rewrite to show Mark II workflow: init -> context -> plan -> (per set: set-init -> discuss -> wave-plan -> execute -> review) -> merge -> cleanup -> new-milestone]

## Available Commands
[17 command sections, each following this template:]

### /rapid:{name}
**{one-line description from SKILL.md frontmatter}**

What it does:
- {bullet points derived from SKILL.md behavior}

Usage:
```
/rapid:{name} [args if any]
```

{Additional details: modes, subagents spawned, state transitions, flags}
```

**Command grouping order (by workflow stage):**
1. Setup: install, init, help, context
2. Planning: plan, assumptions
3. Set lifecycle: set-init, discuss, wave-plan
4. Execution: execute, status, pause, resume
5. Quality: review
6. Integration: merge, cleanup
7. Lifecycle: new-milestone

**CRITICAL:** Do NOT copy v1.0 DOCS.md descriptions. Every command description must come from reading the actual current SKILL.md file. The v1.0 descriptions are inaccurate for: init, help, plan, execute, status, merge (all significantly changed). And 6 commands are entirely missing from v1.0 DOCS.md.

**Version number:** Read .claude-plugin/plugin.json to get the current version. Use that version in the docs header. Do not change plugin.json.

Leave a clear marker comment at the end of the file: `<!-- PLAN 02: Architecture, agents, libraries, CLI reference, state machine, configuration sections will be added below -->`
  </action>
  <verify>
    <automated>grep -c "/rapid:" DOCS.md | awk '{if ($1 >= 17) print "PASS: " $1 " command references found"; else print "FAIL: only " $1 " command references, need 17"}'</automated>
  </verify>
  <done>DOCS.md contains header, installation, quick start, and all 17 command reference sections with accurate v2.0 descriptions derived from SKILL.md files. Each command section has: one-line description, bullet list of behavior, usage example.</done>
</task>

<task type="auto">
  <name>Task 2: Add workflow lifecycle and key concepts sections to DOCS.md</name>
  <files>DOCS.md</files>
  <action>
Append workflow lifecycle and key concepts sections to DOCS.md after the command reference and before the plan-02 marker comment.

**Workflow Lifecycle section:**
Show the complete Mark II workflow as a diagram and explanation:

```
INSTALL -> INIT -> CONTEXT -> PLAN -> [ per set: SET-INIT -> DISCUSS -> WAVE-PLAN -> EXECUTE -> REVIEW ] -> MERGE -> CLEANUP -> NEW-MILESTONE
```

Explain each stage in 1-2 sentences. Emphasize:
- The per-set loop runs in parallel across developers
- Within each set, waves execute sequentially (wave 1 completes before wave 2)
- Within each wave, jobs execute in parallel
- Review gates prevent merging untested code

**Key Concepts section:**
Document these concepts (all must be present):

1. **Sets** -- Independent workstreams in isolated git worktrees (updated from v1.0)
2. **Waves** -- Dependency-ordered execution groups within sets (updated)
3. **Jobs** -- Granular work units within waves (NEW -- equivalent to v1.0 "plans")
4. **State Machine** -- Hierarchical JSON state (Project > Milestone > Set > Wave > Job) with validated transitions (NEW)
5. **Milestones** -- Version cycles with archive/re-plan lifecycle (NEW)
6. **Interface Contracts** -- Machine-verifiable JSON schemas (kept from v1.0)
7. **Set Initialization** -- Worktree + branch + scoped CLAUDE.md per set (NEW)
8. **Wave Discussion** -- Structured capture of implementation vision before planning (NEW)
9. **Wave Planning Pipeline** -- Research -> wave plan -> job plans -> contract validation (NEW)
10. **Review Pipeline** -- Unit test + adversarial bug hunt (hunter/advocate/judge) + UAT (NEW)
11. **5-Level Conflict Detection** -- Textual, structural, dependency, API, semantic (NEW)
12. **4-Tier Resolution Cascade** -- Deterministic, heuristic, AI-assisted, human escalation (NEW)
13. **Bisection Recovery** -- Binary search to isolate breaking set interactions (NEW)
14. **Rollback** -- Cascade revert for problematic merges (NEW)
15. **Planning Gates** -- Enforcement that all sets planned before execution (kept)
16. **Wave Reconciliation** -- Mandatory contract validation between waves (updated)
17. **File Ownership** -- Every file belongs to exactly one set (kept)

For state machine, include the transition diagrams:
```
Set:  pending -> planning -> executing -> reviewing -> merging -> complete
Wave: pending -> discussing -> planning -> executing -> reconciling -> complete
                                                                      failed -> executing (retry)
Job:  pending -> executing -> complete
                              failed -> executing (retry)
```

Read src/lib/state-transitions.cjs to verify these transitions are accurate.

**Prerequisites section:**
Keep the existing list, verify accuracy against src/lib/prereqs.cjs.
  </action>
  <verify>
    <automated>grep -c "##" DOCS.md | awk '{if ($1 >= 25) print "PASS: " $1 " sections found"; else print "FAIL: only " $1 " sections, expected 25+"}'</automated>
  </verify>
  <done>DOCS.md has complete workflow lifecycle diagram with per-stage explanations, all 17 key concepts documented including state machine transition diagrams, and prerequisites section.</done>
</task>

</tasks>

<verification>
- All 17 commands documented: grep for each `/rapid:` prefix
- 6 new commands present: discuss, wave-plan, set-init, review, new-milestone, resume
- Workflow lifecycle shows per-set loop
- State machine transitions documented
- No v1.0 stale descriptions (no "11 commands", no "6 agents", no "STATE.md" as primary state)
</verification>

<success_criteria>
- DOCS.md contains introduction, installation, quick start, 17 command reference sections, workflow lifecycle, 17 key concepts, state machine transitions, and prerequisites
- Every command description is derived from reading the current SKILL.md file, not from v1.0 DOCS.md
- The 6 new commands (discuss, wave-plan, set-init, review, new-milestone, resume) are all present
- File ends with a marker comment for Plan 02 to continue from
</success_criteria>

<output>
After completion, create `.planning/phases/24-documentation/24-01-SUMMARY.md`
</output>
