---
phase: 22-review-module
plan: 05
type: execute
wave: 1
depends_on: ["22-01"]
files_modified:
  - src/lib/review.cjs
  - .planning/phases/22-review-module/22-01-PLAN.md
autonomous: true
requirements: [REVW-01, REVW-04, REVW-07, REVW-08]
gap_closure: true

must_haves:
  truths:
    - "review.cjs does not reference state-machine.cjs as a dependency (speculative link removed)"
    - "22-01-PLAN.md key_links only contain verified wiring (execute.cjs link retained)"
  artifacts:
    - path: "src/lib/review.cjs"
      provides: "Review library without speculative state-machine dependency"
      contains: "execute.cjs: getChangedFiles"
    - path: ".planning/phases/22-review-module/22-01-PLAN.md"
      provides: "Updated plan with speculative key_link removed"
  key_links:
    - from: "src/lib/review.cjs"
      to: "src/lib/execute.cjs"
      via: "require('./execute.cjs') for getChangedFiles"
      pattern: "execute\\.getChangedFiles"
---

<objective>
Close the single verification gap in Phase 22: remove the speculative `review.cjs -> state-machine.cjs` key_link from 22-01-PLAN.md and clean up the corresponding "future use" comment in review.cjs.

Purpose: The VERIFICATION.md identified that 22-01-PLAN.md declares a key_link for `review.cjs -> state-machine.cjs` but review.cjs never actually requires or uses state-machine.cjs. State transitions are handled via CLI commands in SKILL.md orchestrators, making this link unnecessary. This plan removes the speculative dependency to bring the plan metadata in alignment with the actual implementation.
Output: Updated 22-01-PLAN.md (key_link removed), updated review.cjs (comment corrected)
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-review-module/22-VERIFICATION.md
@.planning/phases/22-review-module/22-01-PLAN.md
@.planning/phases/22-review-module/22-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove speculative state-machine key_link from 22-01-PLAN.md and clean up review.cjs comment</name>
  <files>src/lib/review.cjs, .planning/phases/22-review-module/22-01-PLAN.md</files>
  <action>
Two targeted changes:

1. **In `.planning/phases/22-review-module/22-01-PLAN.md`** frontmatter, remove the speculative key_link entry for state-machine.cjs. The key_links section should only retain the verified execute.cjs link:

```yaml
  key_links:
    - from: "src/lib/review.cjs"
      to: "src/lib/execute.cjs"
      via: "require('./execute.cjs') for getChangedFiles"
      pattern: "execute\\.getChangedFiles"
```

Remove this entry entirely:
```yaml
    - from: "src/lib/review.cjs"
      to: "src/lib/state-machine.cjs"
      via: "require('./state-machine.cjs') for state reads"
      pattern: "state-machine"
```

2. **In `src/lib/review.cjs`** (line 12), remove the speculative dependency comment. Change the JSDoc header from:

```
 * Depends on:
 *   - execute.cjs: getChangedFiles
 *   - state-machine.cjs: readState (future use)
```

To:

```
 * Depends on:
 *   - execute.cjs: getChangedFiles
```

This is a documentation-only change in review.cjs -- no functional code modifications. The module does not and has never required state-machine.cjs.
  </action>
  <verify>
    <automated>grep -c "state-machine" src/lib/review.cjs && echo "FAIL: state-machine still referenced" || echo "PASS: state-machine reference removed"; grep -c "state-machine" .planning/phases/22-review-module/22-01-PLAN.md && echo "FAIL: key_link still present" || echo "PASS: key_link removed"; node --test src/lib/review.test.cjs 2>&1 | tail -1</automated>
  </verify>
  <done>review.cjs no longer references state-machine.cjs in its dependency comment. 22-01-PLAN.md key_links section contains only the verified execute.cjs link. All existing review tests still pass (no functional changes).</done>
</task>

</tasks>

<verification>
- `grep -c "state-machine" src/lib/review.cjs` returns 0 (no references)
- `grep -c "state-machine" .planning/phases/22-review-module/22-01-PLAN.md` returns 0 (key_link removed)
- `node --test src/lib/review.test.cjs` passes (no regressions)
- `grep "execute.cjs" src/lib/review.cjs` still shows the require and dependency comment (retained)
</verification>

<success_criteria>
- Speculative state-machine.cjs key_link removed from 22-01-PLAN.md
- Speculative "future use" comment removed from review.cjs
- Verified execute.cjs link preserved in both files
- All 19 existing review tests pass without modification
- VERIFICATION.md gap is resolved: truth #19 no longer fails because the key_link no longer exists
</success_criteria>

<output>
After completion, create `.planning/phases/22-review-module/22-05-SUMMARY.md`
</output>
