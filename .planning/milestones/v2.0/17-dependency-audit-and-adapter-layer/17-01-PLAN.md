---
phase: 17-dependency-audit-and-adapter-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/17-dependency-audit-and-adapter-layer/DEPENDENCY-MAP.md
  - src/lib/state.cjs
  - src/lib/state.test.cjs
  - src/bin/rapid-tools.cjs
  - src/modules/core/core-state-access.md
  - src/modules/core/core-context-loading.md
autonomous: true
requirements: [STATE-04]

must_haves:
  truths:
    - "DEPENDENCY-MAP.md documents coupling for all 17+ lib modules and their filesystem conventions"
    - "state.cjs and state.test.cjs are deleted from the codebase"
    - "rapid-tools.cjs state commands use state-machine.cjs with hierarchy-aware API"
    - "Agent module .md files reference new CLI commands instead of old state get/update"
  artifacts:
    - path: ".planning/phases/17-dependency-audit-and-adapter-layer/DEPENDENCY-MAP.md"
      provides: "Complete module coupling documentation for downstream phases 18-24"
      contains: "state-machine.cjs"
    - path: "src/bin/rapid-tools.cjs"
      provides: "Rewritten handleState using state-machine.cjs"
      contains: "state-machine.cjs"
    - path: "src/modules/core/core-state-access.md"
      provides: "Updated CLI command reference for agents"
      contains: "STATE.json"
  key_links:
    - from: "src/bin/rapid-tools.cjs"
      to: "src/lib/state-machine.cjs"
      via: "require('../lib/state-machine.cjs')"
      pattern: "require.*state-machine"
---

<objective>
Delete the old state.cjs module, rewrite rapid-tools.cjs state CLI commands to use state-machine.cjs, update agent prompt modules referencing old CLI, and create the DEPENDENCY-MAP.md documenting all v1.0 module coupling.

Purpose: Removes the last consumer of the deprecated STATE.md regex-based parser, wires the CLI to the new hierarchical state machine, and produces the dependency audit document that downstream phases 18-24 reference during planning.

Output: DEPENDENCY-MAP.md, rewritten handleState(), updated agent modules, state.cjs deleted.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-dependency-audit-and-adapter-layer/17-CONTEXT.md
@.planning/phases/17-dependency-audit-and-adapter-layer/17-RESEARCH.md

<interfaces>
<!-- state-machine.cjs API -- the target for all rewrites -->
From src/lib/state-machine.cjs:
```javascript
// Read/Write
async readState(cwd)                    // returns { valid, state } | { valid: false, errors } | null
async writeState(cwd, state)            // validates via Zod, lock-protected, atomic rename

// Finders (throw on not-found)
findMilestone(state, milestoneId)
findSet(state, milestoneId, setId)
findWave(state, milestoneId, setId, waveId)
findJob(state, milestoneId, setId, waveId, jobId)

// Transitions (lock-protected, validate, derive parent status)
async transitionJob(cwd, milestoneId, setId, waveId, jobId, newStatus)
async transitionWave(cwd, milestoneId, setId, waveId, newStatus)
async transitionSet(cwd, milestoneId, setId, newStatus)

// Utilities
createInitialState(projectName, milestoneName)
deriveWaveStatus(jobs)
deriveSetStatus(waves)
detectCorruption(cwd)
recoverFromGit(cwd)
commitState(cwd, message)
```

From src/bin/rapid-tools.cjs (current handleState, lines 190-231):
```javascript
async function handleState(cwd, subcommand, args) {
  let stateModule;
  try {
    stateModule = require('../lib/state.cjs');
  } catch (err) { ... }
  switch (subcommand) {
    case 'get': { /* stateGet(cwd, field) */ }
    case 'update': { /* stateUpdate(cwd, field, value) */ }
  }
}
```

From src/modules/core/core-state-access.md (lines 17-19, must be updated):
```markdown
- `node "${RAPID_TOOLS}" state get [field]` -- Read a specific field from STATE.md
- `node "${RAPID_TOOLS}" state get --all` -- Read the entire STATE.md content
- `node "${RAPID_TOOLS}" state update <field> <value>` -- Update a field in STATE.md
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DEPENDENCY-MAP.md and delete state.cjs</name>
  <files>.planning/phases/17-dependency-audit-and-adapter-layer/DEPENDENCY-MAP.md, src/lib/state.cjs, src/lib/state.test.cjs</files>
  <action>
1. Create DEPENDENCY-MAP.md in the phase directory documenting ALL src/lib/*.cjs modules. For each module include:
   - Module name and purpose (one line)
   - State coupling: YES (reads/writes STATE.md or STATE.json) or NO
   - Filesystem artifacts: list of .planning/ paths it reads/writes
   - Imports: which other lib modules it requires
   - Consumers: which files import it
   - Phase 17 action: DELETE / REWRITE / NO CHANGES
   - Future phase: which phase (18-24) will do full rewrite, if applicable

Organize into two sections:
- "Modules WITH State Coupling" (state.cjs, rapid-tools.cjs, init.cjs, agent .md files)
- "Modules WITHOUT State Coupling" (all others -- worktree, merge, execute, plan, lock, prereqs, stub, assembler, core, verify, teams, context, contract, dag, returns)

Include a section on filesystem path conventions:
- .planning/STATE.md (v1.0, being superseded by STATE.json)
- .planning/STATE.json (v2.0, authoritative)
- .planning/sets/ (planning artifacts, unchanged)
- .planning/worktrees/REGISTRY.json (worktree metadata, unchanged)
- .planning/contracts/ (contract artifacts, unchanged)
- .planning/locks/ (lock mechanism, unchanged)
- .planning/context/ (codebase analysis, unchanged)

Include a note on open integration points:
- REGISTRY.json phase tracking vs STATE.json logical state (deferred to Phase 19)
- execute update-phase vs transitionSet alignment (deferred to Phase 21)

2. Delete src/lib/state.cjs and src/lib/state.test.cjs. These are replaced by state-machine.cjs (Phase 16). No migration needed -- clean break per user decision.

3. Verify no other file imports state.cjs: `grep -rn "require.*['\"].*state.cjs['\"]" src/ --include="*.cjs" | grep -v state-machine | grep -v state-schemas | grep -v state-transitions | grep -v test` should return only rapid-tools.cjs (which is rewritten in Task 2).
  </action>
  <verify>
    <automated>test ! -f src/lib/state.cjs && test ! -f src/lib/state.test.cjs && test -f .planning/phases/17-dependency-audit-and-adapter-layer/DEPENDENCY-MAP.md && echo "PASS"</automated>
  </verify>
  <done>DEPENDENCY-MAP.md exists with entries for all lib modules. state.cjs and state.test.cjs are deleted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite rapid-tools.cjs handleState and update agent modules</name>
  <files>src/bin/rapid-tools.cjs, src/modules/core/core-state-access.md, src/modules/core/core-context-loading.md, src/bin/rapid-tools.test.cjs</files>
  <behavior>
    - `state get --all` returns full STATE.json as formatted JSON to stdout
    - `state get --all` with no STATE.json prints error and exits 1
    - `state get --all` with invalid STATE.json prints validation errors and exits 1
    - `state get milestone <milestoneId>` returns milestone JSON
    - `state get set <milestoneId> <setId>` returns set JSON
    - `state get wave <milestoneId> <setId> <waveId>` returns wave JSON
    - `state get job <milestoneId> <setId> <waveId> <jobId>` returns job JSON
    - `state get` with no args shows usage and exits 1
    - `state transition set <milestoneId> <setId> <newStatus>` transitions set status
    - `state transition wave <milestoneId> <setId> <waveId> <newStatus>` transitions wave status
    - `state transition job <milestoneId> <setId> <waveId> <jobId> <newStatus>` transitions job status
    - `state transition` with insufficient args shows usage and exits 1
    - `state detect-corruption` runs detectCorruption and returns JSON result
    - `state recover` runs recoverFromGit and returns result
  </behavior>
  <action>
1. Create or update src/bin/rapid-tools.test.cjs with tests for the new handleState behavior. Use node:test and node:assert. Tests should create a temp directory with a valid STATE.json (via createInitialState + writeState), then spawn rapid-tools.cjs as a child process to test CLI commands. Use `node --test` runner.

2. Rewrite handleState() in src/bin/rapid-tools.cjs:
   - Replace `require('../lib/state.cjs')` with `require('../lib/state-machine.cjs')`
   - Replace the `get` subcommand:
     - `--all`: call readState(cwd), handle null (no file) and invalid (validation errors), output JSON
     - `milestone <id>`: readState + findMilestone, output JSON
     - `set <milestoneId> <setId>`: readState + findSet, output JSON
     - `wave <milestoneId> <setId> <waveId>`: readState + findWave, output JSON
     - `job <milestoneId> <setId> <waveId> <jobId>`: readState + findJob, output JSON
     - No args: print usage help for state commands, exit 1
   - Replace the `update` subcommand with `transition`:
     - `set <milestoneId> <setId> <newStatus>`: call transitionSet
     - `wave <milestoneId> <setId> <waveId> <newStatus>`: call transitionWave
     - `job <milestoneId> <setId> <waveId> <jobId> <newStatus>`: call transitionJob
     - Output result as JSON
   - Add `detect-corruption` subcommand: call detectCorruption(cwd), output JSON
   - Add `recover` subcommand: call recoverFromGit(cwd), output JSON
   - Wrap all async calls in try/catch, output `{ error: message }` on failure

3. Update USAGE string at top of rapid-tools.cjs:
   - Replace `state get [field]` / `state update` lines with new hierarchy-aware commands:
     ```
     state get --all                                    Read full STATE.json
     state get milestone <id>                           Read milestone
     state get set <milestoneId> <setId>                Read set
     state get wave <milestoneId> <setId> <waveId>      Read wave
     state get job <milestoneId> <setId> <waveId> <jobId>  Read job
     state transition set <milestoneId> <setId> <status>   Transition set status
     state transition wave <milestoneId> <setId> <waveId> <status>  Transition wave
     state transition job <milestoneId> <setId> <waveId> <jobId> <status>  Transition job
     state detect-corruption                            Check STATE.json integrity
     state recover                                      Recover STATE.json from git
     ```

4. Update src/modules/core/core-state-access.md:
   - Replace all STATE.md references with STATE.json
   - Replace CLI command examples with new hierarchy-aware commands
   - Replace "state update" with "state transition"
   - Update the Rules section: reads use readState (returns validated JSON), writes use transition* (lock-protected, validates transitions, derives parent status)
   - Keep the RAPID_TOOLS prerequisite check block unchanged

5. Update src/modules/core/core-context-loading.md:
   - Line 17: Replace `state get <field>` with `state get --all` (returns full STATE.json)
   - Replace "STATE.md" with "STATE.json" in the anti-patterns section
  </action>
  <verify>
    <automated>node --test src/bin/rapid-tools.test.cjs</automated>
  </verify>
  <done>rapid-tools.cjs state commands use state-machine.cjs. Old state.cjs import removed. Agent modules reference STATE.json and new CLI commands. All tests pass.</done>
</task>

</tasks>

<verification>
- `grep -rn "require.*state\.cjs" src/ --include="*.cjs" | grep -v state-machine | grep -v state-schemas | grep -v state-transitions | grep -v test | grep -v node_modules` returns empty (no remaining imports of deleted state.cjs)
- `node --test src/bin/rapid-tools.test.cjs` passes
- `grep "STATE.md" src/modules/core/core-state-access.md` returns empty (all references updated)
- DEPENDENCY-MAP.md exists and contains entries for all lib modules
</verification>

<success_criteria>
- state.cjs and state.test.cjs deleted
- rapid-tools.cjs handleState uses state-machine.cjs with get --all/milestone/set/wave/job and transition set/wave/job
- core-state-access.md and core-context-loading.md reference STATE.json and new CLI commands
- DEPENDENCY-MAP.md documents all module coupling with phase assignment for future rewrites
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/17-dependency-audit-and-adapter-layer/17-01-SUMMARY.md`
</output>
