---
phase: 37-technical-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - technical_documentation.md
  - docs/setup.md
  - docs/planning.md
  - docs/execution.md
  - docs/review.md
  - docs/merge-and-cleanup.md
  - docs/configuration.md
autonomous: true
requirements: [DOC-03]

must_haves:
  truths:
    - "technical_documentation.md exists at repo root and the README link resolves"
    - "Index file has a TOC with 1-2 sentence summaries linking to all sub-documents"
    - "Each lifecycle skill doc covers every skill in that stage with full argument syntax"
    - "Skill docs use synopsis+link pattern referencing SKILL.md as authoritative"
    - "Configuration section covers .env, config.json, STATE.json, and key directories"
  artifacts:
    - path: "technical_documentation.md"
      provides: "Index file with TOC and summaries"
      contains: "docs/setup.md"
    - path: "docs/setup.md"
      provides: "Setup stage skills: install, init, context"
      contains: "/rapid:install"
    - path: "docs/planning.md"
      provides: "Planning stage skills: plan, set-init, discuss, wave-plan, plan-set, assumptions"
      contains: "/rapid:wave-plan"
    - path: "docs/execution.md"
      provides: "Execution stage skill: execute"
      contains: "/rapid:execute"
    - path: "docs/review.md"
      provides: "Review stage skill: review"
      contains: "/rapid:review"
    - path: "docs/merge-and-cleanup.md"
      provides: "Merge & Cleanup stage skills: merge, cleanup, new-milestone"
      contains: "/rapid:merge"
    - path: "docs/configuration.md"
      provides: "Configuration reference: .env, config.json, STATE.json schema, directories"
      contains: "RAPID_TOOLS"
  key_links:
    - from: "README.md"
      to: "technical_documentation.md"
      via: "relative markdown link"
      pattern: "\\[technical_documentation\\.md\\]"
    - from: "technical_documentation.md"
      to: "docs/*.md"
      via: "relative markdown links in TOC"
      pattern: "docs/"
    - from: "docs/*.md"
      to: "skills/*/SKILL.md"
      via: "synopsis+link references"
      pattern: "skills/.*/SKILL\\.md"
---

<objective>
Create the technical documentation index file and all lifecycle skill reference docs plus configuration reference.

Purpose: DOC-03 requires a power user reference covering all skills and configuration. The README (Phase 36) already links to `technical_documentation.md` at repo root -- this link must resolve. The index file provides the navigational hub; the lifecycle docs provide skill synopses with argument syntax; the configuration doc covers all settings.

Output: technical_documentation.md (index) + 6 sub-documents in docs/ (5 lifecycle + configuration)
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-technical-documentation/37-CONTEXT.md
@.planning/phases/37-technical-documentation/37-RESEARCH.md
@.planning/phases/36-readme-rewrite/36-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/ directory, index file, and all 5 lifecycle skill docs</name>
  <files>technical_documentation.md, docs/setup.md, docs/planning.md, docs/execution.md, docs/review.md, docs/merge-and-cleanup.md</files>
  <action>
**Step 1: Create docs/ directory**
```bash
mkdir -p docs
```

**Step 2: Create technical_documentation.md at repo root**

This is the index file. Structure:
- Title: "RAPID Technical Documentation"
- Brief intro paragraph (2-3 sentences, conversational-technical tone matching Phase 36 README)
- TOC as a numbered list with each entry being a link + 1-2 sentence summary
- Sections to link (in lifecycle order):
  1. [Setup](docs/setup.md) -- Install, initialize, and generate codebase context
  2. [Planning](docs/planning.md) -- Define sets, discuss requirements, plan waves and jobs
  3. [Execution](docs/execution.md) -- Dispatch parallel job agents and handle re-entry
  4. [Review](docs/review.md) -- Adversarial review pipeline with scoping and staged analysis
  5. [Merge and Cleanup](docs/merge-and-cleanup.md) -- Subagent merge delegation, conflict resolution, worktree cleanup
  6. [Agent Reference](docs/agents.md) -- All 31 agents with type badges and spawn hierarchy
  7. [Configuration](docs/configuration.md) -- Environment variables, project config, state schema, directory layout
  8. [State Machines](docs/state-machines.md) -- Set, wave, and job lifecycle transitions
  9. [Troubleshooting](docs/troubleshooting.md) -- Common failure modes with symptom/cause/fix cards
- After TOC, add a brief "Utility Commands" section listing the 4 utility skills (status, pause, resume, help) with one-line descriptions and argument syntax -- these don't belong to a lifecycle stage
- Close with a note: "Each skill section includes full argument syntax. For implementation details, see the linked SKILL.md files."

IMPORTANT: The file MUST be named exactly `technical_documentation.md` at repo root because README.md already links to it at `[technical_documentation.md](technical_documentation.md)`.

**Step 3: Create docs/setup.md**

Title: "Setup". Cover 3 skills:
- `/rapid:install` -- no args. Synopsis: sets up RAPID_TOOLS path, creates .env, configures shell.
- `/rapid:init` -- no args. Synopsis: spawns 7 research agents (5 parallel researchers + synthesizer + roadmapper) to analyze codebase and create PROJECT.md, REQUIREMENTS.md, ROADMAP.md.
- `/rapid:context` -- no args. Synopsis: generates codebase context documents for an existing project.

Each skill entry follows the synopsis+link pattern from RESEARCH.md:
```
### `/rapid:install`
[2-3 sentence synopsis]
See [skills/install/SKILL.md](../skills/install/SKILL.md) for full details.
```

Read each SKILL.md to extract accurate synopses: `skills/install/SKILL.md`, `skills/init/SKILL.md`, `skills/context/SKILL.md`.

**Step 4: Create docs/planning.md**

Title: "Planning". Cover 6 skills:
- `/rapid:plan` -- no args
- `/rapid:set-init <set-id>` -- initializes a set with worktree
- `/rapid:discuss <wave-id>` or `<set-id> <wave-id>` -- interactive requirement discussion
- `/rapid:wave-plan <wave-id>` or `<set-id> <wave-id>` -- plans jobs for a wave
- `/rapid:plan-set <set-id>` -- auto-chains wave planning across all waves
- `/rapid:assumptions <set-id>` -- documents assumptions for a set

Read each SKILL.md to extract accurate synopses. Include a note about argument patterns: set-id accepts numeric index or string ID; wave-id accepts dot notation (e.g., `1.1`).

**Step 5: Create docs/execution.md**

Title: "Execution". Cover 1 skill:
- `/rapid:execute <set-id> [--fix-issues] [--retry-wave <wave-id>]`

This gets slightly more detail since it's the only skill here. Include:
- Synopsis of parallel job dispatch (one rapid-job-executor per job, waves sequential)
- Smart re-entry behavior (skips completed jobs, retries failed)
- `--fix-issues` mode (spawns rapid-bugfix agents for review findings)
- `--retry-wave` mode (re-executes a specific wave)

Read `skills/execute/SKILL.md` for accurate synopsis. Still use synopsis+link pattern -- do NOT duplicate full step details.

**Step 6: Create docs/review.md**

Title: "Review". Cover 1 skill:
- `/rapid:review <set-id>`

Include:
- Synopsis of the review pipeline stages: scope -> unit test -> bug hunt -> adversarial -> judge -> UAT -> final review
- Note that review runs at set level (not per-wave)
- Scoper agent distributes files by concern to review agents
- Directory chunking for large scopes (>15 files)

Read `skills/review/SKILL.md` for accurate synopsis.

**Step 7: Create docs/merge-and-cleanup.md**

Title: "Merge and Cleanup". Cover 3 skills:
- `/rapid:merge` or `/rapid:merge <set-id>` -- subagent delegation per set, fast-path check, conflict resolution with confidence routing
- `/rapid:cleanup <set-id>` -- removes worktree and cleans up artifacts
- `/rapid:new-milestone` -- no args, advances to next milestone

Read each SKILL.md for accurate synopses: `skills/merge/SKILL.md`, `skills/cleanup/SKILL.md`, `skills/new-milestone/SKILL.md`.

**Tone:** Conversational-technical (matching Phase 36 README tone). Describe current state only -- no version callouts or changelogs.

**Cross-references:** Each lifecycle doc should have a "Next: [next stage](link)" footer for navigation flow.
  </action>
  <verify>
    <automated>test -f technical_documentation.md && test -f docs/setup.md && test -f docs/planning.md && test -f docs/execution.md && test -f docs/review.md && test -f docs/merge-and-cleanup.md && echo "All 6 lifecycle files exist" && grep -c "docs/" technical_documentation.md | xargs -I{} test {} -ge 9 && echo "Index has 9+ doc links" && grep -l "SKILL.md" docs/setup.md docs/planning.md docs/execution.md docs/review.md docs/merge-and-cleanup.md | wc -l | xargs -I{} test {} -eq 5 && echo "All 5 lifecycle docs reference SKILL.md"</automated>
  </verify>
  <done>
  - technical_documentation.md exists at repo root with TOC linking to all 9 sub-documents
  - 5 lifecycle docs exist in docs/ with synopsis+link entries for all 18 skills (14 lifecycle + 4 utility in index)
  - Every skill entry has full argument syntax and references its SKILL.md
  - README.md link to technical_documentation.md resolves
  </done>
</task>

<task type="auto">
  <name>Task 2: Create configuration reference</name>
  <files>docs/configuration.md</files>
  <action>
Create docs/configuration.md covering all configuration options and project structure.

**Sections:**

1. **Environment Variables** -- Table with:
   - `RAPID_TOOLS`: Absolute path to `src/bin/rapid-tools.cjs`. Set by `/rapid:install`. Required for all commands.
   - Note about `.env` file loading (per project CLAUDE.md, commands load from both environment and .env)

2. **Project Configuration (`config.json`)** -- Table with all keys from RESEARCH.md inventory:
   - `mode` (string): Execution mode, e.g. `"yolo"`
   - `parallelization` (boolean): Enable parallel job dispatch
   - `commit_docs` (boolean): Auto-commit documentation files
   - `model_profile` (string): `"quality"` or `"speed"` model selection
   - `workflow.research` (boolean): Enable research phase
   - `workflow.plan_check` (boolean): Enable plan verification
   - `workflow.verifier` (boolean): Enable post-execution verification
   - `granularity` (string): `"fine"` or `"coarse"` job sizing

   Read `src/lib/core.cjs` for `loadConfig()` defaults to document which have defaults vs required.

3. **State Schema (STATE.json)** -- Document the top-level schema from `src/lib/state-schemas.cjs`:
   - ProjectState: version, projectName, currentMilestone, milestones
   - MilestoneState: id, name, sets
   - SetState: id, status (list possible values)
   - WaveState: id, status (list possible values)
   - JobState: id, status, startedAt, completedAt, commitSha, artifacts
   - Reference `src/lib/state-schemas.cjs` for full Zod schema -- do NOT duplicate every field

4. **Key Directories and Files** -- Table from RESEARCH.md:
   - `.planning/` -- all project state
   - `.planning/STATE.json` -- machine-readable state
   - `.planning/config.json` -- project config
   - `.planning/.locks/` -- lock files (gitignored)
   - `.planning/worktrees/REGISTRY.json` -- worktree registry
   - `.planning/sets/{set-name}/` -- per-set artifacts
   - `.planning/waves/{set-name}/{wave-id}/` -- per-wave job plans
   - `.rapid-worktrees/` -- git worktree checkouts

Read `src/lib/core.cjs` and `src/lib/state-schemas.cjs` for accurate content. Use synopsis pattern -- reference source files as authoritative for full details.
  </action>
  <verify>
    <automated>test -f docs/configuration.md && grep -q "RAPID_TOOLS" docs/configuration.md && grep -q "config.json" docs/configuration.md && grep -q "STATE.json" docs/configuration.md && echo "Configuration doc exists with all sections"</automated>
  </verify>
  <done>
  - docs/configuration.md covers .env, config.json, STATE.json schema, and directory layout
  - Config keys documented with types and defaults where applicable
  - STATE.json schema documented at top level with reference to state-schemas.cjs for full definition
  </done>
</task>

</tasks>

<verification>
- README.md link `[technical_documentation.md](technical_documentation.md)` resolves to a real file
- Index file links to all 9 sub-documents (7 created in this plan, 2 in Plan 02)
- All 18 skills appear across the lifecycle docs and utility section
- Each skill has full argument syntax matching its SKILL.md
- No SKILL.md content is duplicated -- only synopses with links
- Configuration section covers all config.json keys and .env variables
</verification>

<success_criteria>
- technical_documentation.md exists at repo root
- 6 sub-documents exist in docs/ (5 lifecycle + configuration)
- Every skill has a synopsis+link entry with argument syntax
- Configuration reference covers .env, config.json, STATE.json, directories
- Conversational-technical tone consistent with Phase 36 README
</success_criteria>

<output>
After completion, create `.planning/phases/37-technical-documentation/37-01-SUMMARY.md`
</output>
