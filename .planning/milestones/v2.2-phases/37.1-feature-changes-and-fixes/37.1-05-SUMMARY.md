---
phase: 37.1-feature-changes-and-fixes
plan: 05
subsystem: infra
tags: [agents, tool-calling, env-loading, build-agents, cli]

# Dependency graph
requires:
  - phase: 37-technical-documentation
    provides: agent build system and module architecture
provides:
  - reliable RAPID_TOOLS env loading in agent context via multi-fallback preamble
  - correct core-state-access module assignments for all CLI-using agents
affects: [all agents using RAPID_TOOLS CLI, set-merger, conflict-resolver]

# Tech tracking
tech-stack:
  added: []
  patterns: [multi-fallback env loading with project-root ancestor traversal]

key-files:
  created: []
  modified:
    - src/modules/core/core-state-access.md
    - src/bin/rapid-tools.cjs
    - agents/rapid-set-merger.md
    - agents/rapid-conflict-resolver.md

key-decisions:
  - "Multi-fallback env preamble: env var > CLAUDE_SKILL_DIR > project root .planning/ ancestor traversal"
  - "Only set-merger and conflict-resolver needed core-state-access added (only roles referencing RAPID_TOOLS)"
  - "Existing state-access assignments on reviewer/verifier/orchestrator kept (not removed -- they benefit from CLI access)"

patterns-established:
  - "Agent env loading: 3-tier fallback (already set, skill context, project root ancestor walk)"

requirements-completed: [FIX-06]

# Metrics
duration: 2min
completed: 2026-03-11
---

# Phase 37.1 Plan 05: Tool-Calling Reliability Summary

**Multi-fallback env preamble for agent context and core-state-access module added to set-merger and conflict-resolver ROLE_CORE_MAP**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-11T11:28:09Z
- **Completed:** 2026-03-11T11:30:30Z
- **Tasks:** 2
- **Files modified:** 35

## Accomplishments
- Fixed core-state-access env preamble with 3-tier fallback: already set, CLAUDE_SKILL_DIR skill context, project root .planning/ ancestor traversal
- Added core-state-access.md to set-merger and conflict-resolver ROLE_CORE_MAP entries (the only roles that reference RAPID_TOOLS in their role modules)
- Rebuilt all 31 agents with correct tool+module combinations
- Verified no agent has Bash+RAPID_TOOLS usage without core-state-access, and no agent has core-state-access without Bash

## Task Commits

Each task was committed atomically:

1. **Task 1: Audit agents and fix core-state-access env preamble** - `04deb28` (fix)
2. **Task 2: Fix ROLE_CORE_MAP and agent tool declarations** - `c34434a` (fix)

## Files Created/Modified
- `src/modules/core/core-state-access.md` - Multi-fallback env preamble for both skill and agent contexts
- `src/bin/rapid-tools.cjs` - ROLE_CORE_MAP: added core-state-access to set-merger and conflict-resolver
- `agents/rapid-set-merger.md` - Rebuilt with core-state-access module included
- `agents/rapid-conflict-resolver.md` - Rebuilt with core-state-access module included
- `agents/rapid-*.md` (all 31) - Rebuilt to pick up updated core-state-access preamble

## Decisions Made
- Multi-fallback env preamble uses project root ancestor traversal (finds .planning/ directory) as agent-context fallback since CLAUDE_SKILL_DIR is not set in agent context
- Only set-merger and conflict-resolver needed core-state-access added -- they are the only roles that reference RAPID_TOOLS in their role module definitions
- Kept existing core-state-access on reviewer, verifier, orchestrator, planner, executor, job-executor (they already had it and benefit from CLI access)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All agents now have correct tool+module assignments for reliable CLI access
- The multi-fallback preamble ensures agents can locate RAPID_TOOLS in any spawn context

## Self-Check: PASSED

All files verified present. All commits verified in git log.

---
*Phase: 37.1-feature-changes-and-fixes*
*Completed: 2026-03-11*
