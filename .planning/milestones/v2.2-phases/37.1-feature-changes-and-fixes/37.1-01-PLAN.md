---
phase: 37.1-feature-changes-and-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/state-schemas.cjs
  - src/lib/state-schemas.test.cjs
  - src/lib/quick.cjs
  - src/lib/quick.test.cjs
  - src/lib/migrate.cjs
  - src/lib/migrate.test.cjs
  - src/bin/rapid-tools.cjs
autonomous: true
requirements: [FIX-03, FIX-04]

must_haves:
  truths:
    - "quickTasks field is optional and backward compatible with existing STATE.json files"
    - "Quick task CRUD operations (add, list, get next ID) work correctly"
    - "Migration detection identifies GSD, openspec, and generic planning patterns"
    - "Migration transformation restructures detected files to RAPID conventions"
    - "Backup is created before any migration transformation"
    - "CLI dispatches migrate and quick subcommands correctly"
  artifacts:
    - path: "src/lib/state-schemas.cjs"
      provides: "quickTasks optional field in ProjectState schema"
      contains: "quickTasks"
    - path: "src/lib/quick.cjs"
      provides: "Quick task tracking logic"
      exports: ["addQuickTask", "listQuickTasks", "getNextQuickTaskId"]
    - path: "src/lib/quick.test.cjs"
      provides: "Unit tests for quick task operations"
      min_lines: 40
    - path: "src/lib/migrate.cjs"
      provides: "Migration detection and transformation logic"
      exports: ["detectFramework", "transformToRapid", "backupPlanning"]
    - path: "src/lib/migrate.test.cjs"
      provides: "Unit tests for migration logic"
      min_lines: 60
    - path: "src/bin/rapid-tools.cjs"
      provides: "CLI dispatch for migrate and quick subcommands"
      contains: "migrate"
  key_links:
    - from: "src/lib/quick.cjs"
      to: "src/lib/state-schemas.cjs"
      via: "imports ProjectState schema with quickTasks"
      pattern: "require.*state-schemas"
    - from: "src/bin/rapid-tools.cjs"
      to: "src/lib/quick.cjs"
      via: "CLI dispatch"
      pattern: "require.*quick"
    - from: "src/bin/rapid-tools.cjs"
      to: "src/lib/migrate.cjs"
      via: "CLI dispatch"
      pattern: "require.*migrate"
---

<objective>
Create the foundational library modules and schema extensions for the /quick and /migrate commands.

Purpose: Both new commands need backend logic (state schema, CRUD operations, migration heuristics) before their SKILL.md files can be written. This plan builds and tests all the library code so Plan 04 can wire the skill files on top.

Output: Tested quick.cjs, migrate.cjs, schema extension, and CLI registration.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37.1-feature-changes-and-fixes/37.1-CONTEXT.md
@.planning/phases/37.1-feature-changes-and-fixes/37.1-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/state-schemas.cjs:
```javascript
const ProjectState = z.object({
  version: z.literal(1),
  projectName: z.string(),
  currentMilestone: z.string(),
  milestones: z.array(MilestoneState).default([]),
  lastUpdatedAt: z.string(),
  createdAt: z.string(),
  // quickTasks MUST be added here as optional
});
```

From src/bin/rapid-tools.cjs (CLI dispatch pattern):
```javascript
// Commands are dispatched via switch/case on args[0]
// New subcommands follow the pattern:
//   case 'migrate': return handleMigrate(args.slice(1));
//   case 'quick': return handleQuick(args.slice(1));
```

From src/lib/core.cjs:
```javascript
const { output, error, findProjectRoot } = require('../lib/core.cjs');
// output(json) -- writes JSON to stdout
// error(msg) -- writes error to stderr and exits 1
// findProjectRoot() -- finds .planning/ directory
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema extension + quick task library</name>
  <files>src/lib/state-schemas.cjs, src/lib/state-schemas.test.cjs, src/lib/quick.cjs, src/lib/quick.test.cjs</files>
  <behavior>
    - Test: ProjectState parses successfully without quickTasks field (backward compat)
    - Test: ProjectState parses with quickTasks as empty array
    - Test: ProjectState parses with populated quickTasks array
    - Test: addQuickTask returns updated state with new task appended, auto-incrementing ID
    - Test: listQuickTasks returns empty array when no tasks exist
    - Test: listQuickTasks returns all tasks in order
    - Test: getNextQuickTaskId returns 1 when no tasks, N+1 when tasks exist
  </behavior>
  <action>
    1. In state-schemas.cjs: Add QuickTask schema (id: z.number(), description: z.string(), date: z.string(), commitHash: z.string().optional(), directory: z.string().optional()). Add quickTasks: z.array(QuickTask).optional().default([]) to ProjectState. Export QuickTask.

    2. Update state-schemas.test.cjs: Add tests for quickTasks backward compatibility (parse without field), parse with empty array, parse with populated array.

    3. Create quick.cjs with:
       - addQuickTask(statePath, description, commitHash, directory) -- reads STATE.json, appends new QuickTask with auto-incremented ID and current date, writes back. Returns the new task object.
       - listQuickTasks(statePath) -- reads STATE.json, returns quickTasks array (or empty array if field missing).
       - getNextQuickTaskId(statePath) -- returns max(existing IDs) + 1, or 1 if none.
       - Use proper-lockfile (acquireLock pattern from existing code) for writes.
       - Use findProjectRoot() to locate .planning/ directory when statePath not provided.

    4. Create quick.test.cjs using node:test. Test all three functions with fixture STATE.json files (use tmp directories). Test backward compat with STATE.json that has no quickTasks field.
  </action>
  <verify>
    <automated>cd ~/Projects/RAPID_alt && node --test src/lib/state-schemas.test.cjs src/lib/quick.test.cjs</automated>
  </verify>
  <done>quickTasks schema extension is backward compatible, quick.cjs exports addQuickTask/listQuickTasks/getNextQuickTaskId, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration detection and transformation library</name>
  <files>src/lib/migrate.cjs, src/lib/migrate.test.cjs</files>
  <behavior>
    - Test: detectFramework returns { type: 'gsd', confidence: 'high', artifacts: [...] } when .planning/STATE.md + phases/ exist with GSD patterns
    - Test: detectFramework returns { type: 'openspec', confidence: 'high', artifacts: [...] } when openspec patterns detected
    - Test: detectFramework returns { type: 'generic', confidence: 'medium', artifacts: [...] } when STATE.md + ROADMAP.md exist but no framework-specific patterns
    - Test: detectFramework returns { type: 'none', confidence: 'high', artifacts: [] } when no planning directory exists
    - Test: backupPlanning creates .planning.bak/ with full copy of .planning/ contents
    - Test: backupPlanning throws if .planning.bak/ already exists
    - Test: transformToRapid restructures detected GSD artifacts to RAPID directory conventions
  </behavior>
  <action>
    1. Create migrate.cjs with:
       - detectFramework(projectRoot) -- Scans for planning directories and files. Checks for GSD patterns (phases/, PLAN.md files, "gsd" references in content), openspec patterns (openspec-specific file patterns), and generic patterns (STATE.md, ROADMAP.md without framework markers). Returns { type, confidence, artifacts, details }.
       - backupPlanning(projectRoot) -- Copies .planning/ to .planning.bak/ using recursive copy. Throws if backup already exists (prevents accidental overwrite).
       - transformToRapid(projectRoot, detectionResult) -- Based on detected framework type, restructures files to RAPID conventions. For GSD: rename phase directories, update state file format references. For generic: create RAPID-compatible directory structure. Returns { transformed: [...], skipped: [...], errors: [...] }.

    2. Create migrate.test.cjs using node:test. Use tmp directories with mock planning structures. Test each detection type independently. Test backup creation and failure on existing backup. Test transformation for GSD-to-RAPID restructuring.

    Note: The actual /rapid:migrate SKILL.md (Plan 04) will use these functions with user confirmation at each step per locked decision. The library functions are pure transformations; the skill handles the interactive flow.
  </action>
  <verify>
    <automated>cd ~/Projects/RAPID_alt && node --test src/lib/migrate.test.cjs</automated>
  </verify>
  <done>migrate.cjs exports detectFramework/backupPlanning/transformToRapid, all unit tests pass including edge cases for each framework type</done>
</task>

<task type="auto">
  <name>Task 3: CLI dispatch registration for migrate and quick</name>
  <files>src/bin/rapid-tools.cjs</files>
  <action>
    1. Add require statements for quick.cjs and migrate.cjs at the top of the dispatch section.

    2. Register 'quick' subcommand in the main dispatch switch:
       - `quick add <description>` -- calls addQuickTask, outputs the new task JSON
       - `quick list` -- calls listQuickTasks, outputs the array
       - `quick next-id` -- calls getNextQuickTaskId, outputs the number

    3. Register 'migrate' subcommand in the main dispatch switch:
       - `migrate detect` -- calls detectFramework, outputs detection JSON
       - `migrate backup` -- calls backupPlanning, outputs success/error
       - `migrate transform` -- reads detection result from stdin, calls transformToRapid, outputs result JSON

    4. Update the USAGE string at the top to include the new commands:
       ```
       quick add <description>         Add a quick task to state
       quick list                      List all quick tasks
       quick next-id                   Get the next quick task ID
       migrate detect                  Detect existing planning framework
       migrate backup                  Backup .planning/ to .planning.bak/
       migrate transform               Transform detected framework to RAPID (stdin: detection JSON)
       ```

    5. Follow the existing dispatch pattern exactly -- use output() for success, error() for failures.
  </action>
  <verify>
    <automated>cd ~/Projects/RAPID_alt && node --test src/lib/quick.test.cjs src/lib/migrate.test.cjs && node src/bin/rapid-tools.cjs --help | grep -q "quick\|migrate"</automated>
  </verify>
  <done>rapid-tools.cjs dispatches migrate and quick subcommands, help text includes new commands, all tests still pass</done>
</task>

</tasks>

<verification>
- All unit tests pass: `cd ~/Projects/RAPID_alt && node --test src/lib/*.test.cjs src/bin/*.test.cjs`
- STATE.json without quickTasks field still parses correctly
- CLI help shows new migrate and quick commands
- No existing tests broken by schema changes
</verification>

<success_criteria>
- quickTasks schema is backward compatible (existing STATE.json files parse without errors)
- quick.cjs CRUD operations work with file-based STATE.json
- migrate.cjs detects GSD, openspec, generic, and no-framework cases
- migrate.cjs creates backups and transforms to RAPID conventions
- rapid-tools.cjs dispatches both new command families
- All new and existing unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/37.1-feature-changes-and-fixes/37.1-01-SUMMARY.md`
</output>
