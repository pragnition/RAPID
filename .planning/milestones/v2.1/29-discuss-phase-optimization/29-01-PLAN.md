---
phase: 29-discuss-phase-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/discuss/SKILL.md
  - .planning/STATE.md
autonomous: true
requirements: [UX-05]

must_haves:
  truths:
    - "Step 4 presents a 'Let Claude decide all' master toggle as an explicit option in the gray area multiSelect"
    - "Step 5 uses a 2-round structure: Round 1 (approach + edge case context for all areas) then Round 2 (specifics + confirmation for all areas)"
    - "Each gray area gets exactly 2 interactions -- Interaction 1 in Round 1, Interaction 2 in Round 2"
    - "Round 2 always runs even when areas were delegated in Round 1"
    - "'Revise' in Round 2 re-presents only that area's Interaction 1, then re-presents that area's Interaction 2"
    - "STATE.md no longer contains the empirical spike blocker note"
  artifacts:
    - path: "skills/discuss/SKILL.md"
      provides: "2-round discuss skill with master delegation toggle"
      contains: "Round 1"
    - path: ".planning/STATE.md"
      provides: "Clean state without spike blocker"
  key_links:
    - from: "skills/discuss/SKILL.md Step 4"
      to: "skills/discuss/SKILL.md Step 5"
      via: "Gray area selection flows into 2-round deep-dive"
      pattern: "Let Claude decide all.*Round 1.*Round 2"
---

<objective>
Rewrite the discuss skill's question flow from a 4-question-per-gray-area loop to a 2-round batched structure that halves sequential user interactions without losing decision quality.

Purpose: Users currently face up to 16 interactions for 4 gray areas (4 questions each). The 2-round model reduces this to 8 interactions by merging approach+edge cases into Interaction 1 and specifics+confirmation into Interaction 2, grouped by round across all areas for cognitive batching.

Output: Updated `skills/discuss/SKILL.md` with 2-round Step 5 and master delegation toggle in Step 4, plus STATE.md cleanup.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-discuss-phase-optimization/29-CONTEXT.md
@.planning/phases/29-discuss-phase-optimization/29-RESEARCH.md
@skills/discuss/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Step 4 master toggle and Step 5 two-round structure</name>
  <files>skills/discuss/SKILL.md</files>
  <action>
Read the full `skills/discuss/SKILL.md`. Modify ONLY Step 4 and Step 5. Steps 1-3, 6, 7, 8, Error Handling, and Key Principles MUST remain identical. Also update line 8 (the preamble sentence that mentions "4-question loop") to say "2-round discussion" instead.

**Step 4 changes -- Add master delegation toggle:**

Update the AskUserQuestion multiSelect prompt. Change the intro text from "Select which to discuss (select none to let me decide all):" to "Select which areas to discuss:". Add "Let Claude decide all" as the FIRST option in the list:

```
Options (each as a selectable item):
1. "Let Claude decide all" -- "Skip discussion, I'll make all decisions based on codebase patterns"
2. "{Gray area title}" -- "{Brief 1-sentence description of why this needs input}"
3. "{Gray area title}" -- "{Brief description}"
...up to 8 areas
```

After the options, add handling logic:
- If the user selects "Let Claude decide all" (regardless of any other selections): Document that all areas are at Claude's discretion and skip to Step 6. This takes precedence over any other selections.
- If the user selects none: Same behavior -- document all areas at Claude's discretion and skip to Step 6. (Backward compatible with old behavior.)
- If the user selects specific gray areas (without "Let Claude decide all"): Record selected gray areas for Step 5.

**Step 5 changes -- Replace 4-question loop with 2-round structure:**

Replace the entire Step 5 content (currently lines ~178-252) with the 2-round batched discussion model. The new Step 5 structure:

```markdown
## Step 5: Deep-Dive Selected Areas (2-Round Discussion)

Process all selected gray areas in two rounds. Track all decisions made across both rounds.

### Round 1: Approach Selection (all areas)

For EACH selected gray area, present Interaction 1 back-to-back. Complete ALL Interaction 1s before moving to Round 2.

#### Interaction 1: Approach + Edge Case Context

Use AskUserQuestion:
```
"How do you want to handle '{gray area title}'?

Context: {2-3 sentences explaining the tradeoffs and why this matters}

Edge case to consider: {1 key edge case or tradeoff most likely to affect the approach choice}

Options:
- "{Approach A}" -- "{Brief description of approach A}"
- "{Approach B}" -- "{Brief description of approach B}"
- "{Approach C}" -- "{Brief description if applicable}"
- "Let Claude decide" -- "I'll choose based on the codebase patterns and contracts"
```

If "Let Claude decide": Record Claude's chosen approach and rationale. This area still appears in Round 2 with Claude's decisions shown as discretion items.

After completing Interaction 1 for ALL selected gray areas, proceed to Round 2.

### Round 2: Specifics & Confirmation (all areas)

For EACH selected gray area, present Interaction 2 back-to-back.

#### Interaction 2: Summary + Specifics

Use AskUserQuestion:
```
"'{Gray area title}' -- decisions so far:
- Approach: {chosen approach from Round 1}
{If delegated: '(Claude's discretion -- chose {approach} because {rationale})'}

Remaining detail: {1-2 specific implementation questions about this area}

Options:
- "{Specific choice A}" -- "{Description}"
- "{Specific choice B}" -- "{Description}"
- "Looks good" -- "Lock all decisions for this area"
- "Revise" -- "Go back and change the approach"
```

**Handling responses:**
- If "Looks good": Lock all decisions for this gray area. Continue to the next area's Interaction 2.
- If a specific choice is selected: Record the choice, then lock all decisions for this area. Continue to the next area's Interaction 2.
- If "Revise": Re-present ONLY this area's Interaction 1 (Round 1 question). After the user re-answers, re-present ONLY this area's Interaction 2. Then continue with the remaining Round 2 areas that have not yet been confirmed. Do NOT re-ask areas already confirmed.
- If this area was delegated in Round 1: Still show the summary with Claude's chosen approach marked as discretion. The user can still "Revise" to take control, or "Looks good" to accept Claude's choice.

After completing Interaction 2 for ALL selected gray areas, proceed to Step 6.
```

IMPORTANT constraints:
- Do NOT modify Steps 1-3, Step 6, Step 7, Step 8, Error Handling, or Key Principles sections
- The "Key Principles" section at the bottom references "Step 5 only" for Claude decides -- keep that accurate since both rounds are under Step 5
- Preserve all existing markdown formatting conventions (heading levels, code fences, horizontal rules between steps)
  </action>
  <verify>
    <automated>bash -c 'FILE=~/Projects/RAPID/skills/discuss/SKILL.md && echo "=== Round structure ===" && grep -c "Round 1\|Round 2\|Interaction 1\|Interaction 2" "$FILE" && echo "=== Master toggle ===" && grep -c "Let Claude decide all" "$FILE" && echo "=== Old 4-question loop removed ===" && grep -c "Question 1:\|Question 2:\|Question 3:\|Question 4:" "$FILE" && echo "=== Steps 1-3 intact ===" && grep -c "Step 1:\|Step 2:\|Step 3:" "$FILE" && echo "=== Steps 6-8 intact ===" && grep -c "Step 6:\|Step 7:\|Step 8:" "$FILE" && echo "=== Preamble updated ===" && grep -c "2-round" "$FILE"'</automated>
  </verify>
  <done>
Step 4 contains "Let Claude decide all" as an explicit first option in the multiSelect. Step 5 describes a 2-round structure with Interaction 1 (approach + edge case) in Round 1 and Interaction 2 (summary + specifics + confirmation) in Round 2. The old 4-question loop (Question 1-4) is fully replaced. Steps 1-3 and 6-8 are unchanged. The preamble references "2-round discussion" instead of "4-question loop".
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove STATE.md spike blocker note</name>
  <files>.planning/STATE.md</files>
  <action>
Read `.planning/STATE.md` and remove the line in the "Blockers/Concerns" section that reads:
```
- AskUserQuestion batching behavior (Phase 29): spike deemed unnecessary -- standard AskUserQuestion usage, no exotic batching
```

This is line 106 in the current file. Remove just this single bullet point. Keep the other blocker entries intact. If this is the last entry in the section, the section heading should remain with no items (or you may remove the heading if the section is now empty and that is cleaner -- use judgment per the existing formatting).
  </action>
  <verify>
    <automated>bash -c 'grep -c "empirical spike\|AskUserQuestion batching behavior" ~/.planning/STATE.md 2>/dev/null; RESULT=$?; if [ "$RESULT" -eq 1 ]; then echo "PASS: blocker removed"; else echo "FAIL: blocker still present"; fi'</automated>
  </verify>
  <done>
The STATE.md "Blockers/Concerns" section no longer contains the AskUserQuestion batching/spike blocker note. Other blockers remain intact.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "Round 1\|Round 2" ~/Projects/RAPID/skills/discuss/SKILL.md` returns >= 4 (round structure present)
2. `grep -c "Question 1:\|Question 2:\|Question 3:\|Question 4:" ~/Projects/RAPID/skills/discuss/SKILL.md` returns 0 (old loop removed)
3. `grep "Let Claude decide all" ~/Projects/RAPID/skills/discuss/SKILL.md` returns a match (master toggle present)
4. `grep "empirical spike" ~/Projects/RAPID/.planning/STATE.md` returns no match (blocker removed)
5. Steps 1-3, 6, 7, 8 section headers present and unmodified in SKILL.md
6. "2-round" appears in the skill preamble (line 8 area)
</verification>

<success_criteria>
- The discuss skill file has a 2-round Step 5 (Round 1: approach+edge for all areas, Round 2: specifics+confirm for all areas)
- Step 4 includes "Let Claude decide all" as the first option in gray area multiSelect
- Both "select none" and "Let Claude decide all" result in full delegation
- Round 2 always runs, even for delegated areas
- "Revise" re-enters only the affected area (not entire Round 1)
- STATE.md spike blocker is gone
- No changes to Steps 1-3, 6, 7, or 8
</success_criteria>

<output>
After completion, create `.planning/phases/29-discuss-phase-optimization/29-01-SUMMARY.md`
</output>
