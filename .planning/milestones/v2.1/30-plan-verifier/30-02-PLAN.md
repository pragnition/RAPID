---
phase: 30-plan-verifier
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - skills/wave-plan/SKILL.md
autonomous: true
requirements:
  - PLAN-04
  - PLAN-05

must_haves:
  truths:
    - "Plan verifier runs automatically at the end of wave-plan, before state transition"
    - "FAIL verdict blocks the wave from transitioning to planning state"
    - "FAIL verdict presents re-plan/override/cancel options to the user"
    - "Re-plan only re-spawns job planners for failing jobs, not the entire wave"
    - "PASS and PASS_WITH_GAPS proceed normally with state transition"
    - "VERIFICATION-REPORT.md is committed alongside other wave planning artifacts"
  artifacts:
    - path: "skills/wave-plan/SKILL.md"
      provides: "Updated wave-plan pipeline with plan verification step"
      contains: "rapid-plan-verifier"
  key_links:
    - from: "skills/wave-plan/SKILL.md"
      to: "agents/rapid-plan-verifier.md"
      via: "Spawn the **rapid-plan-verifier** agent"
      pattern: "rapid-plan-verifier"
    - from: "skills/wave-plan/SKILL.md"
      to: ".planning/waves/{setId}/{waveId}/VERIFICATION-REPORT.md"
      via: "Verifier writes report, skill reads verdict"
      pattern: "VERIFICATION-REPORT"
    - from: "skills/wave-plan/SKILL.md"
      to: "AskUserQuestion"
      via: "FAIL gate with re-plan/override/cancel options"
      pattern: "AskUserQuestion"
---

<objective>
Integrate the plan verifier agent into the wave-plan skill pipeline with FAIL gate handling.

Purpose: The verifier agent (created in Plan 01) needs to be invoked automatically during wave planning. This plan modifies the wave-plan SKILL.md to: (1) defer the state transition from Step 2 to after verification, (2) add a new verification step between job planning and contract validation, (3) handle FAIL verdicts with a user decision gate offering re-plan/override/cancel. This completes PLAN-04 (report output integrated into pipeline) and PLAN-05 (FAIL decision gate).

Output: Updated wave-plan SKILL.md with plan verification integrated as Step 5.5 and state transition deferred to Step 6.5.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-plan-verifier/30-CONTEXT.md
@.planning/phases/30-plan-verifier/30-RESEARCH.md
@.planning/phases/30-plan-verifier/30-01-SUMMARY.md
</context>

<interfaces>
<!-- The executor needs to understand the current SKILL.md structure and where changes go. -->

Current wave-plan SKILL.md step structure (427 lines):
```
Step 1: Environment Setup + Display Stage Banner
Step 2: Resolve Wave and Validate State
  - Includes: `node "${RAPID_TOOLS}" state transition wave "${MILESTONE_ID}" "${SET_ID}" "${WAVE_ID}" planning`
  - THIS LINE MUST BE REMOVED from Step 2 (deferred to new Step 6.5)
Step 3: Spawn Wave Research Agent
Step 4: Spawn Wave Planner Agent
Step 5: Spawn Job Planner Agents (One Per Job)
Step 6: Contract Validation Gate
Step 7: Commit and Present Results
```

Target structure after modification:
```
Step 1: (unchanged)
Step 2: (state transition REMOVED, everything else unchanged)
Step 3: (unchanged)
Step 4: (unchanged)
Step 5: (unchanged)
Step 5.5: NEW - Plan Verification (spawn rapid-plan-verifier, handle verdict)
Step 6: (unchanged - contract validation)
Step 6.5: NEW - State Transition to Planning (moved from Step 2)
Step 7: (updated - commit also includes VERIFICATION-REPORT.md)
```

Structured return protocol the verifier agent uses:
```
<!-- RAPID:RETURN {
  "status": "COMPLETE",
  "verdict": "PASS|PASS_WITH_GAPS|FAIL",
  "failingJobs": ["job-2"],
  "artifacts": ["VERIFICATION-REPORT.md"],
  "notes": ["Coverage: PASS", "Implementability: FAIL (job-2)", "Consistency: PASS"]
} -->
```

AskUserQuestion pattern used in existing SKILL.md:
```
Use AskUserQuestion:
  "Message to user"
  Options:
  - "Option A" -- "Description"
  - "Option B" -- "Description"
```

Verifier agent spawning pattern (from 30-RESEARCH.md):
```
Spawn the **rapid-plan-verifier** agent with this task:

Verify all job plans for wave '{waveId}' in set '{setId}'.

## Wave Plan
{WAVE-PLAN.md full contents}

## Wave Context
{WAVE-CONTEXT.md full contents}

## Job Plans
{All JOB-PLAN.md files concatenated with headers}

## Working Directory
{worktreePath}

## Output
Write VERIFICATION-REPORT.md to .planning/waves/{setId}/{waveId}/VERIFICATION-REPORT.md
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add plan verification step and defer state transition in wave-plan SKILL.md</name>
  <files>skills/wave-plan/SKILL.md</files>
  <action>
Modify `skills/wave-plan/SKILL.md` with three precise changes:

**Change 1: Remove state transition from Step 2**

In Step 2 ("Resolve Wave and Validate State"), find the section with the state transition call:
```
**Transition wave to planning:**

```bash
# (env preamble here)
node "${RAPID_TOOLS}" state transition wave "${MILESTONE_ID}" "${SET_ID}" "${WAVE_ID}" planning
```
```

Remove this entire subsection (the header "**Transition wave to planning:**" and its bash block). The state transition will be added later in the new Step 6.5. Everything else in Step 2 (resolve, status check, WAVE-CONTEXT.md check) stays unchanged.

**Change 2: Add new Step 5.5 between Step 5 and Step 6**

After the "---" separator following Step 5, insert a new section:

```markdown
## Step 5.5: Plan Verification

Display progress: "Verifying job plans for {waveId}..."

Read all context needed for the verifier:
- Read `.planning/waves/{setId}/{waveId}/WAVE-PLAN.md`
- Read `.planning/waves/{setId}/{waveId}/WAVE-CONTEXT.md`
- Read all JOB-PLAN.md files from `.planning/waves/{setId}/{waveId}/` (use Glob to discover `*-PLAN.md` files, then Read each one)

Concatenate all JOB-PLAN.md contents with `### {filename}` headers between them.

Spawn the **rapid-plan-verifier** agent with this task:

```
Verify all job plans for wave '{waveId}' in set '{setId}'.

## Wave Plan
{WAVE-PLAN.md full contents}

## Wave Context
{WAVE-CONTEXT.md full contents}

## Job Plans
{All JOB-PLAN.md file contents, each preceded by ### {filename} header}

## Working Directory
{worktreePath from Step 2}

## Output
Write VERIFICATION-REPORT.md to .planning/waves/{setId}/{waveId}/VERIFICATION-REPORT.md
```

After agent completes:
- Read the structured return to extract `verdict` and `failingJobs`
- Read `.planning/waves/{setId}/{waveId}/VERIFICATION-REPORT.md`

**If verdict is PASS:**
Display: "All plans verified. Proceeding to contract validation."
Continue to Step 6.

**If verdict is PASS_WITH_GAPS:**
Display the gaps from the report's Summary section as a warning.
Display: "Plans verified with minor gaps (see VERIFICATION-REPORT.md). Proceeding."
Continue to Step 6.

**If verdict is FAIL:**
Display the failures from the report's Summary section.

Use AskUserQuestion:
```
"Plan verification FAILED for wave {waveId}:

{Summary section from VERIFICATION-REPORT.md}

Failing jobs: {failingJobs list}

What would you like to do?"
Options:
- "Re-plan failing jobs" -- "Re-spawn job planners only for the failing jobs ({failingJobs}), then re-verify"
- "Override" -- "Proceed despite failures (you take responsibility for plan issues)"
- "Cancel" -- "Stop and investigate (wave stays in discussing state)"
```

If "Re-plan failing jobs":
  - Re-read WAVE-PLAN.md and WAVE-RESEARCH.md
  - For each job ID in `failingJobs`, re-spawn the **rapid-job-planner** agent with the same task prompt as Step 5 but only for that specific job
  - After all re-planned job planners complete, re-run the verification by spawning the **rapid-plan-verifier** agent again
  - If the re-verification also returns FAIL, present AskUserQuestion with only "Override" and "Cancel" options (no second re-plan -- maximum 1 re-plan attempt)
  - If re-verification returns PASS or PASS_WITH_GAPS, continue to Step 6

If "Override":
  Log the override in the terminal: "Proceeding with plan verification overridden by user."
  Continue to Step 6.

If "Cancel":
  Display: "Planning cancelled. Wave remains in 'discussing' state. Investigate the issues in VERIFICATION-REPORT.md and re-run /rapid:wave-plan when ready."
  STOP.
```

**Change 3: Add Step 6.5 and update Step 7**

After Step 6 (Contract Validation Gate) and its "---" separator, insert a new section:

```markdown
## Step 6.5: Transition Wave to Planning

All validation complete. Transition the wave state:

```bash
# (env preamble here)
node "${RAPID_TOOLS}" state transition wave "${MILESTONE_ID}" "${SET_ID}" "${WAVE_ID}" planning
```

This transition was deferred from Step 2 to ensure verification passes before the wave enters `planning` state.

---
```

In Step 7 (Commit and Present Results), update the git add block to also include VERIFICATION-REPORT.md:

Change the existing git add/commit section to:
```bash
# (env preamble here)
git add ".planning/waves/${SET_ID}/${WAVE_ID}/"
if [ -f ".planning/sets/${SET_ID}/VALIDATION-REPORT.md" ]; then
  git add ".planning/sets/${SET_ID}/VALIDATION-REPORT.md"
fi
JOB_COUNT=$(ls ".planning/waves/${SET_ID}/${WAVE_ID}/"*-PLAN.md 2>/dev/null | wc -l)
git commit -m "plan(${SET_ID}): wave ${WAVE_ID} planning complete -- research, wave plan, ${JOB_COUNT} job plans, verification, validation"
```
(The VERIFICATION-REPORT.md is already inside the waves directory, so it is covered by the existing `git add ".planning/waves/${SET_ID}/${WAVE_ID}/"` glob. Just update the commit message to mention "verification".)

Also update the "Key Principles" section at the bottom to add:
```
- **Plan verification gate:** Plans are verified for coverage, implementability, and consistency BEFORE contract validation. FAIL blocks state transition.
```

And update the existing "State transitions" principle to:
```
- **State transitions:** `discussing -> planning` is deferred until AFTER plan verification and contract validation both pass. This ensures a FAIL verdict blocks execution.
```

IMPORTANT: Do NOT renumber existing steps. Keep Steps 1-7 as-is and use 5.5 and 6.5 for the new steps. This preserves backward compatibility with any documentation referencing step numbers.
  </action>
  <verify>
    <automated>cd /home/kek/Projects/RAPID && grep -q "rapid-plan-verifier" skills/wave-plan/SKILL.md && grep -q "Step 5.5" skills/wave-plan/SKILL.md && grep -q "Step 6.5" skills/wave-plan/SKILL.md && grep -q "VERIFICATION-REPORT" skills/wave-plan/SKILL.md && ! awk '/^## Step 2:/,/^## Step [0-9]/' skills/wave-plan/SKILL.md | grep -q 'state transition wave.*planning' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
wave-plan SKILL.md contains:
- Step 5.5 with rapid-plan-verifier agent spawn
- Step 6.5 with state transition (moved from Step 2)
- State transition removed from Step 2
- FAIL gate with AskUserQuestion offering re-plan/override/cancel
- Re-plan limited to 1 attempt (second FAIL shows override/cancel only)
- VERIFICATION-REPORT.md included in Step 7 commit
- Key Principles updated to document verification gate
  </done>
</task>

</tasks>

<verification>
- `grep "rapid-plan-verifier" skills/wave-plan/SKILL.md` returns matches (agent referenced)
- `grep "Step 5.5" skills/wave-plan/SKILL.md` returns match (new step exists)
- `grep "Step 6.5" skills/wave-plan/SKILL.md` returns match (deferred transition exists)
- Step 2 no longer contains `state transition wave.*planning` (deferred to Step 6.5)
- The FAIL handling includes AskUserQuestion with three options
- Re-plan flow re-spawns only failingJobs, not all jobs
- Maximum 1 re-plan attempt (second FAIL shows only override/cancel)
</verification>

<success_criteria>
- The wave-plan pipeline includes plan verification as Step 5.5 between job planning and contract validation
- State transition is deferred from Step 2 to Step 6.5 (after all validation passes)
- A FAIL verdict blocks the wave from entering planning state and presents the user with re-plan/override/cancel options (PLAN-05)
- PASS and PASS_WITH_GAPS allow normal progression
- Re-plan targets only failing jobs, not the entire wave
- VERIFICATION-REPORT.md is committed with other wave planning artifacts (PLAN-04 integration)
</success_criteria>

<output>
After completion, create `.planning/phases/30-plan-verifier/30-02-SUMMARY.md`
</output>
