---
phase: 27.1-skill-to-agent-overhaul
plan: 02
type: execute
wave: 2
depends_on: ["27.1-01"]
files_modified:
  - skills/init/SKILL.md
  - skills/wave-plan/SKILL.md
  - skills/review/SKILL.md
  - skills/execute/SKILL.md
  - skills/merge/SKILL.md
  - skills/context/SKILL.md
  - skills/plan/SKILL.md
  - skills/set-init/SKILL.md
autonomous: true
requirements:
  - AGENT-04
  - AGENT-05

must_haves:
  truths:
    - "Skills reference agents by registered name (e.g., 'Spawn the rapid-bug-hunter agent') instead of cat-ing role modules"
    - "Skills pass only task-specific context (set ID, wave ID, file lists, worktree path) -- not role instructions"
    - "Every skill that spawns agents includes a '## Working Directory' section in the spawn prompt"
    - "No skill file contains 'cat src/modules/roles/' references"
    - "No skill file pastes role module content inline into Agent tool prompts"
  artifacts:
    - path: "skills/review/SKILL.md"
      provides: "Review skill with 6 agent-reference spawns"
      contains: "rapid-bug-hunter"
    - path: "skills/init/SKILL.md"
      provides: "Init skill with 8 agent-reference spawns"
      contains: "rapid-codebase-synthesizer"
    - path: "skills/wave-plan/SKILL.md"
      provides: "Wave-plan skill with 3 agent-reference spawns"
      contains: "rapid-wave-researcher"
    - path: "skills/execute/SKILL.md"
      provides: "Execute skill with job-executor and bugfix agent refs"
      contains: "rapid-job-executor"
    - path: "skills/merge/SKILL.md"
      provides: "Merge skill with merger agent ref"
      contains: "rapid-merger"
  key_links:
    - from: "skills/review/SKILL.md"
      to: "agents/rapid-bug-hunter.md"
      via: "agent name reference in spawn instructions"
      pattern: "rapid-bug-hunter"
    - from: "skills/init/SKILL.md"
      to: "agents/rapid-codebase-synthesizer.md"
      via: "agent name reference in spawn instructions"
      pattern: "rapid-codebase-synthesizer"
    - from: "skills/wave-plan/SKILL.md"
      to: "agents/rapid-wave-researcher.md"
      via: "agent name reference in spawn instructions"
      pattern: "rapid-wave-researcher"
---

<objective>
Update all 8 agent-spawning skills to reference registered agent types by name instead of reading role modules inline.

Purpose: This eliminates namespace fallthrough where Claude Code falls through to other plugins (e.g., gsd-review:*) because the spawned agents don't match any registered type. After this change, every Agent tool call matches a registered `agents/*.md` file.

Output: 8 updated skill files with clean agent-reference spawn patterns, passing worktree paths, and zero inline role module reading.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27.1-skill-to-agent-overhaul/27.1-CONTEXT.md
@.planning/phases/27.1-skill-to-agent-overhaul/27.1-RESEARCH.md
@.planning/phases/27.1-skill-to-agent-overhaul/27.1-01-SUMMARY.md

<interfaces>
<!-- Target spawn pattern (from RESEARCH.md Example 3) -->

BEFORE (current pattern in skills):
```markdown
Read the role module:
```bash
cat src/modules/roles/role-bug-hunter.md
```
Spawn bug-hunter subagent via Agent tool:
```
You are a bug-hunter analyzing wave '{waveId}' in set '{setId}'.
## Your Role
{content of role-bug-hunter.md}
## Scoped Files
{file list}
```
```

AFTER (target pattern):
```markdown
Spawn the **rapid-bug-hunter** agent with this task:
```
Analyze wave '{waveId}' in set '{setId}'.
## Scoped Files (ONLY report bugs in these files)
{scoped file list}
## Wave Context
{wave context}
## Working Directory
{worktreePath}
## Return Format
Return findings via:
<!-- RAPID:RETURN {"status":"COMPLETE","data":{"findings":[...]}} -->
```
```

Note: The agent already knows its role, identity, return protocol from its pre-assembled system prompt. The skill only passes task-specific context.

<!-- Agent files that now exist in agents/ (from Plan 01) -->
All 26 agents are registered in agents/ with names like:
- rapid-codebase-synthesizer, rapid-research-stack, rapid-research-features, etc. (init skill)
- rapid-wave-researcher, rapid-wave-planner, rapid-job-planner (wave-plan skill)
- rapid-unit-tester, rapid-bug-hunter, rapid-devils-advocate, rapid-judge, rapid-bugfix, rapid-uat (review skill)
- rapid-job-executor, rapid-bugfix (execute skill)
- rapid-merger (merge skill)
- rapid-context-generator (context skill)
- rapid-planner (plan skill)
- rapid-set-planner (set-init skill)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update init, wave-plan, and context skills</name>
  <files>skills/init/SKILL.md, skills/wave-plan/SKILL.md, skills/context/SKILL.md</files>
  <action>
These 3 skills are the most complex agent-spawning conversions. Update each one following the pattern below.

**For each skill:**
1. Find every instance where the skill reads a role module (`cat src/modules/roles/role-*.md`) or references role module content
2. Replace with: `Spawn the **rapid-{role}** agent with this task:` followed by ONLY task-specific context
3. Remove ALL inline role module content -- the agent's system prompt already contains it
4. Add a `## Working Directory` section to every spawn prompt
5. Keep the return format instructions if they exist (agents need to know the expected output shape)

**skills/init/SKILL.md (8 agent conversions):**

This skill spawns 8 agents:
- `rapid-codebase-synthesizer` -- Step 1 codebase analysis
- `rapid-research-stack` -- Step 3a research
- `rapid-research-features` -- Step 3b research
- `rapid-research-architecture` -- Step 3c research
- `rapid-research-pitfalls` -- Step 3d research
- `rapid-research-oversights` -- Step 3e research
- `rapid-research-synthesizer` -- Step 4 synthesis
- `rapid-roadmapper` -- Step 5 roadmap

For each:
- Remove the `cat src/modules/roles/role-*.md` instruction
- Remove the "Pass the role instructions from `role-*.md`" instruction
- Replace with `Spawn the **rapid-{role}** agent with this task:` and provide only:
  - Project name, description, team size (from init input)
  - Paths to read (e.g., codebase analysis results, research outputs)
  - Working directory: `{projectRoot}`
  - Expected output format/file

**skills/wave-plan/SKILL.md (3 agent conversions):**

This skill spawns 3 agents:
- `rapid-wave-researcher` -- Step 2 research
- `rapid-wave-planner` -- Step 3 wave planning
- `rapid-job-planner` -- Step 4 job planning (spawned per-job)

For each:
- Remove "Include the full contents of src/modules/roles/role-*.md as instructions"
- Replace with agent name reference
- Pass: set ID, wave ID, relevant plan/contract paths, worktree path

**skills/context/SKILL.md (1 agent conversion):**

This skill spawns 1 agent:
- `rapid-context-generator`

- Remove "The role instructions from `role-context-generator.md`" references
- Replace with agent name reference
- Pass: project root path, output directory

**Anti-patterns to avoid:**
- Do NOT duplicate role instructions -- the whole point is agents contain their own prompts
- Do NOT pass entire file contents via spawn prompt -- pass file PATHS and let agents read them
- Do NOT remove the return format expectations from spawn prompts -- agents need to know what shape of data to return
  </action>
  <verify>
    <automated>cd /home/kek/Projects/RAPID && grep -c "cat src/modules/roles" skills/init/SKILL.md skills/wave-plan/SKILL.md skills/context/SKILL.md | grep -v ":0$" | wc -l | grep -q "^0$" && echo "PASS: No inline role module references" || echo "FAIL: Still has inline role module references"</automated>
  </verify>
  <done>
    - skills/init/SKILL.md references 8 agents by name (rapid-codebase-synthesizer, rapid-research-*, rapid-roadmapper)
    - skills/wave-plan/SKILL.md references 3 agents by name (rapid-wave-researcher, rapid-wave-planner, rapid-job-planner)
    - skills/context/SKILL.md references 1 agent by name (rapid-context-generator)
    - Zero `cat src/modules/roles/` references in any of these 3 files
    - Each spawn prompt includes Working Directory section
  </done>
</task>

<task type="auto">
  <name>Task 2: Update review, execute, merge, plan, and set-init skills</name>
  <files>skills/review/SKILL.md, skills/execute/SKILL.md, skills/merge/SKILL.md, skills/plan/SKILL.md, skills/set-init/SKILL.md</files>
  <action>
**skills/review/SKILL.md (6 agent conversions -- LARGEST skill at 832 lines):**

This skill spawns 6 agents across the review cycle:
- `rapid-unit-tester` -- Step 3a unit testing
- `rapid-bug-hunter` -- Step 3b.1 bug hunting
- `rapid-devils-advocate` -- Step 3b.2 devil's advocacy
- `rapid-judge` -- Step 3b.3 judging
- `rapid-bugfix` -- Step 3c bugfixing
- `rapid-uat` -- Step 3d UAT

For EACH agent spawn in the review skill:
1. Remove the `cat src/modules/roles/role-*.md` instruction
2. Remove inline role content (the `{content of role-*.md}` blocks)
3. Replace with `Spawn the **rapid-{role}** agent with this task:`
4. Keep task-specific context: set ID, wave ID, cycle number, scoped file list, wave context
5. Add `## Working Directory\n{worktreePath}` section
6. Keep return format instructions (each agent returns differently)

**CRITICAL for review skill:** The review skill currently has the MOST inline role module references (13+ occurrences). Be thorough -- search for ALL patterns:
- `cat src/modules/roles/role-*.md`
- `{content of role-*.md}`
- `role-*.md` as instructions
- Inline pasted role content between agent spawn blocks

**skills/execute/SKILL.md (2 agent conversions):**

This skill spawns:
- `rapid-job-executor` -- per-job execution
- `rapid-bugfix` -- bugfix after review

- Replace inline role references with agent name references
- Pass: set ID, wave ID, job ID, job plan path, worktree path
- Note: execute skill references `role-executor.md` for v1.0 and `role-job-executor.md` for v2.0 -- update both paths

**skills/merge/SKILL.md (1 agent conversion):**

This skill spawns:
- `rapid-merger` -- conflict resolution

- Remove `node "${RAPID_TOOLS}" assemble-agent merger` call (the agent is now pre-built)
- Remove `role-merger.md` placeholder references
- Replace with agent name reference
- Pass: set ID, conflict details, branch names, worktree path

**skills/plan/SKILL.md (1 agent conversion):**

This skill spawns:
- `rapid-planner` (already partially working since rapid-planner.md exists)

- Normalize to use the same spawn pattern as other skills
- Pass: set ID, wave ID, plan context, worktree path

**skills/set-init/SKILL.md (1 agent conversion):**

This skill spawns:
- `rapid-set-planner`

- Remove any inline role module references
- Replace with agent name reference
- Pass: set name, milestone context, worktree path

**Final sweep:** After all edits, run grep to confirm zero remaining references:
- `grep -r "cat src/modules/roles" skills/` should return nothing
- `grep -r "role-.*\.md" skills/` should only match legitimate file references in comments/documentation, not spawn instructions
- `grep -r "assemble-agent" skills/` should return nothing (replaced by pre-built agents)
  </action>
  <verify>
    <automated>cd /home/kek/Projects/RAPID && grep -r "cat src/modules/roles" skills/ | wc -l | grep -q "^0$" && grep -r "assemble-agent" skills/ | wc -l | grep -q "^0$" && echo "PASS: No inline role refs or assemble-agent calls" || echo "FAIL: Still has old patterns"</automated>
  </verify>
  <done>
    - skills/review/SKILL.md references 6 agents by name with clean spawn patterns
    - skills/execute/SKILL.md references 2 agents by name
    - skills/merge/SKILL.md references 1 agent by name, no assemble-agent call
    - skills/plan/SKILL.md references 1 agent by name
    - skills/set-init/SKILL.md references 1 agent by name
    - Zero `cat src/modules/roles/` in any skill file across entire skills/ directory
    - Zero `assemble-agent` references in any skill file
    - Each spawn prompt includes Working Directory section
  </done>
</task>

</tasks>

<verification>
- `grep -r "cat src/modules/roles" skills/` returns zero results
- `grep -r "assemble-agent" skills/` returns zero results
- `grep -r "{content of role-" skills/` returns zero results
- `grep -rl "rapid-bug-hunter\|rapid-unit-tester\|rapid-devils-advocate\|rapid-judge\|rapid-bugfix\|rapid-uat" skills/review/SKILL.md` confirms review skill references all 6 review agents
- `grep -rl "rapid-codebase-synthesizer\|rapid-research-stack\|rapid-roadmapper" skills/init/SKILL.md` confirms init skill references its agents
- `grep -rl "rapid-wave-researcher\|rapid-wave-planner\|rapid-job-planner" skills/wave-plan/SKILL.md` confirms wave-plan references its agents
- Each skill file that spawns agents includes "Working Directory" text
</verification>

<success_criteria>
- All 8 agent-spawning skills updated to reference registered agents by name
- No skill reads role modules inline (zero `cat src/modules/roles/` matches)
- No skill uses the `assemble-agent` CLI command
- Every agent spawn includes worktree path as Working Directory
- Skills pass only task-specific context, not role instructions
</success_criteria>

<output>
After completion, create `.planning/phases/27.1-skill-to-agent-overhaul/27.1-02-SUMMARY.md`
</output>
