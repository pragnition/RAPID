---
phase: 29.1-make-the-reviewing-set-based-instead-of-wave-based
plan: 03
subsystem: review
tags: [skill-rewrite, set-scoped-review, directory-chunking, wave-attribution, orchestrator]

# Dependency graph
requires:
  - phase: 29.1-01
    provides: Set-scoped review library API (scopeSetForReview, chunkByDirectory, buildWaveAttribution)
  - phase: 29.1-02
    provides: Dual-mode CLI commands (review scope, log-issue, update-issue) and updated agent role modules
provides:
  - Set-level review orchestrator SKILL.md with directory chunking and parallel agent spawning
  - Removed per-wave iteration loop from review pipeline
  - Wave attribution tagging on all findings via waveAttribution map
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns: [set-level review orchestration with directory chunking, parallel agent spawns per chunk up to 5 concurrent, wave attribution via scope command, aggregated acceptance criteria across all waves]

key-files:
  created: []
  modified:
    - skills/review/SKILL.md

key-decisions:
  - "Per-wave loop (Step 3) replaced entirely with single set-level pass (Step 4)"
  - "Acceptance criteria loaded from ALL waves before stage execution (Step 3)"
  - "Chunked stages present combined test plan as single approval, not per-chunk"
  - "UAT always runs on full set scope (never chunked) per locked decision"
  - "Lean review (wave-level) explicitly documented as unaffected in Important Notes"

patterns-established:
  - "Set-level orchestration: scope once, chunk once, run stages on set scope"
  - "Parallel agent spawning per chunk (up to 5 concurrent) for unit test and bug hunt"
  - "Wave attribution lookup pattern: waveAttribution[file] for originatingWave on findings"
  - "Acceptance criteria aggregation: iterate all waves, collect from JOB-PLAN.md files"

requirements-completed: [SET-REVIEW-06, SET-REVIEW-07]

# Metrics
duration: 4min
completed: 2026-03-10
---

# Phase 29.1 Plan 03: Set-Level Review Orchestrator Summary

**Rewritten review SKILL.md from per-wave loop to set-level orchestration with directory chunking, parallel agents, and wave attribution**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-10T02:10:36Z
- **Completed:** 2026-03-10T02:14:55Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Rewrote skills/review/SKILL.md (807 lines) removing the per-wave iteration loop entirely
- Implemented directory chunking orchestration for unit test and bug hunt stages with parallel agent spawns per chunk
- Added set-level scope step consuming chunks and waveAttribution from the review scope CLI command
- Added acceptance criteria aggregation across all waves in the set (Step 3)
- UAT runs once on full set scope (never chunked) per the locked user decision
- All review CLI commands (scope, log-issue, update-issue) use set-id only
- All report artifacts written to `.planning/waves/{setId}/` (no wave subdirectory)
- All findings tagged with originatingWave from waveAttribution map
- Bug hunt cycles 2-3 use flat scope on modified files only (no re-chunking)
- Updated Important Notes with chunking, wave attribution, lean review compatibility

## Task Commits

Each task was committed atomically:

1. **Task 1: Rewrite SKILL.md from per-wave loop to set-level review with chunking** - `05b664d` (feat)

## Files Created/Modified
- `skills/review/SKILL.md` - Set-level review orchestrator with directory chunking, parallel agent spawning, wave attribution, and aggregated acceptance criteria

## Decisions Made
- Per-wave loop (old Step 3 "For Each Wave") replaced entirely with Step 4 "Run Selected Stages on Set Scope"
- Acceptance criteria loading extracted into its own Step 3, iterating all waves before stages run
- Chunked unit test plan presented as single combined approval (grouped by directory chunk)
- UAT never chunked -- tests user workflows across full set scope
- Lean wave-level review documented as explicitly unaffected in Important Notes section
- File slightly exceeds 600-750 line target (807 lines) due to chunking orchestration complexity being inherently detailed

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 29.1 is now fully complete: library (Plan 01), CLI + roles (Plan 02), orchestrator (Plan 03)
- The full review pipeline operates at set level with directory chunking
- Lean wave-level review (used during /rapid:execute reconciliation) is unaffected
- Ready for production use of `/rapid:review <set-id>`

## Self-Check: PASSED

- File exists: skills/review/SKILL.md (807 lines)
- Commit verified: 05b664d (feat)
- Zero "For Each Wave" references
- Zero wave-id in review CLI commands (scope, log-issue, update-issue)
- All REVIEW-*.md paths at set level (.planning/waves/{setId}/)
- 39 chunk references present
- originatingWave in all log-issue calls

---
*Phase: 29.1-make-the-reviewing-set-based-instead-of-wave-based*
*Completed: 2026-03-10*
