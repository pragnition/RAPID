---
phase: 29.1
slug: make-the-reviewing-set-based-instead-of-wave-based
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-10
---

# Phase 29.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | node:test (built-in Node.js test runner) |
| **Config file** | none — uses `node --test` directly |
| **Quick run command** | `node --test src/lib/review.test.cjs` |
| **Full suite command** | `node --test src/lib/*.test.cjs` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `node --test src/lib/review.test.cjs`
- **After every plan wave:** Run `node --test src/lib/*.test.cjs`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 5 seconds

---

## Per-Task Verification Map

| Change Area | Behavior | Test Type | Automated Command | File Exists? | Status |
|-------------|----------|-----------|-------------------|-------------|--------|
| scopeSetForReview | Rename works, same return shape | unit | `node --test src/lib/review.test.cjs` | Needs update | ⬜ pending |
| chunkByDirectory | Chunks files by parent dir, handles threshold | unit | `node --test src/lib/review.test.cjs` | Wave 0 | ⬜ pending |
| buildWaveAttribution | Reads JOB-PLAN.md, builds file->wave map | unit | `node --test src/lib/review.test.cjs` | Wave 0 | ⬜ pending |
| logIssue (set-level) | Writes to set-level path, no wave subdirectory | unit | `node --test src/lib/review.test.cjs` | Needs update | ⬜ pending |
| loadSetIssues (simplified) | Reads single set-level file | unit | `node --test src/lib/review.test.cjs` | Needs update | ⬜ pending |
| updateIssueStatus (simplified) | Updates issue in set-level file | unit | `node --test src/lib/review.test.cjs` | Needs update | ⬜ pending |
| ReviewIssue schema | originatingWave optional field | unit | `node --test src/lib/review.test.cjs` | Needs update | ⬜ pending |
| ReviewIssues schema | waveId removed, setId only | unit | `node --test src/lib/review.test.cjs` | Needs update | ⬜ pending |
| CLI: review scope (set-only) | Accepts set-id only, returns chunks + attribution | integration | Manual | Manual | ⬜ pending |
| CLI: review log-issue (set-only) | Accepts set-id only | integration | Manual | Manual | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `src/lib/review.test.cjs` — update existing tests for set-level API (schema, logIssue, loadSetIssues, updateIssueStatus)
- [ ] `src/lib/review.test.cjs` — add new tests for `chunkByDirectory` function
- [ ] `src/lib/review.test.cjs` — add new tests for `buildWaveAttribution` function
- [ ] `src/lib/review.test.cjs` — verify `scopeSetForReview` (renamed) returns same shape

*Existing infrastructure covers framework; only test stubs need adding.*

---

## Manual-Only Verifications

| Behavior | Why Manual | Test Instructions |
|----------|------------|-------------------|
| CLI: review scope output format | CLI output formatting | Run `rapid-tools.cjs review scope <test-set>`, verify chunked output |
| CLI: review log-issue set-only | End-to-end CLI flow | Run `rapid-tools.cjs review log-issue <set-id>`, verify writes to set-level path |
| SKILL.md orchestration flow | Skill orchestration | Run `/rapid:review` on a test set, verify single-pass (not per-wave) |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 5s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
