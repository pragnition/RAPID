---
phase: 45-documentation-contracts-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - technical_documentation.md
  - docs/setup.md
  - docs/planning.md
  - docs/execution.md
  - docs/review.md
  - docs/merge-and-cleanup.md
  - docs/agents.md
  - docs/configuration.md
  - docs/state-machines.md
  - docs/troubleshooting.md
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "technical_documentation.md is a standalone narrative document covering the full v3.0 lifecycle"
    - "technical_documentation.md does not reference wave-plan, job-plan, or other removed v2 concepts"
    - "docs/ files contain v3-accurate content (DOCS.md links to them and must not break)"
    - "Agent reference lists exactly 26 agents with correct v3 roles"
    - "State machine docs describe set-level lifecycle only (no wave/job state)"
  artifacts:
    - path: "technical_documentation.md"
      provides: "Full v3.0 technical reference"
      min_lines: 200
    - path: "docs/agents.md"
      provides: "All 26 v3 agents with roles and spawn hierarchy"
      contains: "rapid-executor"
    - path: "docs/state-machines.md"
      provides: "Set-level state transitions only"
      contains: "SetStatus"
    - path: "docs/setup.md"
      provides: "v3 setup instructions"
    - path: "docs/planning.md"
      provides: "v3 planning workflow"
    - path: "docs/execution.md"
      provides: "v3 execution workflow"
    - path: "docs/review.md"
      provides: "v3 review pipeline"
    - path: "docs/merge-and-cleanup.md"
      provides: "v3 merge and cleanup workflow"
    - path: "docs/configuration.md"
      provides: "v3 configuration reference"
    - path: "docs/troubleshooting.md"
      provides: "v3 troubleshooting guide"
  key_links:
    - from: "technical_documentation.md"
      to: "docs/"
      via: "markdown links to docs/ files"
      pattern: "docs/"
    - from: "DOCS.md"
      to: "docs/"
      via: "markdown links (must not break)"
      pattern: "docs/"
---

<objective>
Rewrite technical_documentation.md and all docs/ files for v3.0.

Purpose: The current technical_documentation.md is a 47-line index pointing to v2.2 docs/ files. v3.0 needs a workflow-first narrative document covering the full lifecycle, plus updated docs/ files so DOCS.md links remain valid. This plan replaces all v2.2 technical documentation with v3.0-accurate content.

Output: Standalone technical_documentation.md (200+ lines) plus 9 updated docs/ files.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-documentation-contracts-cleanup/45-CONTEXT.md

<interfaces>
<!-- v3.0 command and architecture reference for documentation accuracy -->

v3.0 Set Lifecycle States (SetStatus enum):
- pending -> discussing -> planning -> executing -> complete -> merged

v3.0 STATE.json Schema (set-level only, no wave/job state):
```json
{
  "project": {
    "name": "string",
    "milestone": {
      "name": "string",
      "version": "string",
      "sets": [
        {
          "name": "string",
          "status": "pending|discussing|planning|executing|complete|merged",
          "branch": "string",
          "worktree": "string"
        }
      ]
    }
  }
}
```

v3.0 Agent Registry (26 agents total):
CORE (5, hand-written, SKIP_GENERATION):
- rapid-planner, rapid-executor, rapid-merger, rapid-reviewer (+ orchestrator removed)

RESEARCH (7):
- rapid-research-stack, rapid-research-features, rapid-research-architecture
- rapid-research-pitfalls, rapid-research-oversights, rapid-research-ux
- rapid-research-synthesizer

REVIEW (7):
- rapid-scoper, rapid-unit-tester, rapid-bug-hunter
- rapid-devils-advocate, rapid-judge, rapid-bugfix, rapid-uat

MERGE (2):
- rapid-set-merger, rapid-conflict-resolver

UTILITY (5):
- rapid-roadmapper, rapid-set-planner, rapid-plan-verifier
- rapid-verifier, rapid-codebase-synthesizer

CONTEXT (1):
- rapid-context-generator

v3.0 Skill -> Agent Spawn Map:
- /init: codebase-synthesizer + 6 researchers + synthesizer + roadmapper
- /start-set: set-planner
- /discuss-set --skip: research-stack
- /plan-set: research-stack + planner + plan-verifier
- /execute-set: executor (x waves) + verifier
- /review: scoper + unit-tester(s) + bug-hunter(s) + devils-advocate + judge + bugfix + uat
- /merge: set-merger(s) + conflict-resolver(s)
- /quick: planner + plan-verifier + executor
- /new-version: 6 researchers + synthesizer + roadmapper
- /context: context-generator

v3.0 Key Architectural Decisions:
- Skills are their own orchestrators (no central orchestrator agent)
- Interface contracts via CONTRACT.json (no GATES.json)
- Hybrid agent build: 5 hand-written core + 21 generated via build-agents
- Inline YAML tool docs per agent (not shared reference file)
- Crash recovery via STATE.json atomic writes + git-based recovery
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite technical_documentation.md as standalone v3.0 narrative</name>
  <files>technical_documentation.md</files>
  <action>
Full rewrite of technical_documentation.md. Do NOT edit v2.2 content in place -- start fresh.

Per user decision: "Workflow-first structure: walk through full lifecycle (init -> plan -> execute -> review -> merge) with examples. Commands woven into the narrative rather than isolated reference tables."

**Required structure:**

1. **Title + intro** -- "RAPID Technical Documentation" header. One paragraph: this is the deep reference for RAPID v3.0. Link to README.md for quickstart.

2. **Table of Contents** -- Link to each section below, plus link to docs/ files for topic-specific deep dives.

3. **Workflow Overview** -- ASCII diagram of the full lifecycle:
   ```
   INIT -> START-SET -> DISCUSS-SET -> PLAN-SET -> EXECUTE-SET -> REVIEW -> MERGE
   ```
   Explain each stage in 2-3 sentences. This is the narrative spine.

4. **Project Initialization** (`/rapid:init`)
   - What happens: 6 parallel researchers, codebase synthesizer, research synthesizer, roadmapper
   - Greenfield vs brownfield detection
   - Output: .planning/ directory, ROADMAP.md, STATE.json, CONTRACT.json
   - Batched discovery (4 topic batches, not 8-15 individual questions)

5. **Set Lifecycle** (`/rapid:start-set`, `/rapid:discuss-set`)
   - Starting a set: worktree creation, scoped CLAUDE.md, set planner
   - Discussion: 4 gray areas, batched questions, CONTEXT.md output
   - --skip flag for auto-generated context via rapid-research-stack

6. **Planning** (`/rapid:plan-set`)
   - 3-step pipeline: researcher -> planner -> verifier
   - 2-4 total agent spawns (not 15-20 from v2)
   - Output: PLAN.md files per wave
   - Contract enforcement after planning

7. **Execution** (`/rapid:execute-set`)
   - One rapid-executor agent per wave, waves sequential
   - Artifact-based completion detection (WAVE-COMPLETE.md markers + git commits)
   - Re-entry after crash: reads planning artifacts to determine what's done
   - Lean verification agent after all waves complete

8. **Review** (`/rapid:review`)
   - Scoper categorizes files by concern
   - Three-stage pipeline: unit testing -> bug hunt (hunter + advocate + judge) -> UAT
   - Directory chunking for large scopes (>15 files)
   - Up to 3 fix-and-rehunt cycles

9. **Merge** (`/rapid:merge`)
   - Clean merge fast-path via git merge-tree
   - Set-merger subagent for conflicting merges
   - 5-level conflict detection (textual, structural, dependency, API, semantic)
   - 4-tier resolution cascade
   - Adaptive conflict resolution with confidence bands
   - Contract validation before merge

10. **Auxiliary Commands**
    - /rapid:status -- dashboard, set statuses, next-action suggestions
    - /rapid:install -- shell configuration, RAPID_TOOLS env var, .env
    - /rapid:new-version -- archive milestone, 6-researcher pipeline, new roadmap
    - /rapid:add-set -- add sets mid-milestone, direct STATE.json write
    - /rapid:quick -- ad-hoc tasks without set structure (planner -> verifier -> executor)

11. **Utility Commands**
    - /rapid:assumptions, /rapid:pause, /rapid:resume, /rapid:context, /rapid:cleanup, /rapid:help

12. **Agent Reference** -- Table listing all 26 agents with: name, category (Core/Research/Review/Merge/Utility/Context), what skill spawns it, what it produces. Group by category. Per user decision: "Full agent reference: table/section listing each agent, its role, when spawned, what it produces."

13. **State Machine** -- SetStatus lifecycle: pending -> discussing -> planning -> executing -> complete -> merged. ASCII diagram. Note: v3 has NO wave or job state -- sets are the only stateful entity.

14. **Configuration** -- RAPID_TOOLS env var, .env file, STATE.json schema, .planning/ directory layout.

**Per user decision: no troubleshooting section.**

**Style guidelines:**
- Walk through the lifecycle narratively, weaving commands into the story
- Use code blocks for commands and examples
- No emojis
- Do not reference v2 concepts: wave-plan, job-plan, set-init, orchestrator agent, GATES.json, WaveState, JobState, derived status
- Keep each section substantial but not bloated -- aim for total 300-500 lines
  </action>
  <verify>
    <automated>cd /home/kek/Projects/RAPID && wc -l technical_documentation.md | awk '{if ($1 >= 200) print "PASS: " $1 " lines"; else print "FAIL: only " $1 " lines"}' && grep -c "wave-plan\|job-plan\|set-init\|GATES\|WaveState\|JobState\|orchestrator" technical_documentation.md | grep -q "^0$" && echo "PASS: no v2 refs" || echo "FAIL: v2 refs found"</automated>
  </verify>
  <done>
    - technical_documentation.md is 200+ lines
    - Covers full lifecycle narratively
    - Has agent reference table with all 26 agents
    - Zero references to removed v2 concepts
  </done>
</task>

<task type="auto">
  <name>Task 2: Update docs/ directory files with v3.0 content</name>
  <files>
    docs/setup.md,
    docs/planning.md,
    docs/execution.md,
    docs/review.md,
    docs/merge-and-cleanup.md,
    docs/agents.md,
    docs/configuration.md,
    docs/state-machines.md,
    docs/troubleshooting.md
  </files>
  <action>
Update all 9 docs/ files with v3.0-accurate content. These files are linked from DOCS.md (which is NOT being modified), so the links must remain valid and the content must be accurate.

Each docs/ file should be a focused deep-dive on its topic. Keep them concise but accurate -- technical_documentation.md is the main narrative, these are supplementary reference.

**docs/setup.md** -- v3 setup:
- /rapid:install (shell config, RAPID_TOOLS, .env)
- /rapid:init (6 researchers, greenfield/brownfield, roadmap)
- /rapid:context (codebase analysis, context documents)

**docs/planning.md** -- v3 planning:
- /rapid:start-set (worktree, scoped CLAUDE.md, set-planner)
- /rapid:discuss-set (4 gray areas, batched questions, CONTEXT.md, --skip)
- /rapid:plan-set (3-step pipeline: researcher -> planner -> verifier)
- /rapid:quick (ad-hoc tasks, 3-agent pipeline)
- /rapid:add-set (mid-milestone set addition)
- Remove all references to /rapid:plan, /rapid:wave-plan, /rapid:assumptions (move to utilities note)

**docs/execution.md** -- v3 execution:
- /rapid:execute-set (one executor per wave, sequential waves)
- Artifact-based completion detection
- Re-entry after crash
- Verification agent post-execution
- Remove references to /rapid:execute, job-executor dispatch, parallel jobs

**docs/review.md** -- v3 review:
- /rapid:review (scoper + unit-tester + bug hunt + UAT)
- Concern-group-based chunking
- 3 fix-and-rehunt cycles
- Content is largely stable from v2.2 but remove wave-level references

**docs/merge-and-cleanup.md** -- v3 merge:
- /rapid:merge (clean fast-path, set-merger subagents, 5-level detection, 4-tier resolution)
- /rapid:cleanup (worktree removal)
- /rapid:new-version (replaces /rapid:new-milestone -- archive, 6 researchers, new roadmap)
- Remove references to wave-level merge orchestration

**docs/agents.md** -- v3 agent reference:
- List all 26 agents grouped by category: Core (4), Research (7), Review (7), Merge (2), Utility (5), Context (1)
- For each agent: name, type badge, which skill spawns it, what it produces, brief description
- Show spawn hierarchy diagram
- Note: orchestrator agent is removed in v3; skills dispatch agents directly
- Remove any agents not in the v3 agents/ directory

**docs/configuration.md** -- v3 configuration:
- RAPID_TOOLS env var
- .env file (ANTHROPIC_API_KEY, LANGFUSE_*)
- STATE.json schema (set-level only -- no wave/job state)
- .planning/ directory layout for v3
- Remove wave/job configuration details

**docs/state-machines.md** -- v3 state machine:
- SetStatus: pending -> discussing -> planning -> executing -> complete -> merged
- ASCII transition diagram
- Transition rules and validation
- Crash recovery (detectCorruption, recoverFromGit, atomic writes)
- Remove ALL wave and job state machine content (WaveState, JobState, derived status)

**docs/troubleshooting.md** -- v3 troubleshooting:
- Common failure modes with symptom/cause/fix
- Update for v3 commands (e.g., "execute-set fails" not "execute fails")
- Remove wave/job-specific failure modes
- Add: STATE.json corruption recovery, worktree conflicts, contract validation failures

**For all files:**
- No emojis
- No references to: wave-plan, job-plan, set-init (old name), orchestrator agent, GATES.json, WaveState, JobState
- Use v3 command names consistently
  </action>
  <verify>
    <automated>cd /home/kek/Projects/RAPID && for f in docs/setup.md docs/planning.md docs/execution.md docs/review.md docs/merge-and-cleanup.md docs/agents.md docs/configuration.md docs/state-machines.md docs/troubleshooting.md; do [ -f "$f" ] && echo "OK: $f exists" || echo "FAIL: $f missing"; done && grep -rl "wave-plan\|job-plan\|/rapid:set-init\|GATES\.json\|WaveState\|JobState" docs/ && echo "FAIL: v2 refs in docs/" || echo "PASS: no v2 refs in docs/"</automated>
  </verify>
  <done>
    - All 9 docs/ files updated with v3.0 content
    - Zero references to removed v2 concepts across all docs/ files
    - DOCS.md links (which point to docs/) remain valid
    - docs/agents.md lists exactly 26 agents
    - docs/state-machines.md describes set-level state only
  </done>
</task>

</tasks>

<verification>
```bash
# 1. technical_documentation.md exists and is substantial
wc -l technical_documentation.md

# 2. All docs/ files exist
ls docs/setup.md docs/planning.md docs/execution.md docs/review.md docs/merge-and-cleanup.md docs/agents.md docs/configuration.md docs/state-machines.md docs/troubleshooting.md

# 3. No v2 concepts anywhere in docs
grep -rn "wave-plan\|job-plan\|/rapid:set-init\|GATES\.json\|WaveState\|JobState" technical_documentation.md docs/ && echo "FAIL" || echo "OK"

# 4. Agent count in docs/agents.md
grep -c "rapid-" docs/agents.md
```
</verification>

<success_criteria>
- technical_documentation.md is a standalone 200+ line narrative covering the full v3.0 lifecycle
- All 9 docs/ files contain v3.0-accurate content
- Agent reference documents all 26 agents
- State machine docs describe set-level lifecycle only
- Zero v2 concept references across all documentation
- DOCS.md links to docs/ files remain valid
</success_criteria>

<output>
After completion, create `.planning/phases/45-documentation-contracts-cleanup/45-03-SUMMARY.md`
</output>
