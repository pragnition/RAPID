---
phase: 40-cli-surface-utility-commands
plan: 03
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - skills/review/SKILL.md
  - skills/merge/SKILL.md
autonomous: true
requirements: [CMD-06, CMD-07]

must_haves:
  truths:
    - "/review gates on set status = 'complete' before allowing review"
    - "/review does not reference wave-level state traversal for orchestration"
    - "/review uses v3 command names in next-step suggestions"
    - "/merge checks set status = 'complete' (not wave completion)"
    - "/merge auto-transitions set to 'merged' after successful merge to main"
    - "/merge does not use execute wave-status for readiness checks"
    - "/merge uses v3 command names in suggestions"
  artifacts:
    - path: "skills/review/SKILL.md"
      provides: "v3 review orchestrator"
      contains: "complete"
      min_lines: 400
    - path: "skills/merge/SKILL.md"
      provides: "v3 merge orchestrator"
      contains: "merged"
      min_lines: 300
  key_links:
    - from: "skills/review/SKILL.md"
      to: "rapid-tools.cjs state get --all"
      via: "set status check for 'complete'"
      pattern: "state get --all"
    - from: "skills/review/SKILL.md"
      to: "rapid-tools.cjs state transition set"
      via: "transition to reviewing"
      pattern: "state transition set"
    - from: "skills/merge/SKILL.md"
      to: "rapid-tools.cjs state get --all"
      via: "set status check for 'complete'"
      pattern: "state get --all"
    - from: "skills/merge/SKILL.md"
      to: "rapid-tools.cjs state transition set"
      via: "auto-transition to merged"
      pattern: "state transition set.*merged"
---

<objective>
Update /review and /merge skills to work with the simplified set-level state schema from Phase 38, gating on 'complete' status and using v3 command references.

Purpose: Review and merge pipelines validate set status correctly without wave/job state traversal, and auto-transition sets through the lifecycle.
Output: Updated review SKILL.md and merge SKILL.md with set-level state handling.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-cli-surface-utility-commands/40-CONTEXT.md
@.planning/phases/40-cli-surface-utility-commands/40-01-SUMMARY.md

<interfaces>
<!-- State machine API for review and merge -->

From src/lib/state-schemas.cjs:
```javascript
const SetStatus = z.enum(['pending', 'discussing', 'planning', 'executing', 'complete', 'merged']);
```

From src/lib/state-machine.cjs:
```javascript
// Key exports for review/merge
readState(cwd)              // -> { valid, state, errors } or null
findMilestone(state, mId)   // -> milestone with .sets array
findSet(state, mId, sId)    // -> set with .status
transitionSet(cwd, mId, sId, newStatus) // atomic transition
```

CLI commands:
- `node "${RAPID_TOOLS}" state get --all` -- full STATE.json
- `node "${RAPID_TOOLS}" state transition set <mId> <sId> <status>` -- atomic transition
- `node "${RAPID_TOOLS}" review scope <set-id>` -- scopes changed files (wave-id arg now optional)
- `node "${RAPID_TOOLS}" merge order` -- DAG merge order
- `node "${RAPID_TOOLS}" merge status` -- merge pipeline status
- `node "${RAPID_TOOLS}" merge execute <set>` -- performs the merge
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update /review skill for v3.0 set-level state handling</name>
  <files>skills/review/SKILL.md</files>
  <action>
    Update skills/review/SKILL.md with set-level state changes. This is a targeted update, NOT a full rewrite. The review pipeline internals (unit test, bug hunt, UAT) stay intact per CONTEXT.md decision. Changes:

    **Step 0c: Update status gate**
    Change the status check from `executing` or `reviewing` to `complete` or `reviewing`:
    - Old: "The set status MUST be `executing` or `reviewing`"
    - New: "The set status MUST be `complete` or `reviewing`. If the set is in any other status (e.g., `pending`, `planning`, `executing`): Cannot review set '{set-id}' -- current status is '{status}'. Set must be in 'complete' or 'reviewing' state. Run `/rapid:execute-set {set-id}` first."

    Note: This gates on 'complete' per CONTEXT.md decision. Phase 44 will implement the execute -> complete transition. The cross-phase dependency is documented but not blocked.

    **Step 0d: Transition to 'reviewing'**
    Change from: "If the set is currently in `executing` state, transition it to `reviewing`"
    To: "If the set is currently in `complete` state, transition it to `reviewing`"

    **Step 3: Load Acceptance Criteria -- simplify wave references**
    The existing code reads JOB-PLAN.md files across all waves. This still works because planning artifacts exist on disk regardless of state schema. However, update the description to clarify this reads planning artifacts (not state):
    - Change: "Read ALL JOB-PLAN.md files across ALL waves" to "Read ALL JOB-PLAN.md files from the set's planning directory"
    - The actual CLI call `wave-plan list-jobs` still works (CLI internals deferred to Phase 45)

    **Review artifact paths -- keep as-is**
    Review artifacts write to `.planning/waves/{setId}/REVIEW-*.md`. These paths are planning artifact paths, not state paths. They continue to work correctly. Do NOT change these paths.

    **Step 6: Update next-step suggestions**
    Change: `/rapid:merge {setIndex}` stays (already correct)
    Change: `/rapid:review {setIndex}` stays (already correct)
    Remove any references to `/rapid:execute` (use `/rapid:execute-set` if referenced)

    **Important notes section updates:**
    - Change "The set transitions `executing` -> `reviewing`" to "The set transitions `complete` -> `reviewing`"
    - Remove the note about lean wave-level review (wave-level review concept is removed in v3)
    - Update any `/rapid:execute` references to `/rapid:execute-set`

    **Do NOT change:**
    - Review pipeline internals (unit test chunking, bug hunt adversarial pipeline, UAT)
    - Scoper agent integration
    - RAPID:RETURN parsing
    - Review artifact write paths
    - CLI subcommand calls (review scope, review log-issue, etc.) -- deferred to Phase 45
  </action>
  <verify>
    <automated>grep "'complete'" skills/review/SKILL.md && grep -c "execute-set" skills/review/SKILL.md | xargs test 0 -lt && ! grep "executing.*or.*reviewing" skills/review/SKILL.md && echo "Review updated"</automated>
  </verify>
  <done>
    Review gates on 'complete' status (not 'executing').
    Transition is from 'complete' to 'reviewing' (not 'executing' to 'reviewing').
    Next-step suggestions use v3 command names (execute-set).
    Important notes reflect v3 state flow.
    Review pipeline internals unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update /merge skill for v3.0 set-level state handling</name>
  <files>skills/merge/SKILL.md</files>
  <action>
    Update skills/merge/SKILL.md with set-level state changes. Per CONTEXT.md: "Rewrite merge state handling to fully embrace simplified schema -- not just patching wave/job references." This is a deeper update than review. Changes:

    **Step 1c: Remove wave-status dependency**
    Remove the `execute wave-status` call:
    ```bash
    # REMOVE THIS LINE:
    node "${RAPID_TOOLS}" execute wave-status
    ```
    Instead, check set readiness directly from STATE.json:
    ```bash
    node "${RAPID_TOOLS}" state get --all
    ```
    Parse the state to find sets with status='complete'. A set is ready to merge when its status is 'complete' (not 'phase=Done' from old wave/job traversal).

    **Step 1c: Update readiness check language**
    Change: "Identify which sets are ready to merge (phase=Done, mergeStatus=pending)"
    To: "Identify which sets are ready to merge (status='complete' in STATE.json, mergeStatus=pending in MERGE-STATE)"

    **Step 6: Add auto-transition to 'merged'**
    After `merge execute` returns `merged: true`, auto-transition the set status:
    ```bash
    node "${RAPID_TOOLS}" state transition set <milestone> <setName> merged
    ```
    Display: "[{waveNum}/{totalWaves}] {setName}: MERGED and status updated to 'merged' (commit {commitHash})"

    This is the key new behavior per CONTEXT.md: "Auto-transition set to 'merged' status after successful merge to main"

    **Step 8: Update next-step suggestions**
    Change: `/rapid:new-milestone` to `/rapid:new-version`
    Change: `/rapid:cleanup` stays
    Change: `/rapid:status` stays

    **Wave terminology in merge context**
    The merge pipeline's concept of "waves" is about merge ordering (DAG waves), NOT execution waves. This is a valid concept that remains -- sets merge in DAG-defined wave order. Do NOT remove wave terminology from the merge pipeline's processing loop. The DAG wave concept (Wave 1 sets merge first, then Wave 2) is independent of the old execution wave/job hierarchy.

    **Update Important Notes:**
    - Remove "Sequential within waves" note's reference to execution wave context (keep the merge-wave-ordering concept)
    - Add note: "Set status auto-transitions to 'merged' after successful merge. This is the terminal set lifecycle state."
    - Update any `/rapid:execute` references to `/rapid:execute-set`

    **Do NOT change:**
    - Merge pipeline internals (fast-path, subagent dispatch, conflict detection, resolution cascade)
    - MERGE-STATE.json handling
    - DAG ordering logic
    - Bisection recovery
    - Rollback flow
    - Adaptive conflict resolution (Step 3e)
    - CLI subcommand calls (merge order, merge execute, etc.)
  </action>
  <verify>
    <automated>grep "state transition set.*merged" skills/merge/SKILL.md && grep "'complete'" skills/merge/SKILL.md && ! grep "execute wave-status" skills/merge/SKILL.md && grep "new-version" skills/merge/SKILL.md && echo "Merge updated"</automated>
  </verify>
  <done>
    Merge checks set status='complete' for readiness (not wave completion).
    Merge auto-transitions set to 'merged' after successful merge.
    execute wave-status call removed.
    Next-step suggestions use v3 command names (new-version).
    DAG wave ordering preserved (this is merge ordering, not execution waves).
    Merge pipeline internals unchanged.
  </done>
</task>

</tasks>

<verification>
- Review gates on 'complete' status, transitions to 'reviewing'
- Merge checks 'complete' status, auto-transitions to 'merged'
- No `execute wave-status` call in merge skill
- Both skills use v3 command names in suggestions
- Both skills preserve pipeline internals (review pipeline, merge pipeline)
</verification>

<success_criteria>
- `grep "'complete'" skills/review/SKILL.md skills/merge/SKILL.md` matches both files
- `grep "state transition set.*merged" skills/merge/SKILL.md` matches (auto-transition)
- `grep "execute wave-status" skills/merge/SKILL.md` returns no matches
- Both files use /rapid:execute-set (not /rapid:execute) in suggestions
</success_criteria>

<output>
After completion, create `.planning/phases/40-cli-surface-utility-commands/40-03-SUMMARY.md`
</output>
