---
phase: 40-cli-surface-utility-commands
plan: 04
type: execute
wave: 1
depends_on: ["40-01"]
files_modified:
  - src/lib/display.cjs
  - src/lib/display.test.cjs
autonomous: true
gap_closure: true
requirements:
  - CMD-06
  - CMD-07
  - CMD-08
  - CMD-12
  - UX-03
  - UX-04

must_haves:
  truths:
    - "display banner start-set produces a branded ANSI banner with 'STARTING SET' verb"
    - "display banner discuss-set produces a branded ANSI banner with 'DISCUSSING SET' verb"
    - "display banner execute-set produces a branded ANSI banner with 'EXECUTING SET' verb"
    - "display banner new-version produces a branded ANSI banner with 'NEW VERSION' verb"
  artifacts:
    - path: "src/lib/display.cjs"
      provides: "v3 stage name registration in STAGE_VERBS and STAGE_BG"
      contains: "start-set.*STARTING SET"
    - path: "src/lib/display.test.cjs"
      provides: "Test coverage for all 12 stage names (8 legacy + 4 v3)"
      contains: "start-set.*discuss-set.*execute-set.*new-version"
  key_links:
    - from: "skills/start-set/SKILL.md"
      to: "src/lib/display.cjs"
      via: "display banner start-set CLI call"
      pattern: "STAGE_VERBS\\['start-set'\\]"
    - from: "skills/discuss-set/SKILL.md"
      to: "src/lib/display.cjs"
      via: "display banner discuss-set CLI call"
      pattern: "STAGE_VERBS\\['discuss-set'\\]"
    - from: "skills/execute-set/SKILL.md"
      to: "src/lib/display.cjs"
      via: "display banner execute-set CLI call"
      pattern: "STAGE_VERBS\\['execute-set'\\]"
---

<objective>
Fix broken banner display for v3 renamed commands by registering 4 new stage names in display.cjs.

Purpose: Phase 40 renamed 4 skills (start-set, discuss-set, execute-set, new-version) which call `display banner <stage>` at startup. The display module was not updated, so all 4 produce "[RAPID] Unknown stage: X" instead of branded banners. This is a functional regression that affects every invocation of these commands.

Output: Updated display.cjs with 4 new entries in both STAGE_VERBS and STAGE_BG maps, plus updated tests covering all 12 stage names.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-cli-surface-utility-commands/40-VERIFICATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/display.cjs:
```javascript
const STAGE_VERBS = {
  'init': 'INITIALIZING',
  'set-init': 'PREPARING',
  'discuss': 'DISCUSSING',
  'wave-plan': 'PLANNING',
  'plan-set': 'PLANNING SET',
  'execute': 'EXECUTING',
  'review': 'REVIEWING',
  'merge': 'MERGING',
};

const STAGE_BG = {
  'init': '\x1b[104m',        // bright blue
  'set-init': '\x1b[104m',    // bright blue
  'discuss': '\x1b[104m',     // bright blue
  'wave-plan': '\x1b[104m',   // bright blue
  'plan-set': '\x1b[104m',    // bright blue
  'execute': '\x1b[102m',     // bright green
  'review': '\x1b[101m',      // bright red
  'merge': '\x1b[101m',       // bright red
};

module.exports = { renderBanner, STAGE_VERBS, STAGE_BG };
```

Existing test file: `src/lib/display.test.cjs` uses `node:test` + `node:assert/strict`. Tests reference `expectedStages` arrays of 8 entries that need updating to 12.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add v3 stage names to display.cjs and update tests</name>
  <files>src/lib/display.cjs, src/lib/display.test.cjs</files>
  <behavior>
    - Test: STAGE_VERBS contains 12 entries (8 legacy + 4 new: start-set, discuss-set, execute-set, new-version)
    - Test: STAGE_BG contains 12 entries matching the same 12 stage names
    - Test: STAGE_VERBS['start-set'] === 'STARTING SET'
    - Test: STAGE_VERBS['discuss-set'] === 'DISCUSSING SET'
    - Test: STAGE_VERBS['execute-set'] === 'EXECUTING SET'
    - Test: STAGE_VERBS['new-version'] === 'NEW VERSION'
    - Test: renderBanner('start-set') returns ANSI-colored banner containing 'STARTING SET' (not 'Unknown stage')
    - Test: renderBanner('discuss-set') returns ANSI-colored banner containing 'DISCUSSING SET'
    - Test: renderBanner('execute-set') returns ANSI-colored banner containing 'EXECUTING SET'
    - Test: renderBanner('new-version') returns ANSI-colored banner containing 'NEW VERSION'
    - Test: start-set, discuss-set use bright blue bg (104m); execute-set uses bright green bg (102m); new-version uses bright blue bg (104m)
    - Test: All 12 stages produce consistent padded width banners
  </behavior>
  <action>
    1. Update display.test.cjs FIRST (RED phase):
       - Change all `expectedStages` arrays from 8 entries to 12 by adding 'start-set', 'discuss-set', 'execute-set', 'new-version'
       - In the "has expected verb mappings" test, add assertions:
         assert.equal(display.STAGE_VERBS['start-set'], 'STARTING SET');
         assert.equal(display.STAGE_VERBS['discuss-set'], 'DISCUSSING SET');
         assert.equal(display.STAGE_VERBS['execute-set'], 'EXECUTING SET');
         assert.equal(display.STAGE_VERBS['new-version'], 'NEW VERSION');
       - In the "maps all N stages" tests, change "8 stages" to "12 stages" in description strings
       - In the planning stages BG test, add 'start-set' and 'discuss-set' to the planningStages array
       - Add a new test: 'new v3 execution stage (execute-set) uses green background' asserting STAGE_BG['execute-set'] === '\x1b[102m]'
       - In renderBanner section, add tests for each new stage producing valid ANSI banners with correct verb strings
       - Update the "all N stages produce valid banner strings" test from 8 to 12
       - Update the "banner output ends with ANSI reset code" test from 8 to 12
       - Update the "consistent padded width" test from 8 to 12
       - Update the "planning stages use blue background" test to include start-set, discuss-set, new-version
       - Update the "execution stages use green background" test to include execute-set
       - Run tests: `node --test src/lib/display.test.cjs` -- tests should FAIL (RED)

    2. Update display.cjs (GREEN phase):
       - Add 4 entries to STAGE_VERBS:
         'start-set': 'STARTING SET',
         'discuss-set': 'DISCUSSING SET',
         'execute-set': 'EXECUTING SET',
         'new-version': 'NEW VERSION',
       - Add 4 entries to STAGE_BG:
         'start-set': '\x1b[104m',    // bright blue (planning stage)
         'discuss-set': '\x1b[104m',  // bright blue (planning stage)
         'execute-set': '\x1b[102m',  // bright green (execution stage)
         'new-version': '\x1b[104m',  // bright blue (lifecycle stage)
       - Update the JSDoc comment on renderBanner to list all 12 stage names
       - Update the STAGE_BG group comment to include the new stages in the correct groups
       - Run tests: `node --test src/lib/display.test.cjs` -- all tests should PASS (GREEN)

    3. Verify CLI integration:
       - Run `node src/bin/rapid-tools.cjs display banner start-set` -- should show colored banner with "STARTING SET"
       - Run `node src/bin/rapid-tools.cjs display banner discuss-set` -- should show "DISCUSSING SET"
       - Run `node src/bin/rapid-tools.cjs display banner execute-set` -- should show "EXECUTING SET"
       - Run `node src/bin/rapid-tools.cjs display banner new-version` -- should show "NEW VERSION"
       - Confirm none produce "Unknown stage"

    Note on Gap 2 (command registry in rapid-tools.cjs): The CONTEXT.md explicitly defers CLI subcommand changes to Phase 45. The help skill (skills/help/SKILL.md) satisfies the "help text" portion of success criterion 5. The "command registry" wording in the criterion was ambiguous; since rapid-tools.cjs is an internal agent CLI tool (not user-facing), and CONTEXT.md says deprecation stubs live as skill files (not in rapid-tools.cjs), the help skill alone satisfies the intent. No code change needed.
  </action>
  <verify>
    <automated>cd /home/kek/Projects/RAPID && node --test src/lib/display.test.cjs && node src/bin/rapid-tools.cjs display banner start-set 2>&1 | grep -v "Unknown stage" && node src/bin/rapid-tools.cjs display banner discuss-set 2>&1 | grep -v "Unknown stage" && node src/bin/rapid-tools.cjs display banner execute-set 2>&1 | grep -v "Unknown stage" && node src/bin/rapid-tools.cjs display banner new-version 2>&1 | grep -v "Unknown stage"</automated>
  </verify>
  <done>All 12 stage names (8 legacy + 4 v3) are registered in STAGE_VERBS and STAGE_BG. All tests pass. Running `display banner start-set`, `display banner discuss-set`, `display banner execute-set`, and `display banner new-version` via rapid-tools.cjs produces branded ANSI banners (not "Unknown stage" fallback).</done>
</task>

</tasks>

<verification>
1. `node --test src/lib/display.test.cjs` -- all tests pass (including new v3 stage name tests)
2. `node src/bin/rapid-tools.cjs display banner start-set` -- shows colored "STARTING SET" banner
3. `node src/bin/rapid-tools.cjs display banner discuss-set` -- shows colored "DISCUSSING SET" banner
4. `node src/bin/rapid-tools.cjs display banner execute-set` -- shows colored "EXECUTING SET" banner
5. `node src/bin/rapid-tools.cjs display banner new-version` -- shows colored "NEW VERSION" banner
6. None of the above produce "[RAPID] Unknown stage: X" output
</verification>

<success_criteria>
- display.cjs STAGE_VERBS has exactly 12 entries (8 legacy + 4 v3)
- display.cjs STAGE_BG has exactly 12 entries matching STAGE_VERBS keys
- All display.test.cjs tests pass with 12-stage coverage
- CLI `display banner` commands for start-set, discuss-set, execute-set, new-version all produce branded ANSI banners
- Gap 1 from VERIFICATION.md is fully closed
- Gap 2 from VERIFICATION.md is clarified as satisfied by help skill per CONTEXT.md scope deferral
</success_criteria>

<output>
After completion, create `.planning/phases/40-cli-surface-utility-commands/40-04-SUMMARY.md`
</output>
