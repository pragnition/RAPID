---
phase: 38-state-machine-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/state-schemas.cjs
  - src/lib/state-schemas.test.cjs
  - src/lib/state-transitions.cjs
  - src/lib/state-transitions.test.cjs
  - src/lib/lock.cjs
  - src/lib/lock.test.cjs
autonomous: true
requirements: [STATE-01, STATE-02, STATE-03, STATE-05]

must_haves:
  truths:
    - "SetStatus enum contains exactly: pending, discussing, planning, executing, complete, merged"
    - "SetState schema is flat: { id, status } with no waves or jobs arrays"
    - "WaveState, JobState, WaveStatus, JobStatus schemas no longer exist"
    - "pending can transition to both discussing and planning (branch point)"
    - "discussing -> planning -> executing -> complete -> merged is the full forward chain"
    - "No back-transitions are allowed -- all transitions are strictly forward"
    - "merged is terminal with no valid transitions"
    - "Stale lock files owned by dead processes are cleaned up automatically"
    - "WAVE_TRANSITIONS and JOB_TRANSITIONS no longer exist"
  artifacts:
    - path: "src/lib/state-schemas.cjs"
      provides: "Zod schemas for SetStatus, SetState, MilestoneState, ProjectState"
      exports: ["SetStatus", "SetState", "MilestoneState", "ProjectState"]
    - path: "src/lib/state-schemas.test.cjs"
      provides: "Unit tests for simplified schemas"
      min_lines: 80
    - path: "src/lib/state-transitions.cjs"
      provides: "SET_TRANSITIONS map and validateTransition function"
      exports: ["SET_TRANSITIONS", "validateTransition"]
    - path: "src/lib/state-transitions.test.cjs"
      provides: "Unit tests for simplified transitions"
      min_lines: 80
    - path: "src/lib/lock.cjs"
      provides: "acquireLock, isLocked, ensureLocksDir, cleanStaleLocks"
      exports: ["acquireLock", "isLocked", "ensureLocksDir", "cleanStaleLocks"]
    - path: "src/lib/lock.test.cjs"
      provides: "Unit tests for lock including PID-based stale cleanup"
      min_lines: 100
  key_links:
    - from: "src/lib/state-transitions.cjs"
      to: "src/lib/state-schemas.cjs"
      via: "SET_TRANSITIONS keys must match SetStatus enum values"
      pattern: "SET_TRANSITIONS.*pending.*discussing.*planning.*executing.*complete.*merged"
    - from: "src/lib/lock.cjs"
      to: "process.kill(pid, 0)"
      via: "PID existence check for stale lock cleanup"
      pattern: "process\\.kill.*0"
---

<objective>
Rewrite state-schemas.cjs, state-transitions.cjs, and lock.cjs to collapse the state hierarchy to set-level only, add the new SetStatus enum (pending | discussing | planning | executing | complete | merged), remove all wave/job schemas and transitions, and add PID-based stale lock cleanup. Each source file gets a companion test rewrite.

Purpose: These three modules are the foundation that state-machine.cjs (Plan 02) imports from. They must be correct and tested before the state machine can be rewritten.
Output: 3 source files rewritten + 3 test files rewritten, all tests passing.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-state-machine-simplification/38-CONTEXT.md
@.planning/phases/38-state-machine-simplification/38-RESEARCH.md

<interfaces>
<!-- Current exports from state-schemas.cjs (will be reduced): -->
<!-- OLD: JobStatus, JobState, WaveStatus, WaveState, SetStatus, SetState, MilestoneState, ProjectState -->
<!-- NEW: SetStatus, SetState, MilestoneState, ProjectState (only these 4) -->

<!-- Current exports from state-transitions.cjs (will be reduced): -->
<!-- OLD: SET_TRANSITIONS, WAVE_TRANSITIONS, JOB_TRANSITIONS, validateTransition -->
<!-- NEW: SET_TRANSITIONS, validateTransition (only these 2) -->
<!-- validateTransition signature changes from (entityType, currentStatus, nextStatus) to (currentStatus, nextStatus) -->

<!-- Current exports from lock.cjs (will be expanded): -->
<!-- OLD: acquireLock, isLocked, ensureLocksDir -->
<!-- NEW: acquireLock, isLocked, ensureLocksDir, cleanStaleLocks -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite state-schemas.cjs and state-schemas.test.cjs</name>
  <files>src/lib/state-schemas.cjs, src/lib/state-schemas.test.cjs</files>
  <behavior>
    - SetStatus.parse('discussing') succeeds; SetStatus.parse('merged') succeeds
    - SetStatus.parse('reviewing') throws; SetStatus.parse('merging') throws; SetStatus.parse('failed') throws
    - SetState.parse({ id: 'auth', status: 'pending' }) succeeds with no extra fields
    - SetState.parse({ id: 'auth', status: 'pending', waves: [...] }) strips the waves key silently (Zod default behavior)
    - SetState.parse({ id: 'auth' }) defaults status to 'pending'
    - MilestoneState.parse({ id: 'm1', name: 'M1', sets: [{ id: 's1', status: 'discussing' }] }) succeeds
    - ProjectState round-trip: parse(stringify(parse(validInput))) === parse(validInput)
    - JobStatus, JobState, WaveStatus, WaveState are NOT exported (require('./state-schemas.cjs').JobStatus === undefined)
    - Module exports exactly 4 keys: SetStatus, SetState, MilestoneState, ProjectState
  </behavior>
  <action>
Rewrite src/lib/state-schemas.cjs as a clean file with these exact contents:

```javascript
'use strict';
const { z } = require('zod');

const SetStatus = z.enum(['pending', 'discussing', 'planning', 'executing', 'complete', 'merged']);

const SetState = z.object({
  id: z.string(),
  status: SetStatus.default('pending'),
});

const MilestoneState = z.object({
  id: z.string(),
  name: z.string(),
  sets: z.array(SetState).default([]),
});

const ProjectState = z.object({
  version: z.literal(1),
  projectName: z.string(),
  currentMilestone: z.string(),
  milestones: z.array(MilestoneState).default([]),
  lastUpdatedAt: z.string(),
  createdAt: z.string(),
});

module.exports = { SetStatus, SetState, MilestoneState, ProjectState };
```

Then rewrite src/lib/state-schemas.test.cjs with tests covering all behaviors above. Use `node:test` describe/it blocks and `node:assert/strict`. Group tests by schema (SetStatus, SetState, MilestoneState, ProjectState) plus a "removed exports" group that asserts JobStatus/JobState/WaveStatus/WaveState are undefined.

Note: The old test file has 211 lines covering wave/job schemas -- those tests are all deleted. Write new tests only for the simplified schemas.
  </action>
  <verify>
    <automated>node --test src/lib/state-schemas.test.cjs</automated>
  </verify>
  <done>SetStatus enum has exactly 6 values (pending/discussing/planning/executing/complete/merged), SetState is { id, status } only, no wave/job schemas exported, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite state-transitions.cjs and state-transitions.test.cjs</name>
  <files>src/lib/state-transitions.cjs, src/lib/state-transitions.test.cjs</files>
  <behavior>
    - validateTransition('pending', 'discussing') succeeds (no throw)
    - validateTransition('pending', 'planning') succeeds (branch point -- --skip flow)
    - validateTransition('discussing', 'planning') succeeds
    - validateTransition('planning', 'executing') succeeds
    - validateTransition('executing', 'complete') succeeds
    - validateTransition('complete', 'merged') succeeds
    - validateTransition('merged', anything) throws with "terminal state" message
    - validateTransition('pending', 'executing') throws with "Valid transitions from" message
    - validateTransition('discussing', 'pending') throws (no back-transitions)
    - validateTransition('complete', 'executing') throws (no back-transitions)
    - validateTransition('bogus', 'planning') throws with "Unknown status" message
    - validateTransition takes exactly 2 args (currentStatus, nextStatus) -- no entityType
    - SET_TRANSITIONS has exactly 6 keys matching SetStatus values
    - WAVE_TRANSITIONS and JOB_TRANSITIONS are NOT exported
    - Module exports exactly 2 keys: SET_TRANSITIONS, validateTransition
  </behavior>
  <action>
Rewrite src/lib/state-transitions.cjs as a clean file. Use the exact SET_TRANSITIONS map from 38-RESEARCH.md Pattern 2. The validateTransition function takes (currentStatus, nextStatus) -- NOT (entityType, currentStatus, nextStatus). This is a signature change from v2.x. Remove ENTITY_MAPS, WAVE_TRANSITIONS, JOB_TRANSITIONS entirely.

Then rewrite src/lib/state-transitions.test.cjs with tests covering all behaviors above. Test every valid transition path (the full chain: pending->discussing->planning->executing->complete->merged AND the skip path: pending->planning->executing->complete->merged). Test every invalid transition (back-transitions, skipping states, unknown statuses). The old test file has 146 lines covering entity-type transitions -- those are all deleted.

Key: Test the branch point at `pending` exhaustively. This is the most unusual part of the transition map.
  </action>
  <verify>
    <automated>node --test src/lib/state-transitions.test.cjs</automated>
  </verify>
  <done>validateTransition accepts 2 args (not 3), SET_TRANSITIONS has pending->['discussing','planning'] branch, no wave/job transitions exported, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Simplify lock.cjs with PID-based stale cleanup and update lock.test.cjs</name>
  <files>src/lib/lock.cjs, src/lib/lock.test.cjs</files>
  <behavior>
    - cleanStaleLocks(cwd) removes lock artifacts (target file + .lock dir) when owning PID is dead
    - cleanStaleLocks(cwd) leaves lock artifacts alone when owning PID is alive
    - cleanStaleLocks(cwd) handles missing locks dir gracefully (no throw)
    - cleanStaleLocks(cwd) handles unparseable target files gracefully (skip, no throw)
    - isProcessAlive(process.pid) returns true (current process is alive)
    - isProcessAlive(99999999) returns false (non-existent PID)
    - acquireLock still works (acquire + release cycle, concurrent acquire waits)
    - isLocked still works (false when unlocked, true when locked)
    - ensureLocksDir still works (creates dir + .gitignore)
    - Module exports: acquireLock, isLocked, ensureLocksDir, cleanStaleLocks
  </behavior>
  <action>
Update src/lib/lock.cjs to add two new functions:

1. `isProcessAlive(pid)` -- uses `process.kill(pid, 0)` to check if process exists. Returns true/false. Not exported (internal helper).

2. `cleanStaleLocks(cwd)` -- reads all `*.target` files in LOCKS_DIR, parses JSON to get `pid`, checks `isProcessAlive(pid)`. If process is dead: removes the `.lock` directory (recursive) and the `.target` file. If process is alive or file can't be parsed: skip. If LOCKS_DIR doesn't exist: return immediately.

Keep all existing functionality (acquireLock, isLocked, ensureLocksDir) unchanged. The lock name used by state-machine.cjs will change from 'state-machine' to 'state' in Plan 02 -- lock.cjs itself is name-agnostic.

Update module.exports to include cleanStaleLocks.

Then update src/lib/lock.test.cjs to add a new describe block for `cleanStaleLocks()` covering all behaviors above. To test "dead PID", create a target file with a PID of 99999999 (almost certainly non-existent) plus a .lock directory, then call cleanStaleLocks and assert both are removed. To test "alive PID", use process.pid. Keep existing test blocks for acquireLock, isLocked, ensureLocksDir.
  </action>
  <verify>
    <automated>node --test src/lib/lock.test.cjs</automated>
  </verify>
  <done>cleanStaleLocks removes dead-PID lock artifacts, preserves live-PID locks, handles edge cases gracefully, all existing lock tests still pass</done>
</task>

</tasks>

<verification>
Run all three test files together to confirm no cross-file issues:

```bash
node --test src/lib/state-schemas.test.cjs src/lib/state-transitions.test.cjs src/lib/lock.test.cjs
```

Verify no wave/job exports remain:
```bash
node -e "const s = require('./src/lib/state-schemas.cjs'); console.log('schemas:', Object.keys(s)); const t = require('./src/lib/state-transitions.cjs'); console.log('transitions:', Object.keys(t)); const l = require('./src/lib/lock.cjs'); console.log('lock:', Object.keys(l));"
```

Expected output:
- schemas: SetStatus, SetState, MilestoneState, ProjectState
- transitions: SET_TRANSITIONS, validateTransition
- lock: acquireLock, isLocked, ensureLocksDir, cleanStaleLocks
</verification>

<success_criteria>
- state-schemas.cjs exports exactly 4 schemas (no wave/job)
- state-transitions.cjs exports SET_TRANSITIONS + validateTransition (no wave/job, no entityType param)
- lock.cjs exports cleanStaleLocks alongside existing functions
- All 3 test files pass with 0 failures
- SetStatus has exactly: pending, discussing, planning, executing, complete, merged
- pending transitions to both discussing and planning (branch verified in tests)
</success_criteria>

<output>
After completion, create `.planning/phases/38-state-machine-simplification/38-01-SUMMARY.md`
</output>
