---
phase: 42-core-agent-rewrites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bin/rapid-tools.cjs
  - src/lib/tool-docs.cjs
  - src/lib/build-agents.test.cjs
  - src/lib/tool-docs.test.cjs
  - src/modules/core/core-identity.md
  - agents/rapid-orchestrator.md
  - src/modules/roles/role-orchestrator.md
autonomous: true
requirements: [AGENT-04]

must_haves:
  truths:
    - "Orchestrator is completely removed from all registries, files, and tests"
    - "core-identity.md describes the v3 workflow (init > start-set > discuss-set > plan-set > execute-set > review > merge)"
    - "core-identity.md has no references to wave-plan, set-init, orchestrator, or wave/job state"
    - "Build pipeline produces 26 agents (22 generated + 4 stubs) after orchestrator removal"
    - "All test assertions updated for 26 roles, 4 core agents, and CORE comment prefix"
  artifacts:
    - path: "src/modules/core/core-identity.md"
      provides: "v3 workflow description with independent sets model"
      contains: "plan-set"
    - path: "src/bin/rapid-tools.cjs"
      provides: "Registry maps without orchestrator entries"
    - path: "src/lib/build-agents.test.cjs"
      provides: "Updated test assertions for 26 roles, 4 core agents"
  key_links:
    - from: "src/lib/build-agents.test.cjs"
      to: "src/bin/rapid-tools.cjs"
      via: "ALL_26_ROLES array matches production ROLE_CORE_MAP"
      pattern: "ALL_26_ROLES"
    - from: "src/modules/core/core-identity.md"
      to: "agents/rapid-*.md"
      via: "build-agents copies identity into generated agents"
      pattern: "plan-set.*execute-set"
---

<objective>
Remove the orchestrator agent from all registries, files, and tests, then rewrite core-identity.md for the v3 workflow with independent sets model.

Purpose: Clean infrastructure prerequisite for Plans 02-03 -- the orchestrator must be gone and identity must be correct before hand-writing role sections into the 4 core agents.
Output: Clean 26-role registries, v3-ready identity module, passing tests with updated assertions.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-core-agent-rewrites/42-CONTEXT.md
@.planning/phases/42-core-agent-rewrites/42-RESEARCH.md

<interfaces>
<!-- Registry maps in rapid-tools.cjs that reference orchestrator (all must be cleaned): -->
<!-- Line 466: ROLE_TOOLS.orchestrator = 'Read, Write, Bash, Grep, Glob, Agent' -->
<!-- Line 496: ROLE_COLORS.orchestrator = 'blue' -->
<!-- Line 532: ROLE_DESCRIPTIONS.orchestrator = 'RAPID orchestrator agent -- ...' -->
<!-- Line 567: ROLE_CORE_MAP.orchestrator = ['core-identity.md', 'core-conventions.md', 'core-returns.md'] -->
<!-- Line 707: SKIP_GENERATION = ['orchestrator', 'planner', 'executor', 'merger', 'reviewer'] -->

<!-- In tool-docs.cjs: -->
<!-- Line 113: ROLE_TOOL_MAP.orchestrator = ['state-get', 'state-get-all', ...] -->

<!-- In build-agents.test.cjs: -->
<!-- Line 10: ALL_27_ROLES array includes 'orchestrator' -->
<!-- Line 22: CORE_AGENTS = ['orchestrator', 'planner', 'executor', 'merger', 'reviewer'] -->
<!-- Line 32: EXPECTED_ROLE_CORE_MAP.orchestrator entry -->
<!-- Line 72: "generates exactly 27 .md files (22 generated + 5 stubs)" -->
<!-- Line 84: "Built 22 agents (5 core skipped)" -->
<!-- Line 146-167: STUB comment checks include orchestrator -->
<!-- Line 160-167: Phase 42 TODO checks include orchestrator -->
<!-- Line 298: KNOWN_OVERSIZED list (may need audit but orchestrator not in it) -->

<!-- In tool-docs.test.cjs: -->
<!-- Line 62: expectedRoles includes 'orchestrator' -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove orchestrator from all registries, delete files, update tests</name>
  <files>
    src/bin/rapid-tools.cjs,
    src/lib/tool-docs.cjs,
    src/lib/build-agents.test.cjs,
    src/lib/tool-docs.test.cjs,
    agents/rapid-orchestrator.md,
    src/modules/roles/role-orchestrator.md
  </files>
  <action>
    1. In `src/bin/rapid-tools.cjs`:
       - Remove `orchestrator` entry from ROLE_TOOLS map (~line 466)
       - Remove `orchestrator` entry from ROLE_COLORS map (~line 496)
       - Remove `orchestrator` entry from ROLE_DESCRIPTIONS map (~line 532)
       - Remove `orchestrator` entry from ROLE_CORE_MAP map (~line 567)
       - Remove `'orchestrator'` from SKIP_GENERATION array (~line 707) -- array becomes `['planner', 'executor', 'merger', 'reviewer']`

    2. In `src/lib/tool-docs.cjs`:
       - Remove `orchestrator` entry from ROLE_TOOL_MAP (~line 113)

    3. In `src/lib/build-agents.test.cjs`:
       - Rename `ALL_27_ROLES` to `ALL_26_ROLES`, remove `'orchestrator'` from the array
       - Update `CORE_AGENTS` to `['planner', 'executor', 'merger', 'reviewer']` (4 entries, not 5)
       - Remove `'orchestrator'` entry from `EXPECTED_ROLE_CORE_MAP`
       - Update assertion "generates exactly 27 .md files" -> "generates exactly 26 .md files (22 generated + 4 stubs)"
       - Update all `ALL_27_ROLES` references to `ALL_26_ROLES` throughout the file
       - Update assertion "Built 22 agents (5 core skipped)" -> "Built 22 agents (4 core skipped)"
       - Update STUB comment check: instead of checking for `<!-- STUB: Core agent`, check for `<!-- CORE: Hand-written agent` (the Plans 02-03 will write files with this prefix, but the test should accept EITHER `STUB` or `CORE` during the transition -- use regex: `/^<!-- (STUB|CORE):/`)
       - Update "Phase 42 TODO" check: change to verify that core agent files contain a `<role>` section with content (more than just a TODO comment) -- check that the `<role>` section contains `# Role:` header. Since Plans 02-03 haven't run yet, make this test LENIENT during transition: check that `<role>` tags exist (already passes), but add a NEW test that warns if TODO placeholder is still present (use `it.todo` or a skip condition)

    4. In `src/lib/tool-docs.test.cjs`:
       - Remove `'orchestrator'` from the expectedRoles array (~line 62)

    5. Delete files:
       - Delete `agents/rapid-orchestrator.md`
       - Delete `src/modules/roles/role-orchestrator.md`

    6. Run `node ~/Projects/RAPID/src/bin/rapid-tools.cjs build-agents` to regenerate all 22 generated agents (now with only 4 core skipped, and orchestrator agent file gone)

    IMPORTANT: The build-agents output should now say "Built 22 agents (4 core skipped)" since orchestrator is no longer in SKIP_GENERATION.
  </action>
  <verify>
    <automated>cd /home/kek/Projects/RAPID && node --test src/lib/build-agents.test.cjs src/lib/tool-docs.test.cjs</automated>
  </verify>
  <done>
    - `agents/rapid-orchestrator.md` does not exist
    - `src/modules/roles/role-orchestrator.md` does not exist
    - `grep -r "orchestrator" src/bin/rapid-tools.cjs` returns only non-registry references (if any)
    - SKIP_GENERATION has exactly 4 entries: planner, executor, merger, reviewer
    - All build-agents and tool-docs tests pass with updated counts (26 roles, 4 core)
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite core-identity.md for v3 workflow and run build-agents</name>
  <files>
    src/modules/core/core-identity.md
  </files>
  <action>
    Rewrite `src/modules/core/core-identity.md` with the following changes:

    1. **Workflow section** -- Replace the 7-step v2 workflow with v3:
       ```
       1. **init** -- Research codebase and generate project roadmap
       2. **start-set** -- Create isolated worktree for a set
       3. **discuss-set** -- Capture implementation vision into CONTEXT.md
       4. **plan-set** -- Research and produce PLAN.md per wave
       5. **execute-set** -- Implement tasks from PLAN.md files
       6. **review** -- Code review before merge
       7. **merge** -- Merge set branch into main with conflict resolution
       ```

    2. **Remove wave/job references** -- Delete "Steps 3-6 repeat for each wave within a set" and any other wave/job language

    3. **Add independent sets model** -- Add: "Steps 2-7 repeat for each set in the milestone. Sets are independent -- they can be started, planned, executed, reviewed, and merged in any order."

    4. **Remove orchestrator references** -- If any language references an orchestrator agent or central coordinator, remove it. Skills dispatch agents directly.

    5. Keep ALL other sections intact (Working Directory, Tool Invocation, Context Loading, State Rules) -- these are still correct for v3.

    6. After editing, run `node ~/Projects/RAPID/src/bin/rapid-tools.cjs build-agents` to propagate the updated identity to all 22 generated agents.

    7. Verify propagation: spot-check one generated agent (e.g., `rapid-bugfix.md`) to confirm it contains "plan-set" and "execute-set" in its identity section, and does NOT contain "wave-plan" or "set-init".
  </action>
  <verify>
    <automated>cd /home/kek/Projects/RAPID && grep -q "plan-set" src/modules/core/core-identity.md && grep -q "execute-set" src/modules/core/core-identity.md && grep -q "start-set" src/modules/core/core-identity.md && ! grep -q "wave-plan" src/modules/core/core-identity.md && ! grep -q "set-init" src/modules/core/core-identity.md && echo "PASS: identity v3 verified" && node --test src/lib/build-agents.test.cjs</automated>
  </verify>
  <done>
    - core-identity.md contains the 7-step v3 workflow (init, start-set, discuss-set, plan-set, execute-set, review, merge)
    - No references to wave-plan, set-init, orchestrator, or "Steps 3-6 repeat for each wave"
    - Independent sets language present
    - Generated agents (e.g., rapid-bugfix.md) contain updated identity
    - All tests pass
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `node --test src/lib/build-agents.test.cjs src/lib/tool-docs.test.cjs` -- all pass
2. `ls agents/rapid-orchestrator.md` -- file does not exist
3. `ls agents/rapid-*.md | wc -l` -- returns 26
4. `grep "plan-set" agents/rapid-bugfix.md` -- confirms identity propagation
5. `grep -c "orchestrator" src/bin/rapid-tools.cjs` -- minimal (only non-registry mentions if any)
</verification>

<success_criteria>
- Orchestrator fully removed from production code, agent files, and tests
- core-identity.md describes v3 7-step workflow with independent sets
- Build pipeline produces 26 agents (22 generated + 4 stubs)
- All tests pass with updated assertions
</success_criteria>

<output>
After completion, create `.planning/phases/42-core-agent-rewrites/42-01-SUMMARY.md`
</output>
