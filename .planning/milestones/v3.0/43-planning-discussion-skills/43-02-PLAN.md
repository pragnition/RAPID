---
phase: 43-planning-discussion-skills
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/start-set/SKILL.md
  - skills/discuss-set/SKILL.md
autonomous: true
requirements:
  - CMD-02
  - CMD-03
  - PLAN-02
  - PLAN-03
  - UX-01
  - UX-02

must_haves:
  truths:
    - "/start-set creates a worktree scaffold and generates SET-OVERVIEW.md"
    - "/start-set does NOT auto-chain into discuss-set -- just suggests it as next step"
    - "/start-set suggests /rapid:discuss-set at set level (no wave reference)"
    - "/discuss-set operates at set level, not wave level"
    - "/discuss-set identifies 4 gray areas and lets user select which to discuss"
    - "/discuss-set batches all questions per area into a single AskUserQuestion call"
    - "/discuss-set --skip auto-generates CONTEXT.md from roadmap + codebase scan + researcher agent"
    - "/discuss-set writes CONTEXT.md (not WAVE-CONTEXT.md) to .planning/sets/{set-id}/"
    - "/discuss-set transitions set from pending to discussing"
    - "Both commands render a progress breadcrumb at completion"
    - "Both commands suggest exactly one next action"
  artifacts:
    - path: "skills/start-set/SKILL.md"
      provides: "v3 start-set skill with worktree creation, set-planner, no auto-chain"
      min_lines: 120
    - path: "skills/discuss-set/SKILL.md"
      provides: "v3 discuss-set skill with set-level 4-area Q&A, --skip flag, CONTEXT.md output"
      min_lines: 250
  key_links:
    - from: "skills/start-set/SKILL.md"
      to: "agents/rapid-set-planner.md"
      via: "Agent tool spawn for SET-OVERVIEW.md"
      pattern: "Spawn.*rapid-set-planner"
    - from: "skills/discuss-set/SKILL.md"
      to: ".planning/sets/{set-id}/CONTEXT.md"
      via: "Write tool to create CONTEXT.md"
      pattern: "CONTEXT\\.md"
    - from: "skills/discuss-set/SKILL.md"
      to: "agents/rapid-researcher.md"
      via: "Agent tool spawn for --skip auto-context"
      pattern: "rapid-researcher"
---

<objective>
Rewrite /start-set and /discuss-set SKILL.md files for v3.0 with set-level discussion, 4-area Q&A model, --skip auto-context, and progress breadcrumbs.

Purpose: start-set is a lightweight setup command. discuss-set is the core vision capture -- moving from per-wave discussion to per-set discussion with a structured 4 gray area Q&A model. The --skip flag enables autonomous context generation for users who want to skip discussion.

Output: Rewritten skills/start-set/SKILL.md and skills/discuss-set/SKILL.md
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/start-set/SKILL.md
@skills/discuss-set/SKILL.md
@agents/rapid-set-planner.md
@src/lib/state-transitions.cjs
@src/lib/display.cjs
@src/lib/resolve.cjs

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/state-transitions.cjs:
```javascript
const SET_TRANSITIONS = {
  pending:     ['discussing', 'planning'],
  discussing:  ['planning'],
  planning:    ['executing'],
  executing:   ['complete'],
  complete:    ['merged'],
  merged:      []
};
```

From src/lib/resolve.cjs (key exports):
```javascript
// resolveSet(input) -- takes numeric "1" or string "auth-system", returns { resolvedId, numericIndex, setIndex }
// resolveWave(input) -- takes dot notation "1.1" or wave ID, returns { setId, waveId, setIndex, waveIndex }
```

State transition CLI:
```bash
node "${RAPID_TOOLS}" state transition set "${MILESTONE_ID}" "${SET_ID}" discussing
```

v3 planning artifacts layout:
```
.planning/sets/{set-id}/
  CONTRACT.json          # Generated at init time
  DEFINITION.md          # Set scope
  SET-OVERVIEW.md        # Generated by start-set (rapid-set-planner)
  CONTEXT.md             # Generated by discuss-set (NEW -- replaces WAVE-CONTEXT.md)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update start-set SKILL.md for v3 (remove auto-chain, add breadcrumb)</name>
  <files>skills/start-set/SKILL.md</files>
  <action>
Update skills/start-set/SKILL.md with these targeted v3 changes. The existing skill is 183 lines and mostly correct -- this is an update, not a full rewrite.

**Frontmatter:** Update description to remove "runs set planner" detail if present. Keep allowed-tools.

**Steps 1-4 (Resolve, Validate, Worktree, Set Planner):** Keep largely unchanged. These steps work correctly for v3.

**Step 5 (Next Step):** CHANGE the next-step suggestion:
- OLD: `/rapid:discuss-set {setIndex}.1` (wave-level dot notation)
- NEW: `/rapid:discuss-set {setIndex}` (set-level, no wave reference)
- The suggestion text should say "(Discuss set {setId})" not "(Discuss wave 1 of {setId})"
- Do NOT auto-chain into discuss-set. Just display the suggestion and stop.

**NEW Step 6: Progress Breadcrumb:**
- After the next-step suggestion, render the progress breadcrumb:
```
init [done] > start-set [done] > discuss-set > plan-set > execute-set > review > merge
```
- "init" and "start-set" are marked as done. All others are pending.
- Use simple markdown formatting.

**Error handling:** Add breadcrumb to error messages too. If worktree creation fails:
```
init [done] > start-set [FAILED] > discuss-set > plan-set > execute-set > review > merge
Error: {error message}
Next: Fix the issue and re-run /rapid:start-set {setIndex}
```

**Anti-patterns to avoid:**
- Do NOT reference wave IDs in the next-step suggestion. v3 discuss-set takes a set reference, not a wave reference.
- Do NOT auto-chain into discuss-set (per CONTEXT.md locked decision).
- Do NOT reference `/rapid:discuss-set {setIndex}.1` -- use `/rapid:discuss-set {setIndex}` instead.
  </action>
  <verify>
    <automated>grep -c "\.1\b" skills/start-set/SKILL.md | head -1</automated>
    Should be 0 (no dot notation wave references in next-step suggestions).

    Additional checks:
    - grep "discuss-set.*setIndex" skills/start-set/SKILL.md should NOT contain dot notation
    - grep "breadcrumb\|Breadcrumb\|start-set.*done" skills/start-set/SKILL.md should find breadcrumb
    - grep "auto-chain" skills/start-set/SKILL.md should NOT appear (the skill just suggests, doesn't chain)
  </verify>
  <done>
    - start-set SKILL.md updated with set-level next-step suggestion (/rapid:discuss-set {setIndex})
    - No auto-chain into discuss-set
    - Progress breadcrumb rendered at completion and in error messages
  </done>
</task>

<task type="auto">
  <name>Task 2: Full rewrite of discuss-set SKILL.md for v3 (set-level, 4-area Q&A, --skip flag)</name>
  <files>skills/discuss-set/SKILL.md</files>
  <action>
Full rewrite of skills/discuss-set/SKILL.md. The v2 skill operates at wave level with 5-8 gray areas and a 2-round deep-dive. v3 operates at set level with exactly 4 gray areas and batched questions per area.

**Frontmatter:**
```yaml
---
description: Capture developer implementation vision for a set via structured discussion or auto-generate context with --skip
allowed-tools: Bash(rapid-tools:*), Read, Write, AskUserQuestion, Glob, Grep, Agent
---
```

**Title and introduction:**
```markdown
# /rapid:discuss-set -- Set Discussion

You are the RAPID set discussion facilitator. This skill captures developer implementation vision for a SET (not a wave) before autonomous planning begins.
```

**Step 1: Environment Setup + Banner** -- Keep the env preamble pattern and `display banner discuss-set`. Identical to v2.

**Step 2: Resolve Set (NOT Wave)**
- Accept set identifier (numeric "1" or string "auth-system"). Use `resolve set` (NOT `resolve wave`).
- Parse: SET_ID, SET_INDEX from resolved result.
- Load STATE.json via `state get --all`. Extract MILESTONE_ID.
- Status check: Set must be in 'pending' or 'discussing' state:
  - If 'pending': Proceed normally.
  - If 'discussing': Offer re-discuss or view existing CONTEXT.md.
  - If 'planning' or later: Inform user set has already been discussed and planned. Suggest /rapid:plan-set. STOP.
- **Check for --skip flag:** If the user invoked `/rapid:discuss-set --skip {setIndex}` or `/rapid:discuss-set {setIndex} --skip`, note the --skip flag for Step 4.

**Step 3: Gather Context**
- Read set-level artifacts:
  - `.planning/sets/${SET_ID}/CONTRACT.json`
  - `.planning/sets/${SET_ID}/DEFINITION.md`
  - `.planning/sets/${SET_ID}/SET-OVERVIEW.md`
  - `.planning/ROADMAP.md` (for set description)
- Use Grep/Glob to find and read source files referenced in CONTRACT.json exports.
- Display set summary: "Set {SET_ID}: {brief scope from DEFINITION.md or ROADMAP.md}"

**Step 4: --skip Branch**
If --skip flag is set:
1. Display: "Auto-generating CONTEXT.md for set '{SET_ID}' (--skip mode)..."
2. Spawn a lightweight **rapid-researcher** agent with this task:
   ```
   Generate auto-context for set '{SET_ID}' (--skip mode).

   ## Sources
   1. ROADMAP.md set description: {set description from ROADMAP.md}
   2. CONTRACT.json: {CONTRACT.json contents}
   3. SET-OVERVIEW.md: {SET-OVERVIEW.md contents if exists}
   4. Scan the codebase files in the set's file ownership

   ## Output
   Write CONTEXT.md to .planning/sets/{SET_ID}/CONTEXT.md with this format:
   - domain section: Set scope and boundary from ROADMAP.md
   - decisions section: All items marked as "Claude's Discretion" (no user decisions captured)
   - code_context section: Patterns discovered from codebase scan
   - deferred section: Empty (no discussion to defer ideas from)
   ```
3. After agent completes, verify CONTEXT.md was written.
4. Skip to Step 7 (State Transition).

**Step 5: Identify 4 Gray Areas (interactive mode)**
If --skip is NOT set:
1. Analyze set context (CONTRACT.json, DEFINITION.md, SET-OVERVIEW.md, ROADMAP.md, source files).
2. Identify exactly 4 gray areas -- implementation facets where:
   - Multiple valid approaches exist
   - Integration points are ambiguous
   - User experience decisions are needed
   - Performance/quality tradeoffs exist
3. Present gray areas using AskUserQuestion:
   ```
   "I've analyzed set '{SET_ID}' and identified 4 areas that would benefit from your input.
   Select which areas you'd like to discuss:"
   Options:
   1. "Let Claude decide all" -- "Skip discussion, all decisions at Claude's discretion"
   2. "{Gray area 1 title}" -- "{1-sentence description}"
   3. "{Gray area 2 title}" -- "{1-sentence description}"
   4. "{Gray area 3 title}" -- "{1-sentence description}"
   5. "{Gray area 4 title}" -- "{1-sentence description}"
   ```
4. If "Let Claude decide all": Record all 4 areas as Claude's discretion. Skip to Step 7.
5. Otherwise: Record selected areas for Step 6.

**Step 6: Deep-Dive Selected Areas (batched per area)**
For EACH selected gray area (in order):
1. Present ONE AskUserQuestion that covers the area comprehensively:
   ```
   "{Gray area title}

   Context: {2-3 sentences on tradeoffs}

   Questions:
   1. {Question about approach}
   2. {Question about edge case or tradeoff}
   3. {Question about specific detail}

   Answer all questions above, or type 'Claude decides' to let me handle this area."
   ```
   This is a freeform AskUserQuestion -- the user answers all 2-3 questions about the area in one response.
2. Parse the user's response. Extract decisions.
3. If user said "Claude decides": Record as Claude's discretion with rationale.

After ALL selected areas are discussed:
- Compile follow-up questions ONLY if genuine gaps remain after all 4 areas were covered.
- If gaps exist: ONE final AskUserQuestion with remaining questions (not per-area).
- If no gaps: Skip follow-up entirely.

**Step 7: State Transition**
- Transition set from 'pending' to 'discussing':
  ```bash
  node "${RAPID_TOOLS}" state transition set "${MILESTONE_ID}" "${SET_ID}" discussing
  ```
- If set is already in 'discussing' (re-discuss scenario), this is fine -- catch the error and continue.

**Step 8: Write CONTEXT.md**
- Write `.planning/sets/${SET_ID}/CONTEXT.md` using the Write tool.
- Format:
  ```markdown
  # CONTEXT: {SET_ID}

  **Set:** {SET_ID}
  **Generated:** {date}
  **Mode:** {interactive | auto-skip}

  <domain>
  ## Set Boundary
  {Scope of what this set covers -- from DEFINITION.md and ROADMAP.md}
  </domain>

  <decisions>
  ## Implementation Decisions

  ### {Area 1 Title}
  - {Decision from discussion or "Claude's Discretion"}

  ### {Area 2 Title}
  - ...

  ### Claude's Discretion
  - {Areas where user selected "Let Claude decide"}
  </decisions>

  <specifics>
  ## Specific Ideas
  - {Any specific ideas mentioned during discussion}
  </specifics>

  <code_context>
  ## Existing Code Insights
  {Patterns, integration points, reusable code discovered during context gathering}
  </code_context>

  <deferred>
  ## Deferred Ideas
  - {Ideas mentioned but out of set scope}
  </deferred>
  ```

**Step 9: Commit and Next Steps**
- Commit CONTEXT.md:
  ```bash
  git add ".planning/sets/${SET_ID}/CONTEXT.md"
  git commit -m "discuss-set(${SET_ID}): capture set implementation vision"
  ```
- Display next step: `/rapid:plan-set {SET_INDEX}`
- Display progress breadcrumb:
  ```
  init [done] > start-set [done] > discuss-set [done] > plan-set > execute-set > review > merge
  ```

**Error handling (UX-01):**
- On any error, show breadcrumb with failure point:
  ```
  init [done] > start-set [done] > discuss-set [FAILED: {brief error}] > plan-set > ...
  ```
- Show what is done, what failed, what to run next.

**Anti-patterns to avoid (and WHY):**
- Do NOT use `resolve wave` -- discuss-set operates at set level. Use `resolve set`.
- Do NOT write WAVE-CONTEXT.md -- write CONTEXT.md to `.planning/sets/{set-id}/`.
- Do NOT call `writeWaveContext()` from wave-planning.cjs -- use Write tool directly.
- Do NOT reference wave IDs anywhere in the skill.
- Do NOT call `state transition wave` -- only `state transition set`.
- Do NOT ask more than 4 gray areas -- the locked decision is exactly 4.
- Do NOT ask one question at a time per area -- batch 2-3 questions per area into one AskUserQuestion.
- Do NOT prompt for every implementation detail -- capture vision/what, not implementation/how (per CONTEXT.md decision).
  </action>
  <verify>
    <automated>grep -c "WAVE-CONTEXT\|resolve wave\|state transition wave\|wave.*discuss" skills/discuss-set/SKILL.md | head -1</automated>
    Should be 0 (no wave-level references in the discuss-set skill).

    Additional checks:
    - grep "CONTEXT.md" skills/discuss-set/SKILL.md should find multiple references to set-level CONTEXT.md
    - grep "\-\-skip" skills/discuss-set/SKILL.md should find the --skip flag handling
    - grep "4 gray areas\|4 areas\|exactly 4" skills/discuss-set/SKILL.md should find the gray area count
    - grep "rapid-researcher" skills/discuss-set/SKILL.md should find the --skip researcher spawn
    - grep "breadcrumb" skills/discuss-set/SKILL.md should find progress breadcrumb
    - grep "resolve set" skills/discuss-set/SKILL.md should find set-level resolution
  </verify>
  <done>
    - discuss-set SKILL.md fully rewritten for set-level operation
    - 4 gray area Q&A model with batched questions per area (2-3 per AskUserQuestion)
    - --skip flag spawns lightweight researcher for auto-generated CONTEXT.md
    - CONTEXT.md written to .planning/sets/{set-id}/ (not WAVE-CONTEXT.md)
    - State transitions at set level only (pending -> discussing)
    - Follow-up questions only if genuine gaps remain
    - Progress breadcrumb at completion and in error messages
    - Next step suggests /rapid:plan-set
  </done>
</task>

</tasks>

<verification>
- Read both SKILL.md files and verify valid Markdown
- Verify no v2 wave-level references in either skill (grep for "WAVE-CONTEXT", "resolve wave", "state transition wave")
- Verify discuss-set has --skip flag handling with researcher spawn
- Verify discuss-set has exactly 4 gray areas pattern
- Verify both skills have progress breadcrumbs
- Verify start-set suggests /rapid:discuss-set at set level (no dot notation)
- Verify discuss-set writes CONTEXT.md to .planning/sets/{set-id}/
</verification>

<success_criteria>
- start-set SKILL.md is updated with set-level discuss suggestion and breadcrumb
- discuss-set SKILL.md is a complete rewrite with set-level operation, 4-area Q&A, --skip, CONTEXT.md output
- No wave-level references in either skill
- Both skills follow the env preamble pattern, use rapid-tools.cjs CLI for state
- CONTEXT.md format matches what plan-set (Plan 03) expects as input
</success_criteria>

<output>
After completion, create `.planning/phases/43-planning-discussion-skills/43-02-SUMMARY.md`
</output>
