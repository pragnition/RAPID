---
phase: 43-planning-discussion-skills
plan: 04
type: execute
wave: 1
depends_on: ["43-02"]
files_modified:
  - skills/discuss-set/SKILL.md
autonomous: true
gap_closure: true
requirements: [CMD-03, PLAN-03]

must_haves:
  truths:
    - "/discuss-set --skip spawns an agent that exists in agents/ directory"
    - "--skip auto-context flow will not fail at runtime due to missing agent"
  artifacts:
    - path: "skills/discuss-set/SKILL.md"
      provides: "Set-level discussion with working --skip auto-context"
      contains: "rapid-research-stack"
  key_links:
    - from: "skills/discuss-set/SKILL.md"
      to: "agents/rapid-research-stack.md"
      via: "Agent tool spawn in --skip branch"
      pattern: "rapid-research-stack"
---

<objective>
Fix discuss-set --skip branch to spawn an existing agent (rapid-research-stack instead of non-existent rapid-researcher).

Purpose: The --skip branch in discuss-set SKILL.md spawns `rapid-researcher` which does not exist in agents/. This causes a runtime failure when users run `/rapid:discuss-set --skip`. The correct agent is `rapid-research-stack` -- the general-purpose researcher used by plan-set.
Output: Fixed discuss-set SKILL.md with correct agent reference.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-planning-discussion-skills/43-02-SUMMARY.md
@.planning/phases/43-planning-discussion-skills/43-VERIFICATION.md

<interfaces>
<!-- The discuss-set SKILL.md Step 4 (--skip branch) spawns an agent. -->
<!-- Available research agents in agents/ directory: -->
<!-- rapid-research-stack.md, rapid-research-features.md, rapid-research-architecture.md -->
<!-- rapid-research-pitfalls.md, rapid-research-oversights.md, rapid-research-ux.md -->
<!-- rapid-research-synthesizer.md -->
<!-- There is NO rapid-researcher.md -->
<!-- plan-set SKILL.md already correctly uses rapid-research-stack (line 101) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix --skip agent name in discuss-set SKILL.md</name>
  <files>skills/discuss-set/SKILL.md</files>
  <action>
In `skills/discuss-set/SKILL.md`, Step 4 (--skip Branch), line 117:
Change `rapid-researcher` to `rapid-research-stack`.

The line currently reads:
```
2. Spawn a lightweight **rapid-researcher** agent with this task:
```

Change it to:
```
2. Spawn a lightweight **rapid-research-stack** agent with this task:
```

This is the same agent that plan-set SKILL.md uses (line 101) for set-scoped research. It is a general-purpose codebase researcher suitable for auto-context generation.

Do NOT change any other part of the file. The task prompt, output format, and surrounding logic are all correct.
  </action>
  <verify>
    <automated>grep -n "rapid-research-stack" skills/discuss-set/SKILL.md && ! grep -n "rapid-researcher[^-]" skills/discuss-set/SKILL.md && echo "PASS: correct agent name" || echo "FAIL: agent name mismatch"</automated>
  </verify>
  <done>
- discuss-set SKILL.md Step 4 spawns `rapid-research-stack` (not `rapid-researcher`)
- The agent `agents/rapid-research-stack.md` exists in the agents directory
- No other references to the non-existent `rapid-researcher` remain in the file
- The rest of the SKILL.md is unchanged
  </done>
</task>

</tasks>

<verification>
1. `grep -c "rapid-research-stack" skills/discuss-set/SKILL.md` returns >= 1
2. `grep -c "rapid-researcher" skills/discuss-set/SKILL.md` returns 0 (no occurrences of the non-existent agent name)
3. `ls agents/rapid-research-stack.md` confirms the target agent exists
4. `wc -l skills/discuss-set/SKILL.md` still shows ~335 lines (no unintended changes)
</verification>

<success_criteria>
- The --skip branch in discuss-set SKILL.md spawns `rapid-research-stack` which exists in `agents/`
- No runtime failure when Claude processes the --skip Agent spawn instruction
- Verification gap from 43-VERIFICATION.md truth #10 is fully closed (PARTIAL -> VERIFIED)
</success_criteria>

<output>
After completion, create `.planning/phases/43-planning-discussion-skills/43-04-SUMMARY.md`
</output>
