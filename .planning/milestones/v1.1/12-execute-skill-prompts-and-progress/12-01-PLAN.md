---
phase: 12-execute-skill-prompts-and-progress
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skills/execute/SKILL.md]
autonomous: true
requirements: [PROMPT-05, PROMPT-06, PROMPT-07, PROMPT-08, PROG-01]

must_haves:
  truths:
    - "When agent teams are available, developer sees structured Agent Teams vs Subagents choice with tradeoff descriptions"
    - "When agent teams are unavailable, no prompt is shown and subagents are used silently"
    - "Each paused set gets its own structured Resume/Restart/Skip prompt with task progress counts"
    - "Planning gate override shows structured Override/Cancel prompt with unplanned sets listed and risk in description"
    - "Wave reconciliation shows dynamic structured prompt with options matching result status (PASS/hard blocks/soft blocks)"
    - "After execution complete, developer sees structured View status/Start merge/Done next-action prompt"
    - "No STOP or halt keywords remain in execute SKILL.md"
    - "Progress text is printed before and after subagent spawns showing wave context, set names, phases, and timestamps"
  artifacts:
    - path: "skills/execute/SKILL.md"
      provides: "Execute skill with AskUserQuestion at all decision gates and progress indicators"
      contains: "AskUserQuestion"
  key_links:
    - from: "skills/execute/SKILL.md"
      to: "AskUserQuestion tool"
      via: "allowed-tools frontmatter"
      pattern: "allowed-tools:.*AskUserQuestion"
---

<objective>
Rewrite execute SKILL.md to use structured AskUserQuestion prompts at all decision points, add progress indicators during subagent execution, and remove all STOP keywords.

Purpose: Brings the execute skill in line with the structured prompt patterns established in Phases 10-11, replacing freeform text prompts with consequence-focused AskUserQuestion options and adding progress visibility during long-running subagent operations.

Output: Updated skills/execute/SKILL.md with AskUserQuestion at 6 decision points and progress text at subagent lifecycle boundaries.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-execute-skill-prompts-and-progress/12-CONTEXT.md
@.planning/phases/11-planning-and-status-skill-prompts/11-01-SUMMARY.md
@.planning/phases/11-planning-and-status-skill-prompts/11-02-SUMMARY.md

<interfaces>
<!-- The execute SKILL.md is a prose instruction template (disable-model-invocation: true pattern).
     It currently uses: Read, Write, Bash, Agent as allowed-tools.
     AskUserQuestion must be added to allowed-tools frontmatter.

     Phase 10/11 established patterns to follow:
     - consequence-focused option descriptions
     - context-based headers (short, informative)
     - structured-then-freeform for open-ended follow-up
     - dynamic state-dependent options (status skill pattern)
     - dismiss option pattern (always include a no-op exit)
     - second-gate pattern (preview then decide)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AskUserQuestion structured prompts at all decision gates and remove STOP keywords</name>
  <files>skills/execute/SKILL.md</files>
  <action>
Read the current skills/execute/SKILL.md. Make the following changes:

**Frontmatter:** Add AskUserQuestion to allowed-tools list (currently: Read, Write, Bash, Agent).

**Step 0 -- Execution mode (PROMPT-05):**
Replace the current freeform numbered prompt with AskUserQuestion. ONLY when `agentTeamsAvailable` is true:
- Header: "Exec mode"
- Option 1: "Agent Teams" with description "Faster parallel execution via Claude Code agent teams. Locked for entire run."
- Option 2: "Subagents" with description "Proven stable execution via subagent spawning. Locked for entire run."
When `agentTeamsAvailable` is false: keep existing silent behavior (no prompt, subagents used automatically).

**Step 1.5 -- Paused set handling (PROMPT-06):**
Replace the current batch numbered prompt with ONE AskUserQuestion PER paused set (individual prompts, not batch):
- Header: the set name (e.g., "auth-api")
- Include task progress in the question text: "Paused at task {tasks_completed}/{tasks_total}"
- Option 1: "Resume" with description "Continues from task {tasks_completed}/{tasks_total} -- completed work preserved"
- Option 2: "Restart" with description "Discards handoff, re-executes all tasks from scratch"
- Option 3: "Skip" with description "Left paused for later /rapid:execute run"
Keep the existing handling logic for each choice (resume command, delete HANDOFF.md, leave paused).

**Step 2 -- Planning gate override (PROMPT-07):**
Replace the current yes/no override prompt with AskUserQuestion when gate is blocked:
- Header: "Planning gate"
- Include unplanned set names in the question text
- Option 1: "Override" with description "Proceeds without complete plans -- sets may fail or produce incomplete work. Unplanned: {set list}"
- Option 2: "Run planning first" with description "Returns to planning before execution. Run /rapid:plan to complete set planning."
Remove the separate STOP block that currently exists when user declines override. Replace with clear exit text: "Execution cancelled. Run /rapid:plan to complete set planning before executing."

**Step 6 -- Plan approval (existing yes/modify/cancel):**
Replace the current freeform "(yes/modify/cancel)" with AskUserQuestion:
- Header: "Wave {N} plans"
- Option 1: "Approve" with description "Begin execution for all {count} sets in Wave {N}"
- Option 2: "Modify" with description "Request changes to one or more set plans"
- Option 3: "Cancel" with description "Stop execution"
For Cancel: replace the "STOP execution" instruction with clear exit text.

**Step 8 -- Wave reconciliation next steps (PROMPT-08):**
Replace the current numbered options and yes/no confirm with dynamic AskUserQuestion based on reconciliation result:

For PASS result:
- Header: "Reconciliation"
- Option 1: "Continue to Wave {N+1}" with description "All contracts satisfied -- proceed to next wave"
- Option 2: "Stop here" with description "Pause execution after successful wave"

For hard blocks:
- Header: "Reconciliation"
- Option 1: "Fix failed sets" with description "Re-execute sets that did not pass verification"
- Option 2: "Cancel execution" with description "Stop execution -- failed sets remain in error state"

For soft blocks:
- Header: "Reconciliation"
- Option 1: "Proceed anyway" with description "Accept soft blocks and continue to Wave {N+1}"
- Option 2: "Fix first" with description "Re-execute to address soft blocks before proceeding"
- Option 3: "Cancel" with description "Stop execution"

Remove the separate "Continue to Wave {N+1}? (yes/no)" confirm that currently follows -- the AskUserQuestion replaces it.

**Step 9 -- Execution complete routing (new):**
After the final summary, add AskUserQuestion for next steps:
- Header: "Execution complete"
- Option 1: "View status" with description "Run /rapid:status to review worktree state"
- Option 2: "Start merge" with description "Begin merge pipeline for completed sets"
- Option 3: "Done" with description "Exit -- sets remain on rapid/{set-name} branches"
Remove the current static "Next steps:" bullet list text block.

**STOP removal:**
Search the entire file for "STOP" and "halt" keywords. Replace each with either:
- A structured AskUserQuestion prompt (if at a decision point -- already handled above), OR
- Clear exit text like "Execution cancelled." or "Return to planning." (if at a terminal path)

**Step 1 -- Ready to begin prompt:**
Replace the current "Ready to begin execution? (yes/no)" with AskUserQuestion:
- Header: "Execution plan"
- Option 1: "Begin" with description "Start executing Wave 1"
- Option 2: "Cancel" with description "Exit without executing"

**Step 5 -- Discuss confirmation:**
Replace "Ready to proceed to planning? (yes/no)" with AskUserQuestion:
- Header: "Discussion complete"
- Option 1: "Continue to planning" with description "Proceed to plan phase for Wave {N} sets"
- Option 2: "Cancel" with description "Stop execution"
  </action>
  <verify>
    <automated>grep -c "AskUserQuestion" skills/execute/SKILL.md | xargs test 1 -le && grep -c "STOP" skills/execute/SKILL.md | xargs test 0 -eq && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
- AskUserQuestion appears in allowed-tools frontmatter
- Steps 0, 1, 1.5, 2, 5, 6, 8, 9 each have AskUserQuestion prompts with consequence-focused descriptions
- No STOP or halt keywords remain in the file
- Silent subagent-only behavior preserved when agent teams unavailable
- Individual per-set prompts for paused sets (not batch)
- Dynamic reconciliation options based on result status
  </done>
</task>

<task type="auto">
  <name>Task 2: Add progress indicators at subagent lifecycle boundaries</name>
  <files>skills/execute/SKILL.md</files>
  <action>
Read the updated skills/execute/SKILL.md (after Task 1). Add progress text instructions at the following points:

**Step 5 (Discuss phase):**
Before spawning each discuss subagent, print progress:
```
Wave {N} -- Discuss ({completed}/{total} sets done)
  {setName}: Discussing
[HH:MM]
```
After each discuss subagent returns, print updated progress with the set marked as discussed.

**Step 6 (Plan phase):**
Before spawning each plan subagent, print progress:
```
Wave {N} -- Plan ({completed}/{total} sets done)
  {setName}: Planning
[HH:MM]
```
After each plan subagent returns, print updated progress.

**Step 7 (Execute phase):**
Before spawning each executor subagent/teammate, print progress:
```
Wave {N} -- Execute ({completed}/{total} sets done)
  {setName}: Executing ({tasks_completed}/{tasks_total} tasks)
[HH:MM]
```
After each executor returns, print updated progress with result status (Done/Paused/Error).

For teams mode (Step 7a), add the progress print before the batch spawn and after each teammate completes (via the completions tracking file check).

For subagent mode (Step 7b), add the progress print before and after each individual subagent spawn.

**Format rules:**
- Multi-line block format (not compact one-liners) per user decision
- Each block includes: wave context (N/M sets done), set name + current phase, timestamp
- Task progress counts shown during execution phase when available from registry or handoff data
- Use print/echo -- these are natural text insertion points between subagent calls, no polling
- Derive timestamps from `date +%H:%M` bash command or inline
  </action>
  <verify>
    <automated>grep -c "sets done" skills/execute/SKILL.md | xargs test 3 -le && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
- Progress text blocks appear before and after subagent spawns in Steps 5, 6, and 7
- Each block shows wave context, set name, phase, and timestamp
- Execution phase blocks include task progress counts
- Teams mode and subagent mode both have progress text
- No polling or streaming -- natural insertion points only
  </done>
</task>

</tasks>

<verification>
1. `grep -c "AskUserQuestion" skills/execute/SKILL.md` returns >= 1 (in frontmatter + referenced in steps)
2. `grep -ci "STOP" skills/execute/SKILL.md` returns 0
3. `grep -c "sets done" skills/execute/SKILL.md` returns >= 3 (progress blocks in discuss, plan, execute)
4. File has AskUserQuestion in allowed-tools line
5. Step 0 has conditional AskUserQuestion (only when teams available)
6. Step 1.5 has per-set AskUserQuestion loop
7. Step 8 has dynamic options based on PASS/hard/soft blocks
</verification>

<success_criteria>
Execute SKILL.md uses structured AskUserQuestion prompts at all 8 decision points (Steps 0, 1, 1.5, 2, 5, 6, 8, 9), shows progress text at subagent lifecycle boundaries (Steps 5, 6, 7), and contains zero STOP/halt keywords. The file follows Phase 10/11 patterns: consequence-focused descriptions, context-based headers, and dynamic state-dependent options.
</success_criteria>

<output>
After completion, create `.planning/phases/12-execute-skill-prompts-and-progress/12-01-SUMMARY.md`
</output>
