---
phase: 11-planning-and-status-skill-prompts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/status/SKILL.md
autonomous: true
requirements: [PROMPT-14]

must_haves:
  truths:
    - "After status dashboard displays, developer sees AskUserQuestion with next-action options based on detected project state"
    - "Options change dynamically based on 5 states: no sets, sets not executing, executing, gate blocked, all done"
    - "A Done viewing option is always available so developer can dismiss without triggering a command"
    - "Header text changes per state to give immediate signal (Next step, Gate blocked, Ready to merge, etc.)"
    - "No STOP keywords remain in status SKILL.md"
  artifacts:
    - path: "skills/status/SKILL.md"
      provides: "Status skill with dynamic AskUserQuestion after dashboard display"
      contains: "AskUserQuestion"
  key_links:
    - from: "skills/status/SKILL.md"
      to: "AskUserQuestion tool"
      via: "allowed-tools frontmatter"
      pattern: "allowed-tools:.*AskUserQuestion"
---

<objective>
Rewrite status SKILL.md to add a dynamic AskUserQuestion prompt after the dashboard display, offering state-appropriate next actions.

Purpose: After viewing status, developers get contextual next-step suggestions as structured options instead of static text guidance, with a dismiss option for check-in-only usage.
Output: Updated status SKILL.md with dynamic next-action routing.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-planning-and-status-skill-prompts/11-CONTEXT.md
@skills/status/SKILL.md

<interfaces>
<!-- Phase 10 established the AskUserQuestion pattern. -->
- AskUserQuestion added to allowed-tools frontmatter
- Headers are context-based to give immediate signal
- Consequence-focused option descriptions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite status SKILL.md Step 4 with dynamic AskUserQuestion next-action routing</name>
  <files>skills/status/SKILL.md</files>
  <action>
Read the current skills/status/SKILL.md.

Modify status SKILL.md with these specific changes:

1. **Frontmatter**: Add AskUserQuestion to the allowed-tools list (currently has Bash, Read).

2. **Step 4 (Summary Guidance)**: Replace the static text guidance with dynamic AskUserQuestion. The executor must detect the current state from the dashboard data gathered in Steps 1-3, then present the appropriate prompt.

   Rewrite Step 4 as follows:

   Based on the data from Steps 1-3, determine the current project state and present an AskUserQuestion with state-appropriate options. Always include "Done viewing" as the last option.

   **State 1: No sets exist**
   - Header: "Next step"
   - Option: "Plan sets" — "Run /rapid:plan to decompose your project into parallel work sets"
   - Option: "Done viewing" — "Exit status"

   **State 2: Sets defined but not executing** (sets exist, no worktrees active, gate open)
   - Header: "Next step"
   - Option: "Start execution" — "Run /rapid:execute to begin working on sets"
   - Option: "Review assumptions" — "Run /rapid:assumptions to review Claude's mental model before executing"
   - Option: "Done viewing" — "Exit status"

   **State 3: Sets are executing** (active worktrees exist)
   - Header: "Execution in progress"
   - Option: "View set details" — "Show detailed status for a specific set"
   - Option: "Done viewing" — "Exit status"

   **State 4: Gate blocked** (a wave gate is blocked per Step 3 output)
   - Header: "Gate blocked"
   - Option: "Complete planning" — "Run /rapid:plan to finish planning for blocked sets: {list missing set names}"
   - Option: "Done viewing" — "Exit status"

   **State 5: All sets done** (all sets at Done phase)
   - Header: "Ready to merge"
   - Option: "Start merge" — "Run /rapid:merge to begin integrating completed sets"
   - Option: "Done viewing" — "Exit status"

   When the developer selects an action option (not "Done viewing"), display the suggested command as guidance text: "Run: `/rapid:{command}`" — do NOT attempt to run the command. The status skill is read-only.

   When "Done viewing" is selected, display "Status check complete." and end the skill.

3. **Step 5 (JSON Output)**: Keep unchanged.

4. **STOP removal**: If any STOP keywords exist in the file, replace them with clear exit text. (Current file may not have explicit STOP keywords, but check and remove any.)

5. Preserve Steps 1, 1.5, 2, 3, and 5 unchanged. Preserve Important Notes unchanged.
  </action>
  <verify>
    <automated>grep -c "AskUserQuestion" skills/status/SKILL.md | grep -qv "^0$" && grep "allowed-tools" skills/status/SKILL.md | grep -q "AskUserQuestion" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Status SKILL.md Step 4 uses dynamic AskUserQuestion with 5 state-dependent option sets, all states include "Done viewing" dismiss option, headers change per state, AskUserQuestion is in allowed-tools frontmatter, no STOP keywords remain.</done>
</task>

</tasks>

<verification>
- `grep "AskUserQuestion" skills/status/SKILL.md` shows hits in frontmatter and Step 4
- `grep "allowed-tools" skills/status/SKILL.md` includes AskUserQuestion
- `grep -c "STOP" skills/status/SKILL.md` returns 0 (or file never had STOP)
- Step 4 contains all 5 state detection cases with corresponding AskUserQuestion prompts
- Every state includes "Done viewing" option
</verification>

<success_criteria>
- Status SKILL.md Step 4 uses AskUserQuestion with dynamic options based on 5 detected states
- Headers change per state (Next step, Execution in progress, Gate blocked, Ready to merge)
- Every state includes "Done viewing" dismiss option
- AskUserQuestion is in allowed-tools frontmatter
- Status skill remains read-only (suggests commands but does not execute them)
</success_criteria>

<output>
After completion, create `.planning/phases/11-planning-and-status-skill-prompts/11-02-SUMMARY.md`
</output>
