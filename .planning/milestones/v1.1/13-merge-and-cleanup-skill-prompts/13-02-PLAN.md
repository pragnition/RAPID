---
phase: 13-merge-and-cleanup-skill-prompts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [skills/cleanup/SKILL.md]
autonomous: true
requirements: [PROMPT-11, ERRR-02]

must_haves:
  truths:
    - "Developer selects worktree from structured AskUserQuestion options (up to 4 worktrees as options)"
    - "If more than 4 worktrees, falls back to text list with freeform input"
    - "Before removal, developer sees structured confirmation listing what gets deleted"
    - "When dirty worktree blocks removal, developer sees Commit / Stash / Force remove / Cancel options"
    - "Force remove triggers double confirmation before executing destructive action"
    - "No STOP or halt keywords exist anywhere in cleanup SKILL.md"
  artifacts:
    - path: "skills/cleanup/SKILL.md"
      provides: "Cleanup skill with AskUserQuestion at all decision gates and dirty worktree recovery"
      contains: "AskUserQuestion"
  key_links:
    - from: "skills/cleanup/SKILL.md"
      to: "AskUserQuestion tool"
      via: "allowed-tools frontmatter"
      pattern: "allowed-tools:.*AskUserQuestion"
---

<objective>
Rewrite cleanup SKILL.md with AskUserQuestion structured prompts for worktree selection, removal confirmation, and dirty worktree recovery with specific resolution steps.

Purpose: Replace freeform text input, yes/no prompts, and bare STOP handling with structured prompts matching Phases 10-12 patterns.
Output: Updated skills/cleanup/SKILL.md with AskUserQuestion at every user-facing decision point.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-merge-and-cleanup-skill-prompts/13-CONTEXT.md
@skills/cleanup/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AskUserQuestion structured prompts for worktree selection, removal confirmation, and dirty worktree recovery</name>
  <files>skills/cleanup/SKILL.md</files>
  <action>
Rewrite skills/cleanup/SKILL.md to replace all freeform prompts and STOP handling with AskUserQuestion structured prompts. Add AskUserQuestion to the allowed-tools frontmatter line.

Specific changes by step:

**Step 1 (no worktrees -- STOP removal):**
- Replace "STOP" with clear exit text: "No active worktrees found. Nothing to clean up." followed by exit (no STOP keyword)

**Step 2 (worktree selection -- PROMPT-11):**
- Replace freeform "Which worktree would you like to clean up?" with AskUserQuestion
- If 4 or fewer worktrees: AskUserQuestion with each worktree as an option
  - Header: "Select worktree to clean up"
  - Each option: set name as label, description shows status (e.g., "Phase: Done, Merge: merged" or "Phase: Executing, Merge: pending")
- If more than 4 worktrees: fall back to numbered text list with freeform input (matches Phase 11 assumptions pattern for >4 items)
- Invalid selection: inform user and re-prompt

**Step 3 (removal confirmation -- PROMPT-11):**
- Replace "Continue? (yes/no)" with AskUserQuestion
- Header: "Remove worktree"
- Options: "Remove worktree" / "Cancel"
- "Remove worktree" description: explicitly lists what gets deleted -- "Deletes .rapid-worktrees/{name} directory. Branch rapid/{name} preserved."
- "Cancel" exits with clear text: "Cleanup cancelled."

**Step 5 (dirty worktree -- ERRR-02):**
- Replace the current "Cannot remove worktree" text block with AskUserQuestion
- Header: "Dirty worktree blocks removal"
- Options: "Commit changes" / "Stash changes" / "Force remove" / "Cancel"
- "Commit changes" description: "Run git -C .rapid-worktrees/{name} add -A && git -C .rapid-worktrees/{name} commit -m 'WIP: save before cleanup'"
- "Stash changes" description: "Run git -C .rapid-worktrees/{name} stash push -m 'rapid-cleanup-stash'"
- "Force remove" description: "Permanently discards uncommitted changes"
- "Cancel" description: "Keep worktree as-is, exit cleanup"
- If "Commit changes" selected: run the commit commands, then retry cleanup
- If "Stash changes" selected: run the stash command, then retry cleanup
- If "Force remove" selected: double confirmation gate -- second AskUserQuestion:
  - Header: "Confirm force removal"
  - Options: "Confirm force remove" / "Cancel"
  - "Confirm force remove" description: "This permanently discards all uncommitted changes in {name}. Cannot be undone."
  - If confirmed: run `node "${RAPID_TOOLS}" worktree cleanup {name} --force` (or `git worktree remove --force .rapid-worktrees/{name}` + registry update)
  - If cancelled: return to main cleanup flow

**Global:**
- Remove ALL occurrences of "STOP" and "halt" keywords
- Replace with structured prompts or clear exit text
- Update Important Notes to mention force removal is now supported via structured prompt (remove the "No force removal" note)
  </action>
  <verify>
    <automated>grep -ciE '\bSTOP\b|\bhalt\b' skills/cleanup/SKILL.md | grep -q '^0$' && grep -c 'AskUserQuestion' skills/cleanup/SKILL.md | grep -qE '^[3-9]|^[1-9][0-9]' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
- Zero STOP/halt keywords in cleanup SKILL.md
- AskUserQuestion in allowed-tools frontmatter
- At least 3 AskUserQuestion prompts (Step 2 selection, Step 3 confirmation, Step 5 dirty recovery)
- Worktree selection uses AskUserQuestion for <=4 worktrees, text list for >4
- Removal confirmation explicitly lists what gets deleted
- Dirty worktree offers Commit/Stash/Force/Cancel with specific commands in descriptions
- Force remove has double confirmation gate
  </done>
</task>

</tasks>

<verification>
- `grep -ciE '\bSTOP\b|\bhalt\b' skills/cleanup/SKILL.md` returns 0
- `grep -c 'AskUserQuestion' skills/cleanup/SKILL.md` returns 3 or more
- `head -5 skills/cleanup/SKILL.md` shows AskUserQuestion in allowed-tools
- Double confirmation gate exists for force removal
</verification>

<success_criteria>
- Cleanup SKILL.md has structured AskUserQuestion prompts at every user-facing decision point
- All STOP/halt keywords eliminated
- Worktree selection is structured (AskUserQuestion for <=4, text list for >4)
- Dirty worktree recovery offers specific resolution commands as structured options
- Force removal protected by double confirmation gate
- Patterns consistent with Phases 10-12
</success_criteria>

<output>
After completion, create `.planning/phases/13-merge-and-cleanup-skill-prompts/13-02-SUMMARY.md`
</output>
