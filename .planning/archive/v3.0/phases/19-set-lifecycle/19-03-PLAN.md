---
phase: 19-set-lifecycle
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/pause/SKILL.md
  - skills/resume/SKILL.md
  - skills/cleanup/SKILL.md
  - skills/context/SKILL.md
  - src/bin/rapid-tools.cjs
  - src/lib/worktree.cjs
  - src/lib/worktree.test.cjs
autonomous: true
requirements: [SETL-05, SETL-06, SETL-07, UX-01]

must_haves:
  truths:
    - "Developer can /pause a set and it writes HANDOFF.md with wave/job snapshot and user notes"
    - "Developer can /resume <set-name> and it restores context from HANDOFF.md + STATE.json"
    - "Developer can /cleanup a set and it removes the worktree, then optionally deletes the branch"
    - "/cleanup blocks removal if uncommitted changes exist and shows fix commands"
    - "/context generates CLAUDE.md and project context files"
    - "AskUserQuestion is used at every decision gate in all three commands"
  artifacts:
    - path: "skills/pause/SKILL.md"
      provides: "Rewritten /pause for per-set handoff with STATE.json"
      min_lines: 60
      contains: "AskUserQuestion"
    - path: "skills/resume/SKILL.md"
      provides: "New /resume command for set resumption"
      min_lines: 60
      contains: "AskUserQuestion"
    - path: "skills/cleanup/SKILL.md"
      provides: "Rewritten /cleanup with branch deletion option"
      min_lines: 60
      contains: "AskUserQuestion"
    - path: "src/bin/rapid-tools.cjs"
      provides: "resume top-level CLI command and worktree delete-branch subcommand"
      contains: "case 'resume'"
    - path: "src/lib/worktree.cjs"
      provides: "deleteBranch function for optional branch cleanup"
      contains: "deleteBranch"
  key_links:
    - from: "skills/pause/SKILL.md"
      to: "src/bin/rapid-tools.cjs"
      via: "node \"${RAPID_TOOLS}\" execute pause"
      pattern: "execute pause"
    - from: "skills/resume/SKILL.md"
      to: "src/bin/rapid-tools.cjs"
      via: "node \"${RAPID_TOOLS}\" resume"
      pattern: "resume"
    - from: "skills/cleanup/SKILL.md"
      to: "src/bin/rapid-tools.cjs"
      via: "node \"${RAPID_TOOLS}\" worktree cleanup and worktree delete-branch"
      pattern: "worktree cleanup"
---

<objective>
Rewrite /pause and /cleanup skills for Mark II, create the new /resume skill, and verify /context works with Mark II patterns. These commands complete the set lifecycle: pause saves state, resume restores it, cleanup removes completed worktrees, and context generates project files.

Purpose: Developers need to pause work mid-set, resume later in a fresh context window, and clean up after merge. Without these, the lifecycle is incomplete.
Output: Rewritten pause/cleanup skills, new resume skill, context skill verification, branch deletion CLI command, deleteBranch function.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-set-lifecycle/19-CONTEXT.md
@.planning/phases/19-set-lifecycle/19-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/execute.cjs:
```javascript
function generateHandoff(checkpointData, setName, pauseCycle)  // returns HANDOFF.md content string
function parseHandoff(handoffContent)  // returns { frontmatter: {}, sections: {} }
```

From src/bin/rapid-tools.cjs (existing execute pause, lines 1159-1206):
```javascript
case 'pause': {
  // Reads checkpoint JSON from stdin
  // Generates HANDOFF.md at .planning/sets/{setName}/HANDOFF.md
  // Updates REGISTRY.json: phase='Paused', pauseCycles++
  // Returns { paused: true, setName, pauseCycles, handoffPath }
}
```

From src/bin/rapid-tools.cjs (existing execute resume, lines 1208-1246):
```javascript
case 'resume': {
  // Validates HANDOFF.md exists
  // Parses handoff, returns { resumed: true, setName, handoff, definitionPath, contractPath }
  // Updates REGISTRY.json: phase='Executing'
}
```

From src/lib/worktree.cjs:
```javascript
function removeWorktree(projectRoot, worktreePath)  // returns { removed: true } or { removed: false, reason: 'dirty' }
function loadRegistry(cwd)
async function registryUpdate(cwd, updateFn)
```

From src/lib/state-machine.cjs:
```javascript
async function readState(cwd)
function findSet(state, milestoneId, setId)
async function transitionSet(cwd, milestoneId, setId, newStatus)
```

From src/bin/rapid-tools.cjs (existing worktree cleanup, lines 869-896):
```javascript
case 'cleanup': {
  // Calls removeWorktree, deregisters from REGISTRY.json
  // Returns { removed: true, setName } or { removed: false, reason, message }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deleteBranch function + resume CLI + delete-branch CLI subcommand</name>
  <files>src/lib/worktree.cjs, src/lib/worktree.test.cjs, src/bin/rapid-tools.cjs</files>
  <behavior>
    - Test 1: deleteBranch(cwd, branchName) calls `git branch -d {branchName}` and returns { deleted: true, branch }
    - Test 2: deleteBranch returns { deleted: false, reason: 'unmerged', branch } when `-d` fails on unmerged branch
    - Test 3: deleteBranch with force=true calls `git branch -D {branchName}` and returns { deleted: true, branch, forced: true }
    - Test 4: deleteBranch throws on invalid branch name (empty, has spaces)
  </behavior>
  <action>
    **1. Add `deleteBranch(cwd, branchName, force = false)` to worktree.cjs:**
    - Calls `git branch -d {branchName}` (or `-D` if force=true) via gitExec
    - Returns `{ deleted: true, branch }` on success
    - On unmerged branch error from `-d`: returns `{ deleted: false, reason: 'unmerged', branch, message: '...' }`
    - On branch not found: returns `{ deleted: false, reason: 'not-found', branch }`
    - Validate branchName is non-empty and has no spaces

    **2. Add `worktree delete-branch` subcommand to handleWorktree in rapid-tools.cjs:**
    - `worktree delete-branch <branch-name> [--force]`: Calls `wt.deleteBranch(cwd, branchName, force)`
    - Returns JSON result
    - Parses `--force` flag from args

    **3. Add `resume` as top-level command in rapid-tools.cjs** (alongside set-init):
    - `resume <set-name>`: Wraps the existing `execute resume` logic but also:
      - Reads STATE.json to get set's current status and wave/job snapshot
      - Validates set is in a paused-compatible state (check REGISTRY.json phase is 'Paused')
      - Returns JSON with: handoff data, set status from STATE.json, current wave/job progress
    - Register `case 'resume'` in the main switch (around line 140)
    - This is a NEW top-level command separate from `execute resume` -- it extends the resume data with STATE.json context

    **4. Export deleteBranch** from worktree.cjs module.exports.
  </action>
  <verify>
    <automated>node --test /home/kek/Projects/RAPID/src/lib/worktree.test.cjs</automated>
  </verify>
  <done>deleteBranch function works with safe (-d) and force (-D) modes. CLI `worktree delete-branch` subcommand exists. `resume` top-level CLI command extends execute resume with STATE.json context.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite /pause, create /resume, rewrite /cleanup SKILL.md files</name>
  <files>skills/pause/SKILL.md, skills/resume/SKILL.md, skills/cleanup/SKILL.md, skills/context/SKILL.md</files>
  <action>
    **1. Rewrite `skills/pause/SKILL.md` for Mark II:**

    Frontmatter: `description: Pause a set and save state for later resumption`, `allowed-tools: Bash, Read, AskUserQuestion`

    Steps:
    - Step 1: Load env (standard pattern)
    - Step 2: Determine set to pause. If no argument, read STATE.json for sets with status 'executing', present via AskUserQuestion. If no sets executing: "No sets are currently executing."
    - Step 3: Gather checkpoint data. Use AskUserQuestion to ask for pause notes: "Add notes for the next developer resuming this set?" with options "Add notes" / "Skip notes". If notes, ask freeform.
    - Step 4: Build checkpoint JSON with: current wave/job status snapshot (read STATE.json), user notes, timestamp. Pipe to `node "${RAPID_TOOLS}" execute pause <set-name>` via stdin.
    - Step 5: Confirm success. Show HANDOFF.md location and pause cycle count. If pauseCycles >= 3, warn about replanning.
    - Step 6: Next steps via AskUserQuestion: "Run /status to see dashboard" / "Run /resume {set} to continue later" / "Done"

    **2. Create `skills/resume/SKILL.md`:**

    Frontmatter: `description: Resume a paused set from its last checkpoint`, `allowed-tools: Bash, Read, AskUserQuestion`

    Steps:
    - Step 1: Load env (standard pattern)
    - Step 2: Determine set to resume. If no argument, list paused sets from REGISTRY.json (`node "${RAPID_TOOLS}" worktree list` + filter phase='Paused'). Present via AskUserQuestion. If no paused sets: "No paused sets found."
    - Step 3: Load resume data. Run `node "${RAPID_TOOLS}" resume <set-name>`. Parse JSON output containing handoff data, state context.
    - Step 4: Display handoff summary. Show: last completed action, pause notes, wave/job progress snapshot, pause cycle number.
    - Step 5: Confirm resume via AskUserQuestion: "Resume set {name} from wave {N}, job {M}?" with options "Resume" / "View HANDOFF.md first" / "Cancel". If "View HANDOFF.md", read and display the file, then ask again.
    - Step 6: On resume confirmed, display: "Set {name} resumed. Worktree at {path}, branch {branch}." Present next step: "The set is ready for continued execution. Run /execute {name} to proceed."

    **3. Rewrite `skills/cleanup/SKILL.md` for Mark II:**

    Frontmatter: `description: Clean up completed set worktrees with safety checks and optional branch deletion`, `allowed-tools: Bash, Read, AskUserQuestion`

    Steps:
    - Step 1: Load env (standard pattern)
    - Step 2: Show current worktrees. Run `node "${RAPID_TOOLS}" worktree status`. If no worktrees: "No active worktrees. Nothing to clean up."
    - Step 3: Select worktree. If argument provided, use it. Otherwise, use AskUserQuestion with one option per worktree (name = set id, description = current phase + branch).
    - Step 4: Safety check. Run `node "${RAPID_TOOLS}" worktree cleanup <set-name>`. If result has `removed: false` and `reason: 'dirty'`:
      - Use AskUserQuestion: "Worktree has uncommitted changes. What would you like to do?"
      - Options: "Commit changes first (git commit)" / "Stash changes (git stash)" / "Force remove (lose changes)" / "Cancel"
      - If force: run cleanup with force flag (or `git worktree remove --force`)
    - Step 5: Branch deletion. After successful worktree removal, use AskUserQuestion: "Also delete branch rapid/{set-name}?"
      - Options: "Yes, delete branch" / "No, keep branch"
      - If yes: run `node "${RAPID_TOOLS}" worktree delete-branch rapid/{set-name}`
      - If deletion fails (unmerged): AskUserQuestion: "Branch is unmerged. Force delete?" with "Force delete (git branch -D)" / "Keep branch"
    - Step 6: Confirm and next steps. "Worktree for set {name} cleaned up." + AskUserQuestion: "Run /status" / "Clean up another set" / "Done"

    **4. Verify `skills/context/SKILL.md` (SETL-07):**
    - Read the existing context SKILL.md
    - If it already generates CLAUDE.md and project context files correctly, leave it unchanged
    - If it needs minor updates to align with Mark II (e.g., reading from STATE.json instead of old state), make those updates
    - The context skill may need to use `.env` fallback if not already present -- check and add if missing
    - This is a verification/minor-update task, not a rewrite

    Apply UX-01: AskUserQuestion at every decision gate across all skills.
  </action>
  <verify>
    <automated>test -f /home/kek/Projects/RAPID/skills/pause/SKILL.md && test -f /home/kek/Projects/RAPID/skills/resume/SKILL.md && test -f /home/kek/Projects/RAPID/skills/cleanup/SKILL.md && grep -q "AskUserQuestion" /home/kek/Projects/RAPID/skills/pause/SKILL.md && grep -q "AskUserQuestion" /home/kek/Projects/RAPID/skills/resume/SKILL.md && grep -q "AskUserQuestion" /home/kek/Projects/RAPID/skills/cleanup/SKILL.md && grep -q "delete-branch" /home/kek/Projects/RAPID/skills/cleanup/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>/pause writes HANDOFF.md with STATE.json wave/job snapshot and user notes. /resume reads HANDOFF.md + STATE.json to restore context. /cleanup removes worktree with dirty-check safety and offers branch deletion via AskUserQuestion. /context verified working with Mark II. All skills use AskUserQuestion at every decision gate.</done>
</task>

</tasks>

<verification>
- `node --test /home/kek/Projects/RAPID/src/lib/worktree.test.cjs` passes with deleteBranch tests
- All four SKILL.md files exist with proper AskUserQuestion usage
- `node /home/kek/Projects/RAPID/src/bin/rapid-tools.cjs worktree delete-branch --help` or usage output works
- `node /home/kek/Projects/RAPID/src/bin/rapid-tools.cjs resume --help` or usage output works
- /cleanup SKILL.md offers branch deletion after worktree removal
</verification>

<success_criteria>
- /pause saves per-set HANDOFF.md with wave/job snapshot
- /resume restores context from HANDOFF.md + STATE.json
- /cleanup blocks on dirty worktrees with fix options, then offers branch deletion
- /context generates CLAUDE.md and project files (verified working)
- All decision points use AskUserQuestion
</success_criteria>

<output>
After completion, create `.planning/phases/19-set-lifecycle/19-03-SUMMARY.md`
</output>
