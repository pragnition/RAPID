---
phase: 24-documentation
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - DOCS.md
  - README.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02

must_haves:
  truths:
    - "DOCS.md documents all 26 agent role modules with their purpose and spawning command"
    - "DOCS.md documents all 22 runtime libraries with their purpose and v2.0 status"
    - "DOCS.md has a CLI reference covering all command groups and subcommands"
    - "DOCS.md documents the state machine architecture with hierarchy, schemas, and lock-protected writes"
    - "DOCS.md documents the .planning/ directory structure including all v2.0 additions"
    - "README.md is under 150 lines with Mark II hierarchy, getting started guide, and link to DOCS.md"
  artifacts:
    - path: "DOCS.md"
      provides: "Architecture, agents, libraries, CLI, state machine, configuration"
      contains: "state-machine.cjs"
    - path: "DOCS.md"
      provides: "Agent role documentation"
      contains: "role-merger.md"
    - path: "README.md"
      provides: "GitHub landing page with Mark II content"
      contains: "Sets/Waves/Jobs"
  key_links:
    - from: "DOCS.md agent roles table"
      to: "src/modules/roles/*.md"
      via: "26 role modules documented"
      pattern: "role-(merger|bug-hunter|wave-planner|job-planner)"
    - from: "DOCS.md CLI reference"
      to: "src/bin/rapid-tools.cjs"
      via: "CLI subcommands documented"
      pattern: "(lock|state|assemble-agent|wave-plan|review|merge)"
    - from: "README.md"
      to: "DOCS.md"
      via: "Link to full documentation"
      pattern: "DOCS.md"
---

<objective>
Complete DOCS.md with architecture, agent roles, runtime libraries, CLI reference, state machine, and configuration sections. Rewrite README.md as a concise Mark II landing page.

Purpose: Plan 01 created the user-facing reference (commands, workflow, concepts). This plan adds the developer-facing reference (architecture, internals) and creates the GitHub landing page that gives new users their first impression of RAPID.

Output: Complete DOCS.md with all sections (user + developer reference), and README.md as a concise (<150 lines) landing page.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-documentation/24-RESEARCH.md
@.planning/phases/24-documentation/24-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add architecture, agents, libraries, CLI reference, state machine, and configuration sections to DOCS.md</name>
  <files>DOCS.md</files>
  <action>
Read DOCS.md (written by Plan 01). Remove the plan-02 marker comment and append the following sections.

**Source files to read:**
- src/modules/roles/ -- list all .md files, read first 5-10 lines of each for purpose (26 role modules)
- src/lib/*.cjs -- list all .cjs files, read JSDoc header of each for purpose (22 libraries)
- src/bin/rapid-tools.cjs -- read the USAGE constant for CLI subcommand reference
- src/lib/state-machine.cjs -- read for state machine implementation details
- src/lib/state-schemas.cjs -- read for Zod schema structure
- src/lib/state-transitions.cjs -- read for transition maps (verify against Plan 01's key concepts)
- config.json -- read for agent assembly configuration structure
- .claude-plugin/plugin.json -- read for version
- src/modules/core-*.md -- list core modules (5 total)

**Sections to write:**

### Architecture section:

**Directory Structure** -- Updated tree showing all v2.0 additions:
```
RAPID/
  .claude-plugin/plugin.json
  commands/ (6 legacy)
  skills/ (17 skills)
  agents/ (gitignored, assembled at runtime)
  src/
    bin/rapid-tools.cjs
    hooks/rapid-task-completed.sh
    lib/ (22 libraries + test files)
    modules/
      core-*.md (5 core modules)
      roles/ (26 role modules)
  config.json
  DOCS.md
  README.md
  LICENSE
  package.json
```

**Agent Assembly** -- Explain the composable module system:
- config.json maps 5 named agents to core modules + role module + context
- 26 role modules can also be loaded inline by SKILL.md orchestrators
- 5 core modules shared across all agents

**Core Agent Modules table** (5 modules from src/modules/core-*.md)

**Agent Roles table** (26 roles from src/modules/roles/):
| # | Role Module | Purpose | Spawned By |
Use the inventory from 24-RESEARCH.md but verify against actual files in src/modules/roles/.

**Runtime Libraries table** (22 libraries from src/lib/):
| # | Library | Purpose | New in v2.0? |
Use the inventory from 24-RESEARCH.md but verify against actual .cjs files. Mark which are new, updated, or unchanged.

### State Machine Architecture section:

**Hierarchy visualization:**
```
ProjectState
  +-- milestones[]
      +-- MilestoneState
          +-- sets[]
              +-- SetState
                  +-- waves[]
                      +-- WaveState
                          +-- jobs[]
                              +-- JobState
```

**Schema overview** -- What each Zod schema validates (from state-schemas.cjs)
**Transition maps** -- Valid transitions per entity type (from state-transitions.cjs)
**Lock-protected writes** -- How state-machine.cjs uses lock.cjs for atomic updates

### CLI Reference section:

Read the USAGE constant from src/bin/rapid-tools.cjs. Organize into a reference table:
| Command Group | Subcommands | Purpose |
Use the inventory from 24-RESEARCH.md but verify against the actual USAGE string.

### Configuration section:

**.planning/ Directory** -- Updated table showing all files including v2.0 additions:
STATE.json, research/, waves/{setId}/{waveId}/, .locks/, worktrees/, MERGE-STATE.json

**Agent Assembly Configuration** -- How config.json works

**Environment Variables table** -- RAPID_TOOLS, CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS

### Dependencies section:
List npm dependencies from package.json with versions and purpose (zod, ajv, ajv-formats, proper-lockfile).

### Footer:
```
---
RAPID v{version from plugin.json} -- Rapid Agentic Parallelizable and Isolatable Development for Claude Code
```

**CRITICAL:** Read the actual source files. Do NOT copy v1.0 DOCS.md content for these sections. The library list, agent list, directory structure, and CLI reference are all significantly different in v2.0.
  </action>
  <verify>
    <automated>bash -c 'ROLES=$(grep -c "role-" DOCS.md); LIBS=$(grep -c "\.cjs" DOCS.md); echo "Agent role refs: $ROLES (need 26+), Library refs: $LIBS (need 22+)"; if [ "$ROLES" -ge 20 ] && [ "$LIBS" -ge 20 ]; then echo "PASS"; else echo "FAIL"; fi'</automated>
  </verify>
  <done>DOCS.md is complete with all sections: introduction, installation, quick start, 17 commands, workflow lifecycle, 17 key concepts, directory structure, agent assembly, 5 core modules, 26 agent roles, 22 runtime libraries, state machine architecture, CLI reference, .planning/ directory, configuration, environment variables, dependencies, and footer with correct version.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md as a concise Mark II landing page</name>
  <files>README.md</files>
  <action>
Rewrite README.md from scratch. Target: under 150 lines, concise GitHub landing page.

**Structure (from research recommendations):**

1. **Title + one-liner** -- RAPID name and tagline

2. **What It Does** -- 4-5 bullet points covering Mark II capabilities:
   - Decomposes work into Sets/Waves/Jobs hierarchy
   - Isolates each set in its own git worktree
   - Validates boundaries with interface contracts
   - Reviews code through adversarial pipeline (unit test + bug hunt + UAT)
   - Merges with 5-level conflict detection and 4-tier resolution

3. **Hierarchy Diagram** -- Visual showing the Sets/Waves/Jobs structure:
   ```
   Project
     Milestone (v1.0, v2.0, ...)
       Set (independent workstream -- own worktree)
         Wave (dependency-ordered group)
           Job (atomic work unit -- own branch)
   ```

4. **Quick Start** -- Installation (marketplace + git clone), then the workflow:
   ```
   /rapid:init         # Set up project
   /rapid:context      # Analyze codebase (brownfield)
   /rapid:plan         # Decompose into sets
   /rapid:set-init     # Create worktree for a set
   /rapid:discuss      # Capture implementation vision
   /rapid:wave-plan    # Create job plans
   /rapid:execute      # Run jobs in parallel
   /rapid:review       # Test and verify
   /rapid:merge        # Integrate to main
   /rapid:cleanup      # Remove worktrees
   ```

5. **Prerequisites** -- Node.js 18+, git 2.30+

6. **Documentation link** -- "See [DOCS.md](DOCS.md) for the full command reference, architecture details, agent system, and configuration."

7. **License** -- MIT

**Style guidelines:**
- No emoji (per project convention for docs files)
- Concise -- every line earns its place
- Link to DOCS.md for details, do not duplicate content
- Under 150 lines total
  </action>
  <verify>
    <automated>bash -c 'LINES=$(wc -l < README.md); SETS=$(grep -c "Sets\|Waves\|Jobs" README.md); echo "Lines: $LINES (want <150), Hierarchy refs: $SETS (want 3+)"; if [ "$LINES" -le 150 ] && [ "$SETS" -ge 3 ]; then echo "PASS"; else echo "FAIL"; fi'</automated>
  </verify>
  <done>README.md is a concise (<150 lines) GitHub landing page with Mark II hierarchy diagram, complete workflow quick start showing all 17 commands, prerequisites, link to DOCS.md, and MIT license.</done>
</task>

</tasks>

<verification>
- DOCS.md total sections count >= 30 (17 commands + workflow + concepts + architecture + agents + libraries + CLI + config + etc.)
- DOCS.md mentions all 26 role modules by filename
- DOCS.md mentions all 22 runtime libraries
- DOCS.md has CLI reference with command groups
- DOCS.md has state machine hierarchy visualization
- README.md < 150 lines
- README.md has hierarchy diagram showing Sets/Waves/Jobs
- README.md links to DOCS.md
- No v1.0 stale content in either file (no "11 commands", no "6 agents", no "17 libraries", no "state.cjs")
</verification>

<success_criteria>
- DOCS.md is complete: all sections from Plan 01 (commands, workflow, concepts) plus architecture, 26 agent roles, 22 libraries, state machine, CLI reference, configuration, dependencies
- README.md is a concise landing page under 150 lines with Mark II hierarchy, quick start, and link to DOCS.md
- Both files reference v2.0 Mark II content accurately derived from reading source files
- No stale v1.0 content remains in either file
</success_criteria>

<output>
After completion, create `.planning/phases/24-documentation/24-02-SUMMARY.md`
</output>
