---
phase: 42-core-agent-rewrites
plan: 04
type: execute
wave: 3
depends_on: ["42-03"]
files_modified: ["agents/rapid-reviewer.md"]
autonomous: true
requirements: [AGENT-04]
gap_closure: true

must_haves:
  truths:
    - "Reviewer agent verdict values match merge.cjs parseReviewVerdict regex exactly"
    - "assembleReviewerPrompt instructions and reviewer agent role section use the same verdict vocabulary"
    - "parseReviewVerdict returns { found: true } for any verdict the reviewer agent can produce"
  artifacts:
    - path: "agents/rapid-reviewer.md"
      provides: "Reviewer role with correct APPROVE/CHANGES/BLOCK verdict vocabulary"
      contains: "APPROVE"
  key_links:
    - from: "agents/rapid-reviewer.md"
      to: "src/lib/merge.cjs"
      via: "VERDICT marker parsed by parseReviewVerdict"
      pattern: "VERDICT:(APPROVE|CHANGES|BLOCK)"
---

<objective>
Align the reviewer agent's verdict vocabulary with the merge pipeline's parsing contract.

Purpose: The reviewer agent (42-03) was written with PASS/CONDITIONAL_PASS/FAIL verdict values, but merge.cjs parseReviewVerdict() only parses APPROVE/CHANGES/BLOCK. This creates a broken contract where any review produced by the current reviewer agent returns { found: false } from parseReviewVerdict(). Fix by updating the agent file to use the established vocabulary (lower risk than modifying production merge pipeline).

Output: Updated agents/rapid-reviewer.md with correct verdict values
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-core-agent-rewrites/42-03-SUMMARY.md
@.planning/phases/42-core-agent-rewrites/42-VERIFICATION.md

<interfaces>
<!-- merge.cjs verdict contract that the reviewer must align with -->

From src/lib/merge.cjs (line 1470):
```javascript
const match = content.match(/<!-- VERDICT:(APPROVE|CHANGES|BLOCK) -->/);
```

From src/lib/merge.cjs (lines 1349-1354):
```javascript
'Write your verdict as one of:',
'- APPROVE: code is ready to merge',
'- CHANGES: fixable issues found (style, missing tests)',
'- BLOCK: critical issues requiring human intervention',
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align reviewer verdict vocabulary with merge.cjs contract</name>
  <files>agents/rapid-reviewer.md</files>
  <action>
Update 4 lines in agents/rapid-reviewer.md to replace the PASS/CONDITIONAL_PASS/FAIL verdict vocabulary with APPROVE/CHANGES/BLOCK, matching the merge.cjs parseReviewVerdict() regex and assembleReviewerPrompt() instructions.

Specific changes:

Line 96 (Responsibilities bullet):
  FROM: "Produce a clear verdict (PASS, CONDITIONAL_PASS, FAIL)"
  TO:   "Produce a clear verdict (APPROVE, CHANGES, BLOCK)"

Line 120 (Verdict Rules):
  FROM: "- **PASS**: No blocking findings"
  TO:   "- **APPROVE**: No blocking findings"

Line 121:
  FROM: "- **CONDITIONAL_PASS**: No blocking findings, but fixable items exist that should be addressed"
  TO:   "- **CHANGES**: No blocking findings, but fixable items exist that should be addressed"

Line 122:
  FROM: "- **FAIL**: One or more blocking findings"
  TO:   "- **BLOCK**: One or more blocking findings"

Do NOT change anything else in the file. The VERDICT marker format on line 124 (`<!-- VERDICT:{verdict} -->`) is already correct and must not be modified.

Why align agent to merge.cjs (not the reverse): merge.cjs is the production merge pipeline used by all existing review flows. It is battle-tested with APPROVE/CHANGES/BLOCK. Changing it would require updating merge.test.cjs (6+ assertions), assembleReviewerPrompt(), and potentially downstream consumers. Changing the agent is a 4-line edit with zero production code risk.
  </action>
  <verify>
    <automated>cd /home/kek/Projects/RAPID && grep -c "APPROVE\|CHANGES\|BLOCK" agents/rapid-reviewer.md | grep -q "^[3-9]" && grep -c "CONDITIONAL_PASS\|verdict.*PASS\|verdict.*FAIL" agents/rapid-reviewer.md | grep -q "^0$" && echo "VERDICT_ALIGNED" || echo "VERDICT_MISALIGNED"</automated>
  </verify>
  <done>agents/rapid-reviewer.md uses APPROVE/CHANGES/BLOCK verdict values that match merge.cjs parseReviewVerdict regex exactly. No occurrences of PASS, CONDITIONAL_PASS, or FAIL as verdict values remain in the file.</done>
</task>

</tasks>

<verification>
1. `grep "APPROVE\|CHANGES\|BLOCK" agents/rapid-reviewer.md` shows the 3 verdict values in the role section
2. `grep "PASS\|CONDITIONAL_PASS\|FAIL" agents/rapid-reviewer.md` returns zero matches (no stale verdict values)
3. `node --test src/lib/merge.test.cjs` -- all parseReviewVerdict tests pass (they test the APPROVE/CHANGES/BLOCK contract)
4. `node --test src/lib/build-agents.test.cjs` -- all build-agents tests still pass (agent file structure unchanged)
5. `wc -c agents/rapid-reviewer.md` -- file size still under 12,288 bytes
</verification>

<success_criteria>
- Reviewer agent verdict vocabulary matches merge.cjs parseReviewVerdict() regex exactly (APPROVE, CHANGES, BLOCK)
- No occurrences of PASS, CONDITIONAL_PASS, or FAIL as verdict values in agents/rapid-reviewer.md
- All existing tests pass (merge.test.cjs, build-agents.test.cjs)
- File size remains under 12KB
</success_criteria>

<output>
After completion, create `.planning/phases/42-core-agent-rewrites/42-04-SUMMARY.md`
</output>
