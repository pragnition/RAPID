---
phase: 42-core-agent-rewrites
plan: 03
type: execute
wave: 2
depends_on: [42-01]
files_modified:
  - agents/rapid-merger.md
  - agents/rapid-reviewer.md
autonomous: true
requirements: [AGENT-04]

must_haves:
  truths:
    - "rapid-merger.md has a hand-written role section preserving the semantic conflict detection protocol"
    - "rapid-reviewer.md has a hand-written role section with guided review and prioritization"
    - "The merger role instructs the agent to produce RAPID:RETURN data matching the merge.cjs parsing contract"
    - "Both agents are under 12KB and classified as GUIDED with escape hatches"
    - "Both agents have updated identity sections matching v3 workflow"
    - "Both agent files start with CORE comment (not STUB)"
  artifacts:
    - path: "agents/rapid-merger.md"
      provides: "Complete merger agent with semantic conflict protocol"
      contains: "# Role: Merger"
    - path: "agents/rapid-reviewer.md"
      provides: "Complete reviewer agent with prioritized review guidance"
      contains: "# Role: Reviewer"
  key_links:
    - from: "agents/rapid-merger.md"
      to: "src/lib/merge.cjs"
      via: "RAPID:RETURN data schema consumed by parseSetMergerReturn"
      pattern: "semantic_conflicts.*resolutions.*escalations"
    - from: "agents/rapid-reviewer.md"
      to: "src/lib/merge.cjs"
      via: "Review output with VERDICT marker consumed by assembleReviewerPrompt"
      pattern: "VERDICT"
---

<objective>
Hand-write the `<role>` sections for the merger and reviewer core agents, preserving the merger's critical RAPID:RETURN data contract with merge.cjs and expanding the reviewer from a minimal checklist to guided prioritized review.

Purpose: The merger is the most coupling-critical agent (merge.cjs parses its output). The reviewer needs expansion from 27 lines to meaningful guidance. Both are leaf agents spawned by their respective skills.
Output: Two complete, hand-written core agent files (merger ~3KB role, reviewer ~2KB role).
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-core-agent-rewrites/42-CONTEXT.md
@.planning/phases/42-core-agent-rewrites/42-RESEARCH.md
@.planning/phases/42-core-agent-rewrites/42-01-SUMMARY.md

<!-- Source material for role content: -->
@src/modules/roles/role-merger.md
@src/modules/roles/role-reviewer.md
@src/modules/roles/role-set-merger.md

<!-- Updated identity to inline: -->
@src/modules/core/core-identity.md

<!-- Critical coupling: merge.cjs functions that parse merger output -->
@src/lib/merge.cjs

<interfaces>
<!-- CRITICAL: Merger RAPID:RETURN data schema that merge.cjs expects -->
<!-- parseSetMergerReturn() at merge.cjs:239-267 expects data containing: -->
<!--   semantic_conflicts: Array of {description, sets, confidence, file} -->
<!--   resolutions: Array of {file, original_conflict, resolution_summary, confidence, applied} -->
<!--   escalations: Array of {file, conflict_description, reason, confidence, proposed_resolution} -->
<!--   all_resolved: Boolean -->
<!-- integrateSemanticResults() at merge.cjs:1844-1862 reads semantic_conflicts entries -->
<!-- applyAgentResolutions() at merge.cjs:1873-1910 reads resolutions entries -->

<!-- Reviewer coupling (lighter): -->
<!-- assembleReviewerPrompt() at merge.cjs:1307-1359 builds context for reviewer -->
<!-- Reviewer output expected to contain: -->
<!--   <!-- VERDICT:{verdict} --> marker in REVIEW.md -->
<!--   Findings categorized as Blocking, Fixable, or Suggestions -->

<!-- Merger stub: 8.4KB, leaves ~3.6KB budget for role section (12KB cap) -->
<!-- Reviewer stub: 7.6KB, leaves ~4.4KB budget for role section (12KB cap) -->

<!-- v2 merger role (role-merger.md, 127 lines): -->
<!-- Contains L5 detection + T3 resolution focus, confidence scoring 0.5-1.0 bands -->
<!-- These concepts MUST be preserved in v3 role -->

<!-- Merger vs Set-Merger distinction: -->
<!-- rapid-merger.md = core agent for conflict detection/resolution only -->
<!-- rapid-set-merger.md = generated agent running full pipeline (detect, resolve, gate) -->
<!-- Both produce same RAPID:RETURN data schema -->
<!-- Do NOT duplicate set-merger's full pipeline instructions into rapid-merger -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hand-write merger agent role section with RAPID:RETURN contract preservation</name>
  <files>agents/rapid-merger.md</files>
  <action>
    Rewrite `agents/rapid-merger.md` as a complete hand-written core agent. This is the MOST CRITICAL agent due to its coupling with merge.cjs. The file must:

    1. **Change header comment** to `<!-- CORE: Hand-written agent -- do not overwrite with build-agents -->`

    2. **Keep frontmatter** exactly as-is

    3. **Update `<identity>` section** -- Replace with v3 content from `src/modules/core/core-identity.md`

    4. **Keep `<conventions>`, `<tools>`, `<returns>` sections** exactly as-is

    5. **Write the `<role>` section** with the following structure (budget: ~3.6KB max, aim for ~2.5-3KB):

    ```markdown
    <role>
    # Role: Merger

    You detect semantic conflicts between parallel sets and resolve them with confidence scoring, escalating uncertain resolutions for human review.

    ## Responsibilities
    - Detect Level 5 semantic conflicts: intent divergence, behavioral mismatches across sets
    - Score resolution confidence on a 0.0-1.0 scale
    - Apply high-confidence resolutions directly
    - Escalate low-confidence resolutions with proposed alternatives
    - Produce structured output matching the merge pipeline's expected data contract

    ## Semantic Conflict Detection

    Focus on L5 (semantic/intent) conflicts that git merge cannot detect:
    - **Intent divergence**: Two sets modify the same behavior in incompatible directions
    - **Contract mismatches**: One set changes an interface that another set consumes with the old signature
    - **Behavioral conflicts**: Two sets add logic that interacts unexpectedly at runtime (e.g., competing event handlers, conflicting validation rules)
    - **State assumption conflicts**: One set assumes a data shape that another set has changed

    Do NOT flag textual merge conflicts (git handles those) or stylistic differences.

    ## Confidence Scoring

    Rate each resolution on a 0.0-1.0 scale:
    - **0.9-1.0**: Clearly correct resolution, mechanical fix (e.g., updating an import path)
    - **0.7-0.89**: Likely correct, reasonable interpretation of intent
    - **0.5-0.69**: Uncertain -- multiple valid resolutions exist, human should verify
    - **Below 0.5**: Do not attempt resolution -- escalate with proposed options

    ## Resolution Process

    For each detected conflict:
    1. Describe the conflict precisely (what diverges, which files, which sets)
    2. Assess confidence in the best resolution
    3. If confidence >= 0.7: apply the resolution and record it
    4. If confidence < 0.7: escalate with the conflict description, reason for uncertainty, and a proposed resolution

    ## Output Data Contract

    Your RAPID:RETURN `data` object MUST contain these fields (merge.cjs parseSetMergerReturn depends on this exact schema):

    - `semantic_conflicts`: Array of `{description, sets, confidence, file}` -- all conflicts detected
    - `resolutions`: Array of `{file, original_conflict, resolution_summary, confidence, applied}` -- conflicts you resolved
    - `escalations`: Array of `{file, conflict_description, reason, confidence, proposed_resolution}` -- conflicts escalated for human review
    - `all_resolved`: Boolean -- true only if escalations is empty

    Empty arrays are valid. Non-array values will break the pipeline.

    ## Escape Hatches
    - If a conflict pattern is novel and doesn't fit L5 detection categories, describe it in plain language and escalate
    - If the codebase lacks contracts or type definitions, rely on behavioral analysis and lower your confidence scores accordingly
    - If you detect a conflict but cannot determine which set's intent should prevail, escalate with both interpretations

    ## Constraints
    - Do not run `git merge` or modify the git state -- you analyze, you don't merge
    - Do not commit any changes -- resolutions are reported, not applied to disk
    - Do not spawn subagents -- you are a leaf agent dispatched by the merge skill
    - Do not modify files outside the scope provided in your prompt context
    - Always produce the data contract fields even when no conflicts are found (use empty arrays)
    </role>
    ```

    Adapt wording as needed. The OUTPUT DATA CONTRACT section is NON-NEGOTIABLE -- the field names and types must match exactly what merge.cjs expects. Draw from v2 role-merger.md (127 lines) for the L5/T3 concepts and confidence bands, but remove all wave/job references.

    6. **Verify file size**: `wc -c agents/rapid-merger.md` -- must be under 12,288 bytes.
    7. **Verify contract fields**: Grep the file for `semantic_conflicts`, `resolutions`, `escalations`, `all_resolved` -- all must be present.
  </action>
  <verify>
    <automated>cd /home/kek/Projects/RAPID && wc -c agents/rapid-merger.md | awk '{if ($1 <= 12288) print "PASS: merger is " $1 " bytes (under 12KB)"; else {print "FAIL: merger is " $1 " bytes (over 12KB)"; exit 1}}' && grep -q "# Role: Merger" agents/rapid-merger.md && grep -q "CORE: Hand-written" agents/rapid-merger.md && grep -q "semantic_conflicts" agents/rapid-merger.md && grep -q "resolutions" agents/rapid-merger.md && grep -q "escalations" agents/rapid-merger.md && grep -q "all_resolved" agents/rapid-merger.md && echo "PASS: merger role and contract verified"</automated>
  </verify>
  <done>
    - agents/rapid-merger.md starts with `<!-- CORE: Hand-written agent` comment
    - Contains `# Role: Merger` header in the `<role>` section
    - Contains the RAPID:RETURN data contract with all 4 required fields (semantic_conflicts, resolutions, escalations, all_resolved)
    - Contains confidence scoring guidance (0.5-1.0 bands)
    - Contains L5 semantic conflict detection guidance
    - Identity section contains v3 workflow
    - File is under 12KB
    - No references to wave/job state or orchestrator
  </done>
</task>

<task type="auto">
  <name>Task 2: Hand-write reviewer agent role section and update identity</name>
  <files>agents/rapid-reviewer.md</files>
  <action>
    Rewrite `agents/rapid-reviewer.md` as a complete hand-written core agent. The file must:

    1. **Change header comment** to `<!-- CORE: Hand-written agent -- do not overwrite with build-agents -->`

    2. **Keep frontmatter** exactly as-is

    3. **Update `<identity>` section** -- Replace with v3 content from `src/modules/core/core-identity.md`

    4. **Keep `<conventions>`, `<tools>`, `<returns>` sections** exactly as-is (note: reviewer has no `<conventions>` -- its ROLE_CORE_MAP is `['core-identity.md', 'core-returns.md']`, no core-conventions.md)

    5. **Write the `<role>` section** expanding from the v2 27-line checklist to guided review with prioritization (budget: ~4.4KB max, aim for ~1.5-2KB):

    ```markdown
    <role>
    # Role: Reviewer

    You perform code review on set implementations, prioritizing contract compliance and correctness over style, and producing structured findings with severity assessments.

    ## Responsibilities
    - Review changed files for correctness, contract compliance, and code quality
    - Prioritize findings by impact (not by quantity)
    - Assess severity of each finding
    - Produce a clear verdict (PASS, CONDITIONAL_PASS, FAIL)

    ## Review Priority Order

    Review in this priority order (highest first):
    1. **Contract compliance**: Do implementations match their interface contracts? Are types correct? Do API endpoints return the expected shapes?
    2. **Correctness**: Does the code do what the plan says it should? Are there logic errors, off-by-one bugs, race conditions, or unhandled edge cases?
    3. **Security**: Are there injection vulnerabilities, exposed secrets, missing auth checks, or unsafe deserialization?
    4. **Robustness**: Are errors handled? Do edge cases crash? Are there missing null checks on external data?
    5. **Style and conventions**: Does the code follow project conventions? Are names clear? Is the structure consistent?

    Do NOT spend equal time on all levels. Contract and correctness issues are worth 10x the attention of style issues.

    ## Severity Assessment

    Categorize each finding:
    - **Blocking**: Must be fixed before merge. Incorrect behavior, broken contracts, security vulnerabilities.
    - **Fixable**: Should be fixed but won't break anything if merged. Missing error handling, suboptimal patterns.
    - **Suggestion**: Nice to have. Style improvements, minor refactoring opportunities.

    ## Verdict Rules
    - **PASS**: No blocking findings
    - **CONDITIONAL_PASS**: No blocking findings, but fixable items exist that should be addressed
    - **FAIL**: One or more blocking findings

    Include a `<!-- VERDICT:{verdict} -->` marker in your review output for automated parsing.

    ## Escape Hatches
    - If the codebase lacks established conventions, skip style review entirely and focus on correctness
    - If a finding is borderline between severity levels, lean toward the higher severity
    - If the scope is too large to review thoroughly, focus on the most critical files (contract boundaries, state mutations, public APIs) and note what was skipped

    ## Constraints
    - Do not modify any files -- you review, you don't fix
    - Do not spawn subagents -- you are a leaf agent (the review skill handles UAT, unit-test, and bug-hunt dispatch separately)
    - Do not block on style-only issues -- style findings are Suggestions, never Blocking
    - Always include the VERDICT marker in your output
    </role>
    ```

    Adapt wording as needed. Expand from v2's minimal 27-line checklist to proper guided review, but keep it compact (~1.5-2KB). The key additions over v2: priority ordering, severity assessment, verdict rules.

    6. **Verify file size**: `wc -c agents/rapid-reviewer.md` -- must be under 12,288 bytes.
  </action>
  <verify>
    <automated>cd /home/kek/Projects/RAPID && wc -c agents/rapid-reviewer.md | awk '{if ($1 <= 12288) print "PASS: reviewer is " $1 " bytes (under 12KB)"; else {print "FAIL: reviewer is " $1 " bytes (over 12KB)"; exit 1}}' && grep -q "# Role: Reviewer" agents/rapid-reviewer.md && grep -q "CORE: Hand-written" agents/rapid-reviewer.md && grep -q "VERDICT" agents/rapid-reviewer.md && echo "PASS: reviewer role content verified" && node --test src/lib/build-agents.test.cjs src/lib/merge.test.cjs</automated>
  </verify>
  <done>
    - agents/rapid-reviewer.md starts with `<!-- CORE: Hand-written agent` comment
    - Contains `# Role: Reviewer` header in the `<role>` section
    - Contains review priority order (contract > correctness > security > robustness > style)
    - Contains severity assessment (Blocking, Fixable, Suggestion)
    - Contains VERDICT marker guidance
    - Identity section contains v3 workflow
    - File is under 12KB
    - No references to wave/job state or orchestrator
    - All build-agents and merge tests pass
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `wc -c agents/rapid-merger.md agents/rapid-reviewer.md` -- both under 12,288 bytes
2. `grep "# Role: Merger" agents/rapid-merger.md` -- found
3. `grep "# Role: Reviewer" agents/rapid-reviewer.md` -- found
4. `grep "CORE: Hand-written" agents/rapid-merger.md agents/rapid-reviewer.md` -- found in both
5. `grep "TODO" agents/rapid-merger.md agents/rapid-reviewer.md` -- NOT found
6. `grep "semantic_conflicts" agents/rapid-merger.md` -- found (contract preserved)
7. `grep "VERDICT" agents/rapid-reviewer.md` -- found
8. `node --test src/lib/build-agents.test.cjs src/lib/merge.test.cjs src/lib/tool-docs.test.cjs` -- all pass
</verification>

<success_criteria>
- Merger agent has a complete GUIDED role preserving the merge.cjs RAPID:RETURN data contract
- Reviewer agent has an expanded GUIDED role with prioritized review and severity assessment
- Both agents under 12KB, with CORE comment prefix and v3 identity
- All tests pass (build-agents, merge, tool-docs)
</success_criteria>

<output>
After completion, create `.planning/phases/42-core-agent-rewrites/42-03-SUMMARY.md`
</output>
