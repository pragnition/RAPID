---
phase: 39-tool-docs-registry-core-module-refactor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/core/core-identity.md
  - src/modules/core/core-conventions.md
  - src/modules/PROMPT-SCHEMA.md
autonomous: true
requirements: [AGENT-02]

must_haves:
  truths:
    - "core-identity.md contains RAPID_TOOLS env var setup block (absorbed from core-context-loading.md)"
    - "core-identity.md contains condensed context loading strategy and state access rules"
    - "core-conventions.md exists with git commit conventions (content from core-git.md)"
    - "core-state-access.md, core-context-loading.md, core-git.md are deleted"
    - "PROMPT-SCHEMA.md documents exactly 5 allowed tags: identity, role, tools, returns, context"
    - "PROMPT-SCHEMA.md specifies conventions as an additional optional tag"
  artifacts:
    - path: "src/modules/core/core-identity.md"
      provides: "Agent identity + tool invocation + context loading + state rules"
      contains: "RAPID_TOOLS"
    - path: "src/modules/core/core-conventions.md"
      provides: "Git commit conventions"
      contains: "Commit Message Format"
    - path: "src/modules/PROMPT-SCHEMA.md"
      provides: "XML schema reference for agent prompts"
      contains: "<identity>"
  key_links:
    - from: "src/modules/core/core-identity.md"
      to: "src/bin/rapid-tools.cjs (ROLE_CORE_MAP)"
      via: "ROLE_CORE_MAP references core-identity.md (updated in Plan 03)"
      pattern: "core-identity\\.md"
    - from: "src/modules/core/core-conventions.md"
      to: "src/bin/rapid-tools.cjs (ROLE_CORE_MAP)"
      via: "ROLE_CORE_MAP references core-conventions.md (updated in Plan 03)"
      pattern: "core-conventions\\.md"
---

<objective>
Consolidate 5 core modules down to 3 (core-identity.md absorbs context-loading + state-access, core-conventions.md replaces core-git.md, core-returns.md unchanged) and create PROMPT-SCHEMA.md defining the XML tag vocabulary for agent prompts.

Purpose: This satisfies AGENT-02 (XML-formatted prompt structure with schema document) and prepares the core module set for the build pipeline update in Plan 03. The 3-module consolidation reduces per-agent token overhead by ~40%.

Output: Modified `core-identity.md`, new `core-conventions.md`, new `PROMPT-SCHEMA.md`, 3 deleted core modules
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-tool-docs-registry-core-module-refactor/39-CONTEXT.md
@.planning/phases/39-tool-docs-registry-core-module-refactor/39-RESEARCH.md

<interfaces>
<!-- Current core modules being consolidated. Read these files to understand full content. -->

From src/modules/core/core-identity.md (2,313 bytes):
- RAPID Agent Identity introduction
- Working Directory protocol (worktree path handling)
- RAPID Workflow (7-step canonical sequence)
- Agent constraints (structured return, file ownership)

From src/modules/core/core-context-loading.md (1,779 bytes):
- Prerequisites section: RAPID_TOOLS env var setup bash block
- Loading Strategy: 5 rules for progressive context loading
- Anti-Patterns: 4 patterns to avoid

From src/modules/core/core-state-access.md (2,767 bytes):
- Prerequisites section: same RAPID_TOOLS env var setup block
- CLI Commands: state read, state transition, state integrity, lock operations
- Rules: 5 rules about state access via CLI

From src/modules/core/core-git.md (1,056 bytes):
- Commit Message Format: type(scope): description
- Rules: 4 rules about atomic commits, specific file staging, verification
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate core modules and create PROMPT-SCHEMA.md</name>
  <files>src/modules/core/core-identity.md, src/modules/core/core-conventions.md, src/modules/PROMPT-SCHEMA.md</files>
  <action>
    **Step 1: Read all 4 current core module files** to understand their full content before making changes.

    **Step 2: Expand core-identity.md** by appending 3 new sections after the existing content. Be aggressive about condensing -- the goal is principles and quick reference, not verbose documentation. The full CLI command list is no longer needed in identity (it moves to tool docs). Target ~3,800 bytes total (currently 2,313).

    Add after existing "RAPID Workflow" section:

    ```markdown
    ## Tool Invocation

    Before running any rapid-tools.cjs command, ensure RAPID_TOOLS is set:

    ```bash
    if [ -z "${RAPID_TOOLS:-}" ] && [ -n "${CLAUDE_SKILL_DIR:-}" ] && [ -f "${CLAUDE_SKILL_DIR}/../../.env" ]; then export $(grep -v '^#' "${CLAUDE_SKILL_DIR}/../../.env" | xargs); fi
    if [ -z "${RAPID_TOOLS}" ]; then echo "[RAPID ERROR] RAPID_TOOLS is not set. Run /rapid:install or ./setup.sh to configure RAPID."; exit 1; fi
    ```

    ## Context Loading

    - Start with your plan/summary files -- they are your primary context
    - Use `state get` CLI for state (never read STATE.json directly)
    - Use Grep/Glob to find relevant files before reading them
    - Never load more than 5 files speculatively
    - Prefer targeted reads over full-file reads (use line ranges)

    ## State Rules

    - All state accessed through CLI (`node "${RAPID_TOOLS}" state ...`) -- never edit .planning/ directly
    - Transition commands handle locking automatically
    - Lock contention retries automatically -- do not retry manually
    - Reads use `readState()` internally with Zod validation
    - Invalid transitions are rejected by the CLI
    ```

    Preserve the existing identity, working directory, and workflow sections exactly as they are. Do NOT include the full CLI command reference (that now lives in tool docs). Do NOT duplicate the RAPID_TOOLS setup block -- include it exactly once in Tool Invocation.

    **Step 3: Create core-conventions.md** with the exact content from the current core-git.md (renamed, same content). Read core-git.md and write its content to core-conventions.md.

    **Step 4: Create PROMPT-SCHEMA.md** at `src/modules/PROMPT-SCHEMA.md`. Use the structure from RESEARCH.md "PROMPT-SCHEMA.md Structure" section. Must document:
    - Overview: agent prompts are XML-tagged sections with Markdown content
    - Required tags table: `<identity>` (core-identity.md), `<role>` (role-{name}.md), `<returns>` (core-returns.md)
    - Optional tags table: `<conventions>` (core-conventions.md), `<tools>` (generated by tool-docs.cjs), `<context>` (runtime injection by skills)
    - Assembly order: frontmatter, identity, conventions, tools, role, returns (context at runtime)
    - Rules: all tags top-level (no nesting), content is Markdown, tags may be omitted, required tags validated at build time (warning if missing)

    **Step 5: Delete the 3 retired modules:**
    - `src/modules/core/core-context-loading.md` -- content absorbed into core-identity.md
    - `src/modules/core/core-state-access.md` -- CLI commands become tool docs entries, rules absorbed into core-identity.md
    - `src/modules/core/core-git.md` -- content moved to core-conventions.md

    Per CONTEXT.md decision: "Retired modules deleted completely -- no stubs or redirect notices."

    **Important:** Do NOT update ROLE_CORE_MAP in rapid-tools.cjs or build-agents.test.cjs in this plan. That happens in Plan 03 (which depends on this plan). The build-agents command will be temporarily broken until Plan 03 runs -- this is expected.
  </action>
  <verify>
    <automated>test -f src/modules/core/core-identity.md && test -f src/modules/core/core-conventions.md && test -f src/modules/PROMPT-SCHEMA.md && test ! -f src/modules/core/core-context-loading.md && test ! -f src/modules/core/core-state-access.md && test ! -f src/modules/core/core-git.md && echo "PASS: All files correct" || echo "FAIL: File check failed"</automated>
  </verify>
  <done>
    - core-identity.md contains Tool Invocation (RAPID_TOOLS setup), Context Loading (5 rules condensed), State Rules (5 rules condensed)
    - core-conventions.md contains git commit conventions (from core-git.md)
    - PROMPT-SCHEMA.md documents 5+1 tags (identity, role, returns required; conventions, tools, context optional) with assembly order
    - core-context-loading.md, core-state-access.md, core-git.md are deleted
    - No critical content lost during consolidation (RAPID_TOOLS setup, loading strategy, state access rules, git conventions all preserved)
  </done>
</task>

</tasks>

<verification>
```bash
# Verify new files exist and old files are deleted
test -f src/modules/core/core-identity.md && echo "identity: OK"
test -f src/modules/core/core-conventions.md && echo "conventions: OK"
test -f src/modules/core/core-returns.md && echo "returns: OK (unchanged)"
test -f src/modules/PROMPT-SCHEMA.md && echo "schema: OK"
test ! -f src/modules/core/core-context-loading.md && echo "context-loading deleted: OK"
test ! -f src/modules/core/core-state-access.md && echo "state-access deleted: OK"
test ! -f src/modules/core/core-git.md && echo "git deleted: OK"

# Verify core-identity.md absorbed key content
grep -q "RAPID_TOOLS" src/modules/core/core-identity.md && echo "RAPID_TOOLS setup: OK"
grep -q "Context Loading" src/modules/core/core-identity.md && echo "Context loading section: OK"
grep -q "State Rules" src/modules/core/core-identity.md && echo "State rules section: OK"

# Verify PROMPT-SCHEMA.md has required tags documented
grep -q "<identity>" src/modules/PROMPT-SCHEMA.md && echo "Schema documents identity tag: OK"
grep -q "<tools>" src/modules/PROMPT-SCHEMA.md && echo "Schema documents tools tag: OK"
```
</verification>

<success_criteria>
- Core modules consolidated from 5 to 3 with no critical content loss
- PROMPT-SCHEMA.md defines the XML tag vocabulary with assembly order
- 3 retired modules deleted (git history preserves old content)
- Total core module token overhead reduced by ~40%
</success_criteria>

<output>
After completion, create `.planning/phases/39-tool-docs-registry-core-module-refactor/39-02-SUMMARY.md`
</output>
