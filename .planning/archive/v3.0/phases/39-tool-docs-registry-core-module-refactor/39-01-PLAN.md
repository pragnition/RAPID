---
phase: 39-tool-docs-registry-core-module-refactor
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/tool-docs.cjs
  - src/lib/tool-docs.test.cjs
autonomous: true
requirements: [AGENT-05]

must_haves:
  truths:
    - "getToolDocsForRole('executor') returns a compact YAML block containing only executor commands"
    - "getToolDocsForRole('research-synthesizer') returns null (role has no CLI commands)"
    - "Every key in every ROLE_TOOL_MAP entry exists in TOOL_REGISTRY"
    - "Tool docs for any single role estimate under 1000 tokens"
  artifacts:
    - path: "src/lib/tool-docs.cjs"
      provides: "TOOL_REGISTRY, ROLE_TOOL_MAP, getToolDocsForRole(), estimateTokens()"
      exports: ["TOOL_REGISTRY", "ROLE_TOOL_MAP", "getToolDocsForRole", "estimateTokens"]
    - path: "src/lib/tool-docs.test.cjs"
      provides: "Unit tests for tool-docs module"
      min_lines: 60
  key_links:
    - from: "src/lib/tool-docs.cjs"
      to: "src/bin/rapid-tools.cjs"
      via: "require('../lib/tool-docs.cjs') in Plan 03"
      pattern: "require.*tool-docs"
---

<objective>
Create the tool-docs.cjs module with TOOL_REGISTRY (static command descriptions), ROLE_TOOL_MAP (per-role command key arrays), and getToolDocsForRole() (returns raw YAML string for embedding).

Purpose: This module is the foundation for AGENT-05 (per-role tool documentation). It provides the data and rendering function that the build pipeline will consume in Plan 03 to inject `<tools>` sections into agent prompts.

Output: `src/lib/tool-docs.cjs` and `src/lib/tool-docs.test.cjs`
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-tool-docs-registry-core-module-refactor/39-CONTEXT.md
@.planning/phases/39-tool-docs-registry-core-module-refactor/39-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tool-docs.cjs with TOOL_REGISTRY, ROLE_TOOL_MAP, getToolDocsForRole(), estimateTokens()</name>
  <files>src/lib/tool-docs.cjs, src/lib/tool-docs.test.cjs</files>
  <behavior>
    - getToolDocsForRole('executor') returns a string starting with "# rapid-tools.cjs commands\n" followed by YAML-formatted command entries for only executor's tools
    - getToolDocsForRole('research-synthesizer') returns null (role not in ROLE_TOOL_MAP)
    - getToolDocsForRole('nonexistent-role') returns null
    - Every key referenced in ROLE_TOOL_MAP exists in TOOL_REGISTRY (no unknown keys)
    - TOOL_REGISTRY entries follow format: 'subcommand args -- description'
    - estimateTokens('hello world') returns Math.ceil(11/4) = 3
    - Tool docs for each role in ROLE_TOOL_MAP are under 1000 estimated tokens
    - getToolDocsForRole() throws if ROLE_TOOL_MAP references a key not in TOOL_REGISTRY (key integrity check)
    - ROLE_TOOL_MAP is a static explicit object (not derived from frontmatter per CONTEXT.md decision)
    - All exports are present: TOOL_REGISTRY, ROLE_TOOL_MAP, getToolDocsForRole, estimateTokens
  </behavior>
  <action>
    **RED phase:** Create `src/lib/tool-docs.test.cjs` with tests for all behaviors listed above. Use `node:test` and `node:assert/strict` (project standard). Tests MUST fail initially (module does not exist yet). Run tests to confirm RED.

    **GREEN phase:** Create `src/lib/tool-docs.cjs` implementing:

    1. `TOOL_REGISTRY` -- A plain object mapping command keys to compact one-liner descriptions. Use the hybrid one-liner format per CONTEXT.md decision: `state-get: state get <entity:milestone|set> <id:str> -- Read entity`. Include the complete registry from RESEARCH.md Code Examples section (all ~50 entries covering state, lock, planning, execution, merge, worktree, review, resolve, context, init, parse-return, verify, display, prereqs commands).

    2. `ROLE_TOOL_MAP` -- A plain object mapping role names to arrays of TOOL_REGISTRY keys. Use the recommended initial set from RESEARCH.md. Roles with no CLI commands (research-*, unit-tester, bug-hunter, devils-advocate, judge, uat, scoper) are NOT in the map. Include all roles that DO use CLI: orchestrator, executor, job-executor, planner, set-planner, reviewer, verifier, merger, set-merger, conflict-resolver, bugfix, wave-planner, job-planner, plan-verifier, wave-analyzer, roadmapper, codebase-synthesizer, context-generator.

    3. `getToolDocsForRole(role)` -- Looks up role in ROLE_TOOL_MAP, returns null if not found or empty. Maps each key to its TOOL_REGISTRY description, throws Error if key not found. Returns formatted string: `# rapid-tools.cjs commands\n  key: description\n  key: description`.

    4. `estimateTokens(text)` -- Returns `Math.ceil(text.length / 4)`. Simple character-based heuristic per RESEARCH.md (no tiktoken dependency).

    Export all four: `module.exports = { TOOL_REGISTRY, ROLE_TOOL_MAP, getToolDocsForRole, estimateTokens }`.

    Run tests to confirm GREEN.

    **REFACTOR phase:** Clean up if needed, ensure consistent formatting. Run tests to confirm still GREEN.
  </action>
  <verify>
    <automated>node --test src/lib/tool-docs.test.cjs</automated>
  </verify>
  <done>
    - src/lib/tool-docs.cjs exports TOOL_REGISTRY, ROLE_TOOL_MAP, getToolDocsForRole, estimateTokens
    - All ROLE_TOOL_MAP keys validated against TOOL_REGISTRY
    - getToolDocsForRole returns raw YAML string for roles with commands, null for roles without
    - Token budget test confirms no role exceeds 1000 estimated tokens
    - All tests pass
  </done>
</task>

</tasks>

<verification>
```bash
node --test src/lib/tool-docs.test.cjs
```
All tests pass. Module exports the 4 expected symbols.
</verification>

<success_criteria>
- tool-docs.cjs exists with complete TOOL_REGISTRY (~50 commands) and ROLE_TOOL_MAP (~18 roles)
- getToolDocsForRole() returns YAML strings ready for `<tools>` tag embedding
- No role exceeds 1000 estimated tokens in tool docs
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/39-tool-docs-registry-core-module-refactor/39-01-SUMMARY.md`
</output>
