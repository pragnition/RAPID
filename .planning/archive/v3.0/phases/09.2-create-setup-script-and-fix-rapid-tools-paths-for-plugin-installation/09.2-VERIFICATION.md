---
phase: 09.2-create-setup-script-and-fix-rapid-tools-paths-for-plugin-installation
verified: 2026-03-05T15:00:00Z
status: passed
score: 3/3 must-haves verified
re_verification: false
---

# Phase 09.2: Create Setup Script and Fix RAPID_TOOLS Paths Verification Report

**Phase Goal:** RAPID has a portable setup system (setup.sh + /rapid:install) that bootstraps RAPID_TOOLS env var for any installation method, with all hardcoded fallback paths removed
**Verified:** 2026-03-05T15:00:00Z
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | setup.sh at repo root bootstraps RAPID_TOOLS env var, validates prereqs, installs deps, and registers plugin -- idempotently | VERIFIED | setup.sh exists (184 lines), passes bash -n syntax check, is executable, contains 5 idempotent steps with skip logic, interactive persistence prompt (shell config vs env file), supports bash/zsh/fish, uses cd+dirname for self-location |
| 2 | /rapid:install skill provides guided setup from within Claude Code for both marketplace and git clone installations | VERIFIED | skills/install/SKILL.md exists (63 lines) with correct frontmatter (disable-model-invocation, allowed-tools), detects marketplace vs clone via CLAUDE_SKILL_DIR, runs setup.sh via relative path, includes manual fallback instructions |
| 3 | All SKILL.md files and core modules use bare ${RAPID_TOOLS} with no fallback paths -- missing env var produces clear error | VERIFIED | Zero matches for 'RAPID_TOOLS:-' in skills/, commands/, src/modules/core/. 12 guard checks found across 10 skills/commands + 2 core modules. Guard message: "[RAPID ERROR] RAPID_TOOLS is not set. Run /rapid:install or ./setup.sh to configure RAPID." |

**Score:** 3/3 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `setup.sh` | Bootstrap script for RAPID installation (min 80 lines) | VERIFIED | 184 lines, executable, bash syntax valid, contains detect_install_method, interactive prompt, fish/zsh/bash support |
| `skills/install/SKILL.md` | /rapid:install skill for in-Claude-Code setup (min 20 lines) | VERIFIED | 63 lines, correct frontmatter, 4-step structure (detect/run/verify/guide) |
| `skills/execute/SKILL.md` | Execute skill with fallbacks removed and guard added | VERIFIED | 27 RAPID_TOOLS references, bare ${RAPID_TOOLS} usage, guard check present |
| `src/modules/core/core-state-access.md` | Core module with tilde fallbacks removed and guard added | VERIFIED | Uses bare ${RAPID_TOOLS}, guard check present in Prerequisites section |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| setup.sh | src/bin/rapid-tools.cjs | RAPID_TOOLS_PATH variable derived from SCRIPT_DIR | WIRED | Line 8: `RAPID_TOOLS_PATH="$SCRIPT_DIR/src/bin/rapid-tools.cjs"` |
| skills/install/SKILL.md | setup.sh | CLAUDE_SKILL_DIR relative path | WIRED | `bash "$RAPID_ROOT/setup.sh"` where RAPID_ROOT is `${CLAUDE_SKILL_DIR}/../..` |
| skills/*/SKILL.md | src/bin/rapid-tools.cjs | bare ${RAPID_TOOLS} env var with guard check | WIRED | All 9 skill files use `node "${RAPID_TOOLS}"` with guard check as first line in bash block |
| src/modules/core/*.md | src/bin/rapid-tools.cjs | bare ${RAPID_TOOLS} env var with guard check | WIRED | Both core modules use `node "${RAPID_TOOLS}"` with guard check |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| SETUP-01 | 09.2-01 | setup.sh bootstraps RAPID_TOOLS for any installation method with idempotent prereq validation, npm install, and plugin registration | SATISFIED | setup.sh has all 5 steps, idempotent checks (grep for RAPID_TOOLS, check node_modules), supports marketplace and clone |
| SETUP-02 | 09.2-01 | /rapid:install skill provides guided in-Claude-Code setup that auto-detects installation method and runs setup.sh | SATISFIED | skills/install/SKILL.md detects marketplace vs clone, runs setup.sh, provides manual fallback |
| SETUP-03 | 09.2-02 | All path fallbacks removed -- bare ${RAPID_TOOLS} with no hardcoded fallback, missing env var produces clear error | SATISFIED | Zero RAPID_TOOLS:- matches in source dirs, 12 guard checks with clear error message |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | - | - | - | No anti-patterns detected |

### Human Verification Required

### 1. Setup Script End-to-End Run

**Test:** Run `./setup.sh` from repo root on a clean environment
**Expected:** All 5 steps complete successfully, RAPID_TOOLS persisted to shell config, plugin registered
**Why human:** Requires interactive input (persistence method choice) and shell environment state

### 2. /rapid:install Skill Invocation

**Test:** Run `/rapid:install` from within Claude Code
**Expected:** Detects installation method, runs setup.sh, verifies RAPID_TOOLS is functional
**Why human:** Requires Claude Code runtime with CLAUDE_SKILL_DIR variable

### 3. Guard Error on Unset RAPID_TOOLS

**Test:** Unset RAPID_TOOLS and invoke any skill (e.g., `/rapid:status`)
**Expected:** Clear error: "[RAPID ERROR] RAPID_TOOLS is not set. Run /rapid:install or ./setup.sh to configure RAPID."
**Why human:** Requires running in Claude Code with env var unset

---

_Verified: 2026-03-05T15:00:00Z_
_Verifier: Claude (gsd-verifier)_
