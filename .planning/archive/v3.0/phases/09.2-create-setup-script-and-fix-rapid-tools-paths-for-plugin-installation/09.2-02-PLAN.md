---
phase: 09.2-create-setup-script-and-fix-rapid-tools-paths-for-plugin-installation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/init/SKILL.md
  - skills/execute/SKILL.md
  - skills/plan/SKILL.md
  - skills/status/SKILL.md
  - skills/merge/SKILL.md
  - skills/pause/SKILL.md
  - skills/cleanup/SKILL.md
  - skills/context/SKILL.md
  - skills/assumptions/SKILL.md
  - skills/help/SKILL.md
  - commands/init.md
  - src/modules/core/core-state-access.md
  - src/modules/core/core-context-loading.md
  - DOCS.md
autonomous: true
requirements:
  - SETUP-03

must_haves:
  truths:
    - "No file in the codebase contains a hardcoded fallback path (RAPID_TOOLS:-)"
    - "All SKILL.md files use bare ${RAPID_TOOLS} for node invocations"
    - "Core modules use bare ${RAPID_TOOLS} with no tilde fallback"
    - "DOCS.md includes setup.sh installation instructions"
    - "/rapid:install appears in help skill command list"
    - "When RAPID_TOOLS is unset, each SKILL.md and core module prints a clear error and exits 1"
  artifacts:
    - path: "skills/execute/SKILL.md"
      provides: "Execute skill with fallbacks removed and RAPID_TOOLS guard added"
      contains: "RAPID_TOOLS"
    - path: "src/modules/core/core-state-access.md"
      provides: "Core module with tilde fallbacks removed and RAPID_TOOLS guard added"
      contains: "RAPID_TOOLS"
  key_links:
    - from: "skills/*/SKILL.md"
      to: "src/bin/rapid-tools.cjs"
      via: "bare ${RAPID_TOOLS} env var (no fallback) with guard check"
      pattern: 'node "\$\{RAPID_TOOLS\}"'
    - from: "src/modules/core/*.md"
      to: "src/bin/rapid-tools.cjs"
      via: "bare ${RAPID_TOOLS} env var (no fallback) with guard check"
      pattern: 'node "\$\{RAPID_TOOLS\}"'
---

<objective>
Remove all hardcoded fallback paths from SKILL.md files, commands, and core modules. Add RAPID_TOOLS guard checks so unset env var produces a clear error. Update DOCS.md and help skill to reflect the new /rapid:install command.

Purpose: Achieve full portability -- RAPID works identically whether installed via marketplace or git clone, with no hardcoded path assumptions. Per user decision, when RAPID_TOOLS is not set, commands fail with a clear error message directing users to /rapid:install.

Output: 14 files updated with fallback paths removed and guard checks added, DOCS.md with new install instructions, help skill listing /rapid:install
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.2-create-setup-script-and-fix-rapid-tools-paths-for-plugin-installation/09.2-CONTEXT.md
@.planning/phases/09.2-create-setup-script-and-fix-rapid-tools-paths-for-plugin-installation/09.2-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove all fallback paths from source files</name>
  <files>
skills/init/SKILL.md
skills/execute/SKILL.md
skills/plan/SKILL.md
skills/status/SKILL.md
skills/merge/SKILL.md
skills/pause/SKILL.md
skills/cleanup/SKILL.md
skills/context/SKILL.md
skills/assumptions/SKILL.md
commands/init.md
src/modules/core/core-state-access.md
src/modules/core/core-context-loading.md
  </files>
  <action>
Perform two sed replacements across all 12 source files:

**Replacement 1 -- $HOME variant** (SKILL.md files and commands/init.md):
```bash
sed -i 's|\${RAPID_TOOLS:-\$HOME/RAPID/src/bin/rapid-tools.cjs}|\${RAPID_TOOLS}|g' \
  skills/init/SKILL.md \
  skills/execute/SKILL.md \
  skills/plan/SKILL.md \
  skills/status/SKILL.md \
  skills/merge/SKILL.md \
  skills/pause/SKILL.md \
  skills/cleanup/SKILL.md \
  skills/context/SKILL.md \
  skills/assumptions/SKILL.md \
  commands/init.md
```

**Replacement 2 -- tilde variant** (core modules):
```bash
sed -i 's|\${RAPID_TOOLS:-~/RAPID/src/bin/rapid-tools.cjs}|\${RAPID_TOOLS}|g' \
  src/modules/core/core-state-access.md \
  src/modules/core/core-context-loading.md
```

**Verification after sed:** Run `grep -r 'RAPID_TOOLS:-' skills/ commands/ src/modules/core/` and confirm ZERO matches in source files (ignore .planning/ directory).

**Important:** Do NOT modify any files in .planning/ -- those are historical records.
  </action>
  <verify>
    <automated>grep -r 'RAPID_TOOLS:-' /home/kek/Projects/RAPID/skills/ /home/kek/Projects/RAPID/commands/ /home/kek/Projects/RAPID/src/modules/core/ && echo "FAIL: fallbacks still present" || echo "PASS: all fallbacks removed"</automated>
  </verify>
  <done>Zero occurrences of "RAPID_TOOLS:-" in skills/, commands/, and src/modules/core/ directories. All 76+ occurrences replaced with bare ${RAPID_TOOLS}.</done>
</task>

<task type="auto">
  <name>Task 2: Add RAPID_TOOLS guard checks to all SKILL.md and core module files</name>
  <files>
skills/init/SKILL.md
skills/execute/SKILL.md
skills/plan/SKILL.md
skills/status/SKILL.md
skills/merge/SKILL.md
skills/pause/SKILL.md
skills/cleanup/SKILL.md
skills/context/SKILL.md
skills/assumptions/SKILL.md
commands/init.md
src/modules/core/core-state-access.md
src/modules/core/core-context-loading.md
  </files>
  <action>
Per user decision and RESEARCH.md Pitfall 6: when RAPID_TOOLS is not set, commands must print a clear error message and exit 1, not silently fail with `node ""`.

**For each of the 10 SKILL.md files and commands/init.md:** Find the FIRST ```bash code block in the file. Insert this guard as the very first line inside that bash block (before any existing commands):

```bash
if [ -z "${RAPID_TOOLS}" ]; then echo "[RAPID ERROR] RAPID_TOOLS is not set. Run /rapid:install or ./setup.sh to configure RAPID."; exit 1; fi
```

For example, in skills/init/SKILL.md the first bash block currently starts with `node "${RAPID_TOOLS}" prereqs`. After this task it should read:
```bash
if [ -z "${RAPID_TOOLS}" ]; then echo "[RAPID ERROR] RAPID_TOOLS is not set. Run /rapid:install or ./setup.sh to configure RAPID."; exit 1; fi
node "${RAPID_TOOLS}" prereqs
```

**For the 2 core module files** (core-state-access.md, core-context-loading.md): Same approach -- find the first ```bash block and insert the identical guard line at the top.

**Approach:** Read each file, locate the first occurrence of a line starting with ````bash`, then insert the guard line immediately after it. Use the Edit tool for precision rather than sed, since the insertion point varies per file.

**Do NOT add the guard to:**
- skills/help/SKILL.md (does not invoke node/RAPID_TOOLS)
- skills/install/SKILL.md (this IS the install skill -- it should not guard against missing RAPID_TOOLS since its purpose is to set it up)
- Any files in .planning/

**Verification:** After all insertions, run `grep -r 'RAPID ERROR.*RAPID_TOOLS' skills/ commands/ src/modules/core/ | wc -l` and confirm it returns 12 (one guard per file).
  </action>
  <verify>
    <automated>count=$(grep -r 'RAPID ERROR.*RAPID_TOOLS is not set' /home/kek/Projects/RAPID/skills/ /home/kek/Projects/RAPID/commands/ /home/kek/Projects/RAPID/src/modules/core/ | wc -l); if [ "$count" -ge 12 ]; then echo "PASS: $count guard checks found"; else echo "FAIL: only $count guard checks found (expected 12)"; fi</automated>
  </verify>
  <done>All 10 SKILL.md files (excluding help and install), commands/init.md, and 2 core module files contain a RAPID_TOOLS guard check that prints "[RAPID ERROR] RAPID_TOOLS is not set. Run /rapid:install or ./setup.sh to configure RAPID." and exits 1 when RAPID_TOOLS is empty/unset.</done>
</task>

<task type="auto">
  <name>Task 3: Update DOCS.md and help skill</name>
  <files>DOCS.md, skills/help/SKILL.md</files>
  <action>
**DOCS.md updates:**

1. Find the installation section. Update it to document two installation methods:

   **Method 1 (Primary): Plugin Marketplace**
   ```
   claude plugin add fishjojo1/RAPID
   ```
   Then run `/rapid:install` from within Claude Code to complete setup.

   **Method 2: Git Clone**
   ```
   git clone https://github.com/fishjojo1/RAPID
   cd RAPID
   ./setup.sh
   ```

2. Add a note that RAPID_TOOLS env var must be set (setup.sh handles this automatically).

3. Add /rapid:install to the command reference section with description: "Install and configure RAPID plugin, set RAPID_TOOLS env var"

**skills/help/SKILL.md updates:**

Read the current help skill. Add `/rapid:install` to the command list with description: "Install and configure RAPID plugin for Claude Code". Place it logically near the top (before /rapid:init) since installation comes first.
  </action>
  <verify>
    <automated>grep -q "rapid:install" /home/kek/Projects/RAPID/DOCS.md && grep -q "rapid:install" /home/kek/Projects/RAPID/skills/help/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>DOCS.md contains setup.sh and marketplace installation instructions plus /rapid:install in command reference. Help skill lists /rapid:install command.</done>
</task>

</tasks>

<verification>
- `grep -r 'RAPID_TOOLS:-' skills/ commands/ src/modules/core/` returns zero matches
- `grep -c 'RAPID_TOOLS' skills/execute/SKILL.md` returns > 20 (confirming references still exist, just without fallback)
- `grep -r 'RAPID ERROR' skills/ commands/ src/modules/core/ | wc -l` returns 12 (guard in each file)
- DOCS.md contains "setup.sh" and "rapid:install"
- skills/help/SKILL.md contains "rapid:install"
</verification>

<success_criteria>
- All 76+ fallback path occurrences across 12 source files replaced with bare ${RAPID_TOOLS}
- No source file (outside .planning/) contains RAPID_TOOLS:- pattern
- All 12 source files (10 SKILL.md + init.md + 2 core modules) have RAPID_TOOLS guard checks
- When RAPID_TOOLS is unset, every skill prints clear error directing to /rapid:install or setup.sh
- DOCS.md documents both marketplace and git clone installation
- Help skill includes /rapid:install
</success_criteria>

<output>
After completion, create `.planning/phases/09.2-create-setup-script-and-fix-rapid-tools-paths-for-plugin-installation/09.2-02-SUMMARY.md`
</output>
