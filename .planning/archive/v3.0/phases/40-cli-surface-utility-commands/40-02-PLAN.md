---
phase: 40-cli-surface-utility-commands
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/status/SKILL.md
  - skills/install/SKILL.md
autonomous: true
requirements: [CMD-08, CMD-12]

must_haves:
  truths:
    - "/rapid:status shows a set-level dashboard (no wave/job breakdown)"
    - "/rapid:status shows last git activity per set (commit timestamp and message)"
    - "/rapid:status suggests per-set next actions based on status"
    - "/rapid:status uses v3 command names in suggestions (start-set, discuss-set, etc.)"
    - "/rapid:install validates installation and updates plugin files for v3"
  artifacts:
    - path: "skills/status/SKILL.md"
      provides: "v3 status dashboard"
      contains: "set-level"
      min_lines: 80
    - path: "skills/install/SKILL.md"
      provides: "v3 install skill"
      contains: "install"
      min_lines: 100
  key_links:
    - from: "skills/status/SKILL.md"
      to: "rapid-tools.cjs worktree status-v2"
      via: "CLI subcommand for state data"
      pattern: "worktree status"
    - from: "skills/status/SKILL.md"
      to: "rapid-tools.cjs state get --all"
      via: "STATE.json read for set statuses"
      pattern: "state get --all"
---

<objective>
Rewrite the /status dashboard for set-level-only display with last git activity and v3 command names, and update /install for v3 compatibility.

Purpose: Users see a clean set-level dashboard without wave/job hierarchy noise, with actionable v3 command suggestions. Install skill reflects v3 structure.
Output: Rewritten status SKILL.md and updated install SKILL.md.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-cli-surface-utility-commands/40-CONTEXT.md

<interfaces>
<!-- Key state-machine exports the status skill depends on -->

From src/lib/state-machine.cjs:
```javascript
module.exports = {
  createInitialState,
  readState,       // Returns { valid, state, errors } or null
  writeState,
  withStateTransaction,
  findMilestone,   // findMilestone(state, milestoneId) -> milestone object with .sets array
  findSet,         // findSet(state, milestoneId, setId) -> set object with .status
  transitionSet,
  addMilestone,
  validateDiskArtifacts,
  detectCorruption,
  recoverFromGit,
  commitState,
};
```

From src/lib/state-schemas.cjs:
```javascript
const SetStatus = z.enum(['pending', 'discussing', 'planning', 'executing', 'complete', 'merged']);
```

CLI commands available:
- `node "${RAPID_TOOLS}" worktree status-v2` -- returns JSON { table, actions, milestone }
- `node "${RAPID_TOOLS}" state get --all` -- returns full STATE.json
- `node "${RAPID_TOOLS}" plan list-sets` -- returns JSON array of set names
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite /status dashboard for v3.0 set-level display</name>
  <files>skills/status/SKILL.md</files>
  <action>
    Rewrite skills/status/SKILL.md from scratch for v3.0. The dashboard must be set-level only (no wave/job hierarchy). Key changes from v2:

    **Frontmatter:** Keep description, allowed-tools (Bash, Read, AskUserQuestion). Update description to: "Show project dashboard with set statuses, last activity, and next actions"

    **Step 1: Load Environment** -- same .env loading preamble pattern as current

    **Step 2: Load Dashboard Data**
    Read STATE.json for set statuses:
    ```bash
    node "${RAPID_TOOLS}" state get --all
    ```

    Parse the JSON to extract currentMilestone and iterate sets. For each set, read its status from the state object.

    Also get last git activity per set by checking branch activity:
    ```bash
    git log -1 --format="%ci|%s" rapid/{setName} 2>/dev/null || echo "no-branch"
    ```

    **Step 3: Display Dashboard**
    Show a compact set-level table:

    ```
    ## {milestoneName} -- Set Dashboard

    | # | Set | Status | Last Activity | Branch |
    |---|-----|--------|---------------|--------|
    | 1 | set-name | executing | 2h ago: "commit msg" | rapid/set-name |
    | 2 | set-name | pending | no branch | -- |
    ```

    Status should be color-coded if possible (just use the raw status text -- Claude will format).

    **Step 4: Per-Set Next Actions**
    Based on set status, suggest the v3 command:
    - pending -> "Run `/rapid:start-set {N}`"
    - discussing -> "Run `/rapid:discuss-set {N}`"
    - planning -> "Run `/rapid:plan-set {N}`"
    - executing -> "Run `/rapid:execute-set {N}`"
    - complete -> "Run `/rapid:review {N}`"
    - merged -> "(done)"

    Use AskUserQuestion with top 4 actions + "Done" option. Use numeric shorthand for set references.

    **Important differences from v2:**
    - NO wave/job hierarchy display (removed entirely)
    - NO status-v2 CLI subcommand (read state directly via `state get --all`)
    - NO wave-plan or set-init references (use v3 names: start-set, discuss-set, etc.)
    - Show last git activity per set (git log on branch)
    - Remove all references to REGISTRY.json for status display (use STATE.json only for status; optionally use worktree list for branch paths)

    **Edge cases:**
    - No sets: "No sets found. Run `/rapid:init` to get started."
    - All merged: "All sets merged. Run `/rapid:new-version` to start next milestone."
    - STATE.json missing: "STATE.json not found. Run `/rapid:init` to initialize."
  </action>
  <verify>
    <automated>grep -c "start-set\|discuss-set\|plan-set\|execute-set\|new-version" skills/status/SKILL.md | xargs test 3 -le && grep -v "wave-plan\|set-init\|wave.*job" skills/status/SKILL.md | wc -l | xargs test 50 -le && echo "Status v3 verified"</automated>
  </verify>
  <done>
    Status SKILL.md shows set-level-only dashboard (no wave/job hierarchy).
    Per-set next-action suggestions use v3 command names.
    Last git activity shown per set.
    Edge cases handled (no sets, all merged, no STATE.json).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update /install skill for v3.0</name>
  <files>skills/install/SKILL.md</files>
  <action>
    Update skills/install/SKILL.md with minimal v3.0 changes per CONTEXT.md decision ("likely minimal changes"):

    1. **Post-install next actions (Step 5):** Update command suggestions to use v3 names:
       - "Run /rapid:help" stays the same
       - "Run /rapid:init" stays the same
       - Add option: "Run /rapid:status" -- "Check project status if already initialized"
       - "Done" stays

    2. **Description:** Update description to include "v3.0" reference if not already present.

    3. **Build agents step:** Verify setup.sh still calls `build-agents`. The install skill already delegates to setup.sh, so this should be fine. No changes needed to the build step itself.

    4. **RAPID version reference:** If the skill mentions a version number, update to v3.0.

    5. **No structural changes needed** -- the install flow (setup.sh, shell detection, .env writing, verification) is version-agnostic. Only command name references in post-install guidance need updating.
  </action>
  <verify>
    <automated>grep "status\|help\|init" skills/install/SKILL.md | head -5 && echo "Install verified"</automated>
  </verify>
  <done>
    Install SKILL.md post-install suggestions use v3 command names.
    Install flow unchanged (setup.sh handles the actual work).
    Version references updated to v3.0 if present.
  </done>
</task>

</tasks>

<verification>
- Status skill shows set-level dashboard without wave/job hierarchy
- Status skill suggests v3 command names (start-set, discuss-set, etc.)
- Install skill post-install actions reference v3 commands
- Neither skill references removed commands (wave-plan, set-init as active commands)
</verification>

<success_criteria>
- Status SKILL.md contains "set-level" or equivalent, no wave/job hierarchy display code
- Status SKILL.md uses v3 command names in action suggestions
- Install SKILL.md post-install guidance references /rapid:help, /rapid:init, /rapid:status
</success_criteria>

<output>
After completion, create `.planning/phases/40-cli-surface-utility-commands/40-02-SUMMARY.md`
</output>
