---
phase: 40-cli-surface-utility-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/start-set/SKILL.md
  - skills/discuss-set/SKILL.md
  - skills/execute-set/SKILL.md
  - skills/new-version/SKILL.md
  - skills/plan/SKILL.md
  - skills/set-init/SKILL.md
  - skills/discuss/SKILL.md
  - skills/execute/SKILL.md
  - skills/new-milestone/SKILL.md
  - skills/wave-plan/SKILL.md
  - skills/help/SKILL.md
autonomous: true
requirements: [UX-03, UX-04]

must_haves:
  truths:
    - "Running /rapid:plan shows a deprecation message directing user to /rapid:plan-set"
    - "Running /rapid:wave-plan shows a deprecation message directing user to /rapid:plan-set"
    - "Running /rapid:set-init shows a deprecation message directing user to /rapid:start-set"
    - "Running /rapid:discuss shows a deprecation message directing user to /rapid:discuss-set"
    - "Running /rapid:execute shows a deprecation message directing user to /rapid:execute-set"
    - "Running /rapid:new-milestone shows a deprecation message directing user to /rapid:new-version"
    - "Skills directories for renamed commands exist with correct content (start-set, discuss-set, execute-set, new-version)"
    - "/rapid:help lists exactly 7 core + 4 auxiliary commands plus help and kept utilities"
  artifacts:
    - path: "skills/start-set/SKILL.md"
      provides: "Renamed skill from set-init"
      contains: "start-set"
    - path: "skills/discuss-set/SKILL.md"
      provides: "Renamed skill from discuss"
      contains: "discuss-set"
    - path: "skills/execute-set/SKILL.md"
      provides: "Renamed skill from execute"
      contains: "execute-set"
    - path: "skills/new-version/SKILL.md"
      provides: "Renamed skill from new-milestone"
      contains: "new-version"
    - path: "skills/plan/SKILL.md"
      provides: "Deprecation stub"
      contains: "Use /rapid:plan-set instead"
    - path: "skills/wave-plan/SKILL.md"
      provides: "Deprecation stub"
      contains: "Use /rapid:plan-set instead"
    - path: "skills/help/SKILL.md"
      provides: "Updated v3 command reference"
      contains: "7 core"
  key_links:
    - from: "skills/start-set/SKILL.md"
      to: "skills/set-init/SKILL.md"
      via: "content copied and renamed"
      pattern: "start-set"
    - from: "skills/help/SKILL.md"
      to: "all skill directories"
      via: "command listing"
      pattern: "init.*start-set.*discuss-set.*plan-set.*execute-set.*review.*merge"
---

<objective>
Restructure skill directories to reflect v3.0 command naming (7 core + 4 auxiliary), create deprecation stubs for removed commands, and rewrite the help command reference.

Purpose: Users see the correct v3.0 command names when using RAPID, and removed v2 commands produce clear migration guidance.
Output: Renamed skill directories, deprecation stub SKILL.md files, updated help reference.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-cli-surface-utility-commands/40-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename skill directories and create deprecation stubs</name>
  <files>
    skills/start-set/SKILL.md,
    skills/discuss-set/SKILL.md,
    skills/execute-set/SKILL.md,
    skills/new-version/SKILL.md,
    skills/plan/SKILL.md,
    skills/set-init/SKILL.md,
    skills/discuss/SKILL.md,
    skills/execute/SKILL.md,
    skills/new-milestone/SKILL.md,
    skills/wave-plan/SKILL.md
  </files>
  <action>
    Create new skill directories with renamed commands by copying and updating existing SKILL.md files:

    1. **skills/start-set/SKILL.md**: Copy content from skills/set-init/SKILL.md. Update all references:
       - Title: "/rapid:start-set" (was "/rapid:set-init")
       - Description frontmatter: update to reference "start-set"
       - Internal references to "set-init" become "start-set"
       - The `set-init create` CLI subcommand references stay as-is (CLI internals not changed per CONTEXT.md -- deferred to Phase 45)

    2. **skills/discuss-set/SKILL.md**: Copy content from skills/discuss/SKILL.md. Update all references:
       - Title: "/rapid:discuss-set" (was "/rapid:discuss")
       - Description frontmatter: update to reference "discuss-set"

    3. **skills/execute-set/SKILL.md**: Copy content from skills/execute/SKILL.md. Update all references:
       - Title: "/rapid:execute-set" (was "/rapid:execute")
       - Description frontmatter: update to reference "execute-set"

    4. **skills/new-version/SKILL.md**: Copy content from skills/new-milestone/SKILL.md. Update all references:
       - Title: "/rapid:new-version" (was "/rapid:new-milestone")
       - Description frontmatter: update to reference "new-version"

    Then replace old skill dirs with deprecation stubs. Each old skill gets a minimal SKILL.md:

    5. **skills/set-init/SKILL.md**: Replace with deprecation stub:
       ```
       ---
       description: "[DEPRECATED] Use /rapid:start-set instead"
       disable-model-invocation: true
       ---
       /rapid:set-init was removed in v3.0. Use /rapid:start-set instead.
       ```

    6. **skills/discuss/SKILL.md**: Replace with deprecation stub pointing to /rapid:discuss-set

    7. **skills/execute/SKILL.md**: Replace with deprecation stub pointing to /rapid:execute-set

    8. **skills/new-milestone/SKILL.md**: Replace with deprecation stub pointing to /rapid:new-version

    9. **skills/plan/SKILL.md**: Replace with deprecation stub:
       ```
       ---
       description: "[DEPRECATED] Use /rapid:plan-set instead"
       disable-model-invocation: true
       ---
       /rapid:plan was removed in v3.0. Project-level decomposition is now handled by /rapid:init. For set planning, use /rapid:plan-set instead.
       ```

    10. **skills/wave-plan/SKILL.md**: Replace with deprecation stub:
        ```
        ---
        description: "[DEPRECATED] Use /rapid:plan-set instead"
        disable-model-invocation: true
        ---
        /rapid:wave-plan was removed in v3.0. Wave planning is now part of /rapid:plan-set, which handles all waves in a single pass. Use /rapid:plan-set instead.
        ```

    All deprecation stubs use `disable-model-invocation: true` so Claude outputs the message directly without needing model processing.
  </action>
  <verify>
    <automated>ls skills/start-set/SKILL.md skills/discuss-set/SKILL.md skills/execute-set/SKILL.md skills/new-version/SKILL.md && grep -l "DEPRECATED" skills/plan/SKILL.md skills/wave-plan/SKILL.md skills/set-init/SKILL.md skills/discuss/SKILL.md skills/execute/SKILL.md skills/new-milestone/SKILL.md && echo "All skill files present"</automated>
  </verify>
  <done>
    4 new skill directories exist with renamed content (start-set, discuss-set, execute-set, new-version).
    6 old skill directories contain deprecation stubs with clear migration messages (plan, wave-plan, set-init, discuss, execute, new-milestone).
    All deprecation stubs use disable-model-invocation: true.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite help command for v3.0 structure</name>
  <files>skills/help/SKILL.md</files>
  <action>
    Rewrite skills/help/SKILL.md to reflect the v3.0 command structure. Keep `disable-model-invocation: true` frontmatter (static output, no model needed).

    The help output must show this structure:

    **7 Core Lifecycle Commands:**
    1. /rapid:init -- Initialize project (research, roadmap)
    2. /rapid:start-set -- Create worktree and begin set development
    3. /rapid:discuss-set -- Capture implementation vision for a set
    4. /rapid:plan-set -- Plan all waves in a set
    5. /rapid:execute-set -- Execute planned work in a set
    6. /rapid:review -- Review completed set (unit test, bug hunt, UAT)
    7. /rapid:merge -- Merge completed sets into main

    **4 Auxiliary Commands:**
    1. /rapid:status -- Project dashboard with next actions
    2. /rapid:install -- Plugin installation and configuration
    3. /rapid:new-version -- Start new milestone/version cycle
    4. /rapid:add-set -- Add sets mid-milestone (Phase 44)

    **Meta:**
    - /rapid:help -- This command reference

    **Kept Utilities (unlisted in primary table but documented):**
    - /rapid:assumptions -- Surface assumptions about a set
    - /rapid:pause -- Pause set work
    - /rapid:resume -- Resume paused set
    - /rapid:context -- Generate codebase context
    - /rapid:cleanup -- Remove completed worktrees

    Update the workflow diagram to show v3 flow:
    ```
    INIT -> START-SET -> DISCUSS-SET -> PLAN-SET -> EXECUTE-SET -> REVIEW -> MERGE
    ```

    Update the "Typical Workflow" section with v3 command names.
    Update the footer: "RAPID v3.0 | 7+4 commands | Rapid Agentic Parallelizable and Isolatable Development"

    Note: /rapid:add-set and /rapid:quick are listed but marked "(Phase 44 -- not yet available)".
  </action>
  <verify>
    <automated>grep -c "start-set\|discuss-set\|plan-set\|execute-set\|new-version" skills/help/SKILL.md | xargs test 5 -le && grep "7+4" skills/help/SKILL.md && echo "Help updated"</automated>
  </verify>
  <done>
    Help SKILL.md shows exactly 7 core + 4 auxiliary commands.
    All command names use v3.0 naming (start-set, discuss-set, etc).
    Workflow diagram reflects v3 flow.
    Footer shows "v3.0" and "7+4 commands".
    Kept utilities documented in a separate section.
  </done>
</task>

</tasks>

<verification>
- All 4 renamed skill directories exist and contain valid SKILL.md files with updated names
- All 6 deprecated skill directories contain minimal deprecation stubs
- Help command shows correct v3.0 structure with 7+4 layout
- No skill directory references wave-plan, set-init (as active commands) in help
</verification>

<success_criteria>
- `ls skills/start-set/SKILL.md skills/discuss-set/SKILL.md skills/execute-set/SKILL.md skills/new-version/SKILL.md` succeeds
- `grep "DEPRECATED" skills/{plan,wave-plan,set-init,discuss,execute,new-milestone}/SKILL.md` matches all 6
- Help SKILL.md contains "7+4" and all v3 command names
</success_criteria>

<output>
After completion, create `.planning/phases/40-cli-surface-utility-commands/40-01-SUMMARY.md`
</output>
