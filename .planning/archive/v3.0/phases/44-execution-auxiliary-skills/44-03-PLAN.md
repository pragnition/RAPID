---
phase: 44-execution-auxiliary-skills
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/new-version/SKILL.md
autonomous: true
requirements:
  - CMD-11

must_haves:
  truths:
    - "/new-version uses 6 researchers (stack, features, architecture, pitfalls, oversights, ux) + synthesizer + roadmapper -- matching /init pipeline"
    - "/new-version roadmapper outputs sets only (no waves) -- consistent with v3 /init"
    - "User gets option to archive old milestone's planning artifacts to .planning/archive/{milestone}/"
    - "STATE.json updated with new milestone via state add-milestone CLI"
  artifacts:
    - path: "skills/new-version/SKILL.md"
      provides: "v3 new-version skill with 6-researcher pipeline and archive option"
      min_lines: 200
  key_links:
    - from: "skills/new-version/SKILL.md"
      to: "agents/rapid-research-stack.md"
      via: "Agent tool spawn for research"
      pattern: "rapid-research-stack"
    - from: "skills/new-version/SKILL.md"
      to: "agents/rapid-research-ux.md"
      via: "Agent tool spawn for UX research"
      pattern: "rapid-research-ux"
    - from: "skills/new-version/SKILL.md"
      to: "agents/rapid-research-synthesizer.md"
      via: "Agent tool spawn for synthesis"
      pattern: "rapid-research-synthesizer"
    - from: "skills/new-version/SKILL.md"
      to: "agents/rapid-roadmapper.md"
      via: "Agent tool spawn for roadmap generation"
      pattern: "rapid-roadmapper"
    - from: "skills/new-version/SKILL.md"
      to: "src/lib/state-machine.cjs"
      via: "state add-milestone CLI"
      pattern: "add-milestone"
---

<objective>
Rewrite /new-version SKILL.md for v3 with 6-researcher pipeline, sets-only roadmap, and archive option.

Purpose: The v2 new-version SKILL.md (236 lines) uses the old 5-researcher pipeline (missing research-ux) and has the roadmapper produce waves within sets. v3 requires 6 researchers (matching /init), roadmapper outputs sets only (wave decomposition happens in /plan-set), and adds an archive option for old milestone artifacts.

Output: Rewritten new-version/SKILL.md (~280 lines) with v3 patterns.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/44-execution-auxiliary-skills/44-CONTEXT.md
@.planning/phases/44-execution-auxiliary-skills/44-RESEARCH.md
@skills/new-version/SKILL.md
@skills/init/SKILL.md
</context>

<interfaces>
<!-- Key patterns from /init that /new-version must match. -->

From skills/init/SKILL.md (6-researcher pipeline):
- Spawns 6 research agents in parallel: rapid-research-stack, rapid-research-features, rapid-research-architecture, rapid-research-pitfalls, rapid-research-oversights, rapid-research-ux
- Each writes to .planning/research/{output-file}.md
- After all 6 complete: spawns rapid-research-synthesizer
- Then spawns rapid-roadmapper which outputs sets (no waves)
- Roadmapper writes to STATE.json and ROADMAP.md

From src/lib/state-machine.cjs:
```javascript
// addMilestone(milestoneId, milestoneName, carryForwardSets)
// CLI: node "${RAPID_TOOLS}" state add-milestone --id "{id}" --name "{name}"
// With carry-forward: echo '{json}' | node "${RAPID_TOOLS}" state add-milestone --id "{id}" --name "{name}"
```

From existing new-version SKILL.md (v2):
- Step 1: Read state
- Step 2: Get milestone details (version, name, goals)
- Step 3: Handle unfinished sets (carry forward)
- Step 4: Create milestone via CLI
- Step 5: Research pipeline (NEEDS UPDATE: 5 -> 6 researchers)
- Step 6: Roadmapper (NEEDS UPDATE: sets only, no waves)
- Step 7: Propose and approve roadmap
- Step 8: Completion summary
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite new-version SKILL.md for v3</name>
  <files>skills/new-version/SKILL.md</files>
  <action>
    Rewrite skills/new-version/SKILL.md for v3. The structure is similar to v2 but with critical updates.

    **Frontmatter:**
    ```yaml
    ---
    description: Complete current milestone and start a new version with 6-researcher pipeline and roadmap generation
    allowed-tools: Bash(rapid-tools:*), Agent, AskUserQuestion, Read, Write, Glob, Grep
    ---
    ```

    IMPORTANT: Remove `disable-model-invocation: true` from v2 frontmatter -- this skill DOES spawn agents.

    **Changes from v2 (apply these precisely):**

    **1. Add Step 0: Environment + Banner (NEW)**
    - Standard v3 env preamble (same as all Phase 43 skills)
    - `node "${RAPID_TOOLS}" display banner new-version`
    - This replaces the v2 Step 0 which was just env loading without banner.

    **2. Step 1: Read Current State (KEEP, minor updates)**
    - Use `node "${RAPID_TOOLS}" state get --all` (same as v2)
    - Display summary of current milestone, sets, statuses (same as v2)
    - Add progress breadcrumb on error (v3 pattern)

    **3. Step 2: Get New Milestone Details (KEEP as-is)**
    - AskUserQuestion for version, name, goals (same as v2)

    **4. Step 3: Handle Unfinished Sets (KEEP as-is)**
    - Same carry-forward logic as v2

    **5. Step 4: Create New Milestone (KEEP as-is)**
    - `node "${RAPID_TOOLS}" state add-milestone --id "{id}" --name "{name}"` with carry-forward pipe

    **6. NEW Step 4.5: Archive Option**
    - After creating milestone, offer archive option:
    - Use AskUserQuestion:
      ```
      "Archive old milestone '{old_milestone}' planning artifacts?"
      Options:
      - "Archive" -- "Move old planning artifacts to .planning/archive/{old_milestone}/"
      - "Keep" -- "Leave artifacts in place. You can archive later."
      ```
    - If "Archive":
      - Create `.planning/archive/{old_milestone}/` directory
      - Move `.planning/sets/{old_sets}` to `.planning/archive/{old_milestone}/sets/`
      - Move `.planning/research/{old_milestone}-*` to `.planning/archive/{old_milestone}/research/`
      - Move `.planning/quick/` contents to `.planning/archive/{old_milestone}/quick/` (if any)
      - Display: "Archived {N} artifacts to .planning/archive/{old_milestone}/"
    - If "Keep": display "Artifacts kept in place." Continue.

    **7. Step 5: Research Pipeline (UPDATE: 5 -> 6 researchers)**
    - Spawn ALL 6 research agents in parallel (matching /init exactly):
      1. rapid-research-stack -> .planning/research/{milestoneId}-research-stack.md
      2. rapid-research-features -> .planning/research/{milestoneId}-research-features.md
      3. rapid-research-architecture -> .planning/research/{milestoneId}-research-architecture.md
      4. rapid-research-pitfalls -> .planning/research/{milestoneId}-research-pitfalls.md
      5. rapid-research-oversights -> .planning/research/{milestoneId}-research-oversights.md
      6. rapid-research-ux -> .planning/research/{milestoneId}-research-ux.md
    - Each agent receives: project brief (milestone goals + name), brownfield context (read existing CODEBASE-ANALYSIS.md if available), working directory
    - After all 6 complete: spawn rapid-research-synthesizer
      - Input: all 6 research files (including UX.md)
      - Output: .planning/research/{milestoneId}-synthesis.md
    - Same error handling as /init: if a researcher fails, offer Retry/Skip/Cancel

    **8. Step 6: Roadmapper Pipeline (UPDATE: sets only, no waves)**
    - Spawn rapid-roadmapper with:
      ```
      Create a roadmap for milestone '{milestoneId}'.

      ## Research Synthesis
      {Full synthesis content}

      ## Milestone Goals
      {goals from Step 2}

      ## Instructions
      1. Decompose the milestone into sets (groupings of related work)
      2. Each set should be independent and parallelizable
      3. Output SETS ONLY -- do NOT decompose into waves or jobs
      4. Wave decomposition happens later in /plan-set
      5. For each set: provide id, description, success criteria, and estimated complexity
      ```
    - Parse roadmapper output for set definitions.

    **9. Step 7: Propose and Approve Roadmap (UPDATE: sets only display)**
    - Display proposed roadmap showing sets (no waves):
      ```
      ## Proposed Roadmap for {milestoneName}

      ### Set 1: {set name}
        Description: ...
        Success Criteria: ...

      ### Set 2: {set name}
        ...
      ```
    - Same Accept/Revise/Cancel flow as v2
    - On Accept: write roadmap to ROADMAP.md, update STATE.json with sets (no waves/jobs)

    **10. Step 8: Completion Summary (UPDATE: v3 wording)**
    - Display summary with v3 next steps:
      ```
      New Milestone Created.

      Milestone: {id} -- {name}
      Sets planned: {count}
      Carried forward: {count} from {previousMilestone}

      Next step: /rapid:start-set 1
      ```
    - Progress breadcrumb at end

    **Anti-Patterns:**
    - Do NOT reference waves or jobs in roadmapper output -- sets only
    - Do NOT spawn only 5 researchers -- MUST be 6 (including rapid-research-ux)
    - Do NOT force archiving -- user chooses
    - Do NOT reference v2 wave-plan or job concepts
    - Do NOT skip the UX researcher

    **Key Principles:**
    - Full 6-researcher + synthesizer pipeline (same depth as /init)
    - Roadmapper outputs sets only (no waves) -- consistent with v3
    - Archive is optional, not forced
    - STATE.json updated via add-milestone CLI
    - v3 skill pattern: env preamble, banner, error breadcrumbs, next step suggestion
  </action>
  <verify>
    <automated>grep -c 'rapid-research-ux' ~/Projects/RAPID/skills/new-version/SKILL.md | xargs test 0 -lt && grep -c 'rapid-research-stack' ~/Projects/RAPID/skills/new-version/SKILL.md | xargs test 0 -lt && grep -c 'rapid-research-synthesizer' ~/Projects/RAPID/skills/new-version/SKILL.md | xargs test 0 -lt && grep -c 'rapid-roadmapper' ~/Projects/RAPID/skills/new-version/SKILL.md | xargs test 0 -lt && echo "PASS: All 8 agents referenced" || echo "FAIL: Missing agent references"</automated>
  </verify>
  <done>new-version/SKILL.md rewritten with: 6-researcher pipeline (including rapid-research-ux), sets-only roadmapper output (no waves/jobs), archive option for old milestone, v3 skill pattern (env preamble, banner, breadcrumbs), no references to waves/jobs in roadmapper instructions</done>
</task>

</tasks>

<verification>
1. `grep -c 'rapid-research-ux' ~/Projects/RAPID/skills/new-version/SKILL.md` > 0 (6th researcher present)
2. `grep -c 'rapid-research-stack\|rapid-research-features\|rapid-research-architecture\|rapid-research-pitfalls\|rapid-research-oversights\|rapid-research-ux' ~/Projects/RAPID/skills/new-version/SKILL.md` >= 6 (all 6 researchers)
3. `grep -c 'wave\|job' ~/Projects/RAPID/skills/new-version/SKILL.md` -- check that any wave/job references are in anti-pattern sections only (explaining what NOT to do)
4. `grep -c 'archive' ~/Projects/RAPID/skills/new-version/SKILL.md` > 0 (archive option present)
5. `grep -c 'display banner new-version' ~/Projects/RAPID/skills/new-version/SKILL.md` > 0 (v3 banner pattern)
6. `head -5 ~/Projects/RAPID/skills/new-version/SKILL.md | grep -c 'disable-model-invocation'` = 0 (v2 flag removed)
</verification>

<success_criteria>
- new-version/SKILL.md uses 6-researcher pipeline matching /init exactly
- Roadmapper instructed to output sets only (no wave decomposition)
- Archive option offered after milestone creation (not forced)
- v3 skill pattern followed (env preamble, banner, breadcrumbs, next step)
- disable-model-invocation removed from frontmatter
</success_criteria>

<output>
After completion, create `.planning/phases/44-execution-auxiliary-skills/44-03-SUMMARY.md`
</output>
