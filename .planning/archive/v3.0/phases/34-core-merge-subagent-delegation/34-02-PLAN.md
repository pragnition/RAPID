---
phase: 34-core-merge-subagent-delegation
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - skills/merge/SKILL.md
autonomous: false
requirements: [MERGE-01, MERGE-02, MERGE-03]

must_haves:
  truths:
    - "SKILL.md dispatches rapid-set-merger subagent per set instead of running detection/resolution inline"
    - "Fast path via git merge-tree skips subagent for zero-conflict sets"
    - "CHECKPOINT returns are auto-retried once with checkpoint data before surfacing to user"
    - "BLOCKED or max-retry sets are collected and presented after the wave with Retry/Skip/Abort options"
    - "Step 8 summary uses in-memory compressedResult data instead of re-reading MERGE-STATE per set"
    - "agentPhase1 transitions from idle to spawned before dispatch and to done/failed after return"
  artifacts:
    - path: "skills/merge/SKILL.md"
      provides: "Restructured merge orchestrator with subagent dispatch, fast path, retry, and recovery"
      min_lines: 400
      contains: "rapid-set-merger"
  key_links:
    - from: "skills/merge/SKILL.md (Step 3b)"
      to: "git merge-tree --write-tree"
      via: "fast-path check before subagent dispatch"
      pattern: "merge-tree.*write-tree"
    - from: "skills/merge/SKILL.md (Step 3c)"
      to: "agents/rapid-set-merger.md"
      via: "Agent tool dispatch with launch briefing"
      pattern: "rapid-set-merger"
    - from: "skills/merge/SKILL.md (Step 3c)"
      to: "src/bin/rapid-tools.cjs (prepare-context)"
      via: "CLI call to assemble launch briefing"
      pattern: "merge prepare-context"
    - from: "skills/merge/SKILL.md (Step 3c)"
      to: "src/bin/rapid-tools.cjs (update-status --agent-phase)"
      via: "CLI call to track agentPhase1 transitions"
      pattern: "update-status.*--agent-phase"
    - from: "skills/merge/SKILL.md (Step 3d)"
      to: "src/lib/merge.cjs (parseSetMergerReturn)"
      via: "Return parsing logic referenced in SKILL.md"
      pattern: "parseSetMergerReturn"
---

<objective>
Restructure skills/merge/SKILL.md to dispatch isolated rapid-set-merger subagents per set. Replace the inline Steps 3-5 (detect, resolve, programmatic gate) with a single dispatch step that includes fast-path checking, subagent dispatch, return collection, retry logic, and post-wave recovery.

Purpose: The orchestrator's context stays lean -- all detection/resolution detail is isolated in subagent context. Only compressed results (~100 tokens/set) remain in orchestrator memory.
Output: Restructured SKILL.md with subagent delegation replacing inline processing.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-core-merge-subagent-delegation/34-CONTEXT.md
@.planning/phases/34-core-merge-subagent-delegation/34-RESEARCH.md
@.planning/phases/34-core-merge-subagent-delegation/34-01-SUMMARY.md

<interfaces>
<!-- Key artifacts from Plan 01 that this plan consumes -->

From agents/rapid-set-merger.md (generated by Plan 01):
- Agent name: rapid-set-merger
- Tools: Read, Write, Edit, Bash, Grep, Glob (no Agent, no AskUserQuestion)
- Color: green
- Core modules: core-identity.md, core-returns.md, core-git.md
- Role: orchestrator-lite per set (L1-L4 CLI, L5+T3/T4 inline, programmatic gate)
- Returns: RAPID:RETURN with {status, data: {semantic_conflicts, resolutions, escalations, gate_passed, all_resolved}}

From src/bin/rapid-tools.cjs (CLI commands from Plan 01):
```bash
# Update status with agent phase tracking
node "${RAPID_TOOLS}" merge update-status <set> <status> --agent-phase <idle|spawned|done|failed>

# Assemble launch briefing for subagent
node "${RAPID_TOOLS}" merge prepare-context <set>
# Output: JSON { setName, briefing, tokenEstimate }
```

From src/lib/merge.cjs (Phase 33 -- existing infrastructure):
```javascript
compressResult(mergeState)
// Returns: { setId, status, conflictCounts: {L1..L5}, resolutionCounts: {T1..T3,escalated}, commitSha }

parseSetMergerReturn(agentOutput)
// Returns: { status: 'COMPLETE'|'BLOCKED'|'CHECKPOINT', data?: {...}, reason?: string }
```

From skills/merge/SKILL.md (current structure to modify):
- Step 1: Load Merge Plan (KEEP unchanged)
- Step 2: Process Each Wave (KEEP, modify loop body)
- Step 3: Detection Pipeline (REPLACE with dispatch)
- Step 4: Resolution Cascade (REPLACE with dispatch)
- Step 5: Programmatic Gate (REPLACE with dispatch)
- Step 6: Merge Set (KEEP unchanged)
- Step 7: Post-Wave Integration Gate (KEEP, add blocked-set recovery before integration gate)
- Step 8: Pipeline Complete (MODIFY to use compressedResult)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure SKILL.md -- replace Steps 3-5 with dispatch, add recovery flow, update Step 8</name>
  <files>skills/merge/SKILL.md</files>
  <action>
    Rewrite `skills/merge/SKILL.md` with the following structural changes. Read the current SKILL.md first to preserve Steps 1, 2, 6, 7, and Important Notes.

    **Header update**: Change "(v2.0)" to "(v2.2)" in the title. Update the opening paragraph to mention "subagent delegation per set" replacing "merger agent for semantic analysis".

    **Step 1 (KEEP)**: Unchanged. Load merge plan, determine order, check status, confirm with user.

    **Step 2 (MODIFY)**: Keep the wave loop structure. Add two tracking variables at the top of the wave loop:
    - `compressedResults` (in-memory map: setName -> compressResult output)
    - `blockedSets` (in-memory list: [{setName, reason, attempts}])

    **Step 3 (REPLACE Steps 3+4+5)**: New "Dispatch Per-Set Merge" step. For each set in the current wave:

    **3a: Check idempotent re-entry**
    ```bash
    node "${RAPID_TOOLS}" merge merge-state {setName}
    ```
    - If status='complete', skip. Display: `> [{waveNum}/{totalWaves}] {setName}: already merged (skipping)`
    - If agentPhase1='done', skip to Step 6 (merge execute). Display: `> [{waveNum}/{totalWaves}] {setName}: detection/resolution done, proceeding to merge`

    **3b: Fast-path check**
    ```bash
    git merge-tree --write-tree HEAD rapid/{setName}
    ```
    - Exit code 0: No conflicts. Skip subagent entirely. Display: `> [{waveNum}/{totalWaves}] {setName}: clean merge (fast path)`. Go directly to Step 6.
    - Exit code 1: Conflicts detected. Continue to 3c.
    - Exit code >1: Error. Continue to 3c (let subagent diagnose).

    **3c: Update status and dispatch subagent**
    ```bash
    node "${RAPID_TOOLS}" merge update-status {setName} resolving --agent-phase spawned
    ```

    Get launch briefing:
    ```bash
    node "${RAPID_TOOLS}" merge prepare-context {setName}
    ```
    Parse the JSON output to get the `briefing` string.

    Spawn the **rapid-set-merger** agent with this task prompt:
    ```
    Merge set '{setName}' branch 'rapid/{setName}' into '{baseBranch}'.

    ## Launch Briefing
    {briefing from prepare-context}

    ## Instructions
    1. Run L1-L4 detection: `node "${RAPID_TOOLS}" merge detect {setName}`
    2. If conflicts found, run T1-T2 resolution: `node "${RAPID_TOOLS}" merge resolve {setName}`
    3. If unresolved conflicts remain, perform L5 semantic analysis and T3/T4 resolution inline (per your role instructions)
    4. Run programmatic gate: `node "${RAPID_TOOLS}" merge review {setName}`
    5. Return structured RAPID:RETURN with results

    ## Working Directory
    {worktreePath}

    ## Return Format
    <!-- RAPID:RETURN {"status":"COMPLETE","data":{"semantic_conflicts":[...],"resolutions":[...],"escalations":[...],"gate_passed":<boolean>,"all_resolved":<boolean>}} -->
    ```

    **3d: Collect and route return**

    Parse the agent's output using parseSetMergerReturn logic:

    - **COMPLETE**:
      - Update status: `node "${RAPID_TOOLS}" merge update-status {setName} resolved --agent-phase done`
      - Read MERGE-STATE for compressResult: `node "${RAPID_TOOLS}" merge merge-state {setName}`
      - Store compressedResult in memory (the in-memory compressedResults map)
      - Check `gate_passed` field. If false, add to blockedSets with reason "programmatic gate failed".
      - If gate_passed is true, continue to Step 3e.

    - **CHECKPOINT (attempt 1)**:
      - Auto-retry once with checkpoint data included in re-dispatch prompt:
        ```
        Merge set '{setName}' branch 'rapid/{setName}' into '{baseBranch}'.

        ## Checkpoint (continuing from previous attempt)
        - Done: {handoff.done}
        - Remaining: {handoff.remaining}
        - Resume from: {handoff.resume}

        ## Launch Briefing
        {briefing}

        ## Instructions
        [same as above]
        ```
      - Parse the retry's return. If COMPLETE, handle as above. If CHECKPOINT again or BLOCKED, set agentPhase1='failed' and add to blockedSets.

    - **BLOCKED or malformed**:
      - Update: `node "${RAPID_TOOLS}" merge update-status {setName} failed --agent-phase failed`
      - Add to blockedSets with reason from parseSetMergerReturn.
      - Continue to next set in the wave.

    **3e: Handle escalations from return data**

    If return data contains escalations (T4 items with confidence < 0.7):
    For each escalation, present to user via AskUserQuestion:
    - question: "Conflict escalation for {file}"
    - options:
      - "Accept AI resolution" -- description: "Apply the AI's proposed resolution despite low confidence ({confidence})"
      - "Skip conflict" -- description: "Leave conflict unresolved and continue"

    NOTE: No "Resolve manually" option per user locked decision (recovery options are Retry/Skip/Abort only).

    If user accepts, apply the proposed resolution to the file in the worktree.
    If user skips, log and continue.

    After escalations handled, proceed to Step 6 (merge execute).

    **Step 6 (KEEP)**: Unchanged. `node "${RAPID_TOOLS}" merge execute {setName}`.

    **Between Step 6 and Step 7 -- NEW: Post-Wave Blocked Set Recovery**

    After all sets in the wave have been processed (dispatched + collected OR fast-pathed + merged):

    If blockedSets is not empty:
    ```
    > **Blocked sets in Wave {waveNum}:**
    > {list of blocked sets with reasons}
    ```

    For each blocked set (max 2 total attempts per set):
    Use AskUserQuestion:
    - question: "Set '{setName}' blocked: {reason}"
    - options:
      - "Retry" -- description: "Re-dispatch subagent for {setName} (attempt {N}/2)"
      - "Skip" -- description: "Skip {setName}, continue pipeline"
      - "Abort" -- description: "Exit merge pipeline"

    If Retry: re-run Step 3c-3d for this set (reset agentPhase1 to spawned, re-dispatch). If retry also fails, auto-escalate with skip/abort only.
    If Skip: continue to next blocked set or integration gate.
    If Abort: exit pipeline.

    **Step 7 (KEEP)**: Post-wave integration gate. Unchanged.

    **Step 8 (MODIFY)**: Pipeline Complete.

    Replace the per-set MERGE-STATE re-read loop:
    ```bash
    for set in {all sets}; do node "${RAPID_TOOLS}" merge merge-state $set; done
    ```

    With in-memory compressedResults:
    ```
    Use the compressedResults collected during Step 3d to build the summary table.
    For each set, display: setId | status | L1-L5 counts | T1-T3+escalated counts | commitSha
    ```

    The summary table format stays the same, but data comes from memory instead of re-reading MERGE-STATE.json.

    **Important Notes section**: Update to reflect:
    - Agent tool spawns rapid-set-merger (not rapid-merger) for per-set dispatch
    - Fast path via git merge-tree skips subagent for clean merges
    - Retry counter is in-memory (not persisted), max 2 attempts per set
    - compressedResult in memory for pipeline summary
    - Keep all existing notes about sequential within waves, bisection auto-trigger, worktrees preserved, idempotent re-entry, etc.

    **Size target**: Keep the restructured SKILL.md under 600 lines. The dispatch step replaces 3 verbose steps (detect, resolve, gate) with one compact dispatch flow.
  </action>
  <verify>
    <automated>cd /home/kek/Projects/RAPID && wc -l skills/merge/SKILL.md && grep -c "rapid-set-merger" skills/merge/SKILL.md && grep -c "merge-tree" skills/merge/SKILL.md && grep -c "agent-phase" skills/merge/SKILL.md</automated>
  </verify>
  <done>SKILL.md restructured: Steps 3-5 replaced with dispatch step (3a-3e). Fast path via git merge-tree. Subagent dispatch with prepareMergerContext briefing. Return collection with COMPLETE/CHECKPOINT/BLOCKED routing. Auto-retry for CHECKPOINT. Post-wave recovery with Retry/Skip/Abort. Step 8 uses in-memory compressedResults. File under 600 lines.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Restructured merge SKILL.md with subagent delegation replacing inline detection/resolution. Includes fast-path (merge-tree), dispatch (rapid-set-merger), retry (CHECKPOINT auto-retry), and recovery (Retry/Skip/Abort) flows.</what-built>
  <how-to-verify>
    1. Read `skills/merge/SKILL.md` and verify the flow makes sense end-to-end:
       - Step 1: Load merge plan (unchanged)
       - Step 2: Wave loop with compressedResults + blockedSets tracking
       - Step 3a: Idempotent re-entry check
       - Step 3b: git merge-tree fast path
       - Step 3c: Dispatch rapid-set-merger with launch briefing
       - Step 3d: Collect return (COMPLETE/CHECKPOINT/BLOCKED routing)
       - Step 3e: Handle T4 escalations
       - Step 6: Merge execute (unchanged)
       - Post-wave: Blocked set recovery (Retry/Skip/Abort)
       - Step 7: Integration gate (unchanged)
       - Step 8: Summary from in-memory data
    2. Verify these locked decisions are honored:
       - Subagent does NOT execute git merge (Step 6 stays in orchestrator)
       - No "Resolve manually" in recovery options (only Retry/Skip/Abort)
       - Max 2 retries per set
       - CHECKPOINT auto-retried ONCE before surfacing
       - Fast path skips subagent for clean merges
       - compressedResult in memory, not re-read from MERGE-STATE
    3. Verify `agents/rapid-set-merger.md` exists and looks correct
    4. Run: `node --test ~/Projects/RAPID/src/lib/merge.test.cjs` -- all tests pass
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `grep -c "rapid-set-merger" /home/kek/Projects/RAPID/skills/merge/SKILL.md` -- references the new agent
2. `grep -c "merge-tree" /home/kek/Projects/RAPID/skills/merge/SKILL.md` -- fast-path check present
3. `grep -c "agent-phase" /home/kek/Projects/RAPID/skills/merge/SKILL.md` -- agentPhase1 tracking used
4. `grep -c "prepare-context" /home/kek/Projects/RAPID/skills/merge/SKILL.md` -- launch briefing assembly
5. `grep -c "CHECKPOINT" /home/kek/Projects/RAPID/skills/merge/SKILL.md` -- retry logic present
6. `grep -c "Retry\|Skip\|Abort" /home/kek/Projects/RAPID/skills/merge/SKILL.md` -- recovery options present
7. `grep "Resolve manually" /home/kek/Projects/RAPID/skills/merge/SKILL.md || echo "NOT FOUND (correct)"` -- no "Resolve manually" in post-wave recovery
8. `wc -l /home/kek/Projects/RAPID/skills/merge/SKILL.md` -- under 600 lines
</verification>

<success_criteria>
- SKILL.md dispatches rapid-set-merger per set instead of running detection/resolution inline
- Fast path via git merge-tree skips subagent when exit code is 0
- CHECKPOINT returns auto-retried once with checkpoint data in re-dispatch prompt
- BLOCKED/max-retry sets collected and presented with Retry/Skip/Abort after wave completes
- Step 8 uses in-memory compressedResults (no per-set MERGE-STATE re-read)
- agentPhase1 transitions tracked via update-status --agent-phase CLI calls
- All user locked decisions honored (no git merge in subagent, no "Resolve manually", max 2 retries)
</success_criteria>

<output>
After completion, create `.planning/phases/34-core-merge-subagent-delegation/34-02-SUMMARY.md`
</output>
