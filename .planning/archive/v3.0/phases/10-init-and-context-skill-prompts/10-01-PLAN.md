---
phase: 10-init-and-context-skill-prompts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/init/SKILL.md
autonomous: true
requirements:
  - PROMPT-01
  - PROMPT-02
  - PROMPT-03

must_haves:
  truths:
    - "When init detects existing .planning/, developer sees AskUserQuestion with Reinitialize/Upgrade/Cancel options and consequence descriptions"
    - "When init asks team size, developer sees AskUserQuestion with Solo/Small team/Medium team/Large team preset options"
    - "When init detects source code, developer sees AskUserQuestion for brownfield/greenfield choice before scaffolding"
    - "When init asks project name, developer sees AskUserQuestion with detected directory name as default plus Custom option"
  artifacts:
    - path: "skills/init/SKILL.md"
      provides: "Init skill with structured AskUserQuestion prompts for all decision gates"
      contains: "AskUserQuestion"
  key_links:
    - from: "skills/init/SKILL.md"
      to: "AskUserQuestion tool"
      via: "allowed-tools frontmatter and prompt instructions"
      pattern: "AskUserQuestion"
    - from: "skills/init/SKILL.md Step 3.5"
      to: "context detect CLI"
      via: "node RAPID_TOOLS context detect"
      pattern: "context detect"
---

<objective>
Rewrite init SKILL.md to use structured AskUserQuestion prompts for all decision gates: existing project detection (PROMPT-01), team size (PROMPT-02), brownfield detection (PROMPT-03), and project name.

Purpose: Replace freeform text prompts with structured options so developers pick from clear choices with consequence descriptions instead of typing free text.
Output: Updated skills/init/SKILL.md with AskUserQuestion calls at every decision point.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-init-and-context-skill-prompts/10-CONTEXT.md
@skills/init/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AskUserQuestion to init SKILL.md allowed-tools and rewrite all decision gates</name>
  <files>skills/init/SKILL.md</files>
  <action>
Read the current skills/init/SKILL.md. Make the following changes:

1. **Frontmatter**: Add `AskUserQuestion` to the `allowed-tools` line:
   ```
   allowed-tools: Read, Write, Bash, Glob, AskUserQuestion
   ```

2. **Step 3 (Existing Project Detection) -- PROMPT-01**: Replace the current freeform "present exactly 3 options" text with an explicit AskUserQuestion call. When `exists` is true, after showing existing files, instruct:

   Use AskUserQuestion with:
   - question: "Existing project detected"
   - Options:
     - "Reinitialize" -- "Back up .planning/ to .planning.backup.{timestamp}/ and create fresh. All previous planning data is preserved in the backup."
     - "Upgrade" -- "Add any missing files to existing .planning/ without overwriting existing content. Your current state is preserved."
     - "Cancel" -- "Stop initialization. No changes will be made."
   - Store the user's selection to pass as --mode to scaffold command.
   - Remove the old "Wait for them to select 1, 2, or 3" text.

3. **New Step 3.5 (Brownfield Detection) -- PROMPT-03**: Insert a new step between existing Step 3 and Step 4. This step:

   a. Runs brownfield detection:
   ```bash
   node "${RAPID_TOOLS}" context detect
   ```
   b. Parses JSON output for `hasSourceCode`.
   c. If `hasSourceCode` is true: Use AskUserQuestion with:
      - question: "Codebase detected"
      - Options:
        - "Brownfield (analyze existing code)" -- "After init completes, RAPID will automatically analyze your codebase and generate context files (CLAUDE.md, style guide, conventions). No separate command needed."
        - "Greenfield (start fresh)" -- "Skip codebase analysis. You can run /rapid:context later after adding code."
      - Store the user's selection. If brownfield: after Step 5 scaffold completes, auto-trigger context generation by instructing the model to invoke the /rapid:context skill (mention this should feel seamless -- init completes then context generation starts).
   d. If `hasSourceCode` is false: Display brief text note: "No source code detected. Run /rapid:context after adding code to generate project context." Do NOT show a prompt.

4. **Step 4 Question A (Project Name)**: Replace the current freeform "Ask: What is the project name?" with AskUserQuestion:
   - question: "Project name"
   - Options:
     - "{detected directory name}" -- "Use the current directory name"
     - "Other" -- "Enter a custom project name"
   - If user picks "Other", then ask freeform for the custom name.
   - To detect directory name, add a bash command before the question: `basename "$(pwd)"` and use its output.

5. **Step 4 Question C (Team Size) -- PROMPT-02**: Replace the current freeform "How many developers" with AskUserQuestion:
   - question: "Team size"
   - Options:
     - "Solo (1 developer)" -- "Single developer workflow. Simpler worktree management."
     - "Small team (2-3 developers)" -- "Recommended for most projects. Balanced parallelism."
     - "Medium team (4-5 developers)" -- "Higher parallelism. More worktrees and sets."
     - "Large team (6+ developers)" -- "Maximum parallelism. Complex merge coordination."
   - Map selection to integer: Solo=1, Small=3, Medium=5, Large=6 for the --team-size argument.

6. **Step 5 (Scaffold)**: After scaffold completes, add a conditional section: "If the user chose Brownfield in Step 3.5, proceed to run `/rapid:context` automatically. The user already consented to codebase analysis -- do not ask for confirmation again. This means the context skill should skip its Step 4 confirmation when auto-triggered from init."

7. **Step 6 (Confirmation)**: Add a note that if brownfield auto-trigger happened, the context generation summary is shown as part of init completion.

Keep all existing bash commands, env loading, and scaffold CLI calls unchanged. Only modify the decision/interaction patterns. Use consequence-focused descriptions per the CONTEXT.md decisions. Keep the IMPORTANT note about not creating .planning/phases/.
  </action>
  <verify>
    <automated>grep -c "AskUserQuestion" /home/kek/Projects/RAPID/skills/init/SKILL.md | xargs test 5 -le</automated>
  </verify>
  <done>
  - Init SKILL.md frontmatter includes AskUserQuestion in allowed-tools
  - Step 3 uses AskUserQuestion for Reinitialize/Upgrade/Cancel with consequence descriptions
  - New Step 3.5 runs context detect and uses AskUserQuestion for Brownfield/Greenfield choice
  - Step 4 Question A uses AskUserQuestion with directory name default + Other option
  - Step 4 Question C uses AskUserQuestion with Solo/Small/Medium/Large team options
  - Brownfield selection auto-triggers /rapid:context after scaffold
  - All descriptions are consequence-focused (explain what happens, not just labels)
  </done>
</task>

</tasks>

<verification>
1. `grep "AskUserQuestion" skills/init/SKILL.md` returns matches in frontmatter and at least 4 decision points
2. `grep -c "AskUserQuestion" skills/init/SKILL.md` returns >= 5
3. `grep "allowed-tools" skills/init/SKILL.md` includes AskUserQuestion
4. `grep "context detect" skills/init/SKILL.md` confirms brownfield detection step exists
5. Manual read confirms consequence-focused option descriptions at every decision gate
</verification>

<success_criteria>
- Init SKILL.md has AskUserQuestion in allowed-tools
- All 4 decision gates (existing project, brownfield, project name, team size) use AskUserQuestion
- Option descriptions explain consequences, not just labels
- Brownfield selection auto-triggers context generation
- No freeform text prompts remain for decision gates (description stays freeform as decided)
</success_criteria>

<output>
After completion, create `.planning/phases/10-init-and-context-skill-prompts/10-01-SUMMARY.md`
</output>
