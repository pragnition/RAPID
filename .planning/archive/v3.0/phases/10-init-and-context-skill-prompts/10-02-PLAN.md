---
phase: 10-init-and-context-skill-prompts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/context/SKILL.md
autonomous: true
requirements:
  - PROMPT-13

must_haves:
  truths:
    - "When context skill detects no source code (greenfield), developer sees AskUserQuestion with Continue anyway/Cancel options instead of a text STOP"
    - "When context is auto-triggered from init brownfield flow, generation confirmation is skipped"
  artifacts:
    - path: "skills/context/SKILL.md"
      provides: "Context skill with structured AskUserQuestion for greenfield detection"
      contains: "AskUserQuestion"
  key_links:
    - from: "skills/context/SKILL.md"
      to: "AskUserQuestion tool"
      via: "allowed-tools frontmatter and Step 1 prompt"
      pattern: "AskUserQuestion"
---

<objective>
Rewrite context SKILL.md to use structured AskUserQuestion for greenfield detection (PROMPT-13) and support skip-confirmation when auto-triggered from init brownfield flow.

Purpose: Replace the text STOP on greenfield detection with a structured prompt giving the developer a real choice, and make the init->context auto-trigger seamless by skipping redundant confirmation.
Output: Updated skills/context/SKILL.md with AskUserQuestion at greenfield detection.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-init-and-context-skill-prompts/10-CONTEXT.md
@skills/context/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AskUserQuestion to context SKILL.md and rewrite greenfield detection and confirmation gates</name>
  <files>skills/context/SKILL.md</files>
  <action>
Read the current skills/context/SKILL.md. Make the following changes:

1. **Frontmatter**: Add `AskUserQuestion` to the `allowed-tools` line:
   ```
   allowed-tools: Read, Write, Bash, Glob, Grep, Agent, AskUserQuestion
   ```

2. **Step 1 (Brownfield Detection) -- PROMPT-13**: Replace the current text STOP when `hasSourceCode` is false. Instead of displaying a message and stopping:

   Use AskUserQuestion with:
   - question: "No source code detected"
   - Options:
     - "Continue anyway" -- "Proceed with context generation using whatever files exist. Results may be minimal."
     - "Cancel" -- "Stop context generation. Run /rapid:context again after adding source code."
   - If user picks "Continue anyway": proceed to Step 2 as normal.
   - If user picks "Cancel": STOP -- do not continue to further steps.
   - Keep the existing logic for `hasSourceCode` is true unchanged (store manifest, continue to Step 2).

3. **Step 4 (User Review and Confirmation)**: Update the confirmation gate to handle auto-trigger mode:

   Add a note at the top of Step 4: "If this context generation was auto-triggered from `/rapid:init` brownfield flow, skip the confirmation prompt below and proceed directly to Step 5. The user already consented to codebase analysis when they chose Brownfield during init."

   Replace the current freeform "Ready to generate these context files? (yes/no)" with AskUserQuestion:
   - question: "Context files ready to generate"
   - Options:
     - "Generate" -- "Write all context files listed above to .planning/context/ and CLAUDE.md to project root."
     - "Cancel" -- "Stop without generating files. No changes will be made."
   - If user picks "Generate": proceed to Step 5.
   - If user picks "Cancel": STOP.
   - Remove the old "If the user declines or requests changes" paragraph about discussing adjustments -- keep it simple with Generate/Cancel per CONTEXT.md decision.

Keep all existing bash commands, env loading, subagent instructions, and file verification unchanged. Only modify the decision/interaction patterns. Keep the Important Notes section at the bottom.
  </action>
  <verify>
    <automated>grep -c "AskUserQuestion" /home/kek/Projects/RAPID/skills/context/SKILL.md | xargs test 2 -le</automated>
  </verify>
  <done>
  - Context SKILL.md frontmatter includes AskUserQuestion in allowed-tools
  - Step 1 greenfield detection uses AskUserQuestion with Continue anyway/Cancel instead of text STOP
  - Step 4 has auto-trigger skip note for init brownfield flow
  - Step 4 uses AskUserQuestion with Generate/Cancel instead of freeform yes/no
  - All descriptions are consequence-focused
  </done>
</task>

</tasks>

<verification>
1. `grep "AskUserQuestion" skills/context/SKILL.md` returns matches in frontmatter and at least 2 decision points
2. `grep -c "AskUserQuestion" skills/context/SKILL.md` returns >= 3
3. `grep "allowed-tools" skills/context/SKILL.md` includes AskUserQuestion
4. `grep -i "stop" skills/context/SKILL.md` no longer has bare STOP at greenfield detection (Step 1)
5. Manual read confirms auto-trigger skip note in Step 4
</verification>

<success_criteria>
- Context SKILL.md has AskUserQuestion in allowed-tools
- Greenfield detection (Step 1) uses AskUserQuestion instead of text STOP
- Step 4 confirmation uses AskUserQuestion with Generate/Cancel
- Auto-trigger from init brownfield flow skips Step 4 confirmation
- No freeform yes/no prompts remain for decision gates
</success_criteria>

<output>
After completion, create `.planning/phases/10-init-and-context-skill-prompts/10-02-SUMMARY.md`
</output>
