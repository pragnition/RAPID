---
phase: 43-planning-discussion-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/init/SKILL.md
autonomous: true
requirements:
  - CMD-01
  - PLAN-04
  - UX-01
  - UX-02

must_haves:
  truths:
    - "/init handles both greenfield (no source code) and brownfield (existing codebase) projects"
    - "/init runs a 6-researcher pipeline (5 domain + UX) in parallel"
    - "/init batches discovery questions by topic area instead of one at a time"
    - "/init roadmapper outputs sets only -- no wave or job structure"
    - "/init generates CONTRACT.json files at init time for each set"
    - "/init STATE.json contains project > milestone > sets hierarchy only (no waves/jobs)"
    - "/init renders a progress breadcrumb at completion"
    - "/init suggests exactly one next action at completion"
  artifacts:
    - path: "skills/init/SKILL.md"
      provides: "v3 init skill with batched discovery, sets-only roadmap, CONTRACT.json at init"
      min_lines: 500
  key_links:
    - from: "skills/init/SKILL.md"
      to: "agents/rapid-roadmapper.md"
      via: "Agent tool spawn with sets-only instruction"
      pattern: "Spawn.*rapid-roadmapper"
    - from: "skills/init/SKILL.md"
      to: "rapid-tools.cjs display banner"
      via: "Bash CLI call"
      pattern: "display banner init"
---

<objective>
Rewrite the /init SKILL.md for v3.0 with batched discovery questions, sets-only roadmap output, CONTRACT.json generation at init time, and simplified STATE.json.

Purpose: The init skill is the entry point for all RAPID projects. v3 simplifies the state model (no waves/jobs in STATE.json) and changes the roadmapper to output sets only. Discovery questions are batched by topic area for a better user experience.

Output: Rewritten skills/init/SKILL.md
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/init/SKILL.md
@agents/rapid-roadmapper.md
@src/lib/display.cjs
@src/lib/state-transitions.cjs

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/state-transitions.cjs:
```javascript
const SET_TRANSITIONS = {
  pending:     ['discussing', 'planning'],
  discussing:  ['planning'],
  planning:    ['executing'],
  executing:   ['complete'],
  complete:    ['merged'],
  merged:      []
};
// No wave or job transitions exist in v3
```

From src/lib/display.cjs:
```javascript
const STAGE_VERBS = {
  'init': 'INITIALIZING',
  'set-init': 'PREPARING',
  // ... other stages
};
function renderBanner(stage, target) { ... }
module.exports = { renderBanner, STAGE_VERBS, STAGE_BG };
```

From rapid-tools.cjs CLI commands used by init:
```
prereqs                        -- Check development environment
prereqs --git-check            -- Verify git repository
init detect                    -- Detect existing project
init scaffold --name --description --team-size --mode  -- Create scaffold
init write-config --model --team-size --name           -- Write config
init research-dir              -- Create research directory
context detect                 -- Brownfield detection
display banner init            -- Show stage banner
plan list-sets                 -- List defined sets
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite init SKILL.md for v3 (batched discovery, sets-only roadmap, CONTRACT.json at init)</name>
  <files>skills/init/SKILL.md</files>
  <action>
Rewrite skills/init/SKILL.md preserving the overall step structure but with these v3 changes:

**Frontmatter:** Keep existing description and allowed-tools.

**Steps 1-3 (Prerequisites, Git Check, Existing Detection):** Keep largely unchanged. These steps are well-structured and work correctly.

**Step 4 (Project Discovery):**
- **4A Logistics:** Keep as-is (project name, team size, model selection).
- **4B Deep Discovery:** CHANGE from "ask ONE question at a time" to batched topic-based questions. Group the 10 discovery areas into 3-4 topic batches:
  - Batch 1: Vision and Users (areas 1-2): Ask all questions about core vision, target users, and audience in a SINGLE AskUserQuestion freeform call. Include 3-5 questions in one prompt.
  - Batch 2: Features and Technical (areas 3-4): Ask about must-have features, scope, tech constraints, deployment target in one AskUserQuestion.
  - Batch 3: Scale and Integrations (areas 5-6): Ask about data volume, traffic, third-party services, auth approach in one AskUserQuestion.
  - Batch 4: Context and Success (areas 7-10): Ask about team experience, inspiration, non-functional requirements, success criteria in one AskUserQuestion.
  - CRITICAL: After each batch, analyze the response for follow-up needs. Only ask follow-up questions for genuinely ambiguous or vague responses. Do NOT re-ask areas already covered.
  - Keep the existing "Completion check" paragraph about briefing 5 independent research agents.
  - Keep the project brief compilation format unchanged.

**Steps 5-6 (Scaffold, Brownfield):** Keep unchanged.

**Step 7 (Parallel Research):** Update to say "6 research agents" (already correct -- 5 domain + UX from Phase 41). No changes needed.

**Step 8 (Research Synthesis):** Keep unchanged.

**Step 9 (Roadmap Generation):** CHANGE the roadmapper spawn instructions:
- In the agent task prompt, ADD explicit instruction: "Output sets ONLY -- do NOT include wave or job structure. Waves are determined later during /plan-set. The return JSON structure should be: { roadmap, state, contracts } where state contains project > milestone > sets (no waves key, no jobs key)."
- Remove any reference to waves or jobs in the return format description.
- Update the "Present the roadmap" summary to show "Number of sets" only (remove "waves" and "total jobs" counts).
- After the user accepts the roadmap, update the state writing section:
  - Write STATE.json with project > milestone > sets structure only
  - Each set in the state has: { id, status: "pending" } -- no waves or jobs arrays
  - CONTRACT.json files are written per set (already in the skill, keep this)

**Step 10 (Completion):** Update the summary display:
- Remove "waves" and "total jobs" from the summary
- Show only "N sets planned"
- Keep the files list but update: remove references to wave-level files

**Step 11 (Next Step):** CHANGE from `/rapid:set-init 1` to `/rapid:start-set 1`. The v3 command name is start-set, not set-init.

**NEW Step (add after Step 11): Progress Breadcrumb:**
- Add a progress breadcrumb at the very end of the skill:
```
init [done] > start-set > discuss-set > plan-set > execute-set > review > merge
```
- Render using a simple markdown format. The breadcrumb shows "init" as done, all other steps as pending.

**UX Pattern (UX-01, UX-02):**
- On ANY error in Steps 5-9, show a breadcrumb with what has been completed and what failed:
  ```
  init [scaffold done, research failed] > start-set > ...
  ```
- The existing AskUserQuestion error recovery pattern (Retry/Skip/Cancel) is correct -- keep it.
- Each command completion suggests exactly one next action (already done in Step 11).

**Anti-patterns to avoid (and WHY):**
- Do NOT reference `state transition wave` or `state transition job` -- these commands do not exist in v3. Only `state transition set` exists.
- Do NOT ask the roadmapper to produce waves or jobs -- v3 defers wave decomposition to /plan-set.
- Do NOT reference WAVE-CONTEXT.md or wave directories -- v3 uses set-level CONTEXT.md only.
- Do NOT reference `/rapid:set-init` -- the v3 command is `/rapid:start-set`.
  </action>
  <verify>
    <automated>grep -c "wave\|WAVE\|job\|JOB" skills/init/SKILL.md | head -1</automated>
    Verify the count is very low (only in "do not" instructions or anti-pattern warnings). The word "wave" should appear only in negative instructions ("do NOT include wave structure") and in the canonical workflow list ("plan-set -- Research and produce PLAN.md per wave"). It should NOT appear as a state entity being created or transitioned.

    Additional checks:
    - grep "start-set" skills/init/SKILL.md should find the next-step suggestion
    - grep "batch\|Batch\|topic" skills/init/SKILL.md should find batched discovery instructions
    - grep "CONTRACT.json" skills/init/SKILL.md should find contract generation at init time
    - grep "breadcrumb\|Breadcrumb\|init.*done" skills/init/SKILL.md should find the progress breadcrumb
  </verify>
  <done>
    - init SKILL.md rewritten with batched discovery (3-4 topic batches instead of one-at-a-time)
    - Roadmapper instructed to output sets only (no waves/jobs in state or roadmap)
    - CONTRACT.json generated at init time per set
    - STATE.json written with project > milestone > sets hierarchy only
    - Progress breadcrumb rendered at completion
    - Next step suggests /rapid:start-set (not /rapid:set-init)
    - No wave/job state references in the skill (only in anti-pattern warnings)
  </done>
</task>

</tasks>

<verification>
- Read skills/init/SKILL.md and verify it compiles as valid Markdown
- Verify no v2 wave/job state references (grep for "state transition wave" and "state transition job" -- should be zero)
- Verify batched discovery questions (grep for "Batch 1" or similar batching pattern)
- Verify CONTRACT.json generation at init time
- Verify sets-only roadmap instruction to roadmapper
- Verify progress breadcrumb at end of skill
- Verify next step is /rapid:start-set
</verification>

<success_criteria>
- /init SKILL.md is a complete, self-contained skill document that Claude can follow without interpretation
- Discovery conversation uses topic-batched questions (3-4 AskUserQuestion calls for discovery, not 8-15)
- Roadmapper produces sets-only output (no waves, no jobs)
- CONTRACT.json files generated at init time for each set
- STATE.json structure is project > milestone > sets (no waves/jobs)
- Progress breadcrumb and next-step suggestion present
</success_criteria>

<output>
After completion, create `.planning/phases/43-planning-discussion-skills/43-01-SUMMARY.md`
</output>
