---
phase: 25-gsd-decontamination
plan: 01
subsystem: infra
tags: [migration, branding, naming, state-management]

# Dependency graph
requires: []
provides:
  - "rapid_state_version in all new STATE.md files generated by init"
  - "migrateStateVersion() silently upgrades gsd_state_version in existing projects"
  - "Legacy mark2-plans/ and .review/ archived under .archive/"
affects: [27-branding, init, state-management]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "require.main guard for exporting functions from CLI entry points"
    - "Migration-on-boot pattern: migrateStateVersion called after findProjectRoot()"

key-files:
  created:
    - ".archive/mark2-plans/"
    - ".archive/review/"
  modified:
    - "src/lib/init.cjs"
    - "src/lib/init.test.cjs"
    - "test/.planning/STATE.md"
    - "src/bin/rapid-tools.cjs"
    - "src/bin/rapid-tools.test.cjs"

key-decisions:
  - "Migration function references gsd_state_version by necessity -- it must detect the old key to migrate it"
  - "Used require.main guard pattern to export migrateStateVersion for testing without affecting CLI behavior"
  - "Legacy directories moved (not git mv) since they were untracked"

patterns-established:
  - "Migration-on-boot: state migration runs silently before any command that needs project root"
  - "Guard-export pattern: module.exports behind require.main check for CLI entry point testability"

requirements-completed: [CLEAN-01, CLEAN-02]

# Metrics
duration: 3min
completed: 2026-03-09
---

# Phase 25 Plan 01: GSD Decontamination Summary

**Renamed gsd_state_version to rapid_state_version with silent migration for existing projects, archived legacy mark2 and review directories**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-09T02:18:08Z
- **Completed:** 2026-03-09T02:22:08Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- generateStateMd() now emits rapid_state_version: 1.0 in YAML frontmatter
- migrateStateVersion() silently rewrites gsd_state_version to rapid_state_version in any project's STATE.md on every CLI invocation
- Legacy mark2-plans/ and .review/ directories archived to .archive/ preserving history
- All 54 init tests and 6 new migration tests pass

## Task Commits

Each task was committed atomically:

1. **Task 1: Rename gsd_state_version to rapid_state_version** - `1e0147e` (feat)
2. **Task 2: Add migration function and archive legacy directories** - `b3e03d6` (feat)

_Note: TDD approach used for both tasks -- tests written/updated before implementation_

## Files Created/Modified
- `src/lib/init.cjs` - Changed template string from gsd_state_version to rapid_state_version
- `src/lib/init.test.cjs` - Updated test assertion + added negative guard test for gsd absence
- `test/.planning/STATE.md` - Updated fixture to use rapid_state_version
- `src/bin/rapid-tools.cjs` - Added migrateStateVersion() function, call site, and module.exports guard
- `src/bin/rapid-tools.test.cjs` - Added 6 migration tests in new describe block + import
- `.archive/mark2-plans/` - Archived legacy v2.0 planning artifacts
- `.archive/review/` - Archived legacy review scope artifacts

## Decisions Made
- Migration function must reference gsd_state_version string literals (to detect and replace), which is acceptable in grep audits
- Used `require.main === module` guard to export migrateStateVersion without breaking CLI execution
- Legacy dirs were untracked so used regular mv instead of git mv

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Legacy directories (mark2-plans/, .review/) were untracked in git, so `git mv` would have failed. Used regular `mv` followed by `git add` instead.

## Pre-existing Test Failures (Out of Scope)

The following failures exist in the test suite but are unrelated to this plan's changes:
- `assembler.test.cjs`: Module count drift (26 vs expected 25) and size threshold exceeded (20.6KB vs 15KB)
- `rapid-tools.test.cjs`: "worktree status outputs human-readable table" BRANCH header assertion

Logged to `deferred-items.md` in this phase directory.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Source code is clean of GSD variable identifiers and key names
- Migration function ensures existing RAPID users' STATE.md files auto-upgrade on next CLI invocation
- Ready for Phase 27 (branding) or any subsequent phases

## Self-Check: PASSED

All files verified present, all commits verified in git log.

---
*Phase: 25-gsd-decontamination*
*Completed: 2026-03-09*
