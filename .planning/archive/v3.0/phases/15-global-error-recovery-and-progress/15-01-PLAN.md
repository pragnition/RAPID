---
phase: 15-global-error-recovery-and-progress
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/init/SKILL.md
  - skills/context/SKILL.md
autonomous: true
requirements:
  - ERRR-03
  - PROG-02

must_haves:
  truths:
    - "Init skill prereq failure offers AskUserQuestion with Retry/Install guide/Cancel instead of bare STOP"
    - "Init skill git decline shows graceful exit with hint instead of bare STOP"
    - "Context skill cancel paths show clean confirmation instead of bare STOP"
    - "Context skill missing .planning/ shows graceful exit with /rapid:init hint instead of bare STOP"
    - "Context skill shows stage-based progress banners during codebase analysis subagent"
    - "No SKILL.md file in the entire skills/ directory contains bare STOP or halt as error handling"
  artifacts:
    - path: "skills/init/SKILL.md"
      provides: "STOP-free init skill with structured error recovery"
      contains: "AskUserQuestion"
    - path: "skills/context/SKILL.md"
      provides: "STOP-free context skill with progress banners"
      contains: "Scanning project"
  key_links:
    - from: "skills/init/SKILL.md"
      to: "AskUserQuestion"
      via: "Tier 1 recovery prompt for prereq blockers"
      pattern: "Retry.*Install guide.*Cancel"
    - from: "skills/context/SKILL.md"
      to: "Agent tool"
      via: "Progress banners before/after subagent spawn"
      pattern: "Scanning project|Analyzing patterns|Generating files"
---

<objective>
Replace all bare STOP/halt error handling in init and context SKILL.md files with structured recovery using the 3-tier system, and add progress banners to context skill during codebase analysis.

Purpose: Final v1.1 cleanup -- no skill should halt without offering the developer a way forward. Context skill gets progress visibility during long analysis operations.
Output: Updated skills/init/SKILL.md and skills/context/SKILL.md
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-global-error-recovery-and-progress/15-CONTEXT.md

@skills/init/SKILL.md
@skills/context/SKILL.md
@skills/execute/SKILL.md (reference for progress banner format)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace STOP handling in init and context SKILL.md with 3-tier recovery</name>
  <files>skills/init/SKILL.md, skills/context/SKILL.md</files>
  <action>
Edit skills/init/SKILL.md -- replace 2 bare STOP points:

1. **Line 31 (prereq blockers) -- Tier 1 AskUserQuestion:** Replace the `**STOP.**` block with:
   - Display the blocker table as before (keep existing table rendering)
   - Then use AskUserQuestion with:
     - question: "Prerequisites missing"
     - Options:
       - "Retry check" -- description: "Re-run prerequisite validation after installing missing tools"
       - "View install guide" -- description: "Show install commands for each missing tool: {list each blocker with its install command inline, e.g., 'git: brew install git / apt install git / winget install Git.Git'}"
       - "Cancel init" -- description: "Exit initialization. No changes made."
   - If "Retry check": loop back to the prereqs command and re-check
   - If "View install guide": display install commands for each blocker, then re-prompt with the same AskUserQuestion (they may retry after installing)
   - If "Cancel init": print "Cancelled. No changes made." and end

2. **Line 48 (git decline) -- Tier 2 graceful exit:** Replace `**STOP.** RAPID requires a git repository for worktree-based parallel development.` with:
   - Print: "RAPID requires a git repository for worktree-based parallel development. Run `git init` when ready, then `/rapid:init` again."
   - End the skill (no AskUserQuestion needed per user decision)

3. **Cleanup lowercase "stop" in descriptions** (lines 69, 190): These are in AskUserQuestion option descriptions and cancel mode text. Replace:
   - Line 69: Change "Stop initialization" to "Exit initialization" in the Cancel option description
   - Line 190: Change "stop here" to "end here" in the cancel mode instruction

Edit skills/context/SKILL.md -- replace 3 bare STOP points:

1. **Line 31 (cancel selection in Step 1) -- Tier 3 clean confirmation:** Replace `STOP -- do not continue to further steps.` with:
   - Print: "Cancelled. No changes made."
   - End the skill

2. **Line 50 (no .planning/ in Step 2) -- Tier 2 graceful exit:** Replace `Then **STOP**.` with:
   - The existing message is already good ("No RAPID project found. Run `/rapid:init` first...")
   - Just remove the word "**STOP**" and replace with: "End the skill."

3. **Line 111 (cancel selection in Step 4) -- Tier 3 clean confirmation:** Replace `STOP.` with:
   - Print: "Cancelled. No changes made."
   - End the skill

4. **Cleanup lowercase "stop" in descriptions** (lines 29, 109): Replace:
   - Line 29: Change "Stop context generation" to "Exit context generation" in the Cancel option description
   - Line 109: Change "Stop without generating files" to "Exit without generating files" in the Cancel option description

Do NOT change any other logic, prompts, or structure. Only replace the STOP/halt patterns.
  </action>
  <verify>
Run: grep -i '\bSTOP\b\|\bhalt\b' skills/init/SKILL.md skills/context/SKILL.md | grep -v "Do NOT" | grep -v "Do not"
Expected: no output (zero matches). The only acceptable "stop" occurrences would be in negation contexts like "Do not stop" which do not exist in these files.

Full audit: grep -ri '\bstop\b\|\bhalt\b' skills/*/SKILL.md
Expected: zero matches across ALL skill files.
  </verify>
  <done>
Init SKILL.md has zero bare STOP/halt -- prereq failure uses Tier 1 AskUserQuestion, git decline uses Tier 2 graceful exit. Context SKILL.md has zero bare STOP/halt -- cancel paths use Tier 3 confirmation, missing state uses Tier 2 graceful exit. All lowercase "stop" in option descriptions replaced with "Exit". Full audit across all 11 SKILL.md files shows zero STOP/halt keywords.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add progress banners to context SKILL.md during codebase analysis</name>
  <files>skills/context/SKILL.md</files>
  <action>
Edit skills/context/SKILL.md to add stage-based progress banners around the subagent operations in Steps 3 and 5. Use a unified banner format matching Phase 12's execute progress pattern (short text banners, no timestamps, no file counts).

Banner format (use right-arrow for consistency):
```
> Scanning project...
```

1. **Step 3 (analysis subagent):** Before spawning the analysis subagent (before the "Use the Agent tool" instruction), add:
   ```
   Print progress banner before spawning the analysis subagent:
   > Scanning project...
   ```

   After the subagent returns and before presenting results, add:
   ```
   Print progress banner:
   > Analyzing patterns...
   ```

2. **Step 5 (write subagent):** Before spawning the write-mode subagent, add:
   ```
   Print progress banner before spawning the write subagent:
   > Generating files...
   ```

The banners are simple text output -- not AskUserQuestion prompts. They indicate stage transitions during long operations. 3 stages total: "Scanning project..." -> "Analyzing patterns..." -> "Generating files..."

Do NOT modify any other logic. Keep existing subagent prompts, user interaction, and file structure intact.
  </action>
  <verify>
Run: grep -n "Scanning project\|Analyzing patterns\|Generating files" skills/context/SKILL.md
Expected: 3 matches -- one for each stage banner.
  </verify>
  <done>
Context SKILL.md shows 3 progress banners during codebase analysis: "Scanning project..." before analysis subagent, "Analyzing patterns..." after analysis returns, "Generating files..." before write subagent. Banner format matches Phase 12's execute progress style.
  </done>
</task>

</tasks>

<verification>
1. `grep -ri '\bstop\b\|\bhalt\b' skills/*/SKILL.md` returns zero matches
2. `grep -c 'AskUserQuestion' skills/init/SKILL.md` returns at least 5 (existing 3 + new prereq recovery prompt)
3. `grep -c 'Scanning project\|Analyzing patterns\|Generating files' skills/context/SKILL.md` returns 3
4. All 11 SKILL.md files are syntactically valid (no broken markdown)
</verification>

<success_criteria>
- Zero STOP/halt keywords across all 11 SKILL.md files
- Init prereq failure presents AskUserQuestion with Retry/Install guide/Cancel options
- Init git decline shows graceful exit message with hint
- Context cancel paths show "Cancelled. No changes made."
- Context missing .planning/ shows graceful exit with /rapid:init hint
- Context skill shows 3 stage-based progress banners during analysis
</success_criteria>

<output>
After completion, create `.planning/phases/15-global-error-recovery-and-progress/15-01-SUMMARY.md`
</output>
