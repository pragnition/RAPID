---
phase: 28-workflow-clarity
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - skills/init/SKILL.md
  - skills/set-init/SKILL.md
  - skills/discuss/SKILL.md
  - skills/wave-plan/SKILL.md
  - skills/execute/SKILL.md
  - skills/review/SKILL.md
  - skills/merge/SKILL.md
autonomous: true
requirements: [UX-04, FLOW-01]

must_haves:
  truths:
    - "Every stage skill prints a next-step command with pre-filled numeric args on completion"
    - "No stage skill ends with an AskUserQuestion routing block"
    - "Linear steps show exactly one next step"
    - "Branching steps (review, merge) show multiple options as print-only text"
    - "Wave-aware skills (discuss, wave-plan, review) use resolve wave --set for two-arg invocation"
  artifacts:
    - path: "skills/init/SKILL.md"
      provides: "Next step block pointing to /rapid:set-init"
      contains: "Next step"
    - path: "skills/set-init/SKILL.md"
      provides: "Next step block pointing to /rapid:discuss"
      contains: "Next step"
    - path: "skills/discuss/SKILL.md"
      provides: "Next step block pointing to /rapid:wave-plan"
      contains: "Next step"
    - path: "skills/wave-plan/SKILL.md"
      provides: "Next step block pointing to /rapid:execute"
      contains: "Next step"
    - path: "skills/execute/SKILL.md"
      provides: "Next step block pointing to /rapid:review"
      contains: "Next step"
    - path: "skills/review/SKILL.md"
      provides: "Branching next steps (merge vs fix vs re-run)"
      contains: "Next step"
    - path: "skills/merge/SKILL.md"
      provides: "Next step block (cleanup or status)"
      contains: "Next step"
  key_links:
    - from: "skills/discuss/SKILL.md"
      to: "resolve wave --set"
      via: "CLI call for two-arg resolution"
      pattern: "resolve wave.*--set"
    - from: "skills/wave-plan/SKILL.md"
      to: "resolve wave --set"
      via: "CLI call for two-arg resolution"
      pattern: "resolve wave.*--set"
    - from: "skills/review/SKILL.md"
      to: "resolve wave --set"
      via: "CLI call for two-arg resolution"
      pattern: "resolve wave.*--set"
---

<objective>
Replace all end-of-skill AskUserQuestion routing blocks with print-only next-step suggestions across all 7 stage skills, and integrate the --set flag from Plan 01 into wave-aware skills.

Purpose: Users see a pasteable next command after every stage instead of interactive prompts. Wave-aware skills use single-call set+wave resolution. The workflow feels faster and more deterministic.
Output: 7 updated SKILL.md files with next-step blocks and --set flag integration.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-workflow-clarity/28-CONTEXT.md
@.planning/phases/28-workflow-clarity/28-RESEARCH.md
@.planning/phases/28-workflow-clarity/28-01-SUMMARY.md

<interfaces>
<!-- Key context the executor needs for skill editing -->

Canonical workflow sequence (from Plan 01's core-identity.md update):
  init -> set-init -> discuss -> wave-plan -> execute -> review -> merge

Next-step format (from CONTEXT.md locked decision):
```
> **Next step:** `/rapid:{command} {numericArgs}`
> *({description})*
```

Branching format (for review and merge):
```
> **Next steps:**
> - `/rapid:merge {setIndex}` -- *Set is ready for merge*
> - `/rapid:review {setIndex}` -- *Re-run review cycle*
> - `/rapid:execute {setIndex} --fix-issues` -- *Fix remaining issues first*
```

From resolve.cjs (Plan 01 output -- new --set flag):
```javascript
// Single-call resolution for wave-aware skills:
// CLI: node "${RAPID_TOOLS}" resolve wave "<wave-input>" --set "<set-input>"
// Returns: { setId, waveId, setIndex, waveIndex, wasNumeric }
```

Stage skills with end-of-skill AskUserQuestion to REMOVE:
1. init -- "Next Steps:" section at end (print-only already, but verify no AskUserQuestion)
2. set-init -- Step 5: "Present Next Steps" with AskUserQuestion
3. discuss -- Final section with AskUserQuestion offering wave-plan/discuss/status
4. wave-plan -- Final section with AskUserQuestion offering execute/plan/view/status
5. execute -- Final section with AskUserQuestion offering status/review/done
6. review -- Step 5: "Next Action" with AskUserQuestion offering merge/re-run/fix/done
7. merge -- Final section with AskUserQuestion offering cleanup/status/done

Mid-flow AskUserQuestion to KEEP (these are not end-of-skill routing):
- review Step 0: set selection (functional)
- review Step 1: stage selection (functional)
- review Step 3a.3: test plan approval (functional)
- review Step 3b.4: deferred finding decision (functional)
- wave-plan Step 2: re-plan/view/cancel decision (functional)
- All error recovery AskUserQuestion blocks (functional)
- status skill's AskUserQuestion (core function, not a stage skill)

Wave-aware skills for --set flag integration (FLOW-01):
- discuss -- accepts (set-id, wave-id) two-arg form
- wave-plan -- accepts (set-id, wave-id) two-arg form
- review -- accepts (set-id) with optional wave-id

Skills' existing argument parsing patterns to look for:
- discuss: resolve set, then resolve wave separately
- wave-plan: resolve set, then resolve wave separately
- review: resolve set (wave is optional for per-wave scope)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update linear stage skills -- init, set-init, discuss, wave-plan, execute</name>
  <files>skills/init/SKILL.md, skills/set-init/SKILL.md, skills/discuss/SKILL.md, skills/wave-plan/SKILL.md, skills/execute/SKILL.md</files>
  <action>
    For each of the 5 linear stage skills, make TWO changes: (A) remove end-of-skill AskUserQuestion routing, and (B) add a print-only next-step block.

    **CRITICAL: Classify every AskUserQuestion in each skill BEFORE editing.** Mid-flow decision AskUserQuestion blocks (e.g., error recovery, re-plan decisions) must be KEPT. Only the FINAL routing/next-steps AskUserQuestion block at skill end is removed.

    **init (skills/init/SKILL.md):**
    - The init skill's end already has a print-only "Next Steps:" block (no AskUserQuestion). Verify this is the case.
    - Replace the existing static "Next Steps:" text with a dynamic next-step block:
      ```
      ## Step N: Next Step

      Determine the first pending set by running:
      ```bash
      SETS_JSON=$(node "${RAPID_TOOLS}" plan list-sets 2>&1)
      ```

      Display:
      > **Next step:** `/rapid:set-init 1`
      > *(Initialize set 1 for development)*

      If the project has no sets yet (e.g., roadmap deferred set creation), display:
      > **Next step:** `/rapid:status`
      > *(View project state)*
      ```
    - The init skill does NOT have an end-of-skill AskUserQuestion (it has error recovery ones -- KEEP those).

    **set-init (skills/set-init/SKILL.md):**
    - Remove Step 5 "Present Next Steps" which uses AskUserQuestion with options for discuss/status/init-another.
    - Replace with:
      ```
      ## Step 5: Next Step

      The set's first wave is wave 1. Display:
      > **Next step:** `/rapid:discuss {setIndex}.1`
      > *(Discuss wave 1 of {setId})*

      Where `{setIndex}` is the numeric index of the set just initialized (obtained from the resolve step earlier in this skill).
      ```

    **discuss (skills/discuss/SKILL.md):**
    - Find the final AskUserQuestion block (after the commit step) that offers wave-plan/discuss-another/status.
    - Remove the entire AskUserQuestion block and its "Based on selection:" routing.
    - Replace with:
      ```
      Display the next step using the setIndex and waveIndex already resolved in Step 2:
      > **Next step:** `/rapid:wave-plan {setIndex}.{waveIndex}`
      > *(Plan wave {waveIndex} of {setId})*
      ```
    - ALSO: Update the argument parsing section to use the new --set flag for two-arg invocation. Where the skill currently does separate resolve-set then resolve-wave calls, replace with:
      ```bash
      RESOLVE_RESULT=$(node "${RAPID_TOOLS}" resolve wave "<wave-input>" --set "<set-input>" 2>&1)
      ```
      This produces a single JSON with setId, waveId, setIndex, waveIndex. The skill should parse all four from this one call.

    **wave-plan (skills/wave-plan/SKILL.md):**
    - Find the final AskUserQuestion block that offers execute/plan-another/view/status.
    - Remove the entire AskUserQuestion block and its "Based on selection:" routing.
    - Replace with a context-aware next step:
      ```
      Determine whether this is the last wave in the set. If not:
      > **Next step:** `/rapid:execute {setIndex}`
      > *(Execute wave {waveIndex} of {setId})*

      The setIndex and waveIndex are already available from the resolution step earlier in this skill.
      ```
    - Keep the mid-flow AskUserQuestion at Step 2 (re-plan/view/cancel decision) -- this is functional, not routing.
    - ALSO: Update argument parsing to use --set flag for two-arg invocation, same pattern as discuss.

    **execute (skills/execute/SKILL.md):**
    - Find the final AskUserQuestion block that offers status/review/done after execution completes.
    - Remove the entire AskUserQuestion block.
    - Replace with:
      ```
      Display the next step:
      > **Next step:** `/rapid:review {setIndex}`
      > *(Review completed work for {setId})*

      Where `{setIndex}` is the numeric index of the set resolved at the start of this skill.
      ```

    **For all skills: do NOT touch the `allowed-tools:` frontmatter line.** AskUserQuestion must stay in the allowed-tools list if other mid-flow AskUserQuestion calls remain in the skill. Only remove AskUserQuestion from allowed-tools if the skill has ZERO remaining AskUserQuestion calls after editing.
  </action>
  <verify>
    <automated>for skill in init set-init discuss wave-plan execute; do echo "=== $skill ===" && tail -20 ~/Projects/RAPID/skills/$skill/SKILL.md | grep -c "AskUserQuestion" && tail -20 ~/Projects/RAPID/skills/$skill/SKILL.md | grep -c "Next step"; done</automated>
  </verify>
  <done>All 5 linear stage skills end with a print-only "Next step:" block. No end-of-skill AskUserQuestion routing blocks remain. Mid-flow AskUserQuestion blocks are untouched. discuss and wave-plan use `resolve wave --set` for two-arg resolution.</done>
</task>

<task type="auto">
  <name>Task 2: Update branching stage skills -- review, merge</name>
  <files>skills/review/SKILL.md, skills/merge/SKILL.md</files>
  <action>
    Review and merge are branching points -- they need MULTIPLE next-step options displayed as print-only text (not AskUserQuestion).

    **review (skills/review/SKILL.md):**
    - Find Step 5 "Next Action" which uses AskUserQuestion with options: proceed-to-merge, re-run review, fix issues, done.
    - Remove the entire AskUserQuestion block and the "If [option]:" conditional routing below it.
    - Replace with a print-only branching block:
      ```
      ## Step 5: Next Steps

      Display the available next steps:

      > **Next steps:**
      > - `/rapid:merge {setIndex}` -- *Set is ready, proceed to merge*
      > - `/rapid:review {setIndex}` -- *Re-run review cycle on this set*
      > - `/rapid:execute {setIndex} --fix-issues` -- *Fix remaining issues first*

      Where `{setIndex}` is the numeric index of the set resolved at Step 0.

      Then exit. Do NOT prompt for selection.
      ```
    - KEEP all mid-flow AskUserQuestion blocks: Step 0 set selection, Step 1 stage selection, Step 3a.3 test plan approval, Step 3b.4 deferred finding decision. These are functional decisions, not routing.
    - ALSO: If review accepts a two-arg form (set + wave), update its argument parsing to use `resolve wave --set` flag. Check the skill's Step 0 to see if it resolves set and wave separately -- if so, update to single --set call.

    **merge (skills/merge/SKILL.md):**
    - Find the final AskUserQuestion block that offers cleanup/status/done after merge completes.
    - Remove the entire AskUserQuestion block.
    - Replace with a print-only branching block:
      ```
      Display the available next steps:

      > **Next steps:**
      > - `/rapid:cleanup` -- *Remove completed worktrees*
      > - `/rapid:status` -- *View project state*
      > - `/rapid:new-milestone` -- *Start planning next milestone (if all sets merged)*
      ```
    - Merge does NOT need --set flag changes (it resolves sets, not waves).

    **For both skills: do NOT remove AskUserQuestion from `allowed-tools:` frontmatter** since both retain mid-flow AskUserQuestion calls. Verify that review still has AskUserQuestion in allowed-tools.
  </action>
  <verify>
    <automated>echo "=== review end ===" && tail -30 ~/Projects/RAPID/skills/review/SKILL.md | grep -c "AskUserQuestion" && tail -30 ~/Projects/RAPID/skills/review/SKILL.md | grep -c "Next step" && echo "=== merge end ===" && tail -30 ~/Projects/RAPID/skills/merge/SKILL.md | grep -c "AskUserQuestion" && tail -30 ~/Projects/RAPID/skills/merge/SKILL.md | grep -c "Next step"</automated>
  </verify>
  <done>Review and merge skills end with print-only branching next-step blocks. No end-of-skill AskUserQuestion routing blocks remain. Mid-flow AskUserQuestion blocks in review are untouched. review uses `resolve wave --set` for two-arg invocation where applicable.</done>
</task>

</tasks>

<verification>
1. For each stage skill, verify the last step is "Next Step" or "Next Steps" (not AskUserQuestion):
   `for s in init set-init discuss wave-plan execute review merge; do echo "$s:"; grep "## Step.*Next" ~/Projects/RAPID/skills/$s/SKILL.md; done`
2. Verify NO end-of-skill AskUserQuestion remains (check last 20 lines of each):
   `for s in init set-init discuss wave-plan execute review merge; do echo "$s:"; tail -20 ~/Projects/RAPID/skills/$s/SKILL.md | grep -c "AskUserQuestion"; done`
   All should output 0.
3. Verify mid-flow AskUserQuestion blocks still exist in review:
   `grep -c "AskUserQuestion" ~/Projects/RAPID/skills/review/SKILL.md` -- should be > 0 (mid-flow ones retained)
4. Verify --set flag usage in wave-aware skills:
   `grep -l "resolve wave.*--set" ~/Projects/RAPID/skills/discuss/SKILL.md ~/Projects/RAPID/skills/wave-plan/SKILL.md ~/Projects/RAPID/skills/review/SKILL.md`
   Should list all three files.
5. Verify "Next step" text appears in all 7 skills:
   `grep -l "Next step" ~/Projects/RAPID/skills/init/SKILL.md ~/Projects/RAPID/skills/set-init/SKILL.md ~/Projects/RAPID/skills/discuss/SKILL.md ~/Projects/RAPID/skills/wave-plan/SKILL.md ~/Projects/RAPID/skills/execute/SKILL.md ~/Projects/RAPID/skills/review/SKILL.md ~/Projects/RAPID/skills/merge/SKILL.md`
   Should list all 7 files.
</verification>

<success_criteria>
- All 7 stage skills have print-only next-step blocks at their end
- Zero end-of-skill AskUserQuestion routing blocks remain across all 7 skills
- Mid-flow AskUserQuestion blocks (error recovery, stage selection, test plan approval) are untouched
- Linear skills (init, set-init, discuss, wave-plan, execute) show exactly one next command
- Branching skills (review, merge) show 2-3 alternative next commands
- discuss, wave-plan, and review use `resolve wave --set` for two-arg invocation
- Next-step commands use numeric indices (not string IDs)
</success_criteria>

<output>
After completion, create `.planning/phases/28-workflow-clarity/28-02-SUMMARY.md`
</output>
