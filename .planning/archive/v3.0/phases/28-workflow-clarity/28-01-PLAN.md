---
phase: 28-workflow-clarity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/resolve.cjs
  - src/lib/resolve.test.cjs
  - src/bin/rapid-tools.cjs
  - src/modules/core/core-identity.md
  - src/modules/roles/role-roadmapper.md
  - src/modules/roles/role-wave-planner.md
  - agents/  # all 26 rebuilt
autonomous: true
requirements: [FLOW-01, FLOW-02, FLOW-03]

must_haves:
  truths:
    - "resolveWave with --set flag resolves both set and wave in a single call"
    - "resolveWave without --set flag works identically to before (backward compat)"
    - "All 26 agent files contain the canonical RAPID workflow sequence"
    - "role-roadmapper.md and role-wave-planner.md both contain 2-4 jobs per wave guidance"
  artifacts:
    - path: "src/lib/resolve.cjs"
      provides: "resolveWave with optional 4th setId parameter"
      exports: ["resolveSet", "resolveWave"]
    - path: "src/lib/resolve.test.cjs"
      provides: "Unit tests for --set flag behavior"
      contains: "resolveWave.*setId"
    - path: "src/bin/rapid-tools.cjs"
      provides: "CLI handler parsing --set flag and passing to resolveWave"
      contains: "--set"
    - path: "src/modules/core/core-identity.md"
      provides: "Canonical workflow order section"
      contains: "RAPID Workflow"
    - path: "src/modules/roles/role-roadmapper.md"
      provides: "Updated job granularity guidance"
      contains: "2-4 jobs per wave"
    - path: "src/modules/roles/role-wave-planner.md"
      provides: "New job count guidance section"
      contains: "2-4 jobs per wave"
  key_links:
    - from: "src/bin/rapid-tools.cjs"
      to: "src/lib/resolve.cjs"
      via: "resolveWave(input, state, cwd, setInput)"
      pattern: "resolveLib\\.resolveWave\\(input.*setInput\\)"
    - from: "src/modules/core/core-identity.md"
      to: "agents/rapid-*.md"
      via: "build-agents propagation"
      pattern: "RAPID Workflow"
---

<objective>
Add --set flag to resolveWave for single-call set+wave resolution, inject canonical workflow order into all 26 agents via core-identity.md, and update job granularity guidance in both role modules.

Purpose: Provides the resolver infrastructure that wave-aware skills will use (Plan 02), ensures all agents know the workflow sequence, and sets coarser job sizing defaults.
Output: Updated resolve.cjs with tests, updated core-identity.md, updated role modules, rebuilt agents.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-workflow-clarity/28-CONTEXT.md
@.planning/phases/28-workflow-clarity/28-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/resolve.cjs (current signature to extend):
```javascript
function resolveSet(input, cwd)
// Returns: { resolvedId: string, numericIndex: number, wasNumeric: boolean }

function resolveWave(input, state, cwd)
// Returns: { setId: string, waveId: string, setIndex: number, waveIndex: number, wasNumeric: boolean }
```

From src/bin/rapid-tools.cjs (CLI handler at line 2593):
```javascript
async function handleResolve(cwd, subcommand, args) {
  const resolveLib = require('../lib/resolve.cjs');
  const input = args[0];
  // switch on subcommand: 'set' | 'wave'
  // wave case reads state, then calls resolveLib.resolveWave(input, stateResult.state, cwd)
}
```

From src/lib/resolve.test.cjs (test helpers):
```javascript
function makeTmpDir() // creates temp dir
function createMockSets(cwd, setNames) // creates .planning/sets/ directories
function makeState(sets) // creates valid ProjectState with milestone 'v1.0'
```

From src/modules/core/core-identity.md (24 lines, add section after "Working Directory"):
```markdown
## Working Directory
// ... existing content about worktree paths ...
// ADD NEW SECTION HERE: ## RAPID Workflow
```

From src/modules/roles/role-roadmapper.md (lines 154-158, section to replace):
```markdown
### Job Granularity
- Each job should be completable by a single agent in one session
- A job roughly corresponds to a v1.0 "plan" in scope (1-3 files modified, clear inputs/outputs)
- Detailed job plans are NOT created here -- only titles and complexity estimates
- Detailed planning happens in Phase 20 via /discuss and /plan per wave
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD -- Add --set flag to resolveWave and CLI handler</name>
  <files>src/lib/resolve.cjs, src/lib/resolve.test.cjs, src/bin/rapid-tools.cjs</files>
  <behavior>
    - Test: resolveWave('wave-01', state, cwd, 'set-01-api') returns { setId: 'set-01-api', waveId: 'wave-01', setIndex: 1, waveIndex: 1, wasNumeric: false }
    - Test: resolveWave('wave-01', state, cwd, '1') resolves numeric set input to 'set-01-api' and returns correct result
    - Test: resolveWave('nonexistent-wave', state, cwd, 'set-01-api') throws "Wave 'nonexistent-wave' not found in set 'set-01-api'"
    - Test: resolveWave('wave-01', state, cwd, 'nonexistent-set') throws set-not-found error (delegates to resolveSet)
    - Test: resolveWave('1.1', state, cwd) still works identically (backward compat -- existing tests pass)
    - Test: resolveWave('wave-02', state, cwd) still works identically (string ID without --set)
  </behavior>
  <action>
    RED phase: Add new test cases to src/lib/resolve.test.cjs in a new describe block "resolveWave -- with setId parameter (FLOW-01)". Create tests for: (1) string setId + string waveId resolves correctly, (2) numeric setId + string waveId resolves correctly, (3) nonexistent wave in valid set throws descriptive error listing available waves, (4) nonexistent set delegates to resolveSet error. Run tests -- new tests MUST fail, existing tests MUST pass.

    GREEN phase: Update resolveWave in src/lib/resolve.cjs to accept optional 4th parameter `setId`. When `setId` is provided:
    1. Call resolveSet(setId, cwd) to resolve the set
    2. Find the set in state.milestones[currentMilestone].sets
    3. Find the wave by ID within that set's waves array
    4. Return { setId: resolved set ID, waveId: input, setIndex: from resolveSet, waveIndex: 1-based index in waves array, wasNumeric: false }
    5. If wave not found, throw with message listing available waves in that set

    When setId is NOT provided, existing logic is unchanged (dot notation and string ID paths).

    CLI handler update in src/bin/rapid-tools.cjs handleResolve function, wave case (line 2613):
    1. Update usage string to: 'Usage: rapid-tools resolve wave <input> [--set <setInput>]'
    2. After reading state, parse --set flag: `const setIdx = args.indexOf('--set'); const setInput = (setIdx !== -1 && args[setIdx + 1]) ? args[setIdx + 1] : undefined;`
    3. Pass setInput to resolveWave: `resolveLib.resolveWave(input, stateResult.state, cwd, setInput)`

    Run all tests -- all MUST pass.
  </action>
  <verify>
    <automated>node --test ~/Projects/RAPID/src/lib/resolve.test.cjs</automated>
  </verify>
  <done>resolveWave accepts optional setId parameter. CLI `resolve wave <id> --set <setId>` works. All existing tests pass (backward compat). New --set tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add workflow order to core-identity.md and update job granularity in role modules</name>
  <files>src/modules/core/core-identity.md, src/modules/roles/role-roadmapper.md, src/modules/roles/role-wave-planner.md</files>
  <action>
    1. Edit src/modules/core/core-identity.md -- add a new section AFTER the "Working Directory" section (after the final paragraph about state living in .planning/ directory). Add:

    ```
    ## RAPID Workflow

    The canonical RAPID workflow sequence is:

    1. **init** -- Research and generate project roadmap
    2. **set-init** -- Claim a set, create isolated worktree
    3. **discuss** -- Capture developer implementation vision per wave
    4. **wave-plan** -- Research specifics and plan jobs for a wave
    5. **execute** -- Dispatch parallel agents per job
    6. **review** -- Unit test, adversarial bug hunt, UAT
    7. **merge** -- Merge set branch into main with conflict resolution

    Steps 3-6 repeat for each wave within a set. Steps 2-7 repeat for each set in the milestone.
    ```

    2. Edit src/modules/roles/role-roadmapper.md -- replace lines 154-158 (the "### Job Granularity" section) with:

    ```
    ### Job Granularity
    - Target 2-4 jobs per wave -- fewer, larger jobs reduce context fragmentation
    - Each job should be completable by a single agent in one session
    - A single agent working on a larger chunk makes better decisions than many agents on tiny pieces
    - More than 4 jobs per wave is allowed ONLY if the wave planner explicitly justifies why in the plan
    - Detailed job plans are NOT created here -- only titles and complexity estimates
    - Detailed planning happens later via /rapid:discuss and /rapid:wave-plan per wave
    ```

    3. Edit src/modules/roles/role-wave-planner.md -- add a new section "### Job Count Guidance" inside the "## Scope and Constraints" section, after "### Behavioral Constraints" (before the final "End your response" block at line 113). Add:

    ```
    ### Job Count Guidance
    - Target 2-4 jobs per wave -- fewer, larger jobs reduce context fragmentation
    - A single agent working on a larger chunk makes better decisions than many agents on tiny pieces
    - More than 4 jobs per wave is allowed ONLY if you explicitly justify why in the WAVE-PLAN.md (e.g., genuinely independent subsystems, file ownership isolation requires it)
    - When in doubt, merge two small jobs into one larger job
    ```
  </action>
  <verify>
    <automated>grep -c "RAPID Workflow" ~/Projects/RAPID/src/modules/core/core-identity.md && grep -c "2-4 jobs per wave" ~/Projects/RAPID/src/modules/roles/role-roadmapper.md && grep -c "2-4 jobs per wave" ~/Projects/RAPID/src/modules/roles/role-wave-planner.md</automated>
  </verify>
  <done>core-identity.md contains RAPID Workflow section. role-roadmapper.md has updated job granularity (no more "1-3 files modified"). role-wave-planner.md has matching job count guidance.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild all 26 agents and verify propagation</name>
  <files>agents/</files>
  <action>
    Run `node ~/Projects/RAPID/src/bin/rapid-tools.cjs build-agents` to regenerate all 26 agent .md files from the updated source modules.

    Verify propagation:
    1. Check that all 26 agent files contain "RAPID Workflow" section: `grep -l "RAPID Workflow" agents/rapid-*.md | wc -l` should output 26
    2. Check that role-roadmapper agent has "2-4 jobs per wave": `grep "2-4 jobs" agents/rapid-roadmapper.md`
    3. Check that role-wave-planner agent has "2-4 jobs per wave": `grep "2-4 jobs" agents/rapid-wave-planner.md`
    4. Verify total agent count is still 26: `ls agents/rapid-*.md | wc -l`

    If any check fails, investigate and fix the source module, then re-run build-agents.
  </action>
  <verify>
    <automated>node ~/Projects/RAPID/src/bin/rapid-tools.cjs build-agents && test $(grep -rl "RAPID Workflow" ~/Projects/RAPID/agents/rapid-*.md | wc -l) -eq 26 && echo "PASS: all 26 agents have workflow section"</automated>
  </verify>
  <done>All 26 agent files rebuilt. Every agent contains the RAPID Workflow section. Roadmapper and wave-planner agents contain "2-4 jobs per wave" guidance.</done>
</task>

</tasks>

<verification>
1. `node --test ~/Projects/RAPID/src/lib/resolve.test.cjs` -- all tests pass including new --set flag tests
2. `grep "RAPID Workflow" ~/Projects/RAPID/src/modules/core/core-identity.md` -- workflow section present in source
3. `grep -c "RAPID Workflow" ~/Projects/RAPID/agents/rapid-*.md | grep -v ":0$" | wc -l` -- equals 26
4. `grep "2-4 jobs per wave" ~/Projects/RAPID/src/modules/roles/role-roadmapper.md` -- updated granularity
5. `grep "2-4 jobs per wave" ~/Projects/RAPID/src/modules/roles/role-wave-planner.md` -- matching guidance
6. `grep "1-3 files modified" ~/Projects/RAPID/src/modules/roles/role-roadmapper.md` -- should return empty (old guidance removed)
</verification>

<success_criteria>
- resolveWave('wave-01', state, cwd, 'set-01-api') resolves both set and wave correctly
- resolveWave('1.1', state, cwd) still works (backward compat)
- CLI `rapid-tools resolve wave wave-01 --set 1` returns valid JSON with both setId and waveId
- All 26 agents contain "RAPID Workflow" section with 7-step sequence
- Both role modules contain "2-4 jobs per wave" guidance
- Old "1-3 files modified" guidance is gone from role-roadmapper.md
</success_criteria>

<output>
After completion, create `.planning/phases/28-workflow-clarity/28-01-SUMMARY.md`
</output>
