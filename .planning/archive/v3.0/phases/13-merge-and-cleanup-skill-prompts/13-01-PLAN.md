---
phase: 13-merge-and-cleanup-skill-prompts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skills/merge/SKILL.md]
autonomous: true
requirements: [PROMPT-09, PROMPT-10, ERRR-01, ERRR-04]

must_haves:
  truths:
    - "Before merge pipeline starts, developer sees structured Start merge / Cancel prompt with inline summary"
    - "Between waves, developer sees structured Continue to Wave N+1 / Pause pipeline prompt"
    - "After pipeline completes, developer sees structured Run cleanup / View status / Done next-action prompt"
    - "When merge conflict occurs, developer sees structured Resolve manually / Show diff / Abort pipeline options"
    - "When integration gate fails, developer sees structured Investigate / Revert wave / Abort pipeline options"
    - "Revert wave triggers double confirmation before executing destructive action"
    - "After reviewer verdict, developer sees clear verdict banner with emoji, label, and findings summary"
    - "BLOCK verdict shows structured View full review / Skip set / Abort pipeline options"
    - "Cleanup escalation shows structured Fix manually / Skip set / Abort pipeline options"
    - "No STOP or halt keywords exist anywhere in merge SKILL.md"
  artifacts:
    - path: "skills/merge/SKILL.md"
      provides: "Merge pipeline with AskUserQuestion at all decision gates and verdict explanations"
      contains: "AskUserQuestion"
  key_links:
    - from: "skills/merge/SKILL.md"
      to: "AskUserQuestion tool"
      via: "allowed-tools frontmatter"
      pattern: "allowed-tools:.*AskUserQuestion"
---

<objective>
Rewrite merge SKILL.md with AskUserQuestion structured prompts at all decision gates, structured error recovery for conflicts and gate failures, and clear verdict explanations with cleanup round tracking.

Purpose: Replace freeform yes/no prompts and bare STOP handling with structured prompts matching Phases 10-12 patterns.
Output: Updated skills/merge/SKILL.md with AskUserQuestion at every user-facing decision point.
</objective>

<execution_context>
@/home/kek/.claude/get-shit-done/workflows/execute-plan.md
@/home/kek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-merge-and-cleanup-skill-prompts/13-CONTEXT.md
@skills/merge/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AskUserQuestion structured prompts at all merge decision gates, verdict explanations, and error recovery</name>
  <files>skills/merge/SKILL.md</files>
  <action>
Rewrite skills/merge/SKILL.md to replace all freeform prompts and STOP handling with AskUserQuestion structured prompts. Add AskUserQuestion to the allowed-tools frontmatter line.

Specific changes by step:

**Step 1 (merge confirmation -- PROMPT-09):**
- Replace `Ask: "Ready to begin merge pipeline? (yes/no)"` with AskUserQuestion
- Header: "Merge plan" (context-based, matches Phase 12 pattern)
- Options: "Start merge" / "Cancel"
- "Start merge" description includes inline summary: set count, wave count, first wave's sets
- "Cancel" exits with clear text (no STOP keyword)

**Step 4 (verdict handling -- ERRR-04):**
- After parsing verdict, display a verdict banner:
  - APPROVE: checkmark emoji + "APPROVED" + 1-line summary of review findings
  - CHANGES: wrench emoji + "CHANGES REQUESTED" + fixable issue count + cleanup rounds remaining (e.g., "Round 1/2")
  - BLOCK: stop-sign emoji + "BLOCKED" + blocking findings summary
- For BLOCK verdict: replace bare report with AskUserQuestion:
  - Header: "Set blocked by reviewer"
  - Options: "View full review" / "Skip set, continue pipeline" / "Abort pipeline"
  - "View full review" shows REVIEW.md contents, then second AskUserQuestion: "Skip set" / "Abort pipeline"

**Step 5 (cleanup escalation -- ERRR-04):**
- After round 2 still CHANGES: replace bare escalation text with AskUserQuestion
- Header: "Cleanup escalation"
- Options: "Fix manually" / "Skip set" / "Abort pipeline"
- "Fix manually" description: "Resolve remaining issues in your terminal, then re-run /rapid:merge {setName}"
- "Skip set" description: "Continue pipeline without this set"
- "Abort pipeline" description: "Halt merge pipeline entirely"
- Add progress banners between cleanup rounds: "Cleanup round {N}/2 -- re-reviewing {setName}..."

**Step 6 (merge conflict -- PROMPT-10, ERRR-01):**
- Replace STOP on conflict with AskUserQuestion:
  - Header: "Merge conflict"
  - Options: "Resolve manually" / "Show diff" / "Abort pipeline"
  - "Resolve manually" description: lists conflicting files + resolve commands (git add, git merge --continue)
  - After "Resolve manually": display file list and commands, then second AskUserQuestion: "Resolved -- continue pipeline" / "Still stuck -- abort"
  - "Show diff" shows the diff output, then returns to same prompt with "Resolve manually" / "Abort pipeline"
  - "Abort pipeline" exits with clear text
- Replace STOP on merge error with AskUserQuestion:
  - Header: "Merge error"
  - Options: "Retry merge" / "Skip set" / "Abort pipeline"

**Step 7 (integration gate -- ERRR-01):**
- Replace STOP on gate failure with AskUserQuestion:
  - Header: "Integration gate failed"
  - Options: "Investigate" / "Revert wave" / "Abort pipeline"
  - "Investigate" description: "View test output and pause for manual fix"
  - "Revert wave" triggers double confirmation: second AskUserQuestion listing which sets will be undone: "This will revert merges for {set list} from this wave. Confirm revert / Cancel"
  - "Abort pipeline" description: "Halt without reverting"
- Replace `Wave {waveNum} complete. Continue to Wave {waveNum+1}? (yes/no)` with AskUserQuestion:
  - Header: "Wave {waveNum} complete"
  - Options: "Continue to Wave {waveNum+1}" / "Pause pipeline"

**Step 8 (pipeline complete -- post-merge routing):**
- After final summary, add AskUserQuestion:
  - Header: "Merge pipeline complete"
  - Options: "Run cleanup" / "View status" / "Done"
  - "Run cleanup" description: "Remove completed worktrees with /rapid:cleanup"
  - "View status" description: "Show project status with /rapid:status"
  - "Done" description: "Exit merge pipeline"

**Global:**
- Remove ALL occurrences of "STOP" and "halt" keywords
- Replace with structured AskUserQuestion prompts or clear exit text ("Merge pipeline cancelled." / "Exiting merge pipeline.")
- Use "Pause here" instead of "Stop here" if any pause points needed (consistent with Phase 12)
  </action>
  <verify>
    <automated>grep -ciE '\bSTOP\b|\bhalt\b' skills/merge/SKILL.md | grep -q '^0$' && grep -c 'AskUserQuestion' skills/merge/SKILL.md | grep -qE '^[6-9]|^[1-9][0-9]' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
- Zero STOP/halt keywords in merge SKILL.md
- AskUserQuestion in allowed-tools frontmatter
- At least 6 AskUserQuestion prompts (Step 1 confirmation, Step 4 BLOCK, Step 5 escalation, Step 6 conflict, Step 7 gate failure + wave continue, Step 8 next-action)
- Verdict banners with emoji + label + findings summary for APPROVE/CHANGES/BLOCK
- Double confirmation gate on "Revert wave"
- Merge conflict shows structured Resolve/Show diff/Abort with follow-up confirmation
  </done>
</task>

</tasks>

<verification>
- `grep -ciE '\bSTOP\b|\bhalt\b' skills/merge/SKILL.md` returns 0
- `grep -c 'AskUserQuestion' skills/merge/SKILL.md` returns 6 or more
- `head -5 skills/merge/SKILL.md` shows AskUserQuestion in allowed-tools
- Verdict banner patterns exist for APPROVE, CHANGES, and BLOCK
- Double confirmation gate exists for wave revert
</verification>

<success_criteria>
- Merge SKILL.md has structured AskUserQuestion prompts at every user-facing decision point
- All STOP/halt keywords eliminated
- Verdict explanations clearly label APPROVE/CHANGES/BLOCK with findings summaries
- Error recovery (conflicts, gate failures) offers structured options instead of halting
- Patterns consistent with Phases 10-12 (context headers, consequence descriptions)
</success_criteria>

<output>
After completion, create `.planning/phases/13-merge-and-cleanup-skill-prompts/13-01-SUMMARY.md`
</output>
