# Wave 2 PLAN: CHANGELOG Authoring and README Changelog Summary

## Objective
Author the v6.1.0 CHANGELOG section summarizing all 6 delivered sets in the milestone, and update the README changelog summary line to reference v6.1.0 as the latest release. This wave produces new prose content rather than mechanical find-and-replace.

## Dependencies
Wave 1 must be complete before this wave begins. The version strings should already be at 6.1.0 so verification commands produce clean results.

## Tasks

### Task 1: Author v6.1.0 CHANGELOG Section
**File:** `/home/kek/Projects/RAPID/docs/CHANGELOG.md`

**Action:** Insert a new v6.1.0 section at line 9 (before the existing v6.0.0 entry). The section must follow the established Keep a Changelog format exactly as used by existing entries.

**Content to insert (between line 8 and the current line 9):**

```markdown
## [v6.1.0] UX & Onboarding (shipped 2026-04-06)

Focused on first-run experience, command discoverability, and documentation quality -- making RAPID approachable for new users while tightening guidance for experienced ones.

### Added
- Unified `renderFooter()` utility applying next-step footer guidance to all lifecycle skills with `/clear` policy (`clear-guidance-and-display`)
- Audit-to-set handoff mechanism: `audit-version` writes structured REMEDIATION artifacts, `add-set` auto-discovers pending remediations, `status` displays pending sets (`audit-handoff`)
- `/rapid:backlog` skill for capturing out-of-scope feature ideas during execution, with audit-version integration and agent prompt updates (`backlog-system`)

### Changed
- README rewrite with context rot problem statement, single install path, `/clear` mental model, annotated quickstart, and "First Project" walkthrough (`readme-and-onboarding`)
- Systematic UX audit covering breadcrumb consistency, error messages, command discoverability, and first-run polish; wired auto-regroup after `add-set` (`ux-audit`)
- Version bump to v6.1.0 across all user-facing files, stale prose fixes, command count update (28 -> 29), Node.js prerequisite correction (20+ -> 22+) (`docs-housekeeping`)

### Fixed
- Node.js prerequisite documentation corrected from 20+ to 22+ in CONTRIBUTING.md and docs/setup.md (`docs-housekeeping`)
```

**Format rules to follow:**
- The header comment `<!-- Generated by /rapid:documentation -- do not edit manually -->` on line 7 must be preserved
- One blank line between the comment and the new section
- Use `## [vX.Y.Z] Milestone Name (shipped YYYY-MM-DD)` format
- Include a 2-3 sentence milestone-level intro paragraph before subsections
- Sub-sections: `### Added`, `### Changed`, `### Fixed`
- Entry format: `- Description text (\`set-name\`)`
- One blank line between the new v6.1.0 section and the existing v6.0.0 section

**What NOT to do:**
- Do NOT modify any existing CHANGELOG entries
- Do NOT remove or modify the header comment
- Do NOT change the v6.0.0 section or any older entries

**Verification:**
```bash
head -30 /home/kek/Projects/RAPID/docs/CHANGELOG.md  # Should show new v6.1.0 section
grep -c "## \[v6.1.0\]" /home/kek/Projects/RAPID/docs/CHANGELOG.md  # Should return 1
grep -c "## \[v6.0.0\]" /home/kek/Projects/RAPID/docs/CHANGELOG.md  # Should still return 1
```

### Task 2: Update README Changelog Summary Line
**File:** `/home/kek/Projects/RAPID/README.md`

**Action:** Replace line 135's changelog summary to reference v6.1.0 as the latest milestone.

**Current text (line 135):**
```
See [CHANGELOG](docs/CHANGELOG.md) for full history. Latest: **v6.0.0 Scale & Quality** (2026-04-01) -- DAGv3 schema, developer group partitioning, Node.js 20+ minimum, enhanced merge pipeline.
```

**New text:**
```
See [CHANGELOG](docs/CHANGELOG.md) for full history. Latest: **v6.1.0 UX & Onboarding** (2026-04-06) -- unified footer guidance, audit-to-set handoff, README rewrite, UX audit, backlog capture, version housekeeping.
```

**Verification:**
```bash
grep -n "6\.0\.0" /home/kek/Projects/RAPID/README.md  # Should return zero results
grep "v6.1.0 UX" /home/kek/Projects/RAPID/README.md  # Should return the updated line
```

## Success Criteria
1. `docs/CHANGELOG.md` contains exactly one `## [v6.1.0]` section with Added, Changed, and Fixed subsections
2. All 6 sets from the v6.1.0 milestone are mentioned in the CHANGELOG entries
3. The milestone intro paragraph is present (2-3 sentences summarizing the "UX & Onboarding" theme)
4. The existing v6.0.0 and older CHANGELOG entries are completely untouched
5. `README.md` line 135 references v6.1.0 as the latest release
6. `grep -rn "6\.0\.0" /home/kek/Projects/RAPID/README.md` returns zero results (all active references now bumped)

## File Ownership (Wave 2)
- `docs/CHANGELOG.md` (new section insertion only)
- `README.md` (line 135 only -- all other README changes were in Wave 1)
