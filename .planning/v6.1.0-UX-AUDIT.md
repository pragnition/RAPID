# v6.1.0 UX Audit Report

**Milestone:** v6.1.0 -- UX & Onboarding
**Auditor:** ux-audit set
**Date:** 2026-04-06
**Status:** Complete

## Pillar 1: Breadcrumb Consistency

Error messages should follow the pattern: `[ERROR] {context}. Run: {recovery command}`
Red ANSI on `[ERROR]` label only, default terminal color for the rest.

| # | Item | Grade | Notes |
|---|------|-------|-------|
| 1.1 | State transition errors include recovery command | Pass | Wave 2, Task 3: state transition errors now include `Run:` recovery commands via `formatBreadcrumb()` |
| 1.2 | Set lifecycle errors (init, plan, execute, merge) include recovery command | Pass | Wave 2, Tasks 2+4: CliError messages in state.cjs use `formatBreadcrumb()` with recovery hints |
| 1.3 | `[ERROR]` label uses red ANSI, rest uses default color | Pass | Wave 2, Task 1: `exitWithError()` wraps `[ERROR]` in `\x1b[31m...\x1b[0m`, respects `NO_COLOR` |
| 1.4 | Error format is compact inline (`[ERROR] {context}. Run: {cmd}`) | Pass | Wave 2, Task 1: `formatBreadcrumb()` helper produces `{context}. Run: {recovery}` format |
| 1.5 | REMEDIATION_HINTS in state-machine.cjs follow new format | Pass | Wave 2, Task 2: all REMEDIATION_HINTS updated to use `formatBreadcrumb()` |

## Pillar 2: Command Discoverability

| # | Item | Grade | Notes |
|---|------|-------|-------|
| 2.1 | USAGE string has workflow-based section headers | Pass | Wave 3, Task 1: `--- Section ---` headers added to USAGE string |
| 2.2 | Unknown command error suggests closest valid command | Deferred | Fuzzy matching for unknown commands is out of scope for this audit |
| 2.3 | `/rapid:status` shows contextual next-step suggestions | Deferred | Requires modifying SKILL.md files, which are not owned by this set |
| 2.4 | Commands grouped: Setup, Planning, Execution, Review & Merge, Utilities | Pass | Wave 3, Task 1: commands reordered under 5 workflow-based sections |

## Pillar 3: First-Run Experience

| # | Item | Grade | Notes |
|---|------|-------|-------|
| 3.1 | Post-init output includes workflow guide | Deferred | Requires modifying the init skill SKILL.md, not an owned file |
| 3.2 | `/rapid:status` shows workflow guide when no sets started | Deferred | Requires modifying the status skill SKILL.md, not an owned file |
| 3.3 | Gap between "project initialized" and "first set" is bridged | Deferred | Depends on 3.1 and 3.2; deferred until those are addressed |

## Pillar 4: Auto-Regroup Wiring

| # | Item | Grade | Notes |
|---|------|-------|-------|
| 4.1 | `partitionIntoGroups()` called after `recalculateDAG()` in add-set | Pass | Wave 1, Task 2: `autoRegroup()` wired into `addSetToMilestone()` after DAG recalculation |
| 4.2 | `teamSize` stored in STATE.json during init | Pass | Wave 1, Task 1: `scaffoldProject()` writes `teamSize` to STATE.json |
| 4.3 | Solo mode (teamSize=1) skips regrouping | Pass | Wave 1, Task 2: `autoRegroup()` returns early when `teamSize <= 1` |
| 4.4 | Missing teamSize in STATE.json (older projects) gracefully skips | Pass | Wave 1, Task 2: `autoRegroup()` returns early when `teamSize` is missing |

## Summary

- **Total items:** 16
- **Pass:** 11
- **Fail:** 0
- **Deferred:** 5
- **Pending:** 0

## Remediation Log

| Item | Action Taken | Commit |
|------|-------------|--------|
| 4.2 | Persist teamSize in STATE.json during init | `ff8a8f8` |
| 4.1, 4.3, 4.4 | Wire autoRegroup into add-set flow with solo/missing guards | `ebd5714` |
| 1.3, 1.4 | Add formatBreadcrumb helper and red ANSI [ERROR] label | `69b413a` |
| 1.5 | Modernize REMEDIATION_HINTS to use formatBreadcrumb | `bcffc1c` |
| 1.1 | Add recovery hints to state transition error messages | `1aaa924` |
| 1.2 | Standardize CliError messages with breadcrumbs in state.cjs | `55aee23` |
| 2.1, 2.4 | Restructure USAGE string with workflow-based section headers | `0b140ea` |
