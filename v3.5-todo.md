Some error logs to fix/work ----------
● Bash(RAPID_ROOT="/home/kek/.claude/plugins/cache/joey-plugins/rapid/3.4.0/skills/merge/../.."
      if [ -z "${RAPID_TOOLS:-}" ] && [ -f "$RAPID_ROOT/.env" ]; then export…)
  ⎿  Error: Exit code 1
     [RAPID ERROR] ENOENT: no such file or directory, open
     '/home/kek/Projects/TRust/.planning/sets/DAG.json'

When the execute agent is done, it does not update the state to complete and therefore the review agent refuses to review. Fix this by making the execute agent set the state to complete at the end/any other way you deem best

Features:

The DAG.json should be updated/created by all the relevant agents
During the init phase, the agent likes to ask the user  a few questions and have the user answer in prose. Do not do this. Instead, we should ask the user a series of questions (AskUserQuestion with 4 sub questions) until the agent is satisfied. Then, the agent should provide a summary of the poroject and ask the user if theres anything else the user would like to add
Furthermore, some projects are bigger than others and different people have different working styles. Therefore we should also ask how "finely grained" the user wants the project to be (suggest number of sets and let the user choose)

When we start a set with solo mode, it should work on the main branch and not a worktree afterwhich, when we run /merge, the merge agent should inform the user that it is a solo set that doesn't require merging. Therefore, when we execute a solo set, we also need to mark the state as merged in the end

We shall now formalize the standardization of UI. We will now have an optional command, /branding. This is meant to optionally run after /init. This will generate a UI branding guideline for furture agents to refer too (naturally the other agents need to know about this as well.) This will trigger an indepth discussion with the user in order to formalize branding guidelines
